# Автоматизированная обработка документов для банкротства

Система для автоматической обработки документов должников через GPT-5 (o1) и заполнения шаблонов документов для процедуры банкротства.

## 📋 Возможности

- 🤖 Автоматическое распознавание типов документов (паспорт, ИНН, СНИЛС, ЕГРН, БКИ и др.)
- 📄 Извлечение данных из PDF через GPT-5 OCR
- 📝 Автоматическое заполнение Word шаблонов:
  - Заявление о банкротстве
  - Список кредиторов и должников
  - Опись имущества
  - Ходатайства
- 💾 База данных должников и документов
- 🌐 Веб-интерфейс для загрузки и обработки

## 🚀 Быстрый старт (Development)

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
DEBUG=True
OPENAI_API_KEY=your-api-key-here
SECRET_KEY=your-secret-key
```

### 3. Запуск в режиме разработки

```bash
python app.py
```

Приложение будет доступно на `http://localhost:5000`

## 🏭 Production запуск

### Вариант 1: Gunicorn (Linux/Mac)

```bash
# Установка
pip install gunicorn

# Запуск
gunicorn --bind 0.0.0.0:5000 --workers 4 --timeout 300 wsgi:app
```

### Вариант 2: Waitress (Windows)

```bash
# Установка
pip install waitress

# Запуск
waitress-serve --host=0.0.0.0 --port=5000 --threads=4 wsgi:app
```

### Вариант 3: Прямой запуск (не рекомендуется для production)

```bash
# Убедитесь что DEBUG=False в .env
python app.py
```

## 🐳 Docker (опционально)

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Установка системных зависимостей для pdf2image
RUN apt-get update && apt-get install -y \
    poppler-utils \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 5000

CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "4", "--timeout", "300", "wsgi:app"]
```

Запуск:
```bash
docker build -t urist-documents .
docker run -p 5000:5000 --env-file .env urist-documents
```

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DEBUG` | Режим отладки (True/False) | False |
| `HOST` | Адрес сервера | 0.0.0.0 |
| `PORT` | Порт сервера | 5000 |
| `OPENAI_API_KEY` | API ключ OpenAI | - |
| `SECRET_KEY` | Секретный ключ Flask | - |
| `ALLOWED_ORIGIN` | CORS домен | http://localhost:3000 |
| `MAX_FILE_SIZE` | Макс. размер файла (байты) | 52428800 (50MB) |
| `DATABASE_PATH` | Путь к БД SQLite | debtors.db |

## 📁 Структура проекта

```
├── app.py                  # Flask приложение
├── processor.py            # Обработчик документов
├── wsgi.py                 # WSGI точка входа
├── fill_all_templates.py   # Скрипт заполнения всех шаблонов
├── requirements.txt        # Зависимости
├── .env.example           # Пример настроек
├── static/                # CSS, JS
├── templates/             # HTML шаблоны (Jinja2)
├── templ/                 # Word шаблоны (.docx)
├── uploads/               # Загруженные PDF
├── outputs/               # Результаты обработки (JSON)
└── resultdoc/             # Заполненные Word документы
```

## 🔐 Безопасность в Production

1. **Обязательно измените SECRET_KEY**:
   ```bash
   python -c "import secrets; print(secrets.token_hex(32))"
   ```

2. **Отключите DEBUG режим**:
   ```env
   DEBUG=False
   ```

3. **Настройте CORS** для вашего домена:
   ```env
   ALLOWED_ORIGIN=https://yourdomain.com
   ```

4. **Используйте HTTPS** (настройте Nginx/Apache reverse proxy)

5. **Ограничьте доступ к файлам**:
   - Добавьте `.env` в `.gitignore`
   - Проверьте права доступа к `uploads/` и `outputs/`

## 🧪 Тестирование

```bash
# Запустите все тесты
python -m pytest

# Или используйте встроенные тесты
python test_ai_detection.py
python test_creditors_aggregation.py
```

## 📝 Использование

### Через веб-интерфейс:

1. Откройте `http://localhost:5000`
2. Загрузите PDF документы
3. Дождитесь обработки
4. Скачайте заполненные Word документы

### Через скрипт:

```bash
# Заполнить все шаблоны из готового JSON
python fill_all_templates.py
```

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи сервера
2. Убедитесь что все зависимости установлены
3. Проверьте корректность `.env` файла
4. Убедитесь что OPENAI_API_KEY действителен

## 📄 Лицензия

Proprietary - все права защищены.

---

**⚠️ ВАЖНО**: Для production обязательно:
- Установите `DEBUG=False`
- Используйте случайный `SECRET_KEY`
- Настройте HTTPS
- Используйте production WSGI сервер (Gunicorn/Waitress)
