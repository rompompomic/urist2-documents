"""Core document processing logic reused by CLI and web service."""

from __future__ import annotations

import base64
import json
import os
import random
import re
import tempfile
import time
import zipfile
import difflib
from dataclasses import dataclass
from datetime import date, datetime
from decimal import Decimal, InvalidOperation
from pathlib import Path
from typing import Any, Dict, Iterable, List, Optional
from zoneinfo import ZoneInfo

from openai import OpenAI
import pypdfium2 as pdfium
import pymupdf
from PIL import Image
from dotenv import load_dotenv
from docxtpl import DocxTemplate, RichText  # Для динамических таблиц
import requests
from bs4 import BeautifulSoup
from urllib.parse import quote_plus
import psutil  # Для мониторинга памяти

# Load environment variables from .env file
load_dotenv()

# === Processing settings ===
GPT_MODEL = "gpt-5-mini"  # Using GPT-5-mini with vision capabilities
TEMPLATE_DOCX = Path("templ") / "Заявление на банкротство.docx"
FILLED_TEMPLATE_SUFFIX = " (заполненное)"
OUTPUT_DIR = Path("resultdoc")  # Папка для всех готовых документов

# Initialize OpenAI client
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

print(f"OK - Using {GPT_MODEL} for document processing")


@dataclass
class DocumentOutput:
    file: str
    document_type: str
    pages: int
    processing_time_seconds: float
    data: Dict[str, Any]
    error: Optional[str] = None
    extracted_text: Optional[str] = None


class DocumentProcessor:
    """Processes PDF documents and aggregates structured data."""
    
    # Кэш для банковских данных (ИНН, адрес)
    _bank_data_cache: Dict[str, tuple[Optional[str], Optional[str]]] = {}

    @staticmethod
    def clear_bank_data_cache() -> None:
        """Очищает кэш банковских данных (ИНН, адреса)"""
        DocumentProcessor._bank_data_cache.clear()
        print("[CACHE] Кэш банковских данных очищен.")

    def __init__(self) -> None:
        pass

    # === Prompt configuration ===
    DOCUMENT_TYPES: Dict[str, Dict[str, Any]] = {
        "паспорт": {
            "keywords": ["паспорт", "passport", "гражданина рф", "гражданин российской федерации", "удостоверяющий личность"],
            "prompt": """Ты обрабатываешь внутренний паспорт гражданина РФ (книжечный образца 1997/2011). Даже если OCR даёт шум вроде повторяющихся строк "подписан на задачу" или пустые страницы, считай это водяными знаками и извлекай нужные поля по макету. Если реквизит нечитаем или отсутствует — ставь null, ничего не выдумывай и не проси дополнительных данных.

ВАЖНО: Выбирай только АКТУАЛЬНУЮ информацию!
- Если видишь несколько записей о регистрации (прописке) — выбери ПОСЛЕДНЮЮ (самую свежую по дате).
- Если есть несколько штампов о браке/разводе — укажи ПОСЛЕДНИЙ статус (актуальное семейное положение).
- Если есть отметка о снятии с регистрации и новая регистрация — бери только действующую прописку.

РАБОТА С РАЗМЫТЫМИ ШТАМПАМИ РЕГИСТРАЦИИ:
- Штамп о регистрации часто размыт или частично нечитаем — это нормально!
- СТАРАЙСЯ разобрать хотя бы: город, улицу, номер дома, номер квартиры
- Ищи ключевые слова в штампе: "ЗАРЕГИСТРИРОВАН", "ЧЕЛЯБИНСК", "УЛ.", "Д.", "КВ."
- Числа (номера домов, квартир) обычно читаются лучше букв
- Дата в штампе обычно внизу, формат ДД.ММ.ГГГГ (например: 29.02.2000)
- Даже если уверен только в части адреса (город + дом) — запиши то, что видишь
- НЕ оставляй поле "Прописка" пустым (null) если хотя бы часть адреса читается!

ФОРМАТИРОВАНИЕ ТЕКСТА:
- Если текст написан ЗАГЛАВНЫМИ БУКВАМИ (CAPS LOCK) — приведи к нормальному виду: "Иванов Иван Иванович", а не "ИВАНОВ ИВАН ИВАНОВИЧ".
- ФИО, адреса, названия организаций — с заглавной буквы, остальное строчными.
- Исключения: аббревиатуры (МВД, ЗАГС, УФМС) оставляй заглавными.
- Убирай лишние пробелы и приводи текст к читаемому виду.

Следуй шагам:
1. Разворот с фото (стр. 2-3):
        - Красный номер справа вверху: 4 цифры серии + 6 цифр номера.
        - Под заголовками "Фамилия", "Имя", "Отчество" — ФИО.
        - Строка "Пол" содержит "Муж" или "Жен".
        - Строка "Дата рождения" — приведи к формату ДД.ММ.ГГГГ даже если написано прописью.
        - КРИТИЧЕСКИ ВАЖНО: Строка "Место рождения" находится СРАЗУ ПОД датой рождения НА СТРАНИЦЕ 2-3:
          * ЭТО НЕ АДРЕС ПРОПИСКИ! Это место, где человек родился (часто другой город/область)
          * КОПИРУЙ ТОЧНО КАК НАПИСАНО В ПАСПОРТЕ! Не добавляй запятые, не меняй структуру, не форматируй!
          * Если написано "ГОР. МИАСС ЧЕЛЯБИНСКОЙ ОБЛ." - пиши "Гор. Миасс Челябинской обл."
          * Если написано "ГОРОД МИАСС ЧЕЛЯБИНСКОЙ ОБЛАСТЬ" - пиши "Город Миасс Челябинской область"
          * Если написано "Р.П. ЛЕБЯЖЬЕ ЛЕБЯЖЬЕВСКИЙ Р-Н КУРГАНСКАЯ ОБЛАСТЬ" - пиши "Р.П. Лебяжье Лебяжьевский р-н Курганская область"
          * Если написано "СЕЛО ПЕТРОВО РЯЗАНСКАЯ ОБЛ." - пиши "Село Петрово Рязанская обл."
          * Если написано "Гор. Миасс" - пиши "Гор. Миасс"
          * Если написано "Г. Миасс" - пиши "Г. Миасс"
          * Если написано "РОССИЯ" или "РФ" или "РОССИЙСКАЯ ФЕДЕРАЦИЯ" - ОБЯЗАТЕЛЬНО включи это
          * Приводи к нормальному виду только CAPS LOCK: "гор." вместо "ГОР.", "Россия" вместо "РОССИЯ"
          * КОПИРУЙ ТОЧНО КАК НАПИСАНО В ПАСПОРТЕ! Не добавляй запятые, не меняй структуру, не форматируй!
          * НЕ ПУТАЙ с адресом регистрации (прописки) со страницы 5-6!
2. Страница с данными о выдаче (стр. 2):
        - Блок "Выдан"/"Кем выдан" — ТОЧНОЕ название подразделения как написано в паспорте (Отделом УФМС, УВМ ГУ МВД и т.д.)
        - КРИТИЧЕСКИ ВАЖНО: Сохраняй ОРИГИНАЛЬНЫЙ текст "Кем выдан" БЕЗ ИЗМЕНЕНИЙ!
          * Если написано "Отделом УФМС России по республике Башкортостан" — так и пиши
          * Если "УВМ ГУ МВД России по Челябинской области" — так и пиши
          * Есил "ТП П. НИЖНЕСОРТЫМСКИЙ ОУФМС РОССИИ ПО ХАНТЫ-МАНСИЙСКОМУ АВНТОНОМ. ОКР -ЮГРЕ В СУРГУТСКОМ РАЙОНЕ" - так и пиши
          * НЕ сокращай, НЕ изменяй формат, копируй точно как в паспорте
          * Приводи к нормальному виду только CAPS LOCK: "Отделом" вместо "ОТДЕЛОМ"
        - Под ним дата выдачи цифрами.
        - Слева внизу код подразделения формата XXX-XXX.
3. Разворот регистрации (стр. 5-6):
        - ВНИМАНИЕ: Это АДРЕС РЕГИСТРАЦИИ (ПРОПИСКА) - НЕ ПУТАЙ с местом рождения со страницы 2-3!
        - МЕСТО РОЖДЕНИЯ (стр. 2-3) ≠ АДРЕС РЕГИСТРАЦИИ (стр. 5-6) - это РАЗНЫЕ вещи!
        - Если несколько записей о регистрации — выбери ПОСЛЕДНЮЮ (с самой поздней датой).
        - Игнорируй отметки "Снят с регистрации" — они указывают на старые адреса.
        - КРИТИЧЕСКИ ВАЖНО: Адрес регистрации может быть в ШТАМПЕ или рукописной записи:
          * Штамп содержит: "ЗАРЕГИСТРИРОВАН", дату внизу, адрес в центре (часто размыт, но читается)
          * Штамп выглядит как прямоугольная рамка с текстом внутри
          * Адрес в штампе может быть написан CAPS LOCK — приведи к нормальному виду
          * ОБЯЗАТЕЛЬНО ИЗВЛЕКИ ПОЛНЫЙ АДРЕС с указанием области/края/республики!
          * КРИТИЧЕСКИ ВАЖНО - РЕГИОН В НАЧАЛЕ: Адрес ВСЕГДА начинается с субъекта РФ (кроме Москвы/Санкт-Петербурга)!
          * Формат адреса: ОБЛАСТЬ/КРАЙ/РЕСПУБЛИКА, город, улица, дом, корпус (если есть), квартира
          * Примеры правильного формата:
            - "Челябинская область, г. Челябинск, ул. Ленина, д. 10, кв. 5"
            - "Краснодарский край, г. Краснодар, ул. Мира, д. 25А, корп. 2, кв. 100"
            - "Республика Башкортостан, г. Уфа, ул. Ленина, д. 1, кв. 1"
            - "г. Москва, ул. Тверская, д. 1, кв. 10" (Москва - федеральный город)
          * Если в штампе область размыта, но есть город - ОПРЕДЕЛИ регион по городу:
            - Челябинск → "Челябинская область"
            - Уфа → "Республика Башкортостан"
            - Новосибирск → "Новосибирская область"
            - Краснодар → "Краснодарский край"
          * В старых штампах часто есть "ЧЕЛЯБИНСКАЯ ОБЛАСТЬ", "Г. ЧЕЛЯБИНСК", "УЛ. КРАСНОМОЛОТОВА" и т.д.
          * НЕ пропускай название области/края - это ОБЯЗАТЕЛЬНАЯ часть адреса!
          * ПРОВЕРЬ: адрес регистрации ВСЕГДА содержит улицу, дом, квартиру (чего нет в месте рождения)
        - Дата регистрации обычно ВНИЗУ штампа цифрами (ДД.ММ.ГГГГ)
        - Рядом может быть подпись должностного лица и печать.
        - Бывает так что написано "кв.", а номер квартиры на следующей строке, внимательнее, пример:
            "ул. Ленина, д. 10, кв.
            2"
        - Всегда извлекай правильные слова из адреса. Никогда не выдавай фейковые данные.
4. Штампы о семейном положении (стр. 7-8):
        - ВНИМАНИЕ: Если несколько штампов — выбери ПОСЛЕДНИЙ (актуальный статус).
        - Если был брак, затем развод — статус "Разведен/Разведена".
        - Если штамп перечёркнут диагональными линиями — это официальный признак аннулирования записи в паспортах РФ.
        - КРИТИЧЕСКИ ВАЖНО: Если есть штамп о браке - извлеки ФИО супруга и дату рождения супруга из штампа!
          * В штампе обычно написано "Зарегистрирован брак с [ФИО супруга]"
          * Пример: "Зарегистрирован брак с Ивановым Иваном Ивановичем, 15.03.1990 г.р."
          * Извлеки ПОЛНОЕ ФИО супруга и запиши в поле "ФИО_супруга"
          * Извлеки ДАТУ РОЖДЕНИЯ супруга (если есть) и запиши в поле "Дата_рождения_супруга" в формате ДД.ММ.ГГГГ
          * Если ФИО супруга не видно или штамп нечитаем - поставь null
          * Если даты рождения супруга нет в штампе - поставь null
        - Если есть запись о браке/разводе — статус, дата, орган, ФИО супруга.
        - Если штампов нет — статус "Не женат"/"Не замужем".
        - Если есть читаемый штамп о браке и далее следующий штамп нечитаемый, кривой — статус "Не женат"/"Не замужем".
5. Печати о детях (стр. 9-20):
        - Каждая печать "Сведения о детях" — ФИО и дата рождения ребёнка.
        - Собери ВСЕ записи о детях (здесь актуальны все).
        - Если не получается прочесть ФИО — ставь null.

Если OCR пустой, ориентируйся на макет: ФИО в верхнем левом блоке, красный номер справа, штамп регистрации под надписью "зарегистрирован".

Верни JSON строго в формате:
{
  "ФИО": "...",
  "Фамилия_инициалы": "Иванов И.И.", #Тут ты сам пишешь на основе ФИО из паспорта
  "Фамилия_инициалы_рп": "Иванова И.И.", #Тут ты сам пишешь на основе ФИО из паспорта, но уже в родительном падеже
  "Пол": "мужской/женский",
  "Дата_рождения": "ДД.ММ.ГГГГ",
  "Место_рождения": "ГОР. МИАСС ЧЕЛЯБИНСКОЙ ОБЛ.", # СТР. 2-3! КОПИРУЙ ТОЧНО КАК В ПАСПОРТЕ! Где РОДИЛСЯ человек. БЕЗ улицы и дома!
  "Место_рождения_не_распознано": false,  // true если текст рукописный и/или нечитаемый
  "Серия": "ХХХХ",
  "Номер": "XXXXXX",
  "Кем_выдан": "...",
  "Дата_выдачи": "ДД.ММ.ГГГГ",
  "Код_подразделения": "ХХХ-ХХХ",
  "Прописка": "Челябинская область, г. Челябинск, ул. Ленина, д. 10, кв. 5", # СТР. 5-6! Где ПРОПИСАН человек. ОБЯЗАТЕЛЬНО: область/край, город, УЛИЦА, ДОМ,
  "Прописка_не_распознана": false, // true если текст рукописный в штампе и/или нечитаемый
  "Дата_прописки": "ДД.ММ.ГГГГ",
  "Семейное_положение": "Женат/Замужем/Не женат/Не замужем/Разведен/Разведена",
  "ФИО_супруга": "Иванов Иван Иванович",  
  "Дата_рождения_супруга": "ДД.ММ.ГГГГ", 
  "Дети": [{"ФИО": "...", "Дата_рождения": "ДД.ММ.ГГГГ"}]
}
КРИТИЧЕСКИ ВАЖНО - НЕ ПУТАЙ ЭТИ ПОЛЯ:
- "Место_рождения" (стр. 2-3): Где человек РОДИЛСЯ. Без улицы/дома. 
- "Прописка" (стр. 5-6): Где человек ПРОПИСАН. ВСЕГДА с улицей и домом. 
- Если штамп регистрации заполнен НЕРАЗБОРЧИВЫМ РУКОПИСНЫМ ПОЧЕРКОМ и ты не можешь прочитать улицу или город - ПОСТАВЬ "Прописка": null и "Прописка_не_распознана": true. ЛУЧШЕ NULL, ЧЕМ ФЕЙК!
- То же самое для Места рождения - если не можешь прочитать, ставь null и "Место_рождения_не_распознано": true.

Дополнительно: 
- Поле "Место_рождения" ОБЯЗАТЕЛЬНО должно быть заполнено в формате: "тип населенного пункта (с сокращением), название, район, область/край". Примеры: "с. Агинское, Саянский р-н, Красноярский край", "г. Москва", "д. Петрово, Рязанская обл.". Если текст нечитабелен, есть засвет на буквах, из-за которого сложно прочитать, не пытайся извлечь хоть что-то — "Место_рождения_не_распознано": true
- Поле "Прописка" ОБЯЗАТЕЛЬНО должно содержать ПОЛНЫЙ адрес с указанием области/края/республики! Примеры: "Челябинская область, г. Челябинск, ул. Ленина, д. 10, кв. 5", "Краснодарский край, г. Краснодар, ул. Мира, д. 25", "г. Москва, ул. Тверская, д. 1". НЕ пропускай название области/края в начале адреса! Если текст нечитабелен — "Прописка_не_распознана": true
Отсутствующие реквизиты ставь null. Ответ должен содержать только чистый JSON без Markdown и пояснений. Если красный номер или текст не читабелен — ключи всё равно присутствуют, значение = null. Никогда не отвечай текстом вне JSON и не задавай вопросов пользователю.""",
        },
        "инн": {
            "keywords": ["инн", "налогоплательщика", "кнд 1122003", "копия инн", "индивидуальный номер налогоплательщика", "фнс", "налоговая служба", "свидетельство инн"],
            "prompt": """Ты обрабатываешь свидетельство или выписку ФНС об ИНН физического лица (форма КНД 1122003 или свидетельство на бумаге).

ВНИМАНИЕ: Это НЕ ЕГРН! Если видишь "Единого государственного реестра недвижимости" или "кадастровый номер" — это не ИНН документ.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО с заглавной буквы, остальное строчными).

ГДЕ ИСКАТЬ ИНН:
1. Старое свидетельство (бумажное):
   - Самое первое число в документе (часто сверху или в центре) — это ИНН
   - РЕГИОНАЛЬНЫЕ ФОРМАТЫ: Башкортостан, Татарстан и другие республики могут иметь особый дизайн
   - В центре документа крупными цифрами: 12 цифр ИНН (может быть с пробелами: "02 56 051 408 09")
   - Выше или ниже указано ФИО
   - Может быть печать налоговой инспекции, герб региона
   - Башкирский формат: сверху номер с пробелами, ниже ФИО

2. Новая выписка (форма КНД 1122003):
   - Шапка: логотип ФНС, название "Сведения об ИНН"
   - Таблица с полями: ФИО, ИНН (12 цифр), дата рождения
   - Правый угол: дата формирования

3. Простая справка/копия:
   - Может быть просто текст с ИНН и ФИО
   - ИНН — это ВСЕГДА 12 цифр (для физических лиц)

КРИТИЧЕСКИ ВАЖНО - ФОРМАТЫ ИНН:
ИНН физического лица может быть записан по-разному:
- Слитно: 745210776146
- С пробелами: 745 210 776 146
- С дефисами: 745-210-776-146
- Группами: 7452 1077 6146
- Прописано через палочки 7 | 4 | 1 | 5 | 0 | 5 | 3 | 6 | 8 | 1 | 1 | 3
- Региональный формат: 02 56 051 408 09 (Башкортостан)
- Частично размыт: некоторые цифры нечитаемы

ИНСТРУКЦИЯ:
- ИНН физлица ВСЕГДА 12 цифр (для юрлиц 10 цифр - это не наш случай!)
- ПЕРВОЕ число в документе часто и есть ИНН — проверь его на 12 цифр
- Если видишь группу из ~12 цифр рядом со словом "ИНН" или просто сверху документа - это он!
- Убери все пробелы, дефисы, точки - оставь только 12 цифр
- Если часть цифр размыта но видно 10-11 цифр - попытайся разобрать остальные
- Ищи ИНН: в самом начале документа, в центре крупными цифрами, в таблице напротив "ИНН", в шапке
- НЕ путай с ОГРН (13-15 цифр), СНИЛС (11 цифр), датами, номерами документов

Верни JSON:
{
  "ФИО": "...",
  "ИНН": "XXXXXXXXXXXX",
  "Дата_рождения": "ДД.ММ.ГГГГ",
  "Дата_формирования": "ДД.ММ.ГГГГ",
  "Вид_документа": "Свидетельство ИНН / Выписка ФНС"
}

ВАЖНО: Если не нашел 12-значный номер ИНН, но видишь ФИО — всё равно верни JSON с ИНН=null.
Никаких комментариев и пояснений!"""
        },
        "снилс": {
            "keywords": ["снилс", "страховое свидетельство", "страховой номер", "зелёная", "страховой номер индивидуального лицевого счета"],
            "prompt": """Ты обрабатываешь страховое свидетельство обязательного пенсионного страхования (СНИЛС) — старый зелёный пластик или новая электронная справка.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО, адреса с заглавной буквы).

КРИТИЧЕСКИ ВАЖНО - ФОРМАТЫ СНИЛС:
СНИЛС может быть записан по-разному:
- Правильно: 123-456-789 12
- Слитно: 12345678912
- С пробелами: 123 456 789 12
- Без дефисов: 123 456 789 12
- С точками: 123.456.789.12
- Частично размыт

ИНСТРУКЦИЯ:
- СНИЛС ВСЕГДА 11 цифр в стандартном формате XXX-XXX-XXX XX
- Ищи ~11 цифр рядом со словами: "СНИЛС", "Страховой номер", "СФР"
- Убери все пробелы, дефисы, точки
- Приведи к правильному формату: XXX-XXX-XXX XX (с дефисами между тройками и пробелом перед последней двойкой)
- Если часть цифр размыта - попытайся разобрать хотя бы 9-10 цифр
- НЕ путай с ИНН (12 цифр), телефоном, другими номерами

1. Старый бланк:
        - Заголовок "Страховое свидетельство" и строка "Страховой номер" — формат XXX-XXX-XXX XX.
        - Ниже блок "Фамилия", "Имя", "Отчество".
        - Внизу слева "Дата рождения" и "Место рождения".
        - Под подписью страхователя — дата выдачи.
2. Новая справка СФР:
        - Шапка "Страховой номер индивидуального лицевого счёта" — рядом СНИЛС.
        - Таблица с ФИО, датой и местом рождения.

Если OCR пустой, используй макет: номер крупно в центре, ФИО блоком ниже, дата рождения под ФИО.

Верни JSON:
{
  "ФИО": "...",
  "СНИЛС": "XXX-XXX-XXX XX",
  "Дата_рождения": "ДД.ММ.ГГГГ",
  "Место_рождения": "...",
  "Дата_выдачи": "ДД.ММ.ГГГГ",
  "Тип_бланка": "карта 1997/справка СФР"
}
Если реквизит не найден, ставь null. Никаких дополнительных комментариев.""",
        },
        "сзиилс": {
            "keywords": ["сзи-илс", "индивидуального лицевого", "ипк", "состоянии индивидуального", "сзиилс", "сведения о застрахованном лице", "индивидуальный лицевой счет", "пенсионные баллы", "стаж", "пфр"],
            "prompt": """Документ "Сведения о состоянии индивидуального лицевого счёта" (форма СЗИ-ИЛС).

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (названия организаций с заглавной).

Найди в шапке: ФИО, СНИЛС, дату рождения, дату формирования ("дата приёма" или "сформировано").
Строка "Величина индивидуального пенсионного коэффициента" — число с точкой.
Внизу раздела указан расчётный пенсионный капитал (рубли и копейки).
Таблица 1 перечисляет периоды с 2015 года: период, работодатель, суммы выплат, взносов, величину ИПК, длительность.
Если есть разделы о периодах до 2015 года — перечисли их отдельным списком.
При слабом OCR ориентируйся на колонки "Период", "Работодатель", "Сумма выплат".

JSON:
{
        "ФИО": "...",
        "СНИЛС": "...",
        "Дата_рождения": "ДД.ММ.ГГГГ",
        "Дата_формирования": "ДД.ММ.ГГГГ",
        "ИПК": "...",
        "Пенсионный_капитал": "...",
        "Периоды": [
                {
                        "Период": "01.01.2019-31.12.2019",
                        "Работодатель": "...",
                        "Сумма_выплат": "...",
                        "Сумма_взносов": "...",
                        "ИПК": "...",
                        "Длительность": "..."
                }
        ],
        "Периоды_до_2015": ["..."]
}
Пустые поля = null, списки допускают [] и null, но ключи обязательны. Без комментариев.""",
        },
        "налоговое_уведомление": {
            "keywords": ["налоговое уведомление", "налоговая задолженность", "налогового уведомления", "налоговое уведомлени", "копия налогового", "транспортный налог", "налог на имущество физических лиц", "всего к уплате", "земельный налог", "фнс", "налоговая служба", "obshchaya zadolzhennost", "единый налоговый счёт", "сумма начисления", "задолженность выставлена", "время получения данных из фнс", "госуслуги налог"],
            "prompt": """Налоговое уведомление ФНС о необходимости уплаты налогов.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

🔴 В ДОКУМЕНТЕ ДВА ВОЗМОЖНЫХ ФОРМАТА:

ФОРМАТ 1 - Классическое налоговое уведомление:
- Есть номер уведомления, дата, срок уплаты
- Список налогов с детализацией по видам

ФОРМАТ 2 - Госуслуги / ЕНС (Единый налоговый счёт):
- Заголовок "Общая задолженность по налоговому счёту"
- Есть "Время получения данных из ФНС", "Сумма начисления", "Плательщик"
- НЕТ номера уведомления, НЕТ срока уплаты, НЕТ детализации налогов

📋 ИНСТРУКЦИИ ПО ИЗВЛЕЧЕНИЮ:

ДЛЯ ОБОИХ ФОРМАТОВ ищи:
- ФИО налогоплательщика (в Госуслугах: поле "Плательщик")
- ИНН (может быть скрыт звёздочками типа "*********110" - извлекай как есть)
- Итоговую сумму к уплате

ДЛЯ ФОРМАТА 1 (классическое уведомление) дополнительно ищи:
- Номер уведомления
- Дату уведомления  
- Срок уплаты
- Список всех налогов (вид, период, сумма каждого)

ДЛЯ ФОРМАТА 2 (Госуслуги):
- Дата = извлеки из "Время получения данных из ФНС" (преобразуй "27.11.25" → "27.11.2025")
- ФИО = из поля "Плательщик"
- Всего_к_уплате = из поля "Сумма начисления" (убери пробелы, запятую замени на точку)
- Налоги = пустой массив []
- Номер и Срок_уплаты = null

JSON (ЕДИНЫЙ ДЛЯ ОБОИХ ФОРМАТОВ):
{
        "Номер": "..." или null,
        "Дата": "ДД.ММ.ГГГГ",
        "ФИО": "...",
        "ИНН": "...",
        "Срок_уплаты": "ДД.ММ.ГГГГ" или null,
        "Налоги": [
                {
                        "Вид": "Транспортный налог/Налог на имущество физических лиц/...",
                        "Период": "ГГГГ",
                        "Сумма": число
                }
        ],
        "Всего_к_уплате": число
}

ВАЖНО:
- Суммы указывай ЧИСЛАМИ (не строками!)
- Для Госуслуг: Налоги = [], Номер = null, Срок_уплаты = null
- Пустые значения = null
- БЕЗ комментариев, БЕЗ текста до или после JSON""",
        },
        "доходы": {
            "keywords": ["2-ндфл", "2ндфл", "кнд 1175018", "справка о доходах", "справка о доходе", "сведения о доходах", "сведения о доходе", "справка ндфл", "форма ндфл", "доходы физического лица", "доходы физлица"],
            "prompt": """Справка о доходах и суммах налога физического лица (форма 2-НДФЛ / КНД 1175018). В одном PDF обычно несколько страниц — каждая за отдельный год.

ВАЖНО: Собери информацию за ВСЕ годы, но при расчётах учитывай, что АКТУАЛЕН последний (самый свежий) год.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (названия организаций с заглавной буквы).

Для каждой страницы определи:
- Год и дату выдачи: строка "за ..." + дата справа.
- Раздел 1: название налогового агента, ИНН, КПП, ОКТМО, телефон.
- Раздел 2: ФИО налогоплательщика, его ИНН, статус, код документа, серия и номер паспорта, дата рождения.
- Раздел 5: общая сумма дохода, налоговая база, налог исчисленный, налог удержанный, налог перечисленный.
- Раздел 6 (если есть): суммы дохода и налога, которые не удержаны.
Если OCR не видит таблицу, используй структуру формы — разделы нумеруются и расположены в одинаковых местах.

Верни JSON:
{
        "ФИО": "...",
        "ИНН": "...",
        "Справки": [
                {
                        "Год": "2021",
                        "Дата_выдачи": "ДД.ММ.ГГГГ",
                        "Налоговый_агент": {
                                "Название": "...",
                                "ИНН": "...",
                                "КПП": "...",
                                "ОКТМО": "...",
                                "Телефон": "..."
                        },
                        "Документ_личности": {
                                "Код": "...",
                                "Серия_номер": "..."
                        },
                        "Общая_сумма_дохода": "...",
                        "Налоговая_база": "...",
                        "Налог_исчисленный": "...",
                        "Налог_удержанный": "...",
                        "Налог_перечисленный": "...",
                        "Неудержанный_доход": "...",
                        "Неудержанный_налог": "..."
                }
        ]
}
Справки должны быть отсортированы по году (от старых к новым). Если какого-то раздела нет, ставь null. Никаких пояснений.""",
        },
        "трудовая": {
            "keywords": ["трудов", "сзв", "трудовой деятельности", "сведения о трудовой", "трудовая книжка", "сзв-стаж", "сзв-м", "пфр", "сведения о трудовой деятельности", "ЭТК", "этк", "выписка из этк", "выписка из ЭТК", "электронная трудовая книжка", "электронная трудовая"],
            "prompt": """Документ СФР "Сведения о трудовой деятельности" (форма СТД-Р/СТД-ПФР).

ВАЖНО: Определи АКТУАЛЬНОЕ место работы!
- Если последняя запись — "Увольнение", значит человек сейчас НЕ работает.
- Если последняя запись — "Приём" или "Перевод", значит человек работает в этой организации.
- Игнорируй записи с отметкой "Отмена" — они недействительны.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (названия организаций, должности с заглавной).

Собери:
- ФИО, дата рождения, СНИЛС из шапки.
- Таблицу записей: для каждой строки укажи работодателя, регистрационный номер СФР, дату события, действие (приём/перевод/увольнение), должность/трудовую функцию, реквизиты документа, признак отмены.
- Раздел "Сведения о трудовой деятельности за периоды до 31 декабря 2019 года" — перечисли периоды текстом.
- Определи актуальное место работы: если последнее событие не "Увольнение" — укажи название работодателя и должность в поле "Текущая_работа".
Если OCR слабый, ориентируйся на заголовок "Сведения о трудовой деятельности зарегистрированного лица" — сразу после него таблица с колонками 1-11.

JSON:
{
        "ФИО": "...",
        "Дата_рождения": "ДД.ММ.ГГГГ",
        "СНИЛС": "...",
        "Текущая_работа": "Название организации, Должность" или null (если уволен),
        "Записи": [
                {
                        "Работодатель": "...",
                        "РегНомер": "...",
                        "Дата": "ДД.ММ.ГГГГ",
                        "Событие": "Приём/Перевод/Увольнение",
                        "Должность": "...",
                        "Документ": "...",
                        "Отмена": false
                }
        ],
        "Периоды_до_2019": ["..."]
}
Записи должны быть отсортированы по дате (от старых к новым). Отсутствующие данные = null. Без комментариев.""",
        },
        "счета": {
            "keywords": ["справка о счетах", "счета", "банков", "номер счета", "расчетный счет", "сведения о счетах", "о счетах"],
            "prompt": """Справка о банковских счетах клиента.

Сразу нормализуй название банка при вставке в json, например: ПАО «Сбербанк», ПАО «Банк ПСБ», ПАО «АК БАРС» Банк и т.д.

🔴 КРИТИЧЕСКИ ВАЖНО - ИЗВЛЕКАЙ ВСЕ *ОТКРЫТЫЕ* СЧЕТА:
- Внимательно просмотри ВЕСЬ документ от начала до конца и найди ВСЕ действующие счета!
- Игнорируй счета, где явно написано "Закрыт", "Closed", "Дата закрытия" (и она прошла).
- Читай таблицу построчно до самого конца документа, не пропускай строки!
- ОБЯЗАТЕЛЬНО включай: текущие счета, счета по вкладу, депозитные счета, карточные счета (только открытые!)
- Если в справке 5 открытых счетов - извлеки все 5! Если 10 - извлеки все 10!
- Один банк может иметь НЕСКОЛЬКО открытых счетов (например: Сбербанк текущий в Москве + Сбербанк вклад в Челябинске + Сбербанк депозит)
- НЕ останавливайся после первых нескольких строк - читай таблицу до конца документа!

🔴 РАБОТА С МНОГОСТРАНИЧНЫМИ ТАБЛИЦАМИ:
- Если таблица разрывается и продолжается на следующей странице:
  1. Если на новой странице НЕТ названия банка, используй название банка из ПРЕДЫДУЩЕЙ строки/страницы.
  2. Если строки идут подряд и у них пустая ячейка "Банк" (или объединенная), считай что это ТОТ ЖЕ БАНК, что и выше.
  3. Если в начале новой страницы повторяются заголовки столбцов ("Номер счета", "Валюта" и т.д.) - ИГНОРИРУЙ их, читай данные дальше.
  4. Если видишь строку с названием банка (например "ПАО Сбербанк"), то все следующие ниже счета относятся к ЭТОМУ банку, пока не встретится другое название.

Поле "Тип":
- Извлекай "Вид счета/вклада", "Тип счета", или "Название продукта" как есть.
- Примеры: "Мир Социальная", "Mastercard Standard", "Вклад Управляй", "Текущий счет"
- Нормализуй только регистр (первая заглавная).
- Если не видно типа - пиши "Текущий счет".

Фильтр по статусу:
- Извлекай ТОЛЬКО ДЕЙСТВУЮЩИЕ (ОТКРЫТЫЕ) счета.
- ИГНОРИРУЙ счета со статусом "Закрыт", "Closed", "Погашен", "Архивный" или если указана дата закрытия.
- Статус ("открыт", "действующий") запиши в поле "Статус".

Определи:
- Организация, выдавшая документ (шапка или подпись).
- ФИО клиента и дата рождения, если указаны.
- ИНН клиента.
- Дата выдачи и исходящий номер (обычно рядом с подписью или заголовком).
- Таблица со счетами: банк (ПОЛНОЕ название с адресом если указан), тип счёта (расчётный, депозитный, карточный), номер/IBAN, валюта, дата открытия, статус, остаток, дополнительные отметки.

КРИТИЧЕСКИ ВАЖНО:
- Поле "Банк": извлекай ПОЛНОЕ название банка БЕЗ адреса и нормализуй: ПАО «Сбербанк», ПАО «АК БАРС» Банк
- Поле "Адрес_банка": ИЗВЛЕКАЙ ПОЛНЫЙ АДРЕС банка из справки целиком!
  Примеры:
  - "420066, г. Казань, ул. Декабристов, 1"
  - "117312, город Москва, ул. Вавилова, д.19"
  - НЕ сокращай адрес, копируй полностью как в документе!

При скане без текста ориентируйся на типовую структуру таблицы с колонками "Банк"/"Номер счёта"/"Статус".

JSON:
{
        "Организация": "...",
        "ФИО": "...",
        "ИНН": "...",
        "Дата_выдачи": "ДД.ММ.ГГГГ",
        "Счета": [
                {
                        "Банк": "полное название банка БЕЗ адреса",
                        "Адрес_банка": "адрес банка из справки (город, улица, дом)",
                        "Тип": "...",
                        "Номер": "...",
                        "Валюта": "...",
                        "Дата_открытия": "ДД.ММ.ГГГГ",
                        "Статус": "...",
                        "Остаток": "..."
                }
        ]
}
В массив "Счета" включай ТОЛЬКО ДЕЙСТВУЮЩИЕ (ОТКРЫТЫЕ) счета. Игнорируй счета со статусом "Закрыт". Если данных нет — null, списки допускают []. Никаких комментариев.""",
        },
        "_уведомлениегрне": {
            "keywords": ["уведомление об отсутствии", "кви-001", "отсутствуют сведения", "отсутствие сведений", "информация отсутствует"],
            "prompt": """Уведомление Росреестра об ОТСУТСТВИИ запрошенных сведений из ЕГРН.

ВНИМАНИЕ: Это НЕ выписка о недвижимости!
- Уведомление = документ о том, что данных НЕТ
- Если видишь "в ЕГРН отсутствуют сведения", "данные не найдены", "сведения отсутствуют" — это уведомление
- Если есть конкретный объект с кадастровым номером — это выписка, не уведомление

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО, адреса с заглавной).

Найди:
- Номер и дату уведомления (в правом верхнем блоке).
- Кому адресовано (заявитель/представитель).
- Вид запрошенной информации, указанный в пункте 1.
- Правообладатель (о ком запрашивались сведения): ФИО, дата рождения, паспорт, СНИЛС, адрес.
- Период, за который запрашивались сведения.
- Территория (где искали).
- Итог: "в ЕГРН отсутствуют сведения" — зафиксируй как результат.
При сканах без текста ориентируйся: основной блок слева, таблица с пунктами 1-2 содержит параметры запроса.

JSON:
{
        "Номер": "...",
        "Дата": "ДД.ММ.ГГГГ",
        "Адресат": "...",
        "Вид_запроса": "...",
        "Период": {
                "С": "ДД.ММ.ГГГГ",
                "По": "ДД.ММ.ГГГГ"
        },
        "Территория": "...",
        "Правообладатель": {
                "ФИО": "...",
                "Дата_рождения": "ДД.ММ.ГГГГ",
                "Паспорт": "...",
                "СНИЛС": "...",
                "Адрес": "..."
        },
        "Результат": "сведения отсутствуют"
}

ВАЖНО: "Результат" должен быть "сведения отсутствуют" — это главный признак уведомления.
Пустые поля = null. Никаких комментариев."""
        },
        "егрн_выписка": {
            "keywords": ["выписка из единого государственного реестра недвижимости", "выписка из егрн", "егрн", "сведения из егрн", "кадастровый номер", "кадастровая стоимость", "объекта недвижимости", "сведения егрн", "реестр недвижимости", "росреестр", "единый государственный реестр недвижимости", "право собственности", "выписка росреестр", "недвижимости о правах отдельного лица", "имевшиеся имеющиеся", "объекты недвижимости", "филиал публично правовой компании", "росреестра по республике", "онлайн выписка из егрн", "онлайн выписка земельный участок", "онлайн выписка квартира", "онлайн выписка дом", "онлайн выписка здание", "онлайн выписка помещение", "выписка из егрн с госуслуг", "сведения из егрн с госуслуг"],
            "prompt": """Выписка из ЕГРН или Сведения из ЕГРН (в т.ч. с Госуслуг) с характеристиками объекта недвижимости.

!!! ВНИМАНИЕ: Извлекай АБСОЛЮТНО ВСЕ объекты, упомянутые в документе оформленные на должника, должник - {debtor_fio} - если есть опечатка, ничего страшного!!!
Если в документе есть кадастровый номер и адрес - это объект. Извлекай его!

АЛГОРИТМ РАБОТЫ (СТРОГО ПО ПОРЯДКУ):

1️⃣ ОПРЕДЕЛЕНИЕ ТИПА ДОКУМЕНТА
   - Если это "Сведения из Единого государственного реестра недвижимости" (часто с Госуслуг) — это ВЫПИСКА об объекте!
   - Это НЕ уведомление об отсутствии, если есть разделы "Сведения об основных характеристиках объекта" и "Сведения о зарегистрированных правах".

2️⃣ ПОИСК ВЛАДЕЛЬЦА (КЛЮЧЕВОЙ МОМЕНТ)
   - Ищи таблицу "Сведения о зарегистрированных правах" (или раздел 2, или пункты 1.1, 1.2...).
   - В поле "Правообладатель" ищи ФИО человека, о котором справка.
   - Если ФИО совпадает (даже частично) с именем того, кто заказал справку - это его собственность/доля.
   - Если собственность "Долевая" - укажи долю (например "1/2").

3️⃣ ФИЛЬТР - ТОЛЬКО ДЕЙСТВИТЕЛЬНЫЕ ОБЪЕКТЫ (АКТИВНЫЕ)
   Мы ищем ТОЛЬКО то имущество, которое принадлежит человеку СЕЙЧАС.

   КАК ПОНЯТЬ, ЧТО ОБЪЕКТ АКТИВЕН (ВКЛЮЧАТЬ):
   - Поле "Дата прекращения права" (или "сведения о прекращении") ПУСТОЕ.
   - Или написано "данные отсутствуют", "не зарегистрировано", "нет", "-".
   - Или написано "Право на недвижимость действующее".

   КАК ПОНЯТЬ, ЧТО ОБЪЕКТ ПРОДАН (НЕ ВКЛЮЧАТЬ):
   - В поле "Дата прекращения права" стоит КОНКРЕТНАЯ ДАТА (например "01.01.2023").
   - В поле "Сведения о прекращении" есть запись о переходе права.

   !!! В JSON ВКЛЮЧАЙ ТОЛЬКО АКТИВНЫЕ ОБЪЕКТЫ !!!
   Если объект продан - ПРОПУСТИ ЕГО.

4️⃣ ДАННЫЕ ОБЪЕКТА
   - Вид объекта: "Квартира", "Земельный участок", "Жилой дом", "Помещение" (если жилое - пиши Квартира).
   - Адрес (местоположение): Полный адрес из поля "Адрес (местоположение)" или "Местоположение".
   - Площадь: Число + единица измерения (кв.м). Если "Уточненная площадь" — бери её.
   - Кадастровая стоимость: Сумма в рублях.
   - Право:
     * Найди раздел "Сведения о зарегистрированных правах".
     * Вид права: "Собственность" (иди "Долевая собственность").
     * Дата и номер регистрации.
     * Если есть запись о "прекращении права" или "переходе права" с более поздней датой — значит объект ПРОДАН/ПЕРЕДАН.
   - Обременения: Проверь раздел "Ограничение прав и обременение объекта недвижимости". Если "не зарегистрировано" или "Данные отсутствуют" — значит обременений нет.

ПРИМЕР ВЫПИСКИ С ГОСУСЛУГ (Сведения...):
Заголовок: "Сведения из Единого государственного реестра недвижимости"
Вид объекта: Земельный участок
Кадастровый номер: 45:08:030801:366
Адрес: Курганская обл, ...
Площадь: 2000 +/- 31.0
...
Сведения о правах: Собственность, № ... от ...

ЭТО ПОЛНОЦЕННЫЙ ОБЪЕКТ! Извлеки его!

РЕЗУЛЬТАТ: {"Есть_недвижимость": true, "Объекты": [{...}]}

УВЕДОМЛЕНИЕ ОБ ОТСУТСТВИИ (имущества НЕТ):
- Точно написано "отсутствует запрошенная Вами информация" или "в ЕГРН отсутствуют сведения".
- Таблицы с характеристиками (площадь, стоимость) ПУСТЫЕ или отсутствуют.
- Только в этом случае: {"Есть_недвижимость": false, "Причина": "Уведомление об отсутствии"}

КРИТИЧЕСКИ ВАЖНО - МНОЖЕСТВЕННЫЕ ОБЪЕКТЫ:
- В ОДНОЙ выписке может быть НЕСКОЛЬКО объектов недвижимости (несколько квартир, участков и т.д.)
- Если 1 участок, 1 дом, 3 квартиры - извлеки ВСЕ 5 объектов.
   - КАЖДЫЙ объект должен быть в JSON-массиве "Объекты".

!!! ПРОВЕРКА - КЛЮЧИ ДОКУМЕНТА (ВАРИАНТ ГОСУСЛУГИ) !!!
   НаГосуслугах часто выписка "Сведения из ЕГРН" содержит разделы:
   
   [Таблица 1]: "Сведения об основных характеристиках объекта недвижимости"
   - Вид объекта недвижимости
   - Кадастровый номер
   - Площадь
   - Кадастровая стоимость
   - Адрес

   [Таблица 2]: "Сведения о зарегистрированных правах"
   1.1 Право собственности
      - Правообладатель: (Фамилия И. О.)
      - Номер регистрации: 12-12-12...
      - Дата регистрации: 25.10.2016
      - Дата, номер и основание государственной регистрации перехода (прекращения) права: "данные отсутствуют"
      ==> Это АКТИВНЫЙ собственник, извлеки его!
   
   1.2 Право собственности ...
      - Дата перехода (прекращения) ... : 01.01.2023
      ==> ЭТО ПРОДАННЫЙ ОБЪЕКТ - НЕ ВЫВОДИ ЕГО!

РЕЗУЛЬТАТ: {"Есть_недвижимость": true, "Объекты": [{...}]}

СТРУКТУРА ВЫПИСКИ ЕГРН (ДВА ВАРИАНТА):

ВАРИАНТ А: СТАНДАРТНАЯ ВЫПИСКА РОСРЕЕСТРА (Разделы 1, 2, ...):
- Раздел 1: "Сведения об основных характеристиках объекта недвижимости" (Характеристики)
- Раздел 2: "Сведения о зарегистрированных правах" (Правообладатели)
- Раздел 4/5: "Ограничение прав и обременение объекта недвижимости"

ВАРИАНТ Б: ВЫПИСКА ГОСУСЛУГИ ("Сведения из ЕГРН"):
- Сначала идет "Сведения об основных характеристиках объекта" (Таблица без номера).
- Затем "Сведения о зарегистрированных правах", где записи нумеруются: 1.1, 1.2, 1.3...
  * 1.1 — обычно текущий собственник (или одна из записей).
  * 1.2, 1.3 — могут быть предыдущие собственники.
- В ЭТОМ СЛУЧАЕ: Проверяй КАЖДУЮ запись (1.1, 1.2, 1.3)!
  - Если запись 1.1 содержит "Право на недвижимость действующее" — это АКТИВНЫЙ собственник! Бери его!
  - Если запись 1.2 содержит "Дата... прекращения... 03.09.2025" — это ИСТОРИЯ (прошлый собственник).

КРИТИЧЕСКИ ВАЖНО - АНАЛИЗ ПРАВ В ГОСУСЛУГАХ:
Если видишь нумерацию 1.1, 1.2, 1.3 внутри "Сведения о правах":
1. Просмотри ВСЕ пункты.
2. Найди тот, где "Дата государственной регистрации прекращения права" = "Данные отсутствуют" ИЛИ написано "Право на недвижимость действующее".
   -> ЭТО ТЕКУЩИЙ СОБСТВЕННИК. Объект принадлежит ему.
3. Если текущего собственника (действующее право) нет — значит объекта нет (или продан).
4. Если текущий собственник = {debtor_fio} (или его супруг/супруга) — ВЫВОДИ ЭТОТ ОБЪЕКТ КАК ИМУЩЕСТВО!

КРИТИЧЕСКИ ВАЖНО - ПРОДАННОЕ ИМУЩЕСТВО:
⚠️ ВАЖНОЕ ИЗМЕНЕНИЕ: Проданные объекты ВКЛЮЧАЙ в массив "Объекты"!
- Ищи строку "Дата государственной регистрации прекращения права" (или "Дата, номер и основание... перехода (прекращения) права") в разделе X.2
- Проверяй статус:
  * Если "данные отсутствуют", "нет", "Право на недвижимость действующее" → имущество В СОБСТВЕННОСТИ
  * Если там ТОЛЬКО дата (например "02.02.2023") без слов "действующее" → имущество ПРОДАНО
- **ВАЖНО**: Проданные объекты ВКЛЮЧАЙ в массив "Объекты" с правообладателями!
- Правообладатель проданного объекта должен иметь "Дата_прекращения_права": "02.02.2023"
- Это нужно для извлечения информации о сделках

ПРИМЕР проданного объекта:
{
  "Объект": {
    "Вид": "Помещение",
    "Кадастровый_номер": "74:36:0212004:1852",
    "Адрес": "г. Челябинск, ул. Салютная, д. 23, кв. 137",
    "Площадь": "39.5"
  },
  "Правообладатели": [{
    "ФИО": "Маликова Валентина Юрьевна",
    "Дата_регистрации": "18.02.2021",
    "Дата_прекращения_права": "02.02.2023"  ← КЛЮЧЕВОЕ: Дата продажи
  }]
}

ВАЖНО: Показывай только АКТУАЛЬНЫХ правообладателей (ДЕЙСТВУЮЩЕЕ ПРАВО)!
- Если в разделе есть "Дата прекращения права" (и она не пустая) - ЗНАЧИТ ОБЪЕКТ ПРОДАН. НЕ ВКЛЮЧАЙ ЕГО В ОБЪЕКТЫ!
- Включай только записи где "Дата..." = "данные отсутствуют", "нет", "-" или есть пометка "действующее".

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО, адреса с заглавной).

РАБОТА С АДРЕСАМИ ОБЪЕКТОВ:
- Адрес недвижимости может быть размыт или написан ЗАГЛАВНЫМИ БУКВАМИ - это нормально!
- Ищи компоненты: регион/область, город/населенный пункт, улица, дом, корпус, квартира/помещение
- Ключевые слова: "УЛ.", "Д.", "ДОМ", "КОРП.", "СТРОЕНИЕ", "СТР.", "КВ.", "ПОМ.", "ОФИС"
- Формат адреса: "г. Челябинск, ул. Красномолотова, д. 65, корп. 1, кв. 35"
- Даже если текст размыт - разбирай по частям: город + улица минимум
- Числа (номера домов, квартир) обычно читаются лучше названий улиц
- Приводи CAPS LOCK к нормальному виду: "г. Челябинск", а не "Г. ЧЕЛЯБИНСК"
- НЕ оставляй "Адрес" пустым (null) если хотя бы город и улица читаются!

ОСОБЕННОСТИ РАСПОЗНАВАНИЯ:
- ВСЕГДА ищи кадастровый номер - он обычно в начале документа в формате XX:XX:XXXXXXX:XXXX
- Если документ многостраничный - проверь ВСЕ страницы на наличие объектов
- В одной выписке может быть НЕСКОЛЬКО объектов - извлекай все
- Площадь может быть указана как "общая", "жилая", "земли" - берм общую
- Стоимость ищи в разделах "Кадастровая стоимость" или "Рыночная стоимость"
- Если видишь таблицу с несколькими правообладателями - извлеки всех с их долями

ИЗВЛЕЧЕНИЕ СДЕЛОК - КРИТИЧЕСКИ ВАЖНО

ДВА ТИПА СДЕЛОК:

1. СДЕЛКА ПРОДАЖИ (должник продал имущество):
   Смотри на поле "дата государственной регистрации прекращения права":
   - Если стоит ДАТА (например "02.02.2023") → владелец ПРОДАЛ имущество в эту дату
   - Если "данные отсутствуют" → владелец ДО СИХ ПОР владеет
   
2. СДЕЛКА ПОКУПКИ (должник купил имущество):
   Смотри на поле "дата государственной регистрации" (дата когда купил):
   - Если дата в последние 3 года (с 2023 по 2025) → это сделка покупки
   - Например: "дата государственной регистрации: 28.02.2023" → купил 28.02.2023

ВАЖНО: Извлекай ОБА типа сделок (и покупку, и продажу) за последние 3 года!

Пример 1 (СДЕЛКА ПРОДАЖИ):
```
    - "дата государственной регистрации прекращения права: 02.02.2023" ← ПРОДАЖА!
```

Пример 2 (ТОЛЬКО ПОКУПКА):
```
    - "дата государственной регистрации: 28.02.2023" ← ПОКУПКА!
```
→ Одна сделка: Есть_сделка=true, Тип_сделки="покупка", Дата_сделки="28.02.2023", Покупатель="Маликова В.Ю."

ЧТО ИЗВЛЕКАТЬ ИЗ ВЫПИСКИ:
- Раздел 1.1: Кадастровый номер (ОБЯЗАТЕЛЬНО!), номер квартала, вид объекта (жильё, земельный участок, помещение и т.п.), адрес, площадь, назначение, количество этажей, материал стен, год постройки, кадастровая стоимость.
- Раздел 1.2: Сведения о правообладателях:
  * ФИО/организация
  * Доля в праве (например: "1/2", "1/1")
  * Тип собственности (индивидуальная собственность/общедолевая собственность/общая совместная собственность)
  * Вид права (например: "собственность", "постоянное пользование")
  * "Основание государственной регистрации" - ОБЯЗАТЕЛЬНО извлеки полностью! (например: "Договор купли-продажи от 15.08.2023 №123", "Договор дарения от 20.01.2020")
  * Дата государственной регистрации права
  * Дата государственной регистрации прекращения права
- Раздел 1.3: Если есть блок "Ограничения/обременения" — перенеси ТОЛЬКО действующие обременения (игнорируй "погашено", "прекращено").
- СДЕЛКА: если видишь смену собственников, переход права — извлеки дату, стороны сделки, основание (купля-продажа, дарение и т.д.).
При отсутствии распознанного текста ориентируйся на таблицы: первая колонка название реквизита, вторая значение.

Если тебе попалась выписка ЕГРН не попадающая под пункты, то все равно извлекай имущество, главное извлечь имущество!

JSON ДЛЯ ВЫПИСКИ С ИМУЩЕСТВОМ (ОБРАТИ ВНИМАНИЕ: "Объекты" - это МАССИВ!):
{
        "Есть_недвижимость": true,
        "Объекты": [
                {
                        "Кадастровый_номер": "...",
                        "Кадастровый_квартал": "...",
                        "Объект": {
                                "Вид": "...",
                                "Адрес": "...",
                                "Площадь": "...",
                                "Назначение": "...",
                                "Этажность": "...",
                                "Материал_стен": "...",
                                "Год_постройки": "..."
                        },
                        "Кадастровая_стоимость": "...",
                        "Правообладатели": [
                                {
                                        "ФИО": "...",
                                        "Доля": "...",
                                        "Тип_собственности": "индивидуальная собственность/общедолевая собственность/общая совместная собственность",
                                        "Вид_права": "...",
                                        "Документ_основание": "ПОЛНЫЙ текст из 'Основание государственной регистрации' (например: 'Договор купли-продажи от 15.08.2023 №123')",
                                        "Дата_регистрации": "ДД.ММ.ГГГГ",
                                        "Дата_прекращения_права": "данные отсутствуют/ДД.ММ.ГГГГ",
                                        "Является_текущим_владельцем": true/false
                                }
                        ],
                        "Обременения": [
                                {
                                        "Вид": "Ипотека в силу закона/Залог/Арест/...",
                                        "Залогодержатель": "ПАО Сбербанк/...",
                                        "Номер_регистрации": "74:36:0212004:1852-74/108/2023-26",
                                        "Дата_регистрации": "ДД.ММ.ГГГГ",
                                        "Дата_прекращения": "данные отсутствуют/ДД.ММ.ГГГГ",
                                        "Погашено": false/true
                                }
                        ],
                        "Сделка": {
                                "Есть_сделка": true/false,
                                "Тип_сделки": "купля-продажа/дарение/наследование/другое",
                                "Дата_сделки": "ДД.ММ.ГГГГ",
                                "Предыдущий_собственник": "ФИО того, у кого Дата_прекращения_права стоит датой",
                                "Новый_собственник": "ФИО того, у кого Дата_прекращения_права = данные отсутствуют",
                                "Документ_основание": "Договор купли-продажи от ДД.ММ.ГГГГ №
                        "Сделка": {
                                "Есть_сделка": true/false,
                                "Тип_сделки": "купля-продажа/дарение/наследование/другое",
                                "Дата_сделки": "ДД.ММ.ГГГГ",
                                "Предыдущий_собственник": "...",
                                "Новый_собственник": "...",
                              (раздел 1.3):
- Ищи раздел 1.3 "Ограничение права и обременение объекта недвижимости"
- Каждое обременение имеет подраздел (1.3.1, 1.3.2 и т.д.) с полями:
  * "Вид" - тип обременения (Ипотека в силу закона, Залог, Арест, Аренда и т.д.)
  * "Номер государственной регистрации" - номер записи (например: "74:36:0212004:1852-74/108/2023-26")
  * "Лицо, в пользу которого установлено ограничение" - залогодержатель/банк
  * "Дата государственной регистрации прекращения" - если есть дата → обременение погашено
- Типы обременений: "Ипотека в силу закона", "Ипотека", "Залог", "Арест", "Запрет на сделки", "Аренда"
- Залогодержатель/банк может быть указан: "АО «Челябинвестбанк»", "ПАО Сбербанк", "Банк ВТБ", "Т-Банк"
- Если "Дата прекращения" стоит → обременение ПОГАШЕНО, не включай в список
- ЕслОбъекты" включай ТОЛЬКО те объекты, где у правообладателей "Дата_прекращения_права" = "данные отсутствуют"
- Если у правообладателя "Дата_прекращения_права" стоит ДАТА (например "02.02.2023") → имущество ПРОДАНО:
  * НЕ включай этот объект в массив "Объекты"
  * ОБЯЗАТЕЛЬНО создай отдельный элемент с "Сделка" для этого проданного объекта
  * В "Сделка" укажи: "Есть_сделка": true
  * Дата_сделки = точная дата из "Дата_прекращения_права" (например "02.02.2023")
  * Предыдущий_собственник = ФИО того, у кого право прекратилось (из раздела 1.2)
  * Новый_собственник = ФИО следующего владельца (если есть раздел 1.2.2 или 1.2.3)
  * Если видишь раздел "1.2" с датой прекращения права И раздел "1.2.1" или "1.2.2" без даты прекращения - это точно продажа!
- В "Обременения" включай ТОЛЬКО те, где "Дата_прекращения" = "данные отсутствуют" (действующие)
- Извлекай ВСЕ сделки за последние 3 года (с 2023 года)
- ВНИМАНИЕ: Если в разделе 1.2 видишь подраздел с "дата государственной регистрации прекращения права" - ЭТО СДЕЛКА!
- Залогодержатель/банк может быть указан полным названием: "АО «Челябинвестбанк»", "ПАО Сбербанк"
- Может быть указан кратко: "Банк ВТБ", "Т-Банк", "Альфа-Банк"
- Обращай внимание на слова: "залогодержатель", "кредитор", "банк", "в пользу"
- Если обременение "погашено" или "прекращено" - укажи в поле "Погашено": true
- В каждом обременении ОБЯЗАТЕЛЬНО указывай залогодержателя/кредитора если он упоминается

КРИТИЧЕСКИ ВАЖНО:
- Если НЕТ кадастрового номера или написано "сведения отсутствуют" / "отсутствует запрошенная информация" — верни:
  {
    "Есть_недвижимость": false,
    "Причина": "Уведомление об отсутствии сведений в ЕГРН"
  }
- В "Правообладатели" и "Обременения" включай ТОЛЬКО актуальные (действующие) записи
- Если видишь переход права собственности — ОБЯЗАТЕЛЬНО заполни "Сделка" с "Есть_сделка": true и датой!
- Извлекай дату сделки из документа-основания: "Договор купли-продажи от 15.08.2023" → "15.08.2023"
- Поля без данных = null, списки допускают []
- Комментарии не добавляй"""
        },
        "пенсии": {
            "keywords": ["справка сфр", "назначенных пенсиях", "ежемесячной денежной выплаты", "едв", "пенсия", "о назначенных пенсиях", "социальных выплатах", "размер пенсии", "пенсионный фонд", "пфр", "справка сфр о назначенных пенсиях", "справка сфр", "справка о размере пенсии и выплатах СФР на текущую дату", "справка о размере пенсии и выплатах СФР", "справка о назначенных пенсиях и социальных выплатах", "пенсии и социальные выплаты", "пенсии и выплаты", "пенсии и социальные выплаты", "пенсии и выплаты", "пенсии и социальные выплаты", "пенсии и выплаты", "справка о выплатах", "сфр", "выплатах за период", "ежемесячная денежная"],
            "prompt": """Справка СФР о назначенных пенсиях и социальных выплатах.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (названия с заглавной).

🔴 ВНИМАНИЕ - СЛОЖНАЯ ЛОГИКА СТАТУСА:
1. Если прямо написано "Сведения о назначенных пенсиях отсутствуют" или "На учете не состоит" — значит выплат НЕТ. Верни пустой список.
2. Фраза "Не являлся(ется) получателем пенсии" означает, что нет именно ПЕНСИИ. 
   🔴 КРИТИЧЕСКОЕ ПРАВИЛО ДЛЯ ВЫПЛАТ ПО УХОДУ:
   Если в тексте (даже внутри предложения, начинающегося с "Не являлся...") встречаются слова:
   - "лицом, осуществляющим уход за нетрудоспособным гражданином"
   - "лицом, осуществляющим уход за ребенком-инвалидом"
   - "лиц, осуществляющих уход"
   
   ТЫ ОБЯЗАН СЧИТАТЬ ЭТО ПОДТВЕРЖДЕНИЕМ ВЫПЛАТЫ!
   Игнорируй отрицание "Не являлся" для этих фраз. В справках СФР это стандартная кривая формулировка, которая на самом деле означает: "Пенсии нет, НО ЯВЛЯЕТСЯ ЛИЦОМ, ОСУЩЕСТВЛЯЮЩИМ УХОД".
   
   ОБЯЗАТЕЛЬНО добавь в список "Выплаты" запись "Выплата по уходу за нетрудоспособным гражданином/ребенком-инвалидом" и сформируй статус: "Является получателем выплаты по уходу...".
   
3. ВНИМАТЕЛЬНО ищи слова "Установлена", "Назначена", "Является получателем".
   - Если видишь "Ежемесячная выплата лицам, осуществляющим уход за детьми-инвалидами" или подобное — ЭТО ВЫПЛАТА!
   - Если видишь перечисление "Не являлся... получателем... [длинный список]" и НЕТ упоминая про уход (см. пункт 2) и НЕТ слов "Установлена/Назначена" — тогда выплат нет.

Определи (если выплаты НАЙДЕНЫ):
- СНИЛС, ФИО, дату рождения (в шапке рядом с номером дела).
- Орган, выдавший справку, номер и дату.
- Перечень ВСЕХ назначенных выплат: название (например "Страховая пенсия", "Выплата по уходу за ребенком-инвалидом"), основание, период, РАЗМЕР (сумма), статус НСУ.
- Размер ежемесячной денежной выплаты (ЕДВ).
- Сформируй ТЕКСТОВОЕ ОПИСАНИЕ СТАТУСА (поле "Текст_статуса"):
  - Пример: "Является получателем ежемесячной выплаты по уходу за ребенком-инвалидом. Получателем пенсии не является."
  - Если ничего нет: "Получателем пенсий, пособий не является."
  - ИГНОРИРУЙ технические надбавки и перерасчеты (индексации, повышения фиксированной выплаты). Пиши только БАЗОВЫЙ вид пенсии.
  - Точку не забывай на конце.
  - Укажи только основное, не нужно перечислять все.
    Пример: В документе есть "Установлена страховая пенсия: по инвалидности" - включаем. "Установлено повышение фиксированной выплаты к страховой пенсии" - не включаем. "Установлена(но) Ежемесячная денежная выплата ветеранам, инвалидам и гражданам, подвергшимся воздействию радиации" - включаем.

JSON:
{
        "ФИО": "...",
        "СНИЛС": "...",
        "Дата_рождения": "ДД.ММ.ГГГГ",
        "Номер_справки": "...",
        "Дата_справки": "ДД.ММ.ГГГГ",
        "Орган": "...",
        "Текст_статуса": "...",  // Сгенерированное описание БЕЗ СУММ
        "Выплаты": [
                {
                        "Вид": "...",
                        "Основание": "...",
                        "Период": {
                                "С": "ДД.ММ.ГГГГ",
                                "По": "ДД.ММ.ГГГГ"/"бессрочно"
                        },
                        "Размер": "...",
                        "НСУ": {
                                "Лекарства": "...",
                                "Санаторий": "...",
                                "Проезд": "..."
                        }
                }
        ],
        "ЕДВ": "...",
        "Основание_выдачи": "..."
}
Если поля отсутствуют — null. Комментарии не добавляй.""",
        },
        "отчет_окб": {
            "keywords": ["кредитный отчет", "окб", "объединенное кредитное бюро", "объединенного кредитного бюро", "кредитная история", "отчет окб", "бюро кредитных историй"],
            "prompt": """Ты помощник для юриста, твоя задача собрать действующие кредиты для банкротства.

СТРУКТУРА ОТЧЕТА ОКБ:
1. Краткая таблица "ДЕЙСТВУЮЩИЕ КРЕДИТНЫЕ ДОГОВОРЫ" (в начале).
2. Детальные блоки по каждому кредиту (далее в документе).

Найди в начале документа (Персональные данные):
- Адрес регистрации (прописка).
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

ИЗВЛЕКАЙ ДАННЫЕ ПО КРЕДИТАМ ТОЛЬКО ИЗ ДЕТАЛЬНЫХ БЛОКОВ! Краткую таблицу используй только для навигации.
ВНИМАНИЕ: Извлекай АБСОЛЮТНО ВСЕ текущие кредиты, не пропускай ни одного блока! Если блок видишь - извлекай.

АЛГОРИТМ ДЛЯ КАЖДОГО КРЕДИТА (БЛОКА):

1. **Кредитор**: Название банка/МФО в заголовке блока (например, "1. БАНК ГПБ АО..."). Нормализуй название (ГПБ -> АО «ГПБ», Сбербанк -> ПАО «Сбербанк»).
2. **Вид**: Тип кредита из заголовка (Кредитная карта, Потребительский кредит, Овердрафт). "Вид займа (кредита)" - бери это если "Тип кредитной линии" стоит "-", если в "Тип кредитной линии" что-то стоит, то бери от туда минуя "Вид займа (кредита)".
3. **Дата_сделки**: 
   - Ищи подраздел "Общие сведения о сделке".
   - Поле: "Дата совершения сделки" (например, 16 сентября 2024).
   - НЕ путай с датой обновления информации!
4. **Сумма_обязательства** (Сколько БРАЛИ):
   - Ищи подраздел "Общие сведения о сделке".
   - Поле: "Сумма и валюта обязательства" (например, 600 000 р.) Бери самую первую сумму!.
5. **Сумма** (Текущий долг/Сколько ДОЛЖНЫ):
   - Ищи подраздел "Сведения о сумме задолженности".
   - Внутри таблица. Нам нужна строка "Общая" (или первая строка с цифрами).
   - Столбец "Всего". Это и есть полная сумма долга (тело + проценты).
   - Пример: Если в строке "Общая" столбце "Всего" стоит "517 346,28 р." — бери это число.

ПРИМЕР РАЗБОРА ИЗ СКРИНШОТА:
- Блок: БАНК ГПБ АО (Кредит «овердрафт»)
- Дата совершения сделки: 16 сентября 2024 -> "16.09.2024"
- Сумма и валюта обязательства: 600 000 р. -> 600000 (Это Сумма_обязательства)
- Таблица "Сведения о сумме задолженности":
  Строка "Общая" -> Столбец "Всего" -> 517 346,28 р. -> 517346.28 (Это Сумма - текущий долг!)
  (Не бери строку "Срочная" для суммы, бери "Общая"!)

ВАЖНО:
- Если кредит закрыт (статус "Закрыт", "Счет закрыт") — ИГНОРИРУЙ его.
- Ищи 10-значный ИНН кредитора в блоке "Сведения об источнике" (ИНН или ИНН/ОГРН).

JSON:
{{
  "Адрес_регистрации": "...",
  "ИНН_должника": "null",
  "Кредиты": [
    {{
      "Кредитор": "...",
      "ИНН_кредитора": "...",
      "Вид": "...",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": 0.00,
      "Сумма": 0.00
    }}
  ],
  "Судебные_акты": []
}}""",
        },
        "отчет_бки": {
            "keywords": ["скоринг", "индивидуальный рейтинг", "кредитная история", "бюро кредитных историй", "кредитный рейтинг", "отчет бки", "скоринговый балл"],
            "prompt": """Ты помощник юриста. Обработай отчет Скоринг Бюро (БКИ).

В конце отчета (Персональные данные) найди:
- Адрес регистрации (место жительства, прописка).
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!
Ищи таблицу "Действующие кредиты, займы, карты". Мы берем данные оттуда.

НОРМАЛИЗАЦИЯ БАНКОВ:
- "ГПБ (ОАО)", "ГПБ (АО)", "Газпромбанк" -> "АО «ГПБ»"
- "Сбербанк" -> "ПАО «Сбербанк»"
- "ВТБ" -> "Банк ВТБ (ПАО)"
- "Коллекторы: ООО Профессиональная Коллекторская Организация «Интел Коллект» (ранее - ООО «Интел коллект») Коллект» (ранее - ООО «Интел коллект»)" -> "ООО ПКО «Интел Коллект»"
    То есть убрать "Коллекторы:", убрать "(ранее - ...)", оставить только "ООО ПКО «Интел Коллект»"

ДЛЯ КАЖДОЙ СТРОКИ ТАБЛИЦЫ:
1. **Кредитор**: Название банка (колонка 1). Нормализуй!
2. **Вид**: Тип обязательства (под названием банка, например "Овердрафт", "Ипотека").
3. **Дата_сделки**: Колонка "Дата начала обязательства".
4. **Сумма_обязательства** (Сколько взяли): Колонка "Сумма / лимит обязательства".
5. **Сумма** (Текущий долг):
   - В СКРИНШОТЕ есть колонки: "Срочный основной долг", "Текущая просрочка", "Общая задолженность".
   - ПРИОРИТЕТ 1: Если есть колонка "Общая" (в блоке задолженность) или "Текущая задолженность" с полной суммой — бери её.
   - ПРИОРИТЕТ 2 (как на фото):
     Сумма = "Срочный основной долг" + "Текущая просрочка (осн.долг, %, пени...)".
   - В примере со скриншота:
     Банк ГПБ (ОАО) от 16.09.2024.
     Срочный осн. долг = 473 797.03
     Текущая просрочка = отсутствует (0)
     ИТОГО Сумма = 473 797.03
   - Другой пример (с просрочкой):
     Срочный долг = 813 242.73
     Просрочка = 29 763.00
     ИТОГО Сумма = 813242.73 + 29763.00 = 843005.73

ВАЖНО:
- Игнорируй закрытые кредиты.
- Если в колонке "Сумма / лимит обязательства" стоит 0 (для овердрафта бывает), но есть текущий долг — бери долг как есть.

JSON:
{{
  "Адрес_регистрации": "...",
  "Кредиты": [
    {{
      "Кредитор": "...",
      "Вид": "...",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": 0.00,
      "Сумма": 0.00
    }}
  ]
}}""",
        },
        "отчет_нбки": {
            "keywords": [],
            #"keywords": ["нбки отчет", "отчет нбки", "национальное бюро кредитных историй", "кредитный отчет для субъекта", "кредитная история нбки", "nbch", "nbki"],
            "prompt": """

ИНСТРУКЦИЯ ПО ОБРАБОТКЕ ЧАСТЕЙ ДОКУМЕНТА (БАТЧИ):
1. Если видишь таблицу или список кредитов без явного заголовка "Обязательства и их исполнение" — ЭТО ПРОДОЛЖЕНИЕ! ОБЯЗАТЕЛЬНО ИЗВЛЕКАЙ ВСЕ КРЕДИТЫ С ЭТОЙ СТРАНИЦЫ!
2. Не пропускай кредиты только потому, что не нашел заголовок раздела. Ищи структуру кредитных записей.

В начале документа (Субъект кредитной истории):
- Адрес регистрации (место жительства, адрес, прописка), самая первая запись о регистрации актуальная, бери её.
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

Найди раздел с заголовком "Обязательства и их исполнение" - там начинается список кредиторов, а заканчивается перед разделом с заголовком "Сведения о долговой нагрузке заемщика без заключенного потребительского займа".

Нормализуй название банка, МФК, МКК.
ПРИМЕРЫ ОБЯЗАТЕЛЬНОЙ НОРМАЛИЗАЦИИ:
- "Сбербанк" -> "ПАО «Сбербанк»"
- "ПСБ", "Промсвязьбанк" -> "ПАО «Промсвязьбанк»"
- "ГПБ", "Газпромбанк", "Ф-Л ГПБ (АО)", "ГПБ (АО)" -> "АО «ГПБ»"
- "ВТБ" -> "Банк ВТБ (ПАО)"
- "Альфа-Банк", "АЛЬФА-БАНК" -> "АО «Альфа-Банк»"
- "Тинькофф", "Т-Банк" -> "АО «Т-Банк»"
- "Совкомбанк" -> "ПАО «Совкомбанк»"

ВАЖНО - ИГНОРИРУЙ ПОГАШЕННЫЕ КРЕДИТЫ:
- Если в столбце "Прекращение обязательства" написано "Надлежащее исполнение обязательства" — кредит ПОГАШЕН, НЕ ВКЛЮЧАЙ его в список! А если стоит "Н/Д" — кредит ДЕЙСТВУЮЩИЙ, ВКЛЮЧАЙ его!
- Если обязательство прекращено — пропускай этот кредит!
- Если в столбце "Состояние" стоит "Статус: Счет закрыт" - кредит ПОГАШЕН, НЕ ВКЛЮЧАЙ его в список!
- Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!

Для КАЖДОГО ДЕЙСТВУЮЩЕГО кредитора:
1. Название банка/кредитора (заголовок раздела)
2. В таблице "Основные сведения об обязательстве":
   - Столбец "Сумма и валюта обязательства" = первоначальная сумма займа (Сумма_обязательства)
   - Столбец "Дата возникновения обязательства" = дата сделки (Дата_сделки)
   - Столбец "ОГРН/ИНН" - там два числа через пробел. ПЕРВОЕ (13 цифр) - это ОГРН, НЕ НУЖНО. ВТОРОЕ (10 цифр) - это ИНН, ОБЯЗАТЕЛЬНО извлеки!
   - Тип кредита/займа (из столбца "Вид займа (кредита)" или аналогичной: Потребительский кредит, Кредитная карта, Микрозайм, Ипотека, Автокредит, и т.д.)
3. В таблице "Задолженность":
   - Найди САМУЮ ПОСЛЕДНЮЮ строку в таблице
   - Возьми значение из столбца "Сумма" (это текущий долг)
   - ИГНОРИРУЙ другие столбцы ("Сумма по осн.долгу", "Сумма по %%", и т.д.)

КРИТИЧЕСКИ ВАЖНО ПРО ИЗВЛЕЧЕНИЕ СПИСКА КРЕДИТОВ:
- ЗАПРЕЩЕНО ОБЪЕДИНЯТЬ КРЕДИТЫ ПО НАЗВАНИЮ БАНКА или ТИПУ!
- Один блок в таблице (один заголовок "Субъект", "Сделка" или номер по порядку) = ОДИН отдельный JSON-объект.
- Если в отчете 5 разных записей от "АО «Т-Банк»" (например, 2 кредитные карты, 1 кредит наличными, 2 ипотеки) - верни РОВНО 5 объектов JSON!
- Если в отчете 8 записей от "АО «ГПБ»" (например, 3 Овердрафта и 5 Потребительских) - верни РОВНО 8 объектов JSON!
- СМОТРИ НА ТИП КРЕДИТА (Овердрафт, Кредитная линия и т.д.). Разные типы - это РАЗНЫЕ кредиты.
- ДАЖЕ ЕСЛИ ТИП КРЕДИТА СОВПАДАЕТ (например, два "Потребительский кредит" от одного банка) - ЭТО РАЗНЫЕ КРЕДИТЫ, если у них разные даты, суммы или счета.
- ДАЖЕ ЕСЛИ СУММЫ ПОХОЖИ - ЭТО РАЗНЫЕ КРЕДИТЫ.
- НИКОГДА НЕ ГРУППИРУЙ. ЛУЧШЕ ДУБЛИ, ЧЕМ ПОТЕРЯ ДАННЫХ.
- КАЖДАЯ СТРОКА ТАБЛИЦЫ "Сведения о счете" ИЛИ КАЖДЫЙ БЛОК ИНФОРМАЦИИ О КРЕДИТЕ ДОЛЖЕН СТАТЬ ОТДЕЛЬНЫМ ЭЛЕМЕНТОМ МАССИВА "Кредиты".

КРИТИЧЕСКИ ВАЖНО ПРО ИЗВЛЕЧЕНИЕ ДАННЫХ:
- Берем ТОЛЬКО последнюю(нижнюю) строку таблицы "Задолженность"
- Берем ТОЛЬКО столбец "Сумма" из этой строки
- Это и есть текущий долг
- НЕ суммируем ничего, просто берем одно значение!
- В столбце "ОГРН/ИНН" берем ВТОРОЕ число (10 цифр) - это ИНН!
- Если наткнешься на раздел с заголовком "Сведения о долговой нагрузке заемщика без заключенного потребительского займа" — прекращай сбор данных!
- Точно перепиши суммы и даты для каждого кредита, чтобы они не перепутались.
- Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!

Нормализуй название банка, обрати внимание на список примеров выше. "ГПБ (АО)" -> "АО «ГПБ»".

ВЕРНИ ТОЛЬКО ВАЛИДНЫЙ JSON БЕЗ ДОПОЛНИТЕЛЬНОГО ТЕКСТА!
Никакого текста до или после JSON!
Только чистый JSON формат!

JSON (СТРОГО этот формат) - Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!:
{{
  "Адрес_регистрации": "...",
  "ИНН_должника": "Null",
  "Кредиты": [
    {{
      "Кредитор": "название (нормализованное)",
      "ИНН_кредитора": "ВТОРОЕ число из столбца ОГРН/ИНН (10 цифр)",
      "Вид": "тип кредита (Потребительский кредит/Кредитная карта/Микрозайм/Ипотека/Автокредит/другое)",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": "первоначальная сумма",
      "Сумма": "последняя строка таблицы Задолженность, столбец Сумма"
    }}
  ]
}}""",
        },
        "гибдд": {
            "keywords": ["гибдд", "гаи", "транспорт", "птс", "стс", "свидетельство о регистрации тс", "паспорт транспортного средства", "автомобил", "мрэо", "госавтоинспекция", "государственная инспекция безопасности дорожного движения", "транспортное средство", "регистрация тс", "мвд", "реестр транспортных средств", "сведения о транспортных средствах", "карточка учета", "vin"],
            "prompt": """Документ ГИБДД о транспортном средстве (ПТС, СТС, выписка из реестра, справка, сведения из реестра).

МЕСТОПОЛОЖЕНИЕ ДАННЫХ В ВЫПИСКЕ (Сведения из государственного реестра):
- Таблица "Сведения из государственного реестра транспортных средств"
1. Транспортное средство (марка, модель) -> Марка_модель
2. Год выпуска -> Год_выпуска
3. Идентификационный номер (VIN) -> VIN (БЕРИ ВСЕ 17 СИМВОЛОВ! Частая ошибка: не брать буквы в начале)
4. Номер кузова -> Номер_кузова
5. Государственный регистрационный номер (в таблице внизу, столбец "Государственный регистрационный номер") -> Гос_номер

⚠️ КРИТИЧЕСКИ ВАЖНО - МНОЖЕСТВЕННЫЕ ТРАНСПОРТНЫЕ СРЕДСТВА:
- В ОДНОМ документе (выписка из реестра) может быть НЕСКОЛЬКО автомобилей
- ОБЯЗАТЕЛЬНО найди ВСЕ транспортные средства в документе
- Если ТС есть, используй формат: {"Есть_транспорт": true, "Транспортные_средства": [авто1, авто2, ...]}
- Если несколько ТС - КАЖДОЕ извлекай в отдельный элемент массива

КРИТИЧЕСКИ ВАЖНО - СПРАВКА ОБ ОТСУТСТВИИ:
- Если написано "сведения отсутствуют", "в реестре отсутствуют", "не зарегистрировано" — это СПРАВКА ОБ ОТСУТСТВИИ транспорта
- В этом случае верни: {"Есть_транспорт": false, "Результат": "транспорт отсутствует", "Является_справкой_ГИБДД": true}
- НЕ извлекай дату справки, период, ФИО — просто верни что транспорта нет
- Пример текста: "В государственном реестре транспортных средств в период с 25.11.2022 по 25.11.2025, сведения отсутствуют"

ВНИМАНИЕ - ЕСЛИ ТРАНСПОРТ ЕСТЬ:
- Если это ДОГОВОР КУПЛИ-ПРОДАЖИ — машина УЖЕ ПРОДАНА! В "Особые_отметки" напиши "Продано по договору от [дата]"
- Если написано "снято с учета", "продажа", "передача" — ОБЯЗАТЕЛЬНО укажи в "Особые_отметки"
- ОБЯЗАТЕЛЬНО извлеки информацию о сделке, если она есть в документе!

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

ИНСТРУКЦИИ ПО ИЗВЛЕЧЕНИЮ ДАННЫХ ИЗ ПТС/СТС/Выписка с госуслуг:

1. МАРКА И МОДЕЛЬ:
   - В ПТС: "Марка, модель ТС"
   - Примеры: "ВАЗ 11183", "LADA KALINA", "Toyota Camry"
   - ОБЯЗАТЕЛЬНО ИЗВЛЕКИ! Даже если текст размыт - попробуй прочитать

2. VIN (ИДЕНТИФИКАЦИОННЫЙ НОМЕР):
   - В ПТС: "Идентификационный номер (VIN)" или просто "VIN"
   - 17 символов: буквы латинские + цифры
   - Примеры: "XTA11183080124632", "JHMCM56557C404453"
   - ОБЯЗАТЕЛЬНО ИЗВЛЕКИ! Это самое важное поле

3. ГОСУДАРСТВЕННЫЙ НОМЕР:
   - В СТС: регистрационный знак
   - Формат: буква + 3 цифры + 2 буквы + регион
   - Примеры: "А123БВ777", "M946MO174"
   - Извлекай даже если не полностью читается
   - В таблицах выписки часто в колонке "Государственный регистрационный номер"

4. ГОД ВЫПУСКА:
   - В ПТС: "Год изготовления ТС"
   - Формат: 4 цифры (например "2007", "2015")
   - ОБЯЗАТЕЛЬНО ИЗВЛЕКИ!

5. ТИП ТС:
   - В ПТС: "Наименование (тип ТС)"
   - Если не указано явно - определи по марке

6. ВИД СОБСТВЕННОСТИ:
   - В ПТС: "Документ на право собственности" (например "частная", "коммерческая"), извлеки только тип, но не номер докумета, например "Документ о праве наследования имущества"

7. РЕГИСТРАЦИОННОЕ ПОДРАЗДЕЛЕНИЕ:
   - В выписке ГИБДД: "Регистрационное подразделение"
   - Пример: "РОО ГИБДД ОМВД России по Коркинскому району"
   - Извлекай полное название подразделения

8. СТОИМОСТЬ ТРАНСПОРТНОГО СРЕДСТВА:
   - В выписке ГИБДД: "Стоимость транспортного средства"
   - Формат: число (например "88000", "250000")
   - Если указано в рублях - извлеки только число

9. СВЕДЕНИЯ О ЗАЛОГЕ:
   - Если указан залог/обременение - извлеки информацию
   - Укажи кредитора (банк или физлицо)
   - Если залога нет - оставь null

ВАЖНО:
- Даже если документ размыт/повернут - СТАРАЙСЯ извлечь хотя бы что-то!
- Если видишь только часть данных - извлекай что видишь, остальное ставь null
- НЕ возвращай пустой JSON если хоть что-то читается!

JSON ФОРМАТ:

Если транспорта НЕТ (справка об отсутствии):
{
    "Есть_транспорт": false,
    "Результат": "транспорт отсутствует",
    "Является_справкой_ГИБДД": true
}

Если транспорт ЕСТЬ (один или несколько):
{
    "Есть_транспорт": true,
    "Транспортные_средства": [
        {
            "Является_справкой_ГИБДД": true/false,
            "Тип_ТС": "легковой/грузовой/мотоцикл/...",
            "Марка_модель": "...",
            "VIN": "...",
            "Гос_номер": "...",
            "Год_выпуска": "ГГГГ",
            "Цвет": "...",
            "Номер_кузова": "...",
            "Вид_собственности": "частная/коммерческая/или текст из пункта 6",
            "Регистрационное_подразделение": "...",
            "Стоимость": "...",
            "Сведения_о_залоге": "...",
            "Собственник": {
                "ФИО": "...",
                "Адрес": "..."
            },
            "Документ": {
                "Тип": "ПТС/СТС/выписка/договор купли-продажи/справка об отсутствии",
                "Серия_номер": "...",
                "Дата_регистрации": "ДД.ММ.ГГГГ"
            },
            "Особые_отметки": "...",
            "Сделка": {
                "Есть_сделка": true/false,
                "Тип_сделки": "купля-продажа/дарение/другое",
                "Дата_сделки": "ДД.ММ.ГГГГ",
                "Покупатель_ФИО": "...",
                "Продавец_ФИО": "...",
                "Стоимость": 0.00
            }
        }
        // ... еще ТС если есть в документе
    ]
}
}

ВАЖНО О ПОЛЕ "Является_справкой_ГИБДД":
- true = это официальная справка ГИБДД о наличии/отсутствии ТС (выписка из реестра)
- false = это ПТС, СТС или договор купли-продажи (НЕ справка)

ВАЖНО: Если это договор купли-продажи ТС или есть информация о передаче права собственности:
- "Есть_сделка": true
- Заполни все поля "Сделка"
Если обычное СТС/ПТС без сделки:
- "Есть_сделка": false
- "Сделка" можно оставить null

Пустые значения = null. Без комментариев.""",
        },
        "учредитель": {
            "keywords": ["егрюл", "учредитель", "устав", "участник", "доля", "акционер", "выписка из егрюл"],
            "prompt": """Документ об учредительстве или участии в организации (выписка ЕГРЮЛ, устав, список акционеров, договор об учреждении).

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

Найди:
- Полное название организации, ИНН, ОГРН.
- Форма собственности (ООО, ОАО, ЗАО и т.д.).
- ФИО участника/учредителя.
- Размер доли/количество акций (в процентах или номинале).
- Номинальная стоимость доли/акций.
- Дата внесения записи/регистрации.
- Адрес регистрации организации.

JSON:
{
        "Организация": {
                "Название": "...",
                "ИНН": "...",
                "ОГРН": "...",
                "Форма": "ООО/ОАО/ЗАО/...",
                "Адрес": "..."
        },
        "Участник": {
                "ФИО": "...",
                "Доля": "...%/... акций",
                "Номинальная_стоимость": "... руб.",
                "Дата_регистрации": "ДД.ММ.ГГГГ"
        }
}
Если участников несколько — верни массив в поле "Участники". Пустые значения = null. Без комментариев.""",
        },
        "ценные_бумаги": {
            "keywords": ["депозитарий", "брокер", "счет депо", "ценные бумаги", "акции", "облигации", "паи", "инвестиционный счет"],
            "prompt": """Выписка из депозитария или брокерский отчет о ценных бумагах.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

Найди:
- Владелец (ФИО, паспорт или ИНН).
- Депозитарий/брокер, номер счета депо/брокерского счета.
- Дата формирования отчета/выписки, период.
- Перечень ценных бумаг: наименование эмитента, тип (акция/облигация/пай/...), количество, номинал, текущая стоимость, ISIN.
- Общая стоимость портфеля.

JSON:
{
        "Владелец": {
                "ФИО": "...",
                "Паспорт": "...",
                "ИНН": "..."
        },
        "Депозитарий": "...",
        "Номер_счета": "...",
        "Дата_выписки": "ДД.ММ.ГГГГ",
        "Период": "с ... по ...",
        "Ценные_бумаги": [
                {
                        "Эмитент": "...",
                        "Тип": "акция/облигация/пай/...",
                        "ISIN": "...",
                        "Количество": ...,
                        "Номинал": "...",
                        "Текущая_стоимость": "..."
                }
        ],
        "Общая_стоимость": "..."
}
Пустые значения = null, списки допускают []. Без комментариев.""",
        },
        "имущественные_права": {
            "keywords": ["договор займа", "дебиторская", "акт сверки", "задолженность", "право требования", "договор аренды"],
            "prompt": """Документ об имущественных правах (договор займа, акт сверки, решение суда о взыскании, договор аренды с правом получения платы).

КРИТИЧЕСКИ ВАЖНО - ОПРЕДЕЛЕНИЕ РОЛИ:
Это имущественное право ТОЛЬКО если владелец документа = КРЕДИТОР (займодавец, арендодатель).
Если владелец документа = ДОЛЖНИК (заёмщик, арендатор) - это НЕ имущественное право, а ОБЯЗАТЕЛЬСТВО!

Проверь:
1. Кто даёт деньги/имущество? → КРЕДИТОР (займодавец, арендодатель)
2. Кто получает деньги/имущество? → ДОЛЖНИК (заёмщик, арендатор)

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

Найди:
- Тип документа (договор займа/акт сверки/решение суда/договор аренды).
- Стороны: 
  * Кредитор (займодавец, арендодатель) - кто ДАЁТ деньги/имущество
  * Должник (заёмщик, арендатор) - кто ПОЛУЧАЕТ деньги/имущество
- Номер договора/дела, дата.
- Сумма задолженности/ежемесячной платы.
- Срок возврата/период аренды.
- Особые условия (проценты, неустойка, график платежей).

JSON:
{
        "Тип": "договор займа/акт сверки/решение суда/договор аренды",
        "Номер": "...",
        "Дата": "ДД.ММ.ГГГГ",
        "Кредитор": "ФИО займодавца/арендодателя (кто ДАЁТ деньги)",
        "Должник": "ФИО заёмщика/арендатора (кто ПОЛУЧАЕТ деньги)",
        "Роль_владельца_документа": "кредитор/должник",
        "Сумма": "...",
        "Срок": "...",
        "Условия": "..."
}
Пустые значения = null. Без комментариев.""",
        },
        "справка_о_задолженности": {
            "keywords": ["справка о задолженности", "задолженность перед", "сумма задолженности", "размер долга", "справка о задолженности перед", "справка о долге", "справка по займу", "справка о задолженности по займу"],
            "prompt": """Справка о задолженности перед кредитором (банк, МФО, МКК).

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

Найди:
- Кредитор: полное название организации (ООО МКК "...", ПАО "...", АО "...").
- Должник: ФИО.
- Номер договора/счета.
- Дата выдачи справки.
- Сумма основного долга (тело кредита).
- Проценты/пени (если указаны отдельно).
- ИТОГОВАЯ СУММА ЗАДОЛЖЕННОСТИ (основной долг + проценты + пени).
- Дата возникновения задолженности, в большинстве написано так: "...был заключен договор потребительского займа от 17.09.2024..." или "что между Вами и Банком 14.11.2025г. заключен Договор", в общем, ищи что похоже на дату сделки, дату возникновения долга, дату заключения договора.

КРИТИЧЕСКИ ВАЖНО: Ищи ИТОГОВУЮ сумму! Она может быть обозначена как:
- "Общая сумма задолженности"
- "Итого к погашению"
- "Размер задолженности"
- "Сумма долга на дату"

JSON:
{
        "Кредитор": "...",
        "Должник": "...",
        "Номер_договора": "...",
        "Дата_справки": "ДД.ММ.ГГГГ",
        "Основной_долг": 12345.67,
        "Проценты": 1234.56,
        "Пени": 123.45,
        "Итого_задолженность": 13703.68,
        "Дата_возникновения": "ДД.ММ.ГГГГ"
}

ВАЖНО: Суммы указывай ТОЛЬКО числами (без "руб.", без пробелов). Если сумма не указана - ставь null.
Пустые значения = null. Без комментариев.""",
        },
        "постановление_пристава": {
            "keywords": ["постановление", "постановления", "фссп", "судебный пристав", "судебного пристава", "исполнительное производство", "исполнительном производстве", "взыскание", "пристав", "федеральная служба судебных приставов", "исполнительный лист", "исполнительное производство", "пристав-исполнитель", "исполнительные производства", "исполнительных производствах", "сведения об исполнительных производствах"],
            "prompt": """Постановление судебного пристава-исполнителя о возбуждении исполнительного производства, расчёте задолженности, или ТАБЛИЦА со сведениями об исполнительных производствах из ФССП.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.
Нормализуй название банка, МФК, МКК, например: сбербанк - ПАО «Сбербанк», псб банк - ПАО «Банк ПСБ», Вэзбанкир ООО МФК - ООО МФК «Вэббанкир», МФК Юпитер 6 ООО - ООО МФК «Юпитер 6» и т.д.

🔴 ДВА ТИПА ДОКУМЕНТОВ:

1️⃣ ПОСТАНОВЛЕНИЯ (несколько страниц с отдельными постановлениями):
   - Каждое постановление начинается с заголовка "Постановление о возбуждении исполнительного производства" + дата
   - Извлеки данные из КАЖДОГО постановления отдельно

2️⃣ ТАБЛИЦА ФССП "Сведения об исполнительных производствах":
   - Таблица с колонками: Должник, Исполнительное производство, Реквизиты документа, Дата окончания/прекращения, Сервис, Предмет исполнения, Отдел судебных приставов, Судебный пристав-исполнитель
   - Каждая строка таблицы = отдельное ИП
   - ВАЖНО: Если в колонке "Дата, причина окончания или прекращения ИП" указана дата (например "29.11.2024 ст. 46 ч. 1 п. 4") — это ИП ЗАКРЫТО, НЕ извлекай его!
   - В таблице ФССП часто НЕТ названия взыскателя - только "Предмет исполнения"
   - ЕСЛИ НАЗВАНИЕ ВЗЫСКАТЕЛЯ НЕ УКАЗАНО ЯВНО - ставь Взыскатель = null, но ОБЯЗАТЕЛЬНО заполни "Предмет_исполнения"

Если в документе нет долгов, например это постановление об удовлетворении ходатайства, то не включай это постановление  в список.

ДЛЯ КАЖДОГО ИП (постановление или строка таблицы) найди:
- Номер исполнительного производства (например: 66725/18/74064-ИП).
- Дата постановления / дата возбуждения ИП.
- Дата окончания ИП (если есть в колонке "Дата окончания/прекращения" или в тексте) - ЕСЛИ ЕСТЬ ДАТА ЗАКРЫТИЯ, ПРОПУСТИ ЭТО ИП!
- Судебный пристав: ФИО.
- Отдел ФССП.
- Должник: ФИО.
- Взыскатель (кредитор): НАЗВАНИЕ организации или ФИО. ЕСЛИ НЕ УКАЗАНО ЯВНО в документе - ставь null! Но обычно пишется в чью пользу, пример: "в пользу взыскателя: ООО ПКО "АйДи Коллект"",  "в пользу взыскателя: Казначейство России "ФНС России"" и т.д.
- ИНН взыскателя (если указан, обычно 10 или 12 цифр).
- Адрес взыскателя (если указан).
- Предмет исполнения: ТОЧНОЕ описание долга из колонки "Предмет исполнения" (например: "Иные взыскания имущественного характера в пользу физических и юридических лиц", "Задолженность по кредитным платежам (кроме ипотеки)").
- Основание: исполнительный лист, судебный приказ (номер, дата).
- Орган, выдавший исполнительный документ.
- Адрес органа.
- Номер дела.
- Сумма долга (основной долг).
- Остаток долга (если указан отдельно).
- Исполнительский сбор (если есть).
- Расходы по исполнительному производству (если есть).
- ИТОГОВАЯ СУММА ВЗЫСКАНИЯ (остаток долга + исполнительский сбор + расходы).

ФОРМАТ ОТВЕТА - ОБЯЗАТЕЛЬНО МАССИВ:
{
  "Постановления": [
    {
        "Номер_ИП": "66725/18/74064-ИП",
        "Дата_постановления": "11.12.2018",
        "Дата_окончания": null,
        "Пристав": "Якупова Ф. В.",
        "Отдел_ФССП": "Сосновское РОСП ГУФССП России по Челябинской области",
        "Должник": "Малина Елена Жалейкина",
        "Взыскатель": "ООО ПКО «АйДи Коллект»",
        "ИНН_взыскателя": null,
        "Адрес_взыскателя": null,
        "Предмет_исполнения": "Иные взыскания имущественного характера в пользу физических и юридических лиц",
        "Основание": "Судебный приказ от 13.11.2018 № 2-2379/2018",
        "Номер_основания": "2-2379/2018",
        "Дата_основания": "13.11.2018",
        "Орган_выдавший": "Судебный участок № 1 Сосновского района Челябинской области",
        "Адрес_органа": null,
        "Номер_дела": "2-2379/2018",
        "Сумма_долга": 75203.08,
        "Остаток_долга": 70151.53,
        "Исполнительский_сбор": 5051.55,
        "Расходы": 0,
        "Итого_взыскание": 75203.08
    },
    {
        "Номер_ИП": "48929/19/74064-ИП",
        "Дата_постановления": "23.07.2019",
        "Дата_окончания": null,
        "Пристав": "Якупова Ф. В.",
        "Отдел_ФССП": "Сосновское РОСП ГУФССП России по Челябинской области",
        "Должник": "Малина Елена Жалейкина",
        "Взыскатель": null,
        "ИНН_взыскателя": null,
        "Адрес_взыскателя": null,
        "Предмет_исполнения": "Задолженность по платежам за жилую площадь, коммунальные платежи",
        "Основание": "Судебный приказ от 29.06.2019 № 2-1226/2019",
        "Номер_основания": "2-1226/2019",
        "Дата_основания": "29.06.2019",
        "Орган_выдавший": "Судебный участок № 1 Сосновского района Челябинской области",
        "Адрес_органа": null,
        "Номер_дела": "2-1226/2019",
        "Сумма_долга": 41152.41,
        "Остаток_долга": 41152.41,
        "Исполнительский_сбор": 2798.25,
        "Расходы": 0,
        "Итого_взыскание": 43950.66
    }
  ]
}
        "Дата_основания": "13.11.2018",
        "Орган_выдавший": "Судебный участок № 1 Сосновского района Челябинской области",
        "Адрес_органа": null,
        "Номер_дела": "2-2379/2018",
        "Сумма_долга": 75203.08,
        "Остаток_долга": 70151.53,
        "Исполнительский_сбор": 5051.55,
        "Расходы": 0,
        "Итого_взыскание": 75203.08
    }
  ]
}

КРИТИЧЕСКИ ВАЖНО:
- НЕ включай ИП с датой окончания/прекращения!
- ВСЕГДА возвращай массив "Постановления", даже если одно ИП!
- Для таблицы ФССП: каждая строка = отдельный элемент массива
- Суммы ТОЛЬКО числами, без текста
- Пустые значения = null
- Если "Остаток_долга" не указан отдельно - используй "Сумма_долга"
- "Итого_взыскание" = Остаток_долга (или Сумма_долга) + Исполнительский_сбор + Расходы

ВЕРНИ ТОЛЬКО ВАЛИДНЫЙ JSON БЕЗ ДОПОЛНИТЕЛЬНОГО ТЕКСТА!
Никакого текста до или после JSON!
Только чистый JSON формат!""",
        },

        "ценное_имущество": {
            "keywords": ["драгоценности", "ювелирные изделия", "золото", "серебро", "украшения", "антиквариат", "картины", "коллекция", "предметы искусства", "опись имущества", "оценка имущества", "акт оценки"],
            "prompt": """Документ о наличных денежных средствах и ином ценном имуществе (опись имущества, акт оценки, декларация о наличии ценностей).

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

Найди:
- Владелец (ФИО).
- Наличные денежные средства (если указаны): сумма в рублях и/или валюте.
- Драгоценности и ювелирные изделия: описание (золотое кольцо, серьги и т.д.), вес/проба, оценочная стоимость.
- Предметы искусства и антиквариат: описание, автор, год, оценочная стоимость.
- Коллекции: описание (монеты, марки, часы), количество предметов, оценочная стоимость.
- Дорогая техника и электроника: описание, марка/модель, год, стоимость.
- Другое ценное имущество: описание, стоимость.
- НЕДВИЖИМОЕ ИМУЩЕСТВО: если в описи указаны квартиры, земельные участки, гаражи или иная недвижимость - извлеки их в отдельный список! Найди адрес, площадь, кадастровый номер (если есть), вид права (собственность/доля).
- Дата оценки/составления описи.
- Оценщик (если есть): ФИО, организация.

JSON:
{
        "Владелец": "...",
        "Дата_составления": "ДД.ММ.ГГГГ",
        "Оценщик": "...",
        "Наличные_средства": {
                "RUB": "...",
                "USD": "...",
                "EUR": "...",
                "Другая_валюта": "..."
        },
        "Имущество": [
                {
                        "Категория": "драгоценности/искусство/коллекция/техника/другое",
                        "Описание": "...",
                        "Характеристики": "...",
                        "Оценочная_стоимость": "..."
                }
        ],
        "Недвижимость": [
            {
                "Вид": "квартира/земельный участок/жилой дом/гараж/...",
                "Адрес": "...",
                "Площадь": "...",
                "Кадастровый_номер": "...",
                "Вид_права": "собственность/долевая (1/2)/...",
                "Описание": "...",
                "Стоимость": "..."
            }
        ],
        "Общая_стоимость": "..."
}
Пустые значения = null, списки допускают []. Без комментариев.""",
        },

        "сделки": {
            "keywords": ["договор купли-продажи", "дкп", "ДКП", "договор дарения", "договор мены", "договор купли продажи", "покупатель", "продавец", "даритель", "одаряемый", "недвижимое имущество", "транспортное средство", "автомобиль", "квартира", "дом", "земельный участок", "акт приема-передачи"],
            "prompt": """Ты обрабатываешь документы о сделках должника за последние 3 года: договоры купли-продажи, дарения, мены недвижимости, транспортных средств и иного ценного имущества.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду.

Найди:
- Тип сделки: купля-продажа, дарение, мена, другое.
- Дата сделки (дата подписания договора).
- Роль должника: покупатель, продавец, даритель, одаряемый, и т.д.
- Предмет сделки: что продано/куплено/подарено (квартира, дом, земельный участок, автомобиль, и т.д.).
- Описание предмета: адрес недвижимости, марка/модель ТС, кадастровый номер, другие характеристики.
- Стоимость сделки (сумма в рублях).
- Вторая сторона сделки: ФИО покупателя/продавца/другой стороны.
- Особые условия: рассрочка, кредит, обременения, и т.д.

КРИТЕРИИ ОТБОРА:
- Анализируй ТОЛЬКО документы о сделках за последние 3 года от текущей даты.
- Сделки с недвижимостью: квартиры, дома, земельные участки, гаражи, нежилые помещения.
- Сделки с транспортом: автомобили, мотоциклы, спецтехника.
- Сделки с иным ценным имуществом (любой стоимости).

JSON:
{
        "Тип_сделки": "купля-продажа/дарение/мена/другое",
        "Дата_сделки": "ДД.ММ.ГГГГ",
        "Роль_должника": "покупатель/продавец/даритель/одаряемый",
        "Предмет_сделки": "квартира/дом/земельный участок/автомобиль/другое",
        "Описание": "...",
        "Адрес_или_характеристики": "...",
        "Кадастровый_номер": "...",
        "Стоимость": 0.00,
        "Вторая_сторона": "ФИО",
        "Особые_условия": "..."
}

Пустые значения = null. Суммы ТОЛЬКО числами. Без комментариев.""",
        },
        
        # === Документы супруга (используют те же промпты что и основные документы) ===
        "паспорт_супруга": {
            "keywords": ["паспорт супруга", "паспорт супруги", "паспорта супруга", "паспорта супруги", "паспорт жены", "паспорта жены", "паспорт мужа", "паспорта мужа", "паспорт заинтересованного лица", "паспорта заинтересованного лица", "passport spouse"],
            "prompt": """Ты обрабатываешь паспорт РФ. Извлеки реквизиты максимально точно.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО, адреса с заглавной буквы).

1. Страница 2-3 (основные данные):
   - В верху слева: серия и номер (пример: 1234 567890 — верни как "1234 567890").
   - Ниже "Фамилия", "Имя", "Отчество" — построчно.
   - Дата рождения формата ДД.ММ.ГГГГ.
   - Место рождения:
     - Приведи к официальному виду
     - НЕ убирай "гор.", "с.", "пос.", "д.", "обл.", "респ.", "край"
     - Применяй правила:
       * "ГОР." → "гор." (строчные)
       * "Г МОСКВА" → "гор. Москва" (добавь точку)
       * "Г.МОСКВА" → "гор. Москва" (пробел)
       * "ГОР МОСКВА" → "гор. Москва"
       * "С. БОЛЬШОЕ" → "с. Большое"
       * "Д КРАСНОЕ" → "д. Красное" (добавь точку)
       * "ПОС.ЮЖНЫЙ" → "пос. Южный" (пробел)
     - Примеры правильных форматов:
       * "гор. Москва"
       * "с. Большое"
       * "д. Красное, Московская обл."
       * "пос. Южный, Краснодарский край"
       * "гор. Санкт-Петербург"
       * "респ. Татарстан, гор. Казань"
     - Если адрес содержит несколько уровней, сохраняй иерархию: "респ. Татарстан, гор. Казань"
   - Пол: "муж" или "жен".
   - Строка "Кем выдан" — название подразделения УФМС/МВД.
   - Дата выдачи: ДД.ММ.ГГГГ.
   - Код подразделения: XXX-XXX.

2. Страница 5 (регистрация):
   - Штамп с адресом (часто размыт или кривой) — попытайся распознать.
   - Адрес регистрации: строковые данные из штампа.
   - Если штамп размыт, используй контекстную логику (первая пропись в паспорте обычно сохраняется).

Если документ старый или новый (2022 г. и позже), алгоритм тот же.

Верни JSON:
{
  "ФИО": "...",
  "Серия_и_номер": "...",
  "Дата_выдачи": "ДД.ММ.ГГГГ",
  "Код_подразделения": "...",
  "Кем_выдан": "...",
  "Дата_рождения": "ДД.ММ.ГГГГ",
  "Место_рождения": "...",
  "Адрес_регистрации": "..."
}
Если реквизит не найден, ставь null. Никаких дополнительных комментариев.""",
        },
        "инн_супруга": {
            "keywords": ["инн супруга", "инна супруга", "инн жены", "инна жены", "инн мужа", "инн заинтересованного лица", "инна заинтересованного лица"],
            "prompt": """Ты обрабатываешь свидетельство о постановке на учёт физического лица (ИНН).

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО с заглавной буквы).

КРИТИЧЕСКИ ВАЖНО - ФОРМАТ ИНН:
ИНН может быть записан по-разному:
- Правильно: 123456789012
- С пробелами: 1234 5678 9012 или 123 456 789 012
- С дефисами: 1234-5678-9012 или 123-456-789-012
- Сгруппированно: (123) 456-789-012
- Частично размыт

ИНСТРУКЦИЯ:
- ИНН ВСЕГДА 12 цифр (физ. лицо) — без пробелов и дефисов
- Ищи ~12 цифр рядом со словами: "ИНН", "Идентификационный номер", "Свидетельство"
- Убери все пробелы, дефисы, скобки
- Приведи к формату: 123456789012
- Если часть цифр размыта - попытайся разобрать хотя бы 10-11 цифр
- НЕ путай с ОГРН (13/15 цифр), СНИЛС (11 цифр), датами, телефонами

СТРУКТУРА ДОКУМЕНТА:
1. Заголовок: "Свидетельство о постановке на учёт физического лица в налоговом органе" или "Свидетельство о постановке на учёт".
2. Серия и номер (не путай с ИНН): обычно формат XX № XXXXXXXXXX (тоже 12 цифр, но в другом месте).
3. ИНН: крупный номер, часто выделен жирным или рамкой — ВСЕГДА 12 цифр.
4. ФИО: блок текста "Фамилия Имя Отчество" или построчно.
5. Подпись и печать налоговой инспекции.

Если OCR пустой, используй визуальную структуру:
- ИНН обычно в центре или сверху справа крупным шрифтом.
- ФИО ниже ИНН.

Верни JSON:
{
  "ФИО": "...",
  "ИНН": "123456789012"
}
Если реквизит не найден, ставь null. Никаких дополнительных комментариев.""",
        },
        "снилс_супруга": {
            "keywords": ["снилс супруга", "снилса супруга", "снилс жены", "снилса жены", "снилс мужа", "снилса мужа", "снилс заинтересованного лица", "снилса заинтересованного лица"],
            "prompt": """Ты обрабатываешь страховое свидетельство обязательного пенсионного страхования (СНИЛС) — старый зелёный пластик или новая электронная справка.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО, адреса с заглавной буквы).

КРИТИЧЕСКИ ВАЖНО - ФОРМАТЫ СНИЛС:
СНИЛС может быть записан по-разному:
- Правильно: 123-456-789 12
- Слитно: 12345678912
- С пробелами: 123 456 789 12
- Без дефисов: 123 456 789 12
- С точками: 123.456.789.12
- Частично размыт

ИНСТРУКЦИЯ:
- СНИЛС ВСЕГДА 11 цифр в стандартном формате XXX-XXX-XXX XX
- Ищи ~11 цифр рядом со словами: "СНИЛС", "Страховой номер", "СФР"
- Убери все пробелы, дефисы, точки
- Приведи к правильному формату: XXX-XXX-XXX XX (с дефисами между тройками и пробелом перед последней двойкой)
- Если часть цифр размыта - попытайся разобрать хотя бы 9-10 цифр
- НЕ путай с ИНН (12 цифр), телефоном, другими номерами

1. Старый бланк:
        - Заголовок "Страховое свидетельство" и строка "Страховой номер" — формат XXX-XXX-XXX XX.
        - Ниже блок "Фамилия", "Имя", "Отчество".
        - Внизу слева "Дата рождения" и "Место рождения".
        - Под подписью страхователя — дата выдачи.
2. Новая справка СФР:
        - Шапка "Страховой номер индивидуального лицевого счёта" — рядом СНИЛС.
        - Таблица с ФИО, датой и местом рождения.

Если OCR пустой, используй макет: номер крупно в центре, ФИО блоком ниже, дата рождения под ФИО.

Верни JSON:
{
  "ФИО": "...",
  "СНИЛС": "XXX-XXX-XXX XX",
  "Дата_рождения": "ДД.ММ.ГГГГ",
  "Место_рождения": "...",
  "Дата_выдачи": "ДД.ММ.ГГГГ",
  "Тип_бланка": "карта 1997/справка СФР"
}
Если реквизит не найден, ставь null. Никаких дополнительных комментариев.""",
        },

        "свидетельство_о_заключении_брака": {
            "keywords": ["свидетельство о заключении брака", "свидетельство о браке", "запись акта о заключении брака", "св-во о заключении брака", "св-во о браке", "о браке"],
            "prompt": """Ты обрабатываешь свидетельство о заключении брака.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО с заглавной буквы).

Найди в документе:
- ФИО супруга (мужа)
- ФИО супруги (жены)
- Дата рождения супруга
- Дата рождения супруги
- Место рождения супруга
- Место рождения супруги
- Дата заключения брака
- Орган ЗАГС
- Номер записи акта

Верни JSON:
{
  "Супруг_ФИО": "...",
  "Супруга_ФИО": "...",
  "Супруг_Дата_рождения": "ДД.ММ.ГГГГ",
  "Супруга_Дата_рождения": "ДД.ММ.ГГГГ",
  "Супруг_Место_рождения": "...",
  "Супруга_Место_рождения": "...",
  "Дата_заключения_брака": "ДД.ММ.ГГГГ",
  "Орган_ЗАГС": "...",
  "Номер_записи_акта": "...",
  "Тип_документа": "свидетельство_о_браке"
}
Если реквизит не найден, ставь null.""",
        },

        "свидетельство_о_расторжении_брака": {
            "keywords": ["свидетельство о расторжении брака", "свидетельство о разводе", "копия свидетельства о расторжении брака", "расторжение брака", "развод", "запись акта о расторжении брака", "св-во о расторжении брака", "св-во о раст брака","св-во л раст брака", "св во о расторжении брака", "св-о о расторжении брака", "св о о расторжении брака"],
            "prompt": """Ты обрабатываешь свидетельство о расторжении брака.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО с заглавной буквы).

Найди в документе:
- ФИО бывшего супруга (мужа)
- ФИО бывшей супруги (жены)
- Дата рождения бывшего супруга
- Дата рождения бывшей супруги
- Дата расторжения брака
- Орган ЗАГС, выдавший свидетельство
- Номер записи акта о расторжении
- Дата выдачи свидетельства
- Фамилии после расторжения брака (если менялись)

КРИТИЧЕСКИ ВАЖНО:
- Различай ФИО бывшего супруга и бывшей супруги
- Дата расторжения брака — это дата официального расторжения в ЗАГС, не дата выдачи свидетельства
- Если указаны фамилии после развода — извлеки их отдельно

Верни JSON:
{
  \"Бывший_супруг_ФИО\": \"ФИО мужа\",
  \"Бывший_супруг_Дата_рождения\": \"ДД.ММ.ГГГГ\",
  \"Бывшая_супруга_ФИО\": \"ФИО жены\",
  \"Бывшая_супруга_Дата_рождения\": \"ДД.ММ.ГГГГ\",
  \"Дата_расторжения_брака\": \"ДД.ММ.ГГГГ\",
  \"Фамилия_супруга_после_развода\": \"...\",
  \"Фамилия_супруги_после_развода\": \"...\",
  \"Орган_ЗАГС\": \"...\",
  \"Номер_записи\": \"...\",
  \"Дата_выдачи\": \"ДД.ММ.ГГГГ\"
}

Если реквизит не найден, ставь null. Никаких дополнительных комментариев.""",
        },

        "свидетельство_о_браке": {
            "keywords": ["свидетельство о заключении брака", "свидетельство о браке", "копия свидетельства о заключении брака", "копия свидетельства о браке", "запись акта о заключении брака"],
            "prompt": """Ты обрабатываешь свидетельство о заключении брака (РФ или других стран СНГ).

🔴 КОНТЕКСТ: ФИО ДОЛЖНИКА = {debtor_fio} - если есть опечатка, ничего страшного

ТВОЯ ЗАДАЧА: Найти в документе СУПРУГА должника (мужа или жену) и вернуть ТОЛЬКО его/её данные.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО с заглавной буквы).

🔴🔴🔴 СТРУКТУРА ДОКУМЕНТА (ВАРИАНТЫ):

ВАРИАНТ 1: СТАНДАРТНОЕ СВИДЕТЕЛЬСТВО РФ (Розовый бланк)
- Обычно одна страница. Текст полностью машинописный.
- ЧЕТКИЙ ПОРЯДОК:
  1. ПЕРВЫМ всегда указывается МУЖ (Фамилия, Имя, Отчество, Дата рождения, Место рождения).
     - Текст: "Гражданин [ФИО МУЖА]..."
  2. ВТОРЫМ всегда указывается ЖЕНА (Фамилия, Имя, Отчество, Дата рождения, Место рождения).
     - Текст: "и гражданка [ФИО ЖЕНЫ]..."
  3. Внизу: "После заключения брака присвоены фамилии:".

ВАРИАНТ 2: СВИДЕТЕЛЬСТВО СТРАН СНГ (Таджикистан и др.)
- Часто две части: Рукописная (верх) и Печатная (низ или 2 стр).
- Ищи явные маркеры "муж:", "жена:" в печатной части.

🎯 АЛГОРИТМ:
1. Найди ДВУХ людей, вступающих в брак.
2. Определи, кто из них ДОЛЖНИК ({debtor_fio}).
   - Сравнивай Фамилию и Имя (отчество может быть сокращено или отсутствовать).
   - Учитывай возможную смену фамилии (смотри графу "После заключения брака присвоены фамилии").
3. Второй человек - это СУПРУГ.
   - СМОТРИ ГРАФУ "После заключения брака присвоены фамилии" (обычно внизу).
   - Если супругу присвоена НОВАЯ фамилия — ИСПОЛЬЗУЙ ИМЕННО ЕЁ!
   - Если супруг Орлова, но в графе "жене присвоена фамилия" стоит Демина — значит супурга ДЕМИНА Ксения Александровна.
   - Извлеки дату рождения.

ПРИМЕР 1 (РФ):
"Демин Алексей Владимирович... и Орлова Ксения Александровна... заключили брак."
Внизу: "После заключения брака присвоены фамилии: мужу Демин, жене Демина".
Должник: Демин А.В.
Супруг (жена): была Орлова -> стала ДЕМИНА.
РЕЗУЛЬТАТ: "Супруг_ФИО": "Демина Ксения Александровна"

ВЕРНИ ТОЛЬКО JSON:
{
  "Супруг_ФИО": "Фамилия Имя Отчество супруга должника (актуальная после брака)",
  "Супруг_Дата_рождения": "ДД.ММ.ГГГГ",
```

🔴 КРИТИЧЕСКИ ВАЖНО:
- ИЩИ ПЕЧАТНУЮ ЧАСТЬ! Не пытайся разбирать рукописный текст!
- СМОТРИ ВСЕ СТРАНИЦЫ документа (печатная часть может быть на 2-й странице)
- ИСПОЛЬЗУЙ явные маркеры "муж:" и "жена:" для определения кто есть кто
- ВСЕГДА возвращай Супруг_ФИО (не null!) если есть хоть одна графа "муж" или "жена"
- В России однополые браки ЗАПРЕЩЕНЫ - если в документе написано "муж" и "жена", значит это ТОЧНО мужчина и женщина

Верни ТОЛЬКО JSON:
{
  "Супруг_ФИО": "Фамилия Имя Отчество супруга должника (актуальная после брака)",
  "Супруг_Дата_рождения": "ДД.ММ.ГГГГ",
  "Дата_заключения_брака": "ДД.ММ.ГГГГ",
  "Орган_ЗАГС": "...",
  "Номер_записи": "...",
  "Дата_выдачи": "ДД.ММ.ГГГГ"
}

Если реквизит не найден, ставь null. Никаких дополнительных комментариев.""",
        },

        "свидетельство_о_рождении": {
            "keywords": ["свидетельство о рождении", "свидетельства о рождении", "свидетельств о рождении", "свидетельство о рождении детей", "свидетельства о рождении детей", "свидетельство о рождении ребенка", "копия свидетельства о рождении", "копии свидетельств о рождении", "копии свидетельств о рождении детей",  "родился", "родилась", "запись акта о рождении", "свидетельства о рождении детей", "ребенок", "св-во о рождении"],
            "prompt": """Ты обрабатываешь свидетельство(а) о рождении ребенка/детей.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО с заглавной буквы).

🔴🔴🔴 КРИТИЧЕСКИ ВАЖНО - ИЗВЛЕКАЙ ВСЕХ ДЕТЕЙ:
- В ОДНОМ файле может быть 1, 2, 3 или БОЛЬШЕ свидетельств о рождении!
- ПРОВЕРЬ КАЖДУЮ СТРАНИЦУ ДОКУМЕНТА ОТ НАЧАЛА ДО КОНЦА
- Считай количество разных ФИО детей на всех страницах:
  • 1 ребенок → массив "Дети" с 1 элементом
  • 2 ребенка → массив "Дети" с 2 элементами
  • 3 ребенка → массив "Дети" с 3 элементами
- НЕ ПРОПУСКАЙ СТРАНИЦЫ! Прочитай весь документ построчно!

АЛГОРИТМ:
1. Прочитай страницу 1 → нашёл ребенка → добавь в массив
2. Прочитай страницу 2 → нашёл ДРУГОГО ребенка → добавь в массив
3. Прочитай страницу 3 → нашёл ЕЩЁ ОДНОГО ребенка → добавь в массив
4. Продолжай до конца документа

КАК РАЗЛИЧАТЬ РАЗНЫЕ СВИДЕТЕЛЬСТВА:
- Каждое свидетельство = ОТДЕЛЬНЫЙ ребенок с РАЗНЫМ ФИО и РАЗНОЙ датой рождения
- Разные имена детей (Артём, Мила, Демид) = разные свидетельства
- Разные даты рождения (30.08.2010, 24.02.2021, 18.03.2015) = разные дети
- Двусторонняя копия ОДНОГО свидетельства (одно ФИО на обеих сторонах) ≠ два свидетельства

Для КАЖДОГО найденного свидетельства извлеки:
- ФИО ребенка (полностью: Фамилия Имя Отчество) - ОБЯЗАТЕЛЬНО
- Дата рождения ребенка - ОБЯЗАТЕЛЬНО
- Место рождения ребенка
- ФИО матери
- ФИО отца
- Орган ЗАГС, выдавший свидетельство
- Номер записи акта
- Дата выдачи свидетельства

РАЗЛИЧАЙ:
- ФИО ребенка (чье это свидетельство) ≠ ФИО матери ≠ ФИО отца
- Если отчество ребенка отсутствует — укажи null

Верни JSON:
{
  "Дети": [
    {
      "Ребенок_ФИО": "Михалёв Артём Максимович",
      "Дата_рождения": "30.08.2010",
      "Место_рождения": "г. Краснотурьинск",
      "Мать_ФИО": "Михалёва Татьяна Александровна",
      "Отец_ФИО": "Михалёв Максим Сергеевич",
      "Орган_ЗАГС": "...",
      "Номер_записи": "174",
      "Дата_выдачи": "08.09.2010"
    },
    {
      "Ребенок_ФИО": "Михалёва Мила Максимовна",
      "Дата_рождения": "24.02.2021",
      "Место_рождения": "г. Миасс",
      "Мать_ФИО": "Михалёва Татьяна Александровна",
      "Отец_ФИО": "Михалёв Максим Сергеевич",
      "Орган_ЗАГС": "...",
      "Номер_записи": "110219740001800274009",
      "Дата_выдачи": "05.03.2021"
    },
    {
      "Ребенок_ФИО": "Михалёв Демид Максимович",
      "Дата_рождения": "18.03.2015",
      "Место_рождения": "г. Миасс",
      "Мать_ФИО": "Михалёва Татьяна Александровна",
      "Отец_ФИО": "Михалёв Максим Сергеевич",
      "Орган_ЗАГС": "...",
      "Номер_записи": "503",
      "Дата_выдачи": "31.03.2015"
    }
  ]
}

ПРАВИЛО:
- 1 свидетельство в документе → массив "Дети" с 1 элементом
- 2 свидетельства в документе → массив "Дети" с 2 элементами  
- 3 свидетельства в документе → массив "Дети" с 3 элементами (как в примере выше!)
- Количество элементов в массиве = количество РАЗНЫХ детей в документе
Если реквизит не найден, ставь null. Никаких дополнительных комментариев.""",
        },

        "общий": {
            "keywords": [],
            "prompt": """Ты анализируешь произвольный документ.

Определи по содержимому ключевые поля (ФИО, даты, суммы, номера) и верни аккуратный JSON.
Если тип не распознан, верни {"raw_text": "..."} с полным текстом.
Не добавляй комментариев, вывод строго JSON.""",
        },

        "справка_о_судимости": {
            "keywords": ["справка о судимости", "сведения о судимости", "справка об отсутствии судимости", "копия справки о судимости", "копия справки об отсутствии судимости", "судимость", "отсутствие судимости", "справку о наличии", "факта уголовного преследования"],
            "prompt": """Ты анализируешь справку о наличии (отсутствии) судимости и (или) факта уголовного преследования либо о прекращении уголовного преследования.

ФОРМАТИРОВАНИЕ: Если текст в CAPS LOCK — приведи к нормальному виду (ФИО с заглавной буквы).

🔴 КРИТИЧЕСКИ ВАЖНО - ДЕВИЧЬЯ ФАМИЛИЯ:
Девичья фамилия (прежняя фамилия) указывается В СКОБКАХ после основной фамилии.
Примеры:
- "Ушакова (Романовна) Валерия Сергеевна" → Фамилия: "Ушакова", Девичья_фамилия: "Романовна", Имя: "Валерия", Отчество: "Сергеевна"
- "Петрова (Сидорова) Анна Ивановна" → Фамилия: "Петрова", Девичья_фамилия: "Сидорова", Имя: "Анна", Отчество: "Ивановна"
- "Иванов Иван Петрович" → Фамилия: "Иванов", Девичья_фамилия: null, Имя: "Иван", Отчество: "Петрович"

АЛГОРИТМ ИЗВЛЕЧЕНИЯ ДЕВИЧЬЕЙ ФАМИЛИИ:
1. Найди ФИО в справке (В поле "(фамилия, имя, отчество (при наличии) проверяемого лица, в том числе имевшиеся ранее, в именительном падеже)")
2. Проверь наличие СКОБОК в ФИО
3. Если есть скобки: текст В СКОБКАХ = девичья фамилия
4. Если скобок нет: девичья фамилия = null

Найди в документе:
- ФИО лица (ОБЯЗАТЕЛЬНО с учетом девичьей фамилии в скобках!)
- Дата рождения
- Место рождения
- Сведения о судимости: "имеется" / "не имеются" / "погашена" / "снята"
- Если судимость ИМЕЕТСЯ или БЫЛА:
  * Статья УК РФ
  * Дата приговора
  * Наименование суда
  * Срок наказания
  * Дата освобождения (если есть)
  * Статус (погашена/снята/действует)
- Орган, выдавший справку (ГИАЦ МВД России, МВД по региону)
- Номер справки
- Дата выдачи справки

Верни JSON:
{
  "ФИО": "Ушакова (Романовна) Валерия Сергеевна",
  "Фамилия": "Ушакова",
  "Девичья_фамилия": "Романовна",
  "Имя": "Валерия",
  "Отчество": "Сергеевна",
  "Дата_рождения": "ДД.ММ.ГГГГ",
  "Место_рождения": "...",
  "Судимость_статус": "не имеется/имеется/погашена/снята",
  "Судимость_детали": {
    "Статья_УК": "...",
    "Дата_приговора": "ДД.ММ.ГГГГ",
    "Суд": "...",
    "Срок_наказания": "...",
    "Дата_освобождения": "ДД.ММ.ГГГГ",
    "Статус": "погашена/снята/действует"
  },
  "Орган_выдачи": "...",
  "Номер_справки": "...",
  "Дата_выдачи": "ДД.ММ.ГГГГ"
}

Если судимости нет: "Судимость_детали": null
Если девичьей фамилии нет: "Девичья_фамилия": null
Пустые значения = null. Никаких дополнительных комментариев.""",
        },
    }

    # === Utilities ===
    @staticmethod
    def normalize_quotes(text: str) -> str:
        """Нормализует кавычки в тексте: заменяет все виды кавычек на елочки.
        
        Правило: первая кавычка - открывающая «, следующая - закрывающая », и т.д.
        
        Args:
            text: Исходный текст
            
        Returns:
            Текст с нормализованными кавычками
        """
        if not text:
            return text
        
        import re
        
        # Заменяем все виды кавычек на временный маркер
        # Прямые кавычки: " " ' '
        # Типографские: " " ' ' « »
        text = re.sub(r'["\"\'\'\"\']', '|QUOTE|', text)
        
        # Заменяем маркеры на елочки попарно
        result = []
        is_opening = True
        for char in text:
            if char == '|':
                # Проверяем, это начало маркера QUOTE
                continue
            result.append(char)
        
        # Более простой подход: заменяем попарно
        parts = text.split('|QUOTE|')
        result = parts[0]
        for i in range(1, len(parts)):
            # Чередуем открывающие и закрывающие кавычки
            if i % 2 == 1:  # Нечетные - открывающие
                result += '«' + parts[i]
            else:  # Четные - закрывающие
                result += '»' + parts[i]
        
        return result

    @staticmethod
    def detect_document_type(filename: str, first_page_text: Optional[str] = None) -> tuple[str, str]:
        """Определяет тип документа по имени файла и содержимому.

        Args:
            filename: Имя файла
            first_page_text: Текст первой страницы (для контекстного распознавания)

        Returns:
            Кортеж (тип документа, промпт для GPT)
        """
        # Заменяем подчеркивания и дефисы на пробелы для лучшего распознавания
        filename_lower = filename.lower().replace('_', ' ').replace('-', ' ')

        # ПРИОРИТЕТ 1: Сначала проверяем документы СУПРУГА и БРАКА (они более специфичны)
        spouse_doc_types = ["паспорт_супруга", "инн_супруга", "снилс_супруга", "свидетельство_о_расторжении_брака", "свидетельство_о_заключении_брака"]
        for doc_type in spouse_doc_types:
            if doc_type in DocumentProcessor.DOCUMENT_TYPES:
                config = DocumentProcessor.DOCUMENT_TYPES[doc_type]
                for keyword in config.get("keywords", []):
                    if keyword in filename_lower:
                        return doc_type, config["prompt"]
        
        # ПРИОРИТЕТ 1.5: БКИ/НБКИ/ОКБ по имени файла (до проверки остальных документов)
        # Порядок проверки: НБКИ -> ОКБ -> БКИ (от самого специфичного к общему)
        if "нбки" in filename_lower or "nbki" in filename_lower or "nbch" in filename_lower:
            config = DocumentProcessor.DOCUMENT_TYPES.get("отчет_нбки", {})
            return "отчет_нбки", config.get("prompt", "")
        
        if "окб" in filename_lower or "okb" in filename_lower:
            config = DocumentProcessor.DOCUMENT_TYPES.get("отчет_окб", {})
            return "отчет_окб", config.get("prompt", "")
        
        if "бки" in filename_lower and "нбки" not in filename_lower:  # Избегаем ложных срабатываний на НБКИ
            config = DocumentProcessor.DOCUMENT_TYPES.get("отчет_бки", {})
            return "отчет_бки", config.get("prompt", "")
        
        # ПРИОРИТЕТ 2: Потом проверяем обычные документы ДОЛЖНИКА
        for doc_type, config in DocumentProcessor.DOCUMENT_TYPES.items():
            # Пропускаем документы супруга, они уже проверены
            if doc_type in spouse_doc_types:
                continue
            for keyword in config.get("keywords", []):
                if keyword in filename_lower:
                    return doc_type, config["prompt"]

        # Если не нашли по имени и есть текст - проверяем по содержимому
        if first_page_text:
            text_lower = first_page_text.lower()

            # Налоговое уведомление / ЕНС - проверяем по содержимому
            if any(kw in text_lower for kw in ["общая задолженность по налоговому счёту", "сумма начисления", "задолженность выставлена", "время получения данных из фнс", "единый налоговый счёт"]):
                config = DocumentProcessor.DOCUMENT_TYPES.get("налоговое_уведомление", {})
                return "налоговое_уведомление", config.get("prompt", "")

            # Постановление пристава - высокий приоритет
            if any(kw in text_lower for kw in ["судебный пристав", "фссп", "исполнительное производство", "исполнительном производстве", "судебного пристава-исполнителя"]):
                config = DocumentProcessor.DOCUMENT_TYPES.get("постановление_пристава", {})
                return "постановление_пристава", config.get("prompt", "")

            # Договор поручительства
            if "поручител" in text_lower and "договор" in text_lower:
                # Это обычный договор, обрабатываем как общий с контекстом о поручительстве
                return "общий", """Договор поручительства.

Найди:
- Номер договора и дату
- Поручитель (кто даёт поручительство): ФИО
- Должник (за кого поручаются): ФИО
- Кредитор (в чью пользу поручительство): название организации
- Сумма обязательства
- Срок действия поручительства

JSON:
{
    "Тип": "Договор поручительства",
    "Номер": "...",
    "Дата": "ДД.ММ.ГГГГ",
    "Поручитель": "...",
    "Должник": "...",
    "Кредитор": "...",
    "Сумма": "...",
    "Срок": "..."
}
"""

            # ЕГРН выписка
            if "единый государственный реестр недвижимости" in text_lower or "кадастровый номер" in text_lower:
                if "отсутствуют сведения" in text_lower or "не найдено" in text_lower:
                    config = DocumentProcessor.DOCUMENT_TYPES.get("егрн_уведомление", {})
                    return "егрн_уведомление", config.get("prompt", "")
                else:
                    config = DocumentProcessor.DOCUMENT_TYPES.get("егрн_выписка", {})
                    return "егрн_выписка", config.get("prompt", "")

            # БКИ/НБКИ/ОКБ отчёты - проверяем по специфичным словам (ПОРЯДОК ВАЖЕН!)
            # Сначала НБКИ (самый специфичный)
            if "нбки" in text_lower or "национальное бюро кредитных историй" in text_lower or "nbch" in text_lower or "nbki" in text_lower:
                config = DocumentProcessor.DOCUMENT_TYPES.get("отчет_нбки", {})
                return "отчет_нбки", config.get("prompt", "")
            
            # Потом ОКБ
            if "окб" in text_lower or "объединенное кредитное бюро" in text_lower:
                config = DocumentProcessor.DOCUMENT_TYPES.get("отчет_окб", {})
                return "отчет_окб", config.get("prompt", "")
            
            # В последнюю очередь БКИ (самый общий)
            if "бки" in text_lower or "скоринг" in text_lower or ("кредитная история" in text_lower and "бюро" in text_lower):
                config = DocumentProcessor.DOCUMENT_TYPES.get("отчет_бки", {})
                return "отчет_бки", config.get("prompt", "")

            # Налоговое уведомление
            if "налоговое уведомление" in text_lower or ("всего к уплате" in text_lower and "фнс" in text_lower):
                config = DocumentProcessor.DOCUMENT_TYPES.get("налоговое_уведомление", {})
                return "налоговое_уведомление", config.get("prompt", "")

            # ГИБДД - заявление или справка (расширенная проверка)
            gibdd_keywords = [
                "гибдд", "гаи", "мрэо", 
                "госавтоинспекция", "госавтоинспекции", 
                "государственная инспекция безопасности дорожного движения",
                "реестр транспортных средств", 
                "реестра транспортных средств", # падежи
                "сведения о транспортных средствах",
                "карточка учета транспортного средства",
                "паспорт транспортного средства"
            ]
            transport_keywords = ["транспорт", "зарегистрирован", "заявление", "автомобил", "тс", "транспортн", "регистрац", "птс", "стс", "справка", "vin", "идентификационный номер"]
            
            has_gibdd = any(kw in text_lower for kw in gibdd_keywords)
            has_transport = any(kw in text_lower for kw in transport_keywords)
            has_strong_transport = any(kw in text_lower for kw in ["vin", "идентификационный номер (vin)", "номер кузова", "номер шасси"])

            # Логика детекции:
            # 1. Есть ключевые слова ведомства/реестра (has_gibdd) -> точно ГИБДД
            # 2. Есть слова про транспорт И (ключевики в имени файла ИЛИ сильные признаки в тексте типа VIN)
            if has_gibdd or (has_transport and (
                any(kw in filename_lower for kw in ["гибдд", "транспорт", "справка", "выписка", "реестр", "птс", "стс"]) or 
                has_strong_transport
            )):
                config = DocumentProcessor.DOCUMENT_TYPES.get("гибдд", {})
                return "гибдд", config.get("prompt", "")

        # По умолчанию - общий тип
        return "общий", DocumentProcessor.DOCUMENT_TYPES.get("общий", {}).get(
            "prompt", "Проанализируй документ и верни JSON"
        )

    @staticmethod
    def get_memory_usage() -> str:
        """Возвращает текущее использование памяти процессом в удобочитаемом формате"""
        process = psutil.Process()
        mem_info = process.memory_info()
        mem_mb = mem_info.rss / 1024 / 1024  # Конвертируем в MB
        mem_gb = mem_mb / 1024  # Конвертируем в GB
        
        if mem_gb >= 1:
            return f"{mem_gb:.2f} GB"
        else:
            return f"{mem_mb:.0f} MB"
    
    @staticmethod
    def log_memory(label: str = ""):
        """Выводит текущее использование памяти в лог"""
        mem_usage = DocumentProcessor.get_memory_usage()
        if label:
            print(f"      [MEMORY] {label}: {mem_usage}")
        else:
            print(f"      [MEMORY] Использование: {mem_usage}")

    @staticmethod
    def clean_json_response(text: str) -> str:
        """Extracts JSON object from text, handling markdown code blocks and extra conversational text."""
        # First try to find a JSON block between curly braces
        match = re.search(r'\{.*\}', text, re.DOTALL)
        if match:
            return match.group(0)
            
        # Fallback to simple stripping if regex fails (e.g. if it's a list [])
        # But for this use case we expect a dict
        text = text.strip()
        if text.startswith("```json"):
            text = text[7:]
        elif text.startswith("```"):
            text = text[3:]
        if text.endswith("```"):
            text = text[:-3]
        return text.strip()

    @staticmethod
    def encode_image_to_base64(image_path: str) -> str:
        """Encode image to base64 for OpenAI API."""
        with open(image_path, "rb") as image_file:
            return base64.b64encode(image_file.read()).decode('utf-8')

    @staticmethod
    def parse_decimal(value: Optional[str]) -> Optional[Decimal]:
        if not value:
            return None
        cleaned = str(value)
        for token in ("\xa0", " ", "руб.", "руб", "р.", "р", "₽"):
            cleaned = cleaned.replace(token, "")
        cleaned = cleaned.replace(",", ".")
        allowed = "0123456789.-"
        normalized = "".join(ch for ch in cleaned if ch in allowed)
        if not normalized or normalized in {"-", ".", "-."}:
            return None
        try:
            return Decimal(normalized)
        except InvalidOperation:
            return None

    @staticmethod
    def format_decimal(value: Decimal) -> str:
        quantized = value.quantize(Decimal("0.01"))
        formatted = f"{quantized:,.2f}".replace(",", " ")
        return formatted.replace(".", ",")

    @staticmethod
    def format_count_with_word(count: int, singular: str, few: str, many: str) -> str:
        """Склоняет слово в зависимости от числа.
        
        Args:
            count: Число
            singular: Форма для 1 (счет)
            few: Форма для 2-4 (счета)
            many: Форма для 5+ и 0 (счетов)
            
        Returns:
            Строка вида "5 счетов", "1 счет", "3 счета"
        """
        if count % 10 == 1 and count % 100 != 11:
            return f"{count} {singular}"
        elif count % 10 in [2, 3, 4] and count % 100 not in [12, 13, 14]:
            return f"{count} {few}"
        else:
            return f"{count} {many}"

    @staticmethod
    def normalize_creditor_name(name: str) -> str:
        """Нормализует название кредитора (банка/МФО/МКК).
        
        Args:
            name: Исходное название
            
        Returns:
            Нормализованное название
        """
        if not name:
            return ""
        
        result = name.strip()
        
        # 0. ИСПРАВЛЕНИЕ БАГА: "Специализированное финан. общество: ..." -> "ООО СФО ..."
        # ПРИМЕР: Специализированное финан. общество: ОБЩЕСТВО С ОГРАНИЧЕННОЙ ОТВЕТСТВЕННОСТЬЮ СФО ПБ СЕРВИС ФИНАНС
        if "Специализированное финан. общество" in result:
             result = result.replace("Специализированное финан. общество:", "").strip()
             result = result.replace("Специализированное финан. общество", "").strip()

        # 1. Замена полных форм ОПФ на сокращенные (ДО очистки префиксов)
        replacements = [
            (r'ОБЩЕСТВО С ОГРАНИЧЕННОЙ ОТВЕТСТВЕННОСТЬЮ', 'ООО'),
            (r'ПУБЛИЧНОЕ АКЦИОНЕРНОЕ ОБЩЕСТВО', 'ПАО'),
            (r'АКЦИОНЕРНОЕ ОБЩЕСТВО', 'АО'),
            (r'ЗАКРЫТОЕ АКЦИОНЕРНОЕ ОБЩЕСТВО', 'ЗАО'),
            (r'МИКРОКРЕДИТНАЯ КОМПАНИЯ', 'МКК'),
            (r'МИКРОФИНАНСОВАЯ КОМПАНИЯ', 'МФК'),
            (r'СПЕЦИАЛИЗИРОВАННОЕ ФИНАНСОВОЕ ОБЩЕСТВО', 'СФО'),
            (r'ПРОФЕССИОНАЛЬНАЯ КОЛЛЕКТОРСКАЯ ОРГАНИЗАЦИЯ', 'ПКО'),
        ]
        
        for full, short in replacements:
            result = re.sub(r'\b' + full + r'\b', short, result, flags=re.IGNORECASE)

        # 2. Убираем префикс "МФО: " и похожие
        for prefix in ["МФО:", "Мфо:", "МФО :", "Мфо :", "Банк: ", "БАНК: ", "Банк :", "БАНК :"]:
            if result.startswith(prefix):
                result = result[len(prefix):].strip()
        
        # 2. Убираем историю переименований в скобках (ранее - ...)
        # Ищем последнюю открывающую скобку с текстом "ранее"
        result = re.sub(r'\s*\(ранее[^)]*\)\s*', ' ', result, flags=re.IGNORECASE)
        
        # 3. Нормализуем кавычки: '' "" → «»
        # СНАЧАЛА обрабатываем парные кавычки с помощью regex
        # Паттерн: ''текст'' → «текст»
        result = re.sub(r"''([^']+)''", r'«\1»', result)
        # Паттерн: ""текст"" → «текст»
        result = re.sub(r'""([^"]+)""', r'«\1»', result)
        # Паттерн: "текст" → «текст»
        result = re.sub(r'"([^"]+)"', r'«\1»', result)
        # Паттерн: 'текст' → «текст»
        result = re.sub(r"'([^']+)'", r'«\1»', result)
        
        # ПОТОМ заменяем умные кавычки (которые остались)
        result = result.replace('"', '«').replace('"', '»')  # Умные английские кавычки
        result = result.replace("'", '«').replace("'", '»')  # Умные одинарные кавычки
        
        # Разворачиваем неправильно развернутые кавычки ««текст»» → «текст»
        result = result.replace('««', '«').replace('»»', '»')
        
        # 4. Нормализуем аббревиатуры (МКК/МФК/ПКО в верхний регистр) ПЕРЕД перестановкой структуры
        result = re.sub(r'\b(мкк|мфк|мфо|пко|ооо|пао|ао)\b', lambda m: m.group(1).upper(), result, flags=re.IGNORECASE)
        
        # 5. Нормализуем структуру: ООО «Да-кредит МКК» → ООО МКК «Да-кредит»
        # Паттерн: ООО «[название] МКК/МФК» → ООО МКК/МФК «[название]»
        patterns = [
            # МКК/МФК в конце названия в кавычках
            (r'ООО\s+«([^»]+?)\s+(МКК|МФК|МФО|ПКО)»', r'ООО \2 «\1»'),
            # МКК/МФК в начале названия в кавычках: ООО «МКК Название» → ООО МКК «Название»
            (r'ООО\s+«(МКК|МФК|МФО|ПКО)\s+([^»]+)»', r'ООО \1 «\2»'),
            (r'Общество с ограниченной ответственностью\s+«([^»]+?)\s+(МКК|МФК|МФО|ПКО)»', r'ООО \2 «\1»'),
            (r'ООО\s+([^\s«]+)\s+(МКК|МФК|МФО|ПКО)', r'ООО \2 «\1»'),  # Без кавычек
        ]
        
        for pattern, replacement in patterns:
            result = re.sub(pattern, replacement, result, flags=re.IGNORECASE)
        
        # 6. Убираем лишние пробелы
        result = ' '.join(result.split())
        
        return result.strip()

    @staticmethod
    def extract_search_keywords(name: str) -> list:
        """Извлекает ключевые слова из названия для поиска в реестрах.
        
        Args:
            name: Название организации
            
        Returns:
            Список ключевых слов
        """
        import re
        
        # СПЕЦИАЛЬНАЯ ОБРАБОТКА для Т-Банк / ТБанк
        name_upper = name.upper()
        if "Т-БАНК" in name_upper or "ТБАНК" in name_upper or "Т БАНК" in name_upper or "T-BANK" in name_upper or "TBANK" in name_upper or "T BANK" in name_upper:
            return ["тбанк", "т-банк", "т банк"]
        
        # Убираем организационно-правовые формы и типы
        clean_name = name
        for prefix in ['ПАО', 'ОАО', 'ЗАО', 'АО', 'ООО', 'МКК', 'МФК', 'МФО', 'ПКО',
                       'Публичное акционерное общество', 'Акционерное общество',
                       'Общество с ограниченной ответственностью',
                       'Микрокредитная компания', 'Микрофинансовая компания',
                       'Микрофинансовая организация', 'Профессиональная коллекторская организация']:
            clean_name = re.sub(r'\b' + re.escape(prefix) + r'\b', '', clean_name, flags=re.IGNORECASE)
        
        # Убираем кавычки
        clean_name = clean_name.replace('«', '').replace('»', '').replace('"', '').replace("'", '')
        
        # Разбиваем на слова и берём значимые (длиннее 2 символов)
        words = [w.strip().lower() for w in clean_name.split() if len(w.strip()) > 2]
        
        return words

    @staticmethod
    def get_bank_address(bank_name: str, inn: Optional[str] = None) -> str:
        """Возвращает юридический адрес банка/МФО.

        Приоритет:
        1. Локальный словарь BANK_ADDRESSES
        2. RusProfile/Zachestnyibiznes (с приоритетом ИНН)
        3. Если ничего не найдено - пустая строка
        
        Args:
            bank_name: Название организации
            inn: ИНН организации (опционально) для более точного поиска
        """
        # Извлекаем ключевые слова для поиска
        keywords = DocumentProcessor.extract_search_keywords(bank_name)
        
        # Определяем тип организации по названию
        name_upper = bank_name.upper()
        # Добавляем ПКО и КОЛЛЕКТОРСКАЯ как признаки МФО/Взыскателя
        is_mfo = any(marker in name_upper for marker in ["МКК", "МФК", "МФО", "ПКО", "МИКРОКРЕДИТНАЯ", "МИКРОФИНАНСОВАЯ", "КОЛЛЕКТОРСКАЯ"])
        is_bank = any(marker in name_upper for marker in ["БАНК", "ПАО", "АО"])
        
        # 1. FALLBACK: словарь адресов банков и МФО (данные на 03.11.2025)
        BANK_ADDRESSES = {
            "СТРАТОСФЕРА": "630005, Новосибирская область, г Новосибирск, ул Фрунзе, зд. 88, помещ. 6",
            "ПОКОЛЕНИЕ": "301336, Тульская область, Веневский район, деревня Татарники, Степная ул, зд. 13, помещ. 2",
            "КЛИМП": "301336, Тульская область, Веневский район, деревня Татарники, Степная ул, зд. 13, помещ. 2",
        }

        # Нормализуем название банка для поиска
        search_name = bank_name.upper().strip()

        # Убираем ОПФ и лишние символы (только целые слова в начале/конце)
        import re
        for opf in ["ПАО", "АО", "ООО", "ОАО", "МФК", "МКК", "ПКО", "КБ"]:
            # Убираем ОПФ только в начале или конце (целое слово)
            search_name = re.sub(r'\b' + opf + r'\b', ' ', search_name)

        search_name = search_name.replace("«", "").replace("»", "").replace('"', "")
        search_name = search_name.replace("-", " ")
        while "  " in search_name:
            search_name = search_name.replace("  ", " ")
        search_name = search_name.strip()

        # Ищем точное совпадение
        if search_name in BANK_ADDRESSES:
            return BANK_ADDRESSES[search_name]

        # Ищем частичное совпадение (самое длинное)
        matches = []
        for key, addr in BANK_ADDRESSES.items():
            # Проверяем наличие ключевых слов из key в search_name
            key_words = set(key.split())
            search_words = set(search_name.split())

            # Если все слова из key есть в search_name (или наоборот)
            if key_words.issubset(search_words) or search_words.issubset(key_words):
                matches.append((len(key), addr))
            # Или если одна строка содержит другую
            elif key in search_name or search_name in key:
                matches.append((len(key), addr))

        if matches:
            matches.sort(reverse=True)
            return matches[0][1]

        # 3. FALLBACK: Парсим адрес с RusProfile если не нашли в локальном словаре
        if inn:
             print(f"[ADDRESS_PARSER] Адрес не найден в словаре для '{bank_name}', пробую RusProfile с ИНН {inn}...")
        else:
             print(f"[ADDRESS_PARSER] Адрес не найден в словаре для '{bank_name}', пробую RusProfile по названию...")

        address_from_rusprofile = DocumentProcessor.parse_address_from_rusprofile(bank_name, inn)
        if address_from_rusprofile:
            return address_from_rusprofile

        # Если не нашли - НЕ ВОЗВРАЩАЕМ "г. Москва", лучше пустую строку, чтобы взялся адрес из документа
        # if any(word in bank_name.upper() for word in ["БАНК", "МФК", "МКК", "МФО"]):
        #    return "г. Москва"

        return ""


    @staticmethod
    def validate_inn(inn: str) -> bool:
        """Проверяет корректность ИНН по контрольной сумме.
        
        Args:
            inn: строка с ИНН (10 или 12 цифр)
            
        Returns:
            True если ИНН валиден, False иначе
        """
        if not inn or not inn.isdigit():
            return False
            
        if len(inn) == 10:
            # ИНН юридического лица (10 цифр)
            coefficients = [2, 4, 10, 3, 5, 9, 4, 6, 8]
            checksum = sum(int(inn[i]) * coefficients[i] for i in range(9)) % 11
            checksum = checksum % 10
            return checksum == int(inn[9])
            
        elif len(inn) == 12:
            # ИНН физического лица (12 цифр)
            # Первая контрольная цифра
            coefficients1 = [7, 2, 4, 10, 3, 5, 9, 4, 6, 8]
            checksum1 = sum(int(inn[i]) * coefficients1[i] for i in range(10)) % 11
            checksum1 = checksum1 % 10
            
            # Вторая контрольная цифра  
            coefficients2 = [3, 7, 2, 4, 10, 3, 5, 9, 4, 6, 8]
            checksum2 = sum(int(inn[i]) * coefficients2[i] for i in range(11)) % 11
            checksum2 = checksum2 % 10
            
            return checksum1 == int(inn[10]) and checksum2 == int(inn[11])
        
        return False


    @staticmethod
    def parse_inn_and_address_from_rusprofile(company_name: str, _depth: int = 0, _try_full_name: bool = True, _try_abbreviation: bool = False) -> tuple[Optional[str], Optional[str]]:
        """Парсит ИНН и адрес организации с RusProfile.ru с валидацией названия
        
        Args:
            company_name: Название организации для поиска
            _depth: Глубина рекурсии (для retry с новыми параметрами)
            _try_full_name: Если True, искать с полным названием (не удалять ОПФ). По умолчанию True (сначала пробуем полное).
            _try_abbreviation: Если True, пробуем искать по аббревиатуре (по последней надежде)
            
        Returns:
            Кортеж (ИНН, адрес) или (None, None) если не найдено
        """
        # DEBUG: Проверка аргументов при входе
        if _try_abbreviation:
            # DEBUG: Принудительно сбрасываем глубину, если она некорректно передалась как >= 3
            if _depth >= 3:
                print(f"[DEBUG_ARGS] _try_abbreviation=True but _depth={_depth}. FORCING RESET TO 0.")
                _depth = 0

        if not company_name or not company_name.strip():
            return None, None
        
        # Максимум 3 попытки с разными User-Agent и IP
        MAX_RETRIES = 3
        
        if _depth >= MAX_RETRIES:
            print(f"[RUSPROFILE] ⚠️ Достигнут лимит попыток ({MAX_RETRIES}) для '{company_name}'")
            return None, None

        try:
            # Импорты внутри метода, чтобы не засорять глобальную область, если они нужны только тут
            import re
            import random
            import difflib
            import requests
            import time
            from urllib.parse import quote_plus
            from bs4 import BeautifulSoup
            
            # Вывод информации о текущей попытке
            if _depth > 0:
                print(f"[RUSPROFILE] 🔄 Попытка #{_depth + 1}/{MAX_RETRIES} для '{company_name}'")
            
            from random_user_agent.user_agent import UserAgent
            import asyncio
            from patchright_scraper import PatchrightScraper, ScraperConfig

            # Очищаем имя ПЕРЕД поиском
            clean_search_query = company_name
            # Убираем явно мусорные префиксы/суффиксы, которые ломают поиск RusProfile
            clean_search_query = re.sub(r'^Специализированное финан\. общество:\s*', '', clean_search_query, flags=re.IGNORECASE)
            clean_search_query = re.sub(r'^ОБЩЕСТВО С ОГРАНИЧЕННОЙ ОТВЕТСТВЕННОСТЬЮ\s*', '', clean_search_query, flags=re.IGNORECASE)
            # Убираем кавычки из поискового запроса для лучшего матчинга
            clean_search_query = re.sub(r'["\'«»]', '', clean_search_query)
            
            # Сохраняем оригинальное название до упрощения
            before_simplification = clean_search_query
            
            # Инициализация переменных для предотвращения UnboundLocalError
            inn = None
            
            # === ФОРМИРОВАНИЕ ЗАПРОСА ===
            
            if _try_abbreviation:
                # ПОСЛЕДНЯЯ ПОПЫТКА: Формируем аббревиатуру
                # Пример: "МКК Универсального Финансирования" -> "МКК УФ"
                # Пример: "МКК КонтораКонтора" -> "МКК КК" (разбиваем по заглавным)
                
                # 1. Извлекаем ОПФ (МКК, МФК, АО...)
                opf_match = re.search(r'^\s*((ООО|АО|ПАО|ЗАО|МКК|МФК|МФО|НКО|ПКО|Банк)\s+)+', clean_search_query, flags=re.IGNORECASE)
                opf_part = opf_match.group(0).strip() if opf_match else ""
                
                # Остальная часть названия
                name_part = clean_search_query[len(opf_part):].strip()
                
                abbr_parts = []
                
                # Разбиваем на слова
                words = name_part.split()
                for word in words:
                    # Если слово длинное и содержит CamelCase (КонтораКонтора) -> КК
                    # Ищем заглавные буквы внутри слова
                    upper_letters = re.findall(r'[A-ZА-ЯЁ]', word)
                    if len(upper_letters) > 1:
                        # CamelCase - берем все заглавные
                        abbr_parts.append("".join(upper_letters))
                    elif len(word) > 0:
                        # Обычное слово (или слитно без заглавных)
                        # Стратегия: если слово длинное (>10 букв) и не CamelCase - берем его целиком, т.к. сократить надежно нельзя
                        # Пример: "МКК Инвестгрупп" -> "МКК Инвестгрупп" (не И)
                        # Пример: "МКК займэкспресс" -> "МКК займэкспресс"
                        if len(word) > 10 and len(words) == 1:
                             abbr_parts.append(word)
                        else:
                             # Иначе берем первую букву
                             abbr_parts.append(word[0].upper())
                
                abbreviation = "".join(abbr_parts)
                
                # Формируем новый запрос: ОПФ + Аббревиатура
                if opf_part:
                    clean_search_query = f"{opf_part} {abbreviation}"
                else:
                    clean_search_query = abbreviation
                
                print(f"[RUSPROFILE] 🔠 Пробуем поиск по аббревиатуре: '{clean_search_query}' (из '{company_name}')")

            # Убираем ООО, АО, ПАО из начала строки для чистоты, если они остались
            # НО ТОЛЬКО если не указан флаг _try_full_name (тогда оставляем полное название)
            # И если это не режим аббревиатуры (где мы сами сформировали запрос)
            elif not _try_full_name:
                # В упрощенном режиме удаляем только организационно-правовую форму (ООО, АО),
                # но СТАРАЕМСЯ СОХРАНИТЬ тип организации (МКК, МФК, Банк, ПКО), так как это важно для уникальности.
                # Пример: "ООО МКК Стратосфера" -> "МКК Стратосфера" (а не просто "Стратосфера", которая может быть питомником)
                
                # Удаляем только "ООО", "АО", "ПАО", "ЗАО"
                clean_search_query = re.sub(r'^((ООО|АО|ПАО|ЗАО)\s+)+', '', clean_search_query, flags=re.IGNORECASE)
                
                # Если после удаления осталось слишком мало текста (например "МКК А"),
                # то возможно стоит искать просто "А" или оставить как есть?
                # Оставляем как есть, так как "МКК А" точнее чем "А".
            
            # Запоминаем, было ли упрощение
            was_simplified = (clean_search_query != before_simplification)

            # Убираем скобки с конца
            clean_search_query = re.sub(r'\)\)+$', '', clean_search_query) 
            
            # Определяем, является ли запрос ИНН (состоит только из цифр, 10 или 12 знаков)
            is_inn_search = bool(re.match(r'^\d{10,12}$', clean_search_query))
            if is_inn_search:
                print(f"[RUSPROFILE] Запрос определен как поиск по ИНН: {clean_search_query}")
            
            # Функция нормализации для сравнения названий
            def normalize_name_for_compare(name: str) -> str:
                if not name: return ""
                n = name.lower()
                # Удаляем длинные ОПФ first
                for entity in ["общество с ограниченной ответственностью", "акционерное общество", "публичное акционерное общество", "специализированное финансовое общество", "специализированное финан. общество", "сфо"]:
                     n = re.sub(r'\b' + re.escape(entity) + r'\b', '', n)
                
                # Удаляем короткие ОПФ
                for entity in ["ооо", "пао", "ао", "зао", "мкк", "мфк", "мфо", "пко", "нко", "банк", "кб"]:
                     n = re.sub(r'\b' + entity + r'\b', '', n)
                     
                # Удаляем кавычки, скобки и спецсимволы
                n = re.sub(r'["«»\(\)]', ' ', n)
                
                # Удаляем "грязные" суффиксы типа ))
                n = n.replace("))", "")
                
                return " ".join(n.split())

            normalized_query = normalize_name_for_compare(clean_search_query)
            
            # Рандомная задержка перед запросом
            time.sleep(random.uniform(1.0, 2.5))
            
            # Используем очищенный запрос
            print(f"[RUSPROFILE] Поиск по запросу: '{clean_search_query}' (orig: '{company_name}')")
            search_url = f"https://www.rusprofile.ru/search?query={quote_plus(clean_search_query)}"
            
            # === ЗАМЕНЕНО НА PATCHRIGHT ===
            async def fetch_with_patchright(url):
                # Настройка скрапера с максимальной маскировкой
                config = ScraperConfig(
                    use_stealth=True,
                    simulate_human=True, # Включаем симуляцию человеческого поведения
                    headless=True,       # Скрытый режим (можно False для отладки)
                    debug=False,
                    use_persistent_context=True # Включаем постоянный контекст
                )
                async with PatchrightScraper(config) as scraper:
                    # fetch_content теперь возвращает [(content, final_url)]
                    results = await scraper.fetch_content(url)
                    if results and len(results) > 0:
                        return results[0]  # (content, final_url)
                    return None, url

            html, response_url = None, None
            try:
                # Попытка определить текущий loop
                try:
                    loop = asyncio.get_running_loop()
                except RuntimeError:
                    loop = None
                
                if loop and loop.is_running():
                     # Мы уже в асинхронном контексте. Нельзя вызывать asyncio.run()
                     # Но наша функция синхронная. 
                     # Вариант 1: nest_asyncio (нужна установка).
                     # Вариант 2: запуск в отдельном потоке.
                     # Попробуем вариант с новым потоком, чтобы изолировать лупы.
                     from concurrent.futures import ThreadPoolExecutor
                     def run_in_new_loop(coro):
                         new_loop = asyncio.new_event_loop()
                         asyncio.set_event_loop(new_loop)
                         try:
                             return new_loop.run_until_complete(coro)
                         finally:
                             new_loop.close()

                     with ThreadPoolExecutor() as executor:
                         # Оборачиваем вызов корутины
                         future = executor.submit(run_in_new_loop, fetch_with_patchright(search_url))
                         result = future.result()
                         if result:
                             html, response_url = result
                else:
                    # Обычный синхронный контекст
                    result = asyncio.run(fetch_with_patchright(search_url))
                    if result:
                        html, response_url = result

            except Exception as e:
                print(f"[RUSPROFILE] Ошибка PatchrightScraper при поиске '{company_name}': {e}")
                if _depth == 0:
                     print(f"[FALLBACK] RusProfile error, trying Zachestnyibiznes...")
                     return DocumentProcessor.parse_inn_and_address_from_zachestnyibiznes(company_name)
                return None, None
            
            # Определяем, произошел ли редирект (RusProfile сразу открыл страницу компании)
            # Patchright уже прошел редиректы и вернул final_url
            response_url = response_url if response_url else search_url
            is_redirected = response_url != search_url and "/search" not in response_url
            
            # Функция нормализации для сравнения названий
            def normalize_name_for_compare(name: str) -> str:
                if not name: return ""
                n = name.lower()
                # Удаляем длинные ОПФ first
                for entity in ["общество с ограниченной ответственностью", "акционерное общество", "публичное акционерное общество", "специализированное финансовое общество", "специализированное финан. общество", "сфо"]:
                     n = re.sub(r'\b' + re.escape(entity) + r'\b', '', n)
                
                # Удаляем короткие ОПФ
                for entity in ["ооо", "пао", "ао", "зао", "мкк", "мфк", "мфо", "пко", "нко", "банк", "кб"]:
                     n = re.sub(r'\b' + entity + r'\b', '', n)
                     
                # Удаляем кавычки, скобки и спецсимволы
                n = re.sub(r'["«»\(\)]', ' ', n)
                
                # Удаляем "грязные" суффиксы типа ))
                n = n.replace("))", "")
                
                return " ".join(n.split())

            normalized_query = normalize_name_for_compare(clean_search_query)

            # Функция нормализации для сравнения названий
            def normalize_name_for_compare(name: str) -> str:
                if not name: return ""
                n = name.lower()
                # Удаляем длинные ОПФ first
                for entity in ["общество с ограниченной ответственностью", "акционерное общество", "публичное акционерное общество", "специализированное финансовое общество", "специализированное финан. общество", "сфо"]:
                     n = re.sub(r'\b' + re.escape(entity) + r'\b', '', n)
                
                # Удаляем короткие ОПФ
                for entity in ["ооо", "пао", "ао", "зао", "мкк", "мфк", "мфо", "пко", "нко", "банк", "кб"]:
                     n = re.sub(r'\b' + entity + r'\b', '', n)
                     
                # Удаляем кавычки, скобки и спецсимволы
                n = re.sub(r'["«»\(\)]', ' ', n)
                
                # Удаляем "грязные" суффиксы типа ))
                n = n.replace("))", "")
                
                return " ".join(n.split())

            normalized_query = normalize_name_for_compare(clean_search_query)
            soup = BeautifulSoup(html, "lxml")
            
            # Проверка на антибот / капчу
            # "Строй..." содержит "ой", поэтому простая проверка на "ой" дает ложные срабатывания
            page_title = soup.title.get_text().lower() if soup.title else ""
            if "ой!" in page_title or "вы робот" in page_title or "captcha" in page_title or "доступ ограничен" in page_title:
                print(f"[RUSPROFILE] Обнаружена капча для '{company_name}'")
                if _depth == 0:
                     print(f"[FALLBACK] RusProfile captcha, trying Zachestnyibiznes...")
                     return DocumentProcessor.parse_inn_and_address_from_zachestnyibiznes(company_name)
                return None, None

            candidates = []
            
            # 1. Если это страница списка результатов
            items = soup.select("div.list-element")
            if items:
                for item in items:
                    name_tag = item.select_one("a.list-element__title")
                    if not name_tag: continue
                    
                    # RusProfile выделяет совпадения тегом <mark>, text(strip=True) склеит их
                    # <mark>OOO</mark> <mark>"..."</mark> -> OOO"..."
                    # Добавим пробелы вокруг mark перед извлечением
                    for mark in name_tag.select("mark"):
                        mark.insert_before(" ")
                        mark.insert_after(" ")

                    raw_name = name_tag.get_text(separator=" ", strip=True)
                    # Чистим лишние пробелы и кавычки для сравнения
                    normalized_name = normalize_name_for_compare(raw_name)

                    # Извлекаем ИНН
                    inn = None
                    for info_block in item.select("div.list-element__row-info span"):
                        text = info_block.get_text()
                        if "ИНН" in text:
                            m = re.search(r"(\d{10}|\d{12})", text)
                            if m: inn = m.group(1)
                    
                    # Считаем схожесть
                    if is_inn_search:
                        # Если искали по ИНН, проверяем на совпадение ИНН, а не названия
                        similarity = 1.0 if inn == clean_search_query else 0.0
                    elif _try_abbreviation:
                        # При поиске по аббревиатуре:
                        # 1. Если нашли полное совпадение с аббревиатурой (например ООО "УФ") - супер
                        # 2. Если нашли полное название - тоже супер
                        short_name_check = difflib.SequenceMatcher(None, clean_search_query.lower(), normalized_name.lower()).ratio()
                        
                        # Извлекаем аббревиатуру из запроса (без ОПФ)
                        # Пример: запрос "МКК УФ" -> "УФ"
                        query_abbr_parts = clean_search_query.split()
                        # Если первый элемент это ОПФ, убираем его
                        if len(query_abbr_parts) > 1 and query_abbr_parts[0].upper() in ["МКК", "МФК", "МФО", "ПКО", "ООО", "АО", "ПАО"]:
                             orig_abbr = "".join(query_abbr_parts[1:]).lower()
                        else:
                             orig_abbr = "".join(query_abbr_parts).lower()

                        # Строим аббревиатуру для НАЙДЕННОГО названия
                        words = normalized_name.split()
                        found_acronym = "".join([w[0] for w in words if w]).lower()
                        
                        # Проверка:
                        # 1. Запрошенная аббревиатура содержится в аббревиатуре найденного названия? (УФ in УФ)
                        # 2. Аббревиатура найденного названия == запрошенной аббревиатуре?
                        
                        # Логируем для отладки
                        # print(f"[DEBUG_ABBR] Запрос авв: '{orig_abbr}', Найдено авв: '{found_acronym}', Name: '{normalized_name}'")

                        if orig_abbr in found_acronym or found_acronym in orig_abbr:
                            similarity = 1.0
                        else:
                            similarity = short_name_check
                    else:
                        similarity = difflib.SequenceMatcher(None, normalized_query, normalized_name).ratio()
                        
                        # ПРОВЕРКА АББРЕВИАТУР (для случаев "Универсального Финансирования" <-> "УФ")
                        # Если одна из строк короткая (до 5 букв), проверяем является ли она аббревиатурой другой
                        if len(normalized_name) < 6 or len(normalized_query) < 6:
                            # Получаем слова из длинной строки
                            long_str = normalized_name if len(normalized_name) > len(normalized_query) else normalized_query
                            short_str = normalized_query if len(normalized_name) > len(normalized_query) else normalized_name
                            
                            # Собираем первые буквы слов длинной строки
                            words = long_str.split()
                            acronym = "".join([w[0] for w in words if w]).lower()
                            
                            # Если аббревиатура совпадает с короткой строкой (или почти совпадает)
                            if acronym == short_str.replace(" ", "") or short_str in acronym:
                                 print(f"[RUSPROFILE] Обнаружено совпадение по аббревиатуре: {short_str} <-> {long_str}")
                                 similarity = max(similarity, 0.95) # Повышаем скор до почти идеального

                    # Извлекаем Адрес
                    inn = None
                    for info_block in item.select("div.list-element__row-info span"):
                        text = info_block.get_text()
                        if "ИНН" in text:
                            m = re.search(r"(\d{10}|\d{12})", text)
                            if m: inn = m.group(1)
                    
                    # Извлекаем Адрес
                    address = None
                    addr_tag = item.select_one("div.list-element__address")
                    if addr_tag:
                         address = addr_tag.get_text(separator=" ", strip=True)
                         
                    link = "https://www.rusprofile.ru" + name_tag["href"] if name_tag.get("href") else None
                    
                    candidates.append({
                        "name": raw_name,
                        "inn": inn,
                        "address": address,
                        "score": similarity,
                        "url": link,
                        "is_main_page": False
                    })
                    
            # 2. Если нас сразу перекинуло на страницу компании (или только 1 результат)
            # Признак страницы компании: есть h1 с itemprop="name", id="clip_inn" или заголовок "ООО ..." и блоки информации
            elif soup.select_one('[id^="clip_inn"]') or (soup.select_one("h1") and "ООО" in soup.select_one("h1").get_text()) or soup.find(string=re.compile("ИНН")):
                h1 = soup.select_one("h1")
                raw_name = h1.get_text(strip=True) if h1 else ""
                
                # ИНН
                inn = None
                inn_tag = soup.select_one('[id^="clip_inn"]')
                if inn_tag:
                    inn = inn_tag.get_text(strip=True)
                else:
                    # Fallback поиска ИНН по тексту
                    inn_match = re.search(r'ИНН(?:/КПП)?\s*(\d{10}|\d{12})', html)
                    if inn_match:
                        inn = inn_match.group(1)

                if is_inn_search:
                     # Если искали по ИНН, проверяем на совпадение ИНН, а не названия
                     similarity = 1.0 if inn == clean_search_query else 0.0
                elif _try_abbreviation:
                     # Если искали по аббревиатуре, считаем идеальным совпадением (мы нашли страницу, это хорошо)
                     # RusProfile не открывает случайные страницы, если запрос точный
                     similarity = 1.0
                else:
                    normalized_name = normalize_name_for_compare(raw_name)
                    similarity = difflib.SequenceMatcher(None, normalized_query, normalized_name).ratio()
                
                # Адрес
                address = None
                addr_tag = soup.select_one("#clip_address")
                if addr_tag:
                    # Чистим мусор (кнопки копирования и скрытые элементы)
                    for dead in addr_tag.select(".copy_button, script, style, meta"):
                        dead.decompose()

                    # Обрабатываем "разорванные" номера домов/помещений типа 5<span class="long_copy">2</span>
                    # Нужно удалить теги span, но оставить их текст
                    # BS4 unwrap() делает это
                    for span in addr_tag.find_all("span", class_="long_copy"):
                         span.unwrap()
                    
                    # Теперь у нас чистый HTML без разрыва слов, можно брать get_text
                    # Используем separator=" " на случай, если части адреса в разных блоках
                    address = addr_tag.get_text(separator=" ", strip=True)
                    
                    # Чистим множественные пробелы и восстанавливаем запятые
                    address = re.sub(r'\s+', ' ', address)
                    address = re.sub(r'\s*,\s*', ', ', address)
                else:
                     # Fallback 1: Поиск по тексту "Юридический адрес"
                    addr_block = soup.find(string=re.compile("Юридический адрес"))
                    if addr_block:
                         parent = addr_block.find_parent("div") or addr_block.find_parent("span")
                         if parent:
                             full_text = parent.get_text(" ", strip=True)
                             address = full_text.replace("Юридический адрес", "").strip() 
                             address = address.split("Сведения об адресе недостоверны")[0].strip()
                    
                    # Fallback 2: Если адрес всё ещё не найден, ищем любой текст, похожий на адрес с индексом
                    if not address:
                        # Ищем паттерн: 6 цифр (индекс) + запятая + город/область
                        # Пример: 123456, г. Москва...
                        # Ограничиваем поиск первыми 2000 символов body, чтобы не цеплять мусор
                        body_text = soup.body.get_text(" ", strip=True)[:5000] if soup.body else ""
                        addr_match = re.search(r'(\d{6},\s*[А-Яа-я\.\s-]+\s*(?:г\.|город|обл\.|край|респ\.|АО|Москва|Санкт-Петербург)[^;]+)', body_text)
                        if addr_match:
                            potential_addr = addr_match.group(1).strip()
                            # Простая проверка длины, чтобы не взять слишком много
                            if 10 < len(potential_addr) < 150:
                                address = potential_addr

                candidates.append({
                    "name": raw_name,
                    "inn": inn,
                    "address": address,
                    "score": similarity,
                    "url": response_url,
                    "is_main_page": True
                })

            if not candidates:
                print(f"[RUSPROFILE] Ничего не найдено для '{company_name}' (depth={_depth})")
                # Не возвращаем сразу, даем шанс рекурсивному поиску по упрощенному названию/аббревиатуре ниже
                
            # Сортируем кандидатов по score (схожести названия) от большего к меньшему
            candidates.sort(key=lambda x: x["score"], reverse=True)

            # Перебираем ВСЕХ кандидатов, пока не найдем подходящего
            # Раньше брали только первого (candidates[0]), и если он не подходил - сдавались
            found_valid = False
            best_candidate = None
            
            for idx, candidate in enumerate(candidates):
                best = candidate
                print(f"[RUSPROFILE] Проверка кандидата #{idx+1}: '{best['name']}' (score={best['score']:.2f})")

                # Минимальный порог similarity для защиты от случайных редиректов
                MIN_SIMILARITY_THRESHOLD = 0.5
                
                # Дополнительная проверка: защита от редиректов на неправильный тип компании
                query_upper = normalized_query.upper()
                result_upper = normalize_name_for_compare(best['name']).upper()
                
                # Финансовые маркеры в запросе
                financial_markers = ['БАНК', 'МКК', 'МФК', 'МФО', 'КРЕДИТ', 'ЗАЙМ', 'ФИНАНС', 'СФО']
                has_financial_in_query = any(marker in query_upper for marker in financial_markers)
                
                # Стоп-слова
                stop_words = ['ОВД', 'ПОЛИЦИЯ', 'МВД', 'АПТЕК', 'СТРОЙ', 'МЕДИЦИН', 'ПОЛИКЛИНИК', 
                             'ШКОЛА', 'БОЛЬНИЦ', 'МУП', 'ГУП', 'АДМИНИСТРАЦИЯ', 'УПРАВЛЕНИЕ']
                has_stopword_in_result = any(word in result_upper for word in stop_words)
                
                # Если ищем финансовую организацию, но в результате нет финансовых маркеров
                has_financial_in_result = any(marker in result_upper for marker in financial_markers)
                
                # Валидация
                is_valid_result = True
                rejection_reason = ""
                
                # 1. Similarity
                if not is_inn_search and best['score'] < MIN_SIMILARITY_THRESHOLD:
                    is_valid_result = False
                    rejection_reason = f"низкое совпадение (score={best['score']:.2f} < {MIN_SIMILARITY_THRESHOLD})"
                
                # 2. ИНН
                elif is_inn_search and best['score'] < 1.0:
                    is_valid_result = False
                    rejection_reason = f"несовпадение ИНН (ожидался {clean_search_query}, найден {best.get('inn')})"

                # 2.1. Аббревиатура
                elif _try_abbreviation and best['score'] < 0.6:
                    is_valid_result = False
                    rejection_reason = f"не найдено совпадений по аббревиатуре (score={best['score']:.2f})"

                # 3. Стоп-слова
                elif has_stopword_in_result:
                    is_valid_result = False
                    rejection_reason = f"найдена неправильная организация (не финансовая)"
                
                # 3. Тип организации
                elif not is_inn_search and has_financial_in_query and not has_financial_in_result and best['score'] < 0.95:
                    # Если совпадение ОЧЕНЬ высокое (0.95+), прощаем отсутствие маркера (вдруг название странное)
                    # Но если ниже 0.95 и нет "МКК/Банк" - бракуем
                    is_valid_result = False
                    rejection_reason = f"искали финансовую организацию, нашли другой тип"
                
                # 4. Валидация ИНН
                if is_valid_result and best['inn']:
                    if not DocumentProcessor.validate_inn(best['inn']):
                        is_valid_result = False
                        rejection_reason = f"ИНН {best['inn']} не прошел валидацию контрольной суммы"
                
                if is_valid_result:
                    print(f"[RUSPROFILE] ✅ Найдена подходящая компания: '{best['name']}' (ИНН: {best.get('inn')})")
                    found_valid = True
                    best_candidate = best
                    break
                else:
                    print(f"[RUSPROFILE] ❌ Кандидат '{best['name']}' ОТКЛОНЕН - {rejection_reason}")

            # Если нашли валидного кандидата - возвращаем его результат
            if found_valid and best_candidate:
                final_address = best_candidate.get("address")
                
                # Если данных не хватает ИЛИ адрес слишком короткий (вероятно просто город), идем на страницу карточки
                # Также проверяем, что в адресе есть индекс (6 цифр)
                import re
                is_bad_address = (
                    not final_address or 
                    len(final_address) < 20 or 
                    "г. Москва" == final_address.strip() or
                    not re.search(r'\d{6}', final_address)
                )
                
                # Если найден URL и (нет ИНН или плохой адрес) и это не главная страница
                if best_candidate.get("url") and (not best_candidate.get("inn") or is_bad_address) and not best_candidate.get("is_main_page"):
                    print(f"[RUSPROFILE] ℹ️ Переходим на карточку для уточнения данных (bad_address={is_bad_address})...")
                    time.sleep(random.uniform(1.5, 3.0))
                    
                    try:
                        card_html = None
                        try:
                            # Логика запуска async функции
                            try:
                                loop = asyncio.get_running_loop()
                            except RuntimeError:
                                loop = None
                            
                            if loop and loop.is_running():
                                 from concurrent.futures import ThreadPoolExecutor
                                 def run_in_new_loop_inner(coro):
                                     new_loop = asyncio.new_event_loop()
                                     asyncio.set_event_loop(new_loop)
                                     try:
                                         return new_loop.run_until_complete(coro)
                                     finally:
                                         new_loop.close()

                                 with ThreadPoolExecutor() as executor:
                                     future = executor.submit(run_in_new_loop_inner, fetch_with_patchright(best_candidate["url"]))
                                     result = future.result()
                                     if result:
                                         card_html, _ = result
                            else:
                                result = asyncio.run(fetch_with_patchright(best_candidate["url"]))
                                if result:
                                    card_html, _ = result
                        except Exception as e_run:
                             print(f"[RUSPROFILE] Ошибка при переходе на карточку: {e_run}")

                        if card_html:
                            card_soup = BeautifulSoup(card_html, "lxml")
                            
                            # Достаем ИНН
                            if not best_candidate.get("inn"):
                                inn_tag = card_soup.select_one('[id^="clip_inn"]')
                                if inn_tag:
                                    extracted_inn = inn_tag.get_text(strip=True)
                                    if DocumentProcessor.validate_inn(extracted_inn):
                                        best_candidate["inn"] = extracted_inn
                                    else:
                                        print(f"[RUSPROFILE] ⚠️ ИНН со страницы карточки не прошел валидацию: {extracted_inn}")
                                
                            # Достаем Адрес
                            addr_tag = card_soup.select_one("#clip_address")
                            if addr_tag:
                                for dead in addr_tag.select(".copy_button, script, style, meta, .company-info__hidden"):
                                    dead.decompose()
                                for span in addr_tag.find_all("span", class_="long_copy"):
                                     span.unwrap()
                                
                                new_address = addr_tag.get_text(separator=" ", strip=True)
                                new_address = re.sub(r'\s+', ' ', new_address)
                                new_address = re.sub(r'\s*,\s*', ', ', new_address)
                                
                                if new_address and len(new_address) > len(final_address or ""):
                                    final_address = new_address
                                    best_candidate["address"] = final_address
                                    print(f"[RUSPROFILE] ✅ Адрес уточнен на карточке: {final_address}")
                                    
                    except Exception as e:
                        print(f"[RUSPROFILE] Не удалось открыть карточку: {e}")

                # Кешируем результат
                if best_candidate:
                    DocumentProcessor._bank_data_cache[company_name] = (best_candidate.get('inn'), best_candidate.get('address'))
                    return best_candidate.get('inn'), best_candidate.get('address')
            
            # === РЕКУРСИВНЫЙ ПОИСК (ЕСЛИ НИЧЕГО НЕ НАШЛИ) ===
            
            # 1. Если искали по полному названию (по умолчанию) и не нашли -> пробуем без ОПФ
            if _try_full_name:
                print(f"[RUSPROFILE] ⚠️ Полное название не сработало, пробуем упрощенное (без ОПФ)...")
                # Рекурсивный вызов с _try_full_name=False
                inn, addr = DocumentProcessor.parse_inn_and_address_from_rusprofile(company_name, _depth=_depth + 1, _try_full_name=False, _try_abbreviation=False)
                if inn: return inn, addr

            # 2. Если уже искали без ОПФ и не нашли -> пробуем аббревиатуру
            #    (Но только если мы еще не в режиме аббревиатуры)
            if not _try_full_name and not _try_abbreviation:
                 # Эвристика: имеет смысл пробовать аббревиатуру, если название состоит из >1 слова
                 if len(company_name.split()) > 1 or len(company_name) > 10:
                     print(f"[RUSPROFILE] ⚠️ Упрощенное название не сработало, пробуем поиск по аббревиатуре...")
                     inn, addr = DocumentProcessor.parse_inn_and_address_from_rusprofile(company_name, _depth=_depth + 1, _try_full_name=False, _try_abbreviation=True)
                     if inn: return inn, addr
            
            # Если мы дошли сюда (цикл завершился неудачно и исключений не было, либо исключение было последней попыткой)
            # И это была корневая попытка (_depth=0) -> пробуем альтернативный сервис
            if _depth == 0 and not _try_abbreviation:
                 print(f"[RUSPROFILE] ❌ Все попытки исчерпаны. Пробуем Zachestnyibiznes...")
                 return DocumentProcessor.parse_inn_and_address_from_zachestnyibiznes(company_name)
                        
            return None, None
            
        except Exception as e:
            print(f"[RUSPROFILE] Критическая ошибка парсинга для '{company_name}': {e}")
            if _depth == 0:
                 return DocumentProcessor.parse_inn_and_address_from_zachestnyibiznes(company_name)
            return None, None

    @staticmethod
    def parse_inn_from_rusprofile(company_name: str) -> Optional[str]:
        """Парсит только ИНН организации с RusProfile.ru (обратная совместимость)
        
        Args:
            company_name: Название организации для поиска
            
        Returns:
            ИНН (10 или 12 цифр) или None если не найден
        """
        inn, _ = DocumentProcessor.parse_inn_and_address_from_rusprofile(company_name)
        return inn
    

    @staticmethod
    def parse_inn_and_address_from_zachestnyibiznes(company_name: str) -> tuple[Optional[str], Optional[str]]:
        """Парсит ИНН и адрес организации с zachestnyibiznes.ru
           (Fallback метод, если RusProfile не нашел)
        """
        if not company_name or not company_name.strip():
            return None, None

        # Очищаем имя ПЕРЕД поиском (убираем ОПФ и кавычки как для RusProfile)
        clean_search_query = company_name
        # УЛУЧШЕНИЕ: Удаляем ВСЕ префиксы подряд (ООО МКК ..., МФК ...)
        clean_search_query = re.sub(r'^((ООО|АО|ПАО|ЗАО|МКК|МФК|МФО|НКО|ПКО|Банк)\s+)+', '', clean_search_query, flags=re.IGNORECASE)
        clean_search_query = re.sub(r'["\'«»]', '', clean_search_query).strip()
        
        print(f"[ZACHESTNYIBIZNES] Поиск по запросу: '{clean_search_query}' (orig: '{company_name}')")
        
        try:
            import requests
            from bs4 import BeautifulSoup
            from urllib.parse import quote_plus
            import difflib
            import random

            # Headers как у браузера
            headers = {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
                "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8",
                "Accept-Language": "ru-RU,ru;q=0.9,en-US;q=0.8,en;q=0.7",
            }
            
            search_url = f"https://zachestnyibiznes.ru/search?query={quote_plus(clean_search_query)}"
            response = requests.get(search_url, headers=headers, timeout=15)
            
            if response.status_code != 200:
                print(f"[ZACHESTNYIBIZNES] Ошибка при поиске: статус {response.status_code}")
                return None, None
                
            soup = BeautifulSoup(response.text, "lxml")
            
            # Выбираем карточки результатов
            # Селектор из примера пользователя: div.background-grey-blue-light
            items = soup.select("div.background-grey-blue-light")
            if not items:
                print(f"[ZACHESTNYIBIZNES] Ничего не найдено")
                return None, None
                
            candidates = []
            normalized_query = clean_search_query.lower()

            for item in items:
                # Название: ссылка внутри p.f-s-16
                name_link = item.select_one("p.f-s-16 a")
                if not name_link: continue
                
                raw_name = name_link.get_text(separator=" ", strip=True)
                
                # Статус: ищем 'Действующее' или 'Ликвидировано'
                status_block = item.select_one("b.text-green") or item.select_one("b.text-trigger")
                is_active = status_block and "Действующее" in status_block.get_text()
                
                if not is_active:
                     # Если не действует - снижаем приоритет, но пока берем
                     pass

                # Адрес: параграф с svg иконкой (обычно 4-й p в блоке col-md-12)
                # Ищем p c SVG и почтовым индексом (6 цифр)
                address = None
                paragraphs = item.select("div.col-md-12 > p")
                for p in paragraphs:
                    text = p.get_text(strip=True)
                    if re.search(r'\d{6}', text) and ("г." in text or "обл." in text or "край" in text):
                        address = text
                        # Убираем SVG иконку (если текст склеился) - хотя get_text обычно ок
                        break
                
                # ИНН: строка с "ИНН" и цифрами
                inn = None
                for p in paragraphs:
                    text = p.get_text()
                    if "ИНН" in text:
                        m = re.search(r"ИНН\s*(\d{10}|\d{12})", text)
                        if m: inn = m.group(1)
                        break
                
                if not inn: continue

                # Считаем релевантность названия
                # Нормализация для сравнения (убираем ОПФ и кавычки)
                normalized_name = raw_name.lower()
                for entity in ["ооо", "пао", "ао", "зао"]:
                     normalized_name = re.sub(r'\b' + entity + r'\b', '', normalized_name)
                normalized_name = re.sub(r'["«»]', '', normalized_name).strip()
                
                similarity = difflib.SequenceMatcher(None, normalized_query, normalized_name).ratio()
                
                # Проверка аббревиатур (как в RusProfile)
                if len(normalized_name) < 6 or len(normalized_query) < 6:
                    long_str = normalized_name if len(normalized_name) > len(normalized_query) else normalized_query
                    short_str = normalized_query if len(normalized_name) > len(normalized_query) else normalized_name
                    words = long_str.split()
                    acronym = "".join([w[0] for w in words if w]).lower()
                    if acronym == short_str.replace(" ", "") or short_str in acronym:
                         similarity = max(similarity, 0.95)

                candidates.append({
                    "name": raw_name,
                    "inn": inn,
                    "address": address,
                    "score": similarity,
                    "is_active": is_active
                })
            
            if not candidates:
                return None, None
                
            # Сортируем: сначала действующие, потом по score
            candidates.sort(key=lambda x: (x["is_active"], x["score"]), reverse=True)
            
            best = candidates[0]
            if best["score"] < 0.5:
                 print(f"[ZACHESTNYIBIZNES] ❌ Низкое совпадение: '{best['name']}' (score={best['score']:.2f})")
                 return None, None
                 
            print(f"[ZACHESTNYIBIZNES] ✅ Найден результат: '{best['name']}' (score={best['score']:.2f}, ИНН={best['inn']})")
            return best["inn"], best["address"]

        except Exception as e:
            print(f"[ZACHESTNYIBIZNES] Ошибка: {e}")
            return None, None

    @staticmethod
    def parse_address_from_rusprofile(company_name: str, inn: Optional[str] = None) -> Optional[str]:
        """Парсит только адрес организации с RusProfile.ru (приоритет по ИНН)
        
        Args:
            company_name: Название организации для поиска
            inn: ИНН организации (опционально)
            
        Returns:
            Адрес или None если не найден
        """
        # Если есть ИНН, пробуем сначала искать по нему
        if inn:
             _, address = DocumentProcessor.parse_inn_and_address_from_rusprofile(inn)
             if address:
                 return address

        # Иначе ищем по названию
        _, address = DocumentProcessor.parse_inn_and_address_from_rusprofile(company_name)
        return address

    @staticmethod
    def get_bank_inn(bank_name: str) -> str:
        """Возвращает ИНН банка/МФО только из RusProfile (если уже в кеше).

        Args:
            bank_name: Название банка/МФО (нормализованное или каноническое)

        Returns:
            ИНН из RusProfile или пустая строка
        """
        # (Удален хардкод словарей по требованию пользователя)

        # Парсим ИНН с RusProfile (берем из кеша если уже запрашивали)
        inn_from_rusprofile = DocumentProcessor.parse_inn_from_rusprofile(bank_name)
        if inn_from_rusprofile:
            return inn_from_rusprofile

        return ""

    @staticmethod
    def normalize_bank_name(name: str) -> tuple[str, str]:
        """Нормализует название банка для объединения дубликатов.

        Возвращает кортеж: (нормализованное_название, каноническое_название)
        - нормализованное_название - для сравнения и группировки
        - каноническое_название - правильный формат для отображения

        Примеры:
            "МТС-БАНК ПАО" -> ("МТС БАНК", "ПАО «МТС-Банк»")
            "ПАО «МТС-Банк»" -> ("МТС БАНК", "ПАО «МТС-Банк»")
            "Сбербанк России" -> ("СБЕРБАНК", "ПАО Сбербанк")
        """
        if not name:
            return ("", "")

        original = name.strip()
        
        # СПЕЦИАЛЬНАЯ ОБРАБОТКА для ВТБ (все варианты написания)
        name_upper = original.upper()
        if "ВТБ" in name_upper and "БАНК" in name_upper:
            return ("ВТБ", "ПАО «ВТБ»")
        
        if "ПКБ" in name_upper and ("НАО" in name_upper or "ПКО" in name_upper):
             return ("НАО ПКБ", "НАО «ПКБ»")

        # ИСКЛЮЧЕНИЕ: "АЛЬФА ФАБРИКА" - это НЕ Альфа-Банк!
        if "ФАБРИКА" in name_upper and "АЛЬФА" in name_upper:
            # Оставляем как есть - это отдельная организация
            return (None, original)
        
        # СНАЧАЛА применяем нормализацию МФО/МКК (убираем "МФО:", историю, кавычки)
        canonical_name = DocumentProcessor.normalize_creditor_name(original)

        # Словарь канонических названий известных банков/МФО
        CANONICAL_NAMES = {
            "СБЕРБАНК": "ПАО «Сбербанк»",
            "СБЕР": "ПАО «Сбербанк»",
            "СБЕР РОССИИ": "ПАО «Сбербанк»",
            "РОССИИ": "ПАО «Сбербанк»",  # Для "Сбербанк России" после удаления "БАНК"
            "ВТБ": "ПАО «ВТБ»",
            "АЛЬФА БАНК": "АО «Альфа-Банк»",
            "АЛЬФА": "АО «Альфа-Банк»",
            "ТИНЬКОФФ": "АО «Тинькофф Банк»",
            "ТИНЬКОФФ БАНК": "АО «Тинькофф Банк»",
            "ТИНКОВ": "АО «Тинькофф Банк»",
            "РОССЕЛЬХОЗБАНК": "АО «Россельхозбанк»",
            "РОССЕЛЬХОЗ": "АО «Россельхозбанк»",
            "ГАЗПРОМБАНК": "АО « Банк ГПБ»",
            "ГАЗПРОМ": "АО «Банк ГПБ»",
            "ГПБ": "АО «Банк ГПБ»",
            "ГПО": "АО «Банк ГПБ»",  # Частая ошибка OCR вместо ГПБ
            "МТС БАНК": "ПАО «МТС-Банк»",
            "МТС": "ПАО «МТС-Банк»",
            "РАЙФФАЙЗЕНБАНК": "АО «Райффайзенбанк»",
            "РАЙФФАЙЗЕН": "АО «Райффайзенбанк»",
            "РОСБАНК": "ПАО «РОСБАНК»",
            "РОС": "ПАО «РОСБАНК»",
            "ХОУМ КРЕДИТ": "АО «Т-Банк»",  # Хоум Кредит продан Тинькофф и переименован в Т-Банк
            "ХОУМ КРЕДИТ ЭНД ФИНАНС": "АО «Т-Банк»",
            "HOME CREDIT": "АО «Т-Банк»",
            "Т БАНК": "АО «Т-Банк»",
            "ТБАНК": "АО «Т-Банк»",
            "Т-БАНК": "АО «Т-Банк»",
            "T BANK": "АО «Т-Банк»",
            "TBANK": "АО «Т-Банк»",
            "T-BANK": "АО «Т-Банк»",
            "РУСФИНАНС БАНК": "ООО КБ «Русфинанс Банк»",
            "РУСФИНАНС": "ООО КБ «Русфинанс Банк»",
            "РУСИНТЕРФИНАНС": "ООО МКК «Русинтерфинанс»",
            "РУСИНЕРФИНАНС": "ООО МКК «Русинтерфинанс»",
            "РУС ИНТЕР ФИНАНС": "ООО МКК «Русинтерфинанс»",
            "СОВКОМБАНК": "ПАО «Совкомбанк»",
            "СОВКОМ": "ПАО «Совкомбанк»",
            "ОТКРЫТИЕ": "ПАО Банк «ФК Открытие»",
            "ПОЧТА БАНК": "ПАО «Почта Банк»",
            "ПОЧТА": "ПАО «Почта Банк»",
            "УРАЛСИБ": "ПАО «БАНК УРАЛСИБ»",
            "УРАЛ СИБ": "ПАО «БАНК УРАЛСИБ»",
            "ПРОМСВЯЗЬБАНК": "ПАО «Промсвязьбанк»",
            "ПРОМСВЯЗЬ": "ПАО «Промсвязьбанк»",
            "ОТП БАНК": "АО «ОТП Банк»",
            "ОТП": "АО «ОТП Банк»",
            "РЕНЕССАНС КРЕДИТ": "ООО КБ «Ренессанс Кредит»",
            "РЕНЕССАНС": "ООО КБ «Ренессанс Кредит»",
            "РУССКИЙ СТАНДАРТ": "АО «Банк Русский Стандарт»",
            "ПСБ": "ПАО «Банк ПСБ»",
            "ПСБ БАНК": "ПАО «Банк ПСБ»",
            "БАНК ПСБ": "ПАО «Банк ПСБ»",
            "АК БАРС": "ПАО «АК БАРС» Банк",
            "АКБАРС": "ПАО «АК БАРС» Банк",
            "НАО ПКБ": "НАО «ПКБ»",
            "НАО ПКО ПКБ": "НАО «ПКБ»",
            "ПКБ": "НАО «ПКБ»",

            # МФО/МФК/МКК
            "ЗАЙМЕР": "ООО МФК «Займер»",
            "ЗАЙМ ЭР": "ООО МФК «Займер»",
            "ВЭББАНКИР": "ООО МКК «ВэбБанкир»",
            "ВЗББАНКИР": "ООО МКК «ВэбБанкир»",
            "ВЕББАНКИР": "ООО МКК «ВэбБанкир»",
            "ВЭЗББАНКИР": "ООО МКК «ВэбБанкир»",
            "ВЭЗБАНКИР": "ООО МКК «ВэбБанкир»",
            "ВЭБ ИР": "ООО МКК «ВэбБанкир»",
            "ВЭЗБ ИР": "ООО МКК «ВэбБанкир»",
            "WEBBANKIR": "ООО МКК «ВэбБанкир»",
            "ТУРБОЗАЙМ": "ООО МКК «ТурбоЗайм»",
            "ТУРБО ЗАЙМ": "ООО МКК «ТурбоЗайм»",
            "ТУРБО": "ООО МКК «ТурбоЗайм»",
            "TURBOZAIM": "ООО МКК «ТурбоЗайм»",
            "МАНИМЕН": "ООО МКК «Мани Мен»",
            "МАНИ МЕН": "ООО МКК «Мани Мен»",
            "МОНЕЙМЕН": "ООО МКК «Мани Мен»",
            "MONEYMAN": "ООО МКК «Мани Мен»",
            "MONEY MAN": "ООО МКК «Мани Мен»",
            "МИГКРЕДИТ": "ООО МФК «МигКредит»",
            "МИГ КРЕДИТ": "ООО МФК «МигКредит»",
            "МИГ": "ООО МФК «МигКредит»",
            "MIGCREDIT": "ООО МФК «МигКредит»",
            "MIGKREDIT": "ООО МФК «МигКредит»",
            "ДЕНЬГИ СРАЗУ": "ООО МКК «Деньги сразу»",
            "DENGISRAZY": "ООО МКК «Деньги сразу»",
            "DENGI SRAZY": "ООО МКК «Деньги сразу»",
            "ЕKAPУСТА": "ООО МКК «Екапуста»",
            "Е КАПУСТА": "ООО МКК «Екапуста»",
            "EKAPUSTA": "ООО МКК «Екапуста»",
            "E KAPUSTA": "ООО МКК «Екапуста»",
            "ЛАЙМ ЗАЙМ": "ООО МФК «Лайм-Займ»",
            "ЛАЙМ": "ООО МФК «Лайм-Займ»",
            "LIME ZAIM": "ООО МФК «Лайм-Займ»",
            "LIMEZAIM": "ООО МФК «Лайм-Займ»",
            "СРОЧНОДЕНЬГИ": "ООО МФК «СрочноДеньги»",
            "СРОЧНО ДЕНЬГИ": "ООО МФК «СрочноДеньги»",
            "SROCHNODENGІ": "ООО МФК «СрочноДеньги»",
            "ДЕНЕЖНАЯ КРЕПОСТЬ": "ООО МКК «Денежная Крепость»",
            "ДЕНЕЖ КРЕПОСТЬ": "ООО МКК «Денежная Крепость»",
            "ЭКВАЗАИМ": "ООО МКК «Эквазайм»",
            "ЭКВАЗАЙМ": "ООО МКК «Эквазайм»",
            "ЭКВА ЗАИМ": "ООО МКК «Эквазайм»",
            "ЭКВА ЗАЙМ": "ООО МКК «Эквазайм»",
            "EKVAZAIM": "ООО МКК «Эквазайм»",
            "БЫСТРОДЕНЬГИ": "ООО МКК «БыстроДеньги»",
            "БЫСТРО ДЕНЬГИ": "ООО МКК «БыстроДеньги»",
            "BISTRODENGI": "ООО МКК «БыстроДеньги»",
            "МИКРОЗАЙМ": "ООО МФК «МикроЗайм»",
            "МИКРО ЗАЙМ": "ООО МФК «МикроЗайм»",
            "MICROZAIM": "ООО МФК «МикроЗайм»",
            "ЗАЙМОГРАД": "ООО МКК «ЗаймоГрад»",
            "ZAIMO GRAD": "ООО МКК «ЗаймоГрад»",
            "ZAIMOGRAD": "ООО МКК «ЗаймоГрад»",
            "РУСИНТЕРФИНАНС": "ООО МКК «Русинтерфинанс»",
            "РУСИНТЕР ФИНАНС": "ООО МКК «Русинтерфинанс»",
            "RUSINTERFINANS": "ООО МКК «Русинтерфинанс»",
            
            # Добавлено по запросу пользователя
            "ВЕРИТАС": "ООО МФК «Веритас»",
            "ЮПИТЕР 6": "ООО МФК «Юпитер 6»",
            "ЮПИТЕР": "ООО МФК «Юпитер 6»",
            "ВЭЗБАНКИР": "ООО МКК «ВэбБанкир»",
            "СБ СЕКЬЮРИТИЗАЦИЯ": "ООО «Сфо Сб Секьюритизация»",
            "СФО СБ СЕКЬЮРИТИЗАЦИЯ": "ООО «Сфо Сб Секьюритизация»",
            "ФИКС ФИНАНС": "ООО МКК «Фиксфинанс»",
            "ФИКСФИНАНС": "ООО МКК «Фиксфинанс»",
            "ТРИУМВИРАТ": "ООО МКК «Триумвират»",
            "ЭЛИСТА ИНВЕСТ": "ООО «ПКО Элиста Инвест»",
            "ПКО ЭЛИСТА ИНВЕСТ": "ООО «ПКО Элиста Инвест»",
            "ЭКВАЗАЙМ": "ООО МКК «Эквазайм»",
            "РЕЙЗОР": "ООО МКК «Рейзор»",
            "ПЕННИ": "ООО МКК «Пенни»",
            "УМНЫЙ ЗАЙМ": "ООО МКК «Умный Займ»",
            "ЗАЙМ ЭКСПРЕСС": "АО «МКК Займ-экспресс»",
            "ЗАЙМЭКСПРЕСС": "АО «МКК Займ-экспресс»",
            "495 КРЕДИТ": "ООО МКК «495 Кредит»",
            "КАНГАРИЯ": "ООО МКК «Кангария»",
            "КИТ КРЕДИТ": "КПК «Кит-Кредит»",
            "БОНЗАЙМ": "ООО МКК «Бонзайм»",
            "КРЕДИТТЕР": "ООО МКК «Кредиттер»",
            "СФ": "ООО МКК «СФ»",
            "ДОПОЛУЧКИНО": "ООО МКК «Дополучкино»",
        }

        # Шаг 1: Убираем организационно-правовые формы и знаки (используем нормализованное название)
        normalized = canonical_name.upper()

        # Убираем кавычки всех типов
        normalized = normalized.replace('"', '').replace("'", '').replace('«', '').replace('»', '')

        # Убираем организационно-правовые формы
        opf_patterns = [
            "ПУБЛИЧНОЕ АКЦИОНЕРНОЕ ОБЩЕСТВО",
            "АКЦИОНЕРНОЕ ОБЩЕСТВО",
            "ОБЩЕСТВО С ОГРАНИЧЕННОЙ ОТВЕТСТВЕННОСТЬЮ",
            "КОММЕРЧЕСКИЙ БАНК",
            "МИКРОФИНАНСОВАЯ КОМПАНИЯ",
            "МИКРОКРЕДИТНАЯ КОМПАНИЯ",
            "МИКРОКРЕДИТНАЯ КОМПАНИЯ",
            "МИКРОФИНАНСОВАЯ ОРГАНИЗАЦИЯ",
            "ПАО", "ОАО", "ЗАО", "АО", "ООО", 
            "МФК", "МКК", "МФО",
            "КБ", "БАНК",
        ]

        for pattern in opf_patterns:
            normalized = normalized.replace(pattern, " ")

        # Убираем скобки и дефисы
        normalized = normalized.replace("(", " ").replace(")", " ")
        normalized = normalized.replace("-", " ")
        normalized = normalized.replace("  ", " ").replace("  ", " ")  # Двойная очистка
        normalized = normalized.strip()

        # Шаг 2: Ищем каноническое название
        canonical = CANONICAL_NAMES.get(normalized)

        # Если не нашли точное совпадение, ищем частичное
        if not canonical:
            # Сначала ищем полное вхождение нормализованного в ключи
            for key, value in CANONICAL_NAMES.items():
                if normalized == key:
                    canonical = value
                    break

            # Затем ищем частичное совпадение (приоритет длинным совпадениям)
            if not canonical:
                matches = []
                # Список стоп-слов для предотвращения ложных срабатываний
                # Если в названии есть эти слова, а в каноническом ключе нет - не матчим
                # Пример: "Ренессанс Страхование" != "Ренессанс Кредит"
                stop_words = ["СТРАХОВАНИЕ", "ЛИЗИНГ", "НПФ", "КАПИТАЛ", "ИНВЕСТИЦИИ", "ЖИЗНЬ", "ГАРАНТ", "ФАКТОРИНГ", "КОЛЛЕКТОР"]

                for key, value in CANONICAL_NAMES.items():
                    # Проверяем наличие стоп-слов
                    has_stop_word = any(sw in normalized and sw not in key for sw in stop_words)
                    if has_stop_word:
                        continue

                    # Проверяем вхождение в обе стороны
                    # НО пропускаем слишком короткие ключи (менее 4 символов) для частичного совпадения
                    # чтобы "АЛЬФА" не совпала с "СФО АЛЬФА ФАБРИКА ПК 1"
                    if key in normalized and len(key) >= 4:
                        # Дополнительная проверка: всегда проверяем границы слов
                        # Чтобы "СБЕР" не сработал на "СБЕРЕГАТЕЛЬНЫЙ"
                        # Разбиваем на слова (normalized уже очищен и lowercase/uppercase)
                        words = normalized.split()
                        
                        # Если ключ состоит из одного слова
                        if " " not in key:
                            if key in words:
                                matches.append((len(key), key, value))
                        else:
                            # Если ключ составной (например "АЛЬФА БАНК")
                            # Проверяем его как подстроку с учетом границ
                            # Добавляем пробелы по краям для проверки
                            if f" {key} " in f" {normalized} ":
                                 matches.append((len(key), key, value))

                    elif normalized in key and len(normalized) >= 4:
                        matches.append((len(key), key, value))

                # Берем самое длинное совпадение
                if matches:
                    matches.sort(reverse=True)  # Сортируем по длине
                    _, normalized, canonical = matches[0]

        # Если не нашли в словаре, используем нормализованное название
        if not canonical:
            # Используем уже нормализованное название (с правильными кавычками и без префиксов)
            canonical = canonical_name

            # Если нормализованное название весь в CAPS, делаем красиво
            if canonical_name.isupper():
                # Извлекаем ОПФ из начала
                opf = ""
                rest = canonical_name
                for pattern in ["ПАО", "АО", "ООО", "ОАО"]:
                    if canonical_name.startswith(pattern + " "):
                        opf = pattern
                        rest = canonical_name[len(pattern):].strip()
                        break

                # Убираем кавычки
                rest = rest.replace('"', '').replace("'", '').replace('«', '').replace('»', '')

                # Делаем первую букву заглавной, остальные строчными
                rest_parts = rest.split()
                rest_formatted = ' '.join(
                    word if word in ["МТС", "ВТБ", "ОТП"] else word.capitalize() 
                    for word in rest_parts
                )

                # Собираем обратно
                if opf:
                    canonical = f'{opf} «{rest_formatted}»'
                else:
                    canonical = rest_formatted

        return (normalized, canonical)

    @staticmethod
    def normalize_creditor_names_with_gpt(creditors: List[str]) -> Dict[str, str]:
        """Нормализует названия кредиторов через GPT для исправления опечаток.
        
        Args:
            creditors: Список названий кредиторов для нормализации
            
        Returns:
            Словарь {оригинальное_название: нормализованное_название}
        """
        if not creditors:
            return {}
        
        prompt = f"""Ты - эксперт по нормализации названий российских банков и МФО.

Исправь опечатки и приведи названия к правильному формату:
- ПАО/АО/ООО/ПКО и так далее в начале
- Название банка/МФО в кавычках-ёлочках «»
- Правильные заглавные буквы
- Опечатки или ошибки в написании, пример "Денежная Крепасть" → "Денежная Крепость"

Примеры:
"Общество С Ограниченной Ответственностью МКК Денежная Крепость" → "ООО МКК «Денежная Крепость»"
"ооо мкк эквазаим" → "ООО МКК «Эквазайм»"
"ПАО СБЕРБАНК" → "ПАО «Сбербанк»"
"АО АЛЬФА-БАНК" → "АО «Альфа-Банк»"
"ООО Профессиональная Коллекторская Организация «Интел Коллект»" → "ООО ПКО «Интел Коллект»"
"АО  «ПКО  ЦДУ»" → "АО ПКО «ЦДУ»"

Названия для нормализации:
{chr(10).join(f"{i+1}. {name}" for i, name in enumerate(creditors))}

Верни ТОЛЬКО JSON в формате:
{{
  "Название 1": "Нормализованное название 1",
  "Название 2": "Нормализованное название 2"
}}"""

        try:
            response = client.chat.completions.create(
                model="gpt-5-mini",
                messages=[{"role": "user", "content": prompt}],
            )
            
            result_text = response.choices[0].message.content.strip()
            
            # Убираем markdown-разметку если есть
            if result_text.startswith("```"):
                result_text = result_text.split("```")[1]
                if result_text.startswith("json"):
                    result_text = result_text[4:]
                result_text = result_text.strip()
            
            normalized = json.loads(result_text)
            print(f"[GPT_NORMALIZE] Нормализовано {len(normalized)} названий кредиторов")
            return normalized
        except Exception as e:
            print(f"[GPT_NORMALIZE] Ошибка нормализации через GPT: {e}")
            return {}

    @staticmethod
    def select_first_entry(cache: Dict[str, List[Dict[str, Any]]], key: str) -> Dict[str, Any]:
        for item in cache.get(key, []):
            if isinstance(item, dict):
                return item
        return {}

    @staticmethod
    def extract_employers(records: Dict[str, Any]) -> str:
        """Извлекает текущее место работы из трудовой книжки."""
        if not records:
            return "Должник в настоящее время не работает"
        
        # Проверяем поле Текущая_работа из GPT
        current_work = records.get("Текущая_работа")
        if current_work and current_work != "Нет":
            # Кавычки-ёлочки
            current_work = current_work.replace('"', '«').replace("'", "«").replace('"', '»').replace("'", "»")
            return f"Должник работает в {current_work}"
        
        # Если нет - проверяем последнюю запись
        entries = records.get("Записи", []) or []
        if not entries:
            return "Должник в настоящее время не работает"
        
        # Последняя запись (записи отсортированы по дате)
        last_entry = entries[-1]
        if last_entry and last_entry.get("Событие") != "Увольнение":
            employer = last_entry.get("Работодатель", "")
            position = last_entry.get("Должность", "")
            if employer:
                workplace = f"{employer}, {position}" if position else employer
                # Кавычки-ёлочки
                workplace = workplace.replace('"', '«').replace("'", "«").replace('"', '»').replace("'", "»")
                return f"Должник работает в {workplace}"
        
        return "Должник в настоящее время не работает"

    @staticmethod
    def extract_current_job(records: Dict[str, Any]) -> str:
        """Извлекает только текущее место работы из трудовой книжки.
        
        Returns:
            Текущее место работы или "Нет" если человек не работает
        """
        if not records:
            return "Нет"
        
        # Проверяем поле Текущая_работа из GPT
        current_work = records.get("Текущая_работа")
        if current_work:
            # Нормализуем кавычки: все прямые кавычки заменяем на елочки (открывающие и закрывающие)
            current_work = DocumentProcessor.normalize_quotes(current_work)
            return current_work
        
        # Если нет - проверяем последнюю запись
        entries = records.get("Записи", []) or []
        if not entries:
            return "Нет"
        
        # Последняя запись (записи отсортированы по дате)
        last_entry = entries[-1]
        if last_entry and last_entry.get("Событие") != "Увольнение":
            employer = last_entry.get("Работодатель", "")
            position = last_entry.get("Должность", "")
            if employer:
                # Нормализуем кавычки
                employer = DocumentProcessor.normalize_quotes(employer)
                position = DocumentProcessor.normalize_quotes(position)
                return f"{employer}, {position}" if position else employer
        
        return "Нет"

    @staticmethod
    def format_interested_party(passport_spouse: Dict[str, Any], inn_spouse: Dict[str, Any], snils_spouse: Dict[str, Any]):
        """Форматирует информацию о супруге (заинтересованном лице) с жирными заголовками.
        
        Args:
            passport_spouse: Данные паспорта супруга
            inn_spouse: Данные ИНН супруга
            snils_spouse: Данные СНИЛС супруга
            
        Returns:
            RichText с форматированием или пустая строка
        """
        if not passport_spouse:
            return ""
        
        # ФИО
        fio = passport_spouse.get("ФИО", "")
        if not fio:
            return ""
        
        # Дата и место рождения
        birth_date = passport_spouse.get("Дата_рождения", "")
        birth_place = passport_spouse.get("Место_рождения", "")
        
        birth_info = ""
        if birth_date:
            birth_info = f"{birth_date} г.р."
            if birth_place:
                birth_info += f", {birth_place}"
        elif birth_place:
            birth_info = birth_place
        
        # Адрес регистрации
        address = passport_spouse.get("Прописка", "")
        
        # Создаем RichText с форматированием Times New Roman 12pt
        # size указывается в половинах пунктов: 12pt = size=24
        rt = RichText()
        rt.add("Заинтересованное лицо:", bold=True, font='Times New Roman', size=24)
        rt.add(f"\n{fio}", font='Times New Roman', size=24)
        
        if birth_info:
            rt.add("\nДата и место рождения: ", bold=True, font='Times New Roman', size=24)
            rt.add(f"{birth_info};", font='Times New Roman', size=24)
        
        if address:
            rt.add("\nАдрес проживания: ", bold=True, font='Times New Roman', size=24)
            rt.add(f"{address};", font='Times New Roman', size=24)
        
        return rt

    @staticmethod
    def get_yekaterinburg_date() -> Dict[str, str]:
        """Получает текущую дату по Екатеринбургскому времени (Asia/Yekaterinburg, UTC+5).
        
        Returns:
            Словарь с ключами:
            - АДень: день (например, "12")
            - АМесяц: месяц на русском с заглавной (например, "Декабря")
            - АГод: полный год (например, "2025")
            - ААГод: последние 2 цифры года (например, "25")
        """
        # Получаем текущую дату по Екатеринбургскому времени
        yekaterinburg_tz = ZoneInfo("Asia/Yekaterinburg")
        now = datetime.now(yekaterinburg_tz)
        
        # Названия месяцев в родительном падеже (кого? чего?)
        months_genitive = {
            1: "Января",
            2: "Февраля",
            3: "Марта",
            4: "Апреля",
            5: "Мая",
            6: "Июня",
            7: "Июля",
            8: "Августа",
            9: "Сентября",
            10: "Октября",
            11: "Ноября",
            12: "Декабря"
        }
        
        return {
            "АДень": str(now.day),
            "АМесяц": months_genitive[now.month],
            "АГод": str(now.year),
            "ААГод": str(now.year)[-2:]  # Последние 2 цифры
        }

    @staticmethod
    def calculate_bank_accounts(data_list: List[Dict[str, Any]]) -> int:
        total_accounts = 0
        for data in data_list:
            accounts = data.get("Счета") or []
            if isinstance(accounts, list):
                total_accounts += len(accounts)
        return total_accounts

    @staticmethod
    def describe_real_estate(data_list: List[Dict[str, Any]]) -> str:
        descriptions: List[str] = []
        for data in data_list:
            obj = data.get("Объект") or {}
            parts: List[str] = []
            vid = obj.get("Вид")
            address = obj.get("Адрес")
            area = obj.get("Площадь")
            if vid:
                parts.append(str(vid))
            if address:
                parts.append(f"адрес: {address}")
            if area:
                parts.append(f"площадь {area}")
            if parts:
                descriptions.append(", ".join(parts))
        return "; ".join(descriptions)

    @staticmethod
    def determine_children_status(children: Optional[List[Dict[str, Any]]], data_map: Dict[str, List[Dict[str, Any]]] = None) -> str:
        """Определяет наличие несовершеннолетних детей.
        
        Args:
            children: список детей из паспорта
            data_map: словарь всех документов (для проверки свидетельств о рождении)
        """
        today = date.today()
        minors: List[str] = []
        
        # Собираем детей из паспорта
        if children:
            for child in children:
                if not isinstance(child, dict):
                    continue
                name = child.get("ФИО") or ""
                dob_str = child.get("Дата_рождения")
                if not dob_str:
                    continue
                try:
                    dob = datetime.strptime(dob_str, "%d.%m.%Y").date()
                    age_years = (today - dob).days / 365.25
                    if age_years < 18:
                        entry = f"{name} {dob_str} г.р." if name else f"{dob_str} г.р."
                        minors.append(entry.strip())
                except ValueError:
                    continue
        
        # Дополнительно проверяем свидетельства о рождении
        if data_map:
            birth_certificates = data_map.get("свидетельство_о_рождении", [])
            for cert in birth_certificates:
                if not isinstance(cert, dict):
                    continue
                
                # НОВЫЙ ФОРМАТ: массив "Дети" (несколько свидетельств в одном PDF)
                children_array = cert.get("Дети")
                if children_array and isinstance(children_array, list):
                    for child_data in children_array:
                        if not isinstance(child_data, dict):
                            continue
                        name = child_data.get("Ребенок_ФИО") or ""
                        dob_str = child_data.get("Дата_рождения")
                        if not dob_str:
                            continue
                        try:
                            dob = datetime.strptime(dob_str, "%d.%m.%Y").date()
                            age_years = (today - dob).days / 365.25
                            if age_years < 18:
                                entry = f"{name} {dob_str} г.р." if name else f"{dob_str} г.р."
                                if entry.strip() not in minors:
                                    minors.append(entry.strip())
                        except ValueError:
                            continue
                else:
                    # СТАРЫЙ ФОРМАТ: одно свидетельство = один ребенок (обратная совместимость)
                    name = cert.get("Ребенок_ФИО") or ""
                    dob_str = cert.get("Дата_рождения")
                    if not dob_str:
                        continue
                    try:
                        dob = datetime.strptime(dob_str, "%d.%m.%Y").date()
                        age_years = (today - dob).days / 365.25
                        if age_years < 18:
                            entry = f"{name} {dob_str} г.р." if name else f"{dob_str} г.р."
                            # Проверяем, что такого ребенка еще нет в списке
                            if entry.strip() not in minors:
                                minors.append(entry.strip())
                    except ValueError:
                        continue
        
        if minors:
            return "; ".join(minors)
        return "нет"

    @staticmethod
    def get_marital_status_key(passport: Dict[str, Any], data_map: Dict[str, List[Dict[str, Any]]] = None) -> str:
        """Формирует значение для ключа {{Женатзамужем}}.
        
        Args:
            passport: данные паспорта должника
            data_map: словарь всех документов (для проверки свидетельств о браке)
            
        Returns:
            Строка: "женат, супруга ФИО ДД.ММ.ГГГГ г.р." или "замужем, супруг ФИО ДД.ММ.ГГГГ г.р." или "не в браке"
        """
        if not passport:
            return "не в браке"
        
        # Определяем пол должника
        gender = (passport.get("Пол") or "").lower()
        is_male = "муж" in gender
        
        # Проверяем семейное положение в паспорте
        marital_status = (passport.get("Семейное_положение") or "").lower()
        
        # Проверяем наличие свидетельства о браке
        has_marriage = False
        spouse_fio = ""
        spouse_dob = ""
        
        if data_map:
            marriage_certs = data_map.get("свидетельство_о_браке", [])
            if marriage_certs:
                has_marriage = True
                # Берем данные из первого свидетельства
                cert = marriage_certs[0]
                if isinstance(cert, dict):
                    # Теперь в свидетельстве о браке только одно поле - Супруг_ФИО (супруг должника)
                    # GPT уже определил кто супруг на основе ФИО должника
                    spouse_fio = cert.get("Супруг_ФИО", "")
                    spouse_dob = cert.get("Супруг_Дата_рождения", "")
                    
                    print(f"[БРАК] Свидетельство о браке найдено:")
                    print(f"  Супруг должника: {spouse_fio} ({spouse_dob})")
        
        # Если нет свидетельства о браке, проверяем ФИО супруга в паспорте
        if not has_marriage and passport.get("ФИО_супруга"):
            spouse_fio = passport.get("ФИО_супруга", "")
            spouse_dob = passport.get("Дата_рождения_супруга", "")
            print(f"[БРАК] ФИО супруга из паспорта: {spouse_fio} ({spouse_dob})")
        
        # Проверяем свидетельство о расторжении брака
        has_divorce = False
        if data_map:
            # Проверяем оба ключа
            divorce_certs = data_map.get("свидетельство_о_расторжении_брака", [])
            divorce_certs_short = data_map.get("свидетельство_о_разводе", [])
            if divorce_certs or divorce_certs_short:
                has_divorce = True
        
        # Формируем ответ
        # Если есть свидетельство о расторжении - брак расторгнут независимо от паспорта
        if has_divorce:
            return "не в браке"
        
        is_married = has_marriage or "женат" in marital_status or "замужем" in marital_status
        not_married = (
            "не женат" in marital_status or 
            "не замужем" in marital_status or 
            "не в браке" in marital_status or
            "не состоит" in marital_status or
            "холост" in marital_status
        )
        
        if not_married:
            return "не в браке"
            
        if not is_married:
            return "не в браке"
        
        # Если в браке, формируем текст
        if is_male:
            status = "женат"
            spouse_title = "супруга"
        else:
            status = "замужем"
            spouse_title = "супруг"
        
        if spouse_fio and spouse_dob:
            return f"{status}, {spouse_title} {spouse_fio} {spouse_dob} г.р."
        elif spouse_fio:
            return f"{status}, {spouse_title} {spouse_fio}"
        else:
            return status.capitalize()

    @staticmethod
    def extract_real_estate_from_notifications(notification_list: List[Dict[str, Any]], owner_fio: str = "") -> List[str]:
        """Извлекает информацию о недвижимости из уведомлений ЕГРН об отсутствии."""
        descriptions = []

        # Извлекаем из уведомлений
        for notification in notification_list:
            # Проверяем, что это уведомление об отсутствии
            if notification.get("Результат") != "сведения отсутствуют":
                continue

            # Проверяем правообладателя
            holder = notification.get("Правообладатель", {})
            holder_fio = holder.get("ФИО", "") if isinstance(holder, dict) else ""

            # Если есть территория (адрес объекта), значит запрос был об этом объекте
            territory = notification.get("Территория", "")

            if territory:
                # Формируем описание из доступных данных
                # Примечание: В уведомлении нет полных данных, но есть адрес
                parts = []
                parts.append("жилой дом")  # предполагаем тип по контексту
                parts.append("общая долевая собственность")
                parts.append("доля не указана")  # без конфига не знаем долю
                parts.append(f"местонахождение: {territory}")

                # Если есть информация о другом правообладателе
                if holder_fio and holder_fio != owner_fio:
                    parts.append(f"(сособственник: {holder_fio})")

                descriptions.append(", ".join(parts))

        return descriptions

    @staticmethod
    def format_real_estate_detailed(data_list: List[Dict[str, Any]], owner_fio: str = "", notification_list: Optional[List[Dict[str, Any]]] = None, inventory_list: Optional[List[Dict[str, Any]]] = None) -> str:
        """Форматирует недвижимость в детальном виде, фильтруя по ФИО владельца.

        Учитывает:
        1. Полные выписки ЕГРН (data_list)
        2. Уведомления об отсутствии ЕГРН (notification_list) - могут указывать на долевую собственность
        3. Описи имущества (inventory_list) - недвижимость, указанная должником в описи
        """
        descriptions: List[str] = []
        processed_cadastral_numbers = set() # Чтобы избежать дублей (например, есть и выписка, и опись)

        # Обрабатываем полные выписки ЕГРН
        if data_list:
            for data in data_list:
                # НОВАЯ СТРУКТУРА: массив "Объекты"
                objects = data.get("Объекты", [])
                
                # Если нет массива "Объекты", значит это старая структура
                if not objects:
                    objects = [data]  # Старая структура: весь data - это один объект
                
                # Обрабатываем каждый объект
                for obj_item in objects:
                    obj = obj_item.get("Объект") or {}
                    kadaster = obj.get("Кадастровый_номер") or obj_item.get("Кадастровый_номер", "")
                    owners = obj_item.get("Правообладатели", [])
                    
                    # Извлекаем основные поля объекта (для логирования)
                    vid = obj.get("Вид", "").lower()
                    address = obj.get("Адрес", "")
                    area = obj.get("Площадь", "")

                    # ФИЛЬТР: Пропускаем проданные объекты (дата прекращения права указана)
                    has_active_ownership = False
                    for право in owners:
                        дата_прекращения = (право.get("Дата_прекращения_права") or 
                                          право.get("дата_прекращения_права") or
                                          право.get("дата_государственной_регистрации_прекращения_права") or
                                          право.get("Дата_государственной_регистрации_прекращения_права") or "")
                        # "данные отсутствуют" -> право НЕ прекращено, значит активное
                        # Если строка пустая -> тоже считаем активным (обычно)
                        # Если есть конкретная дата (например, 03.12.2019) -> значит право прекращено
                        
                        # Но иногда поле называется "дата государственной регистрации прекращения права" = "данные отсутствуют"
                        # Это значит ОБЪЕКТ ЕСТЬ (право НЕ прекращено).
                        
                        # Улучшенная логика проверки прекращения права
                        term_val = str(дата_прекращения).lower().strip()
                        is_terminated = False
                        
                        if term_val:
                            # 1. Если явно написано "отсутствуют" или "нет данных" - право АКТИВНО
                            # Это соответствует требованию: "данные отсутствуют" -> включать в список
                            # Также проверяем фразы типа "Право на недвижимость действующее" (Госуслуги)
                            if (
                                "отсутствуют" in term_val or 
                                "не зарегистрировано" in term_val or 
                                "нет" in term_val or
                                "действующее" in term_val or
                                "активно" in term_val
                            ):
                                is_terminated = False
                                # Debug log
                                if term_val != "данные отсутствуют":
                                    print(f"[REAL_ESTATE] Termination string '{term_val}' treated as ACTIVE")
                            
                            # 2. Если есть цифры (похоже на дату) - право ПРЕКРАЩЕНО
                            elif any(char.isdigit() for char in term_val):
                                is_terminated = True
                            
                            # 3. Иначе (текст без цифр/дат) - право АКТИВНО
                            else:
                                is_terminated = False
                        
                        if not is_terminated:
                            has_active_ownership = True
                            break
                    
                    if not has_active_ownership:
                        print(f"[REAL_ESTATE_DETAILED] Skipping {vid} - no active ownership")
                        continue  # Пропускаем проданные объекты

                    # Если указано ФИО владельца, проверяем, является ли он правообладателем
                    if owner_fio and owners:
                        owner_found = False
                        owner_info = None
                        print(f"[REAL_ESTATE_DETAILED] Checking {len(owners)} owners for {vid}, looking for: {owner_fio}")
                        for owner in owners:
                            if isinstance(owner, dict):
                                owner_name = owner.get("ФИО", "")
                                print(f"[REAL_ESTATE_DETAILED]   Checking owner: {owner_name}")
                                if owner_name and owner_fio.lower() in owner_name.lower():
                                    owner_found = True
                                    owner_info = owner
                                    print(f"[REAL_ESTATE_DETAILED]   MATCH found!")
                                    break

                        # Если владелец не найден среди правообладателей, пропускаем этот объект
                        if not owner_found:
                            print(f"[REAL_ESTATE_DETAILED] Skipping {vid} - owner {owner_fio} not found among {len(owners)} owners")
                            continue
                    else:
                        # Если ФИО не указано, берем первого владельца
                        owner_info = owners[0] if owners and isinstance(owners, list) and len(owners) > 0 else None

                    print(f"[REAL_ESTATE_DETAILED] Object: vid={vid}, cadastral={kadaster}, address={address[:50] if address else 'N/A'}")
                    
                    # Пропускаем квартиры - убрали этот фильтр, чтобы всё попадало в общую строку
                    # if "квартир" in vid or "комнат" in vid or "помещен" in vid:
                    #     print(f"[REAL_ESTATE_DETAILED] Skipping apartment/room: {vid}")
                    #     continue

                    parts = []
                    print(f"[REAL_ESTATE_DETAILED] Creating parts for {vid}, owner_info exists: {owner_info is not None}")

                    # Вид объекта
                    if vid:
                        parts.append(vid)
                        print(f"[REAL_ESTATE_DETAILED]   Added vid: {vid}")

                    # Тип собственности и доля
                    if owners and isinstance(owners, list) and len(owners) > 0:
                        # Используем тип собственности извлеченный GPT из документа
                        ownership_type = ""
                        share = ""
                        if owner_info:
                            ownership_type = (owner_info.get("Тип_собственности") or "").strip()
                            share = (owner_info.get("Доля") or "").strip()
                        
                        # Добавляем тип собственности если он извлечен
                        if ownership_type:
                            parts.append(ownership_type)
                        
                        # Добавляем долю если она указана и это реальная дробь
                        if share and ("/" in share or share == "1"):
                            if share != "1/1" and share != "1":
                                parts.append(f"{share} доля")

                    # Кадастровый номер
                    if kadaster:
                        parts.append(f"кадастровый номер: {kadaster}")

                    # Площадь
                    if area:
                        parts.append(f"площадь: {area} кв. м.")

                    # Местонахождение
                    if address:
                        parts.append(f"местонахождение: {address}")

                    # Основание регистрации
                    if owner_info:
                        doc = owner_info.get("Документ", "")
                        reg_date = owner_info.get("Дата_регистрации", "")
                        if doc:
                            base_text = f"основание регистрации: {doc}"
                            if reg_date:
                                base_text += f", выдан {reg_date}"
                            parts.append(base_text)

                    print(f"[REAL_ESTATE_DETAILED] Parts for {vid}: {parts}")
                    if parts:
                        description = ", ".join(parts)
                        print(f"[REAL_ESTATE_DETAILED] Adding description: {description[:100]}")
                        descriptions.append(description)
                else:
                    print(f"[REAL_ESTATE_DETAILED] No parts for {vid}, skipping")

        # ВАЖНО: Проверяем уведомления ЕГРН, которые могут указывать на долевую собственность
        if notification_list:
            notification_estates = DocumentProcessor.extract_real_estate_from_notifications(
                notification_list, owner_fio
            )
            descriptions.extend(notification_estates)

        # ВАЖНО: Добавляем недвижимость из Описи имущества (если она там есть)
        if inventory_list:
            print(f"[REAL_ESTATE_DETAILED] Checking inventory list ({len(inventory_list)} items)")
            for inv in inventory_list:
                estates = inv.get("Недвижимость", [])
                if not estates:
                    continue
                
                for estate in estates:
                    vid = estate.get("Вид", "") or estate.get("Категория", "") or ""
                    # Если вид явно содержит "авто" или "транспорт" - пропускаем, это движимое
                    if "авто" in vid.lower() or "транспорт" in vid.lower():
                        continue
                        
                    addr = estate.get("Адрес", "")
                    kadaster = estate.get("Кадастровый_номер", "")
                    
                    # Дедупликация по кадастровому номеру и адресу
                    is_duplicate = False
                    if kadaster:
                         for d in descriptions:
                             if kadaster in d:
                                 is_duplicate = True
                                 break
                    
                    if not is_duplicate and addr:
                        for d in descriptions:
                            if addr.lower() in d.lower():
                                is_duplicate = True
                                break
                    
                    if is_duplicate:
                        print(f"[REAL_ESTATE_DETAILED] Skip inventory item (duplicate): {addr} {kadaster}")
                        continue

                    # Формируем описание из описи
                    parts = []
                    if vid: parts.append(vid.capitalize())
                    if kadaster: parts.append(f"кадастровый номер {kadaster}")
                    if addr: parts.append(f"адрес: {addr}")
                    if estate.get("Площадь"): parts.append(f"площадь: {estate.get('Площадь')}")
                    
                    # Вид права
                    right_type = estate.get("Вид_права") or estate.get("Основание")
                    if right_type: parts.append(right_type)
                    
                    if estate.get("Стоимость"): parts.append(f"стоимость: {estate.get('Стоимость')}")
                    
                    if parts:
                        full_desc = ", ".join(parts)
                        print(f"[REAL_ESTATE_DETAILED] Adding from inventory: {full_desc[:100]}")
                        descriptions.append(full_desc)

        return "; ".join(descriptions) if descriptions else "нет"

    @staticmethod
    def format_movable_property(data_list: List[Dict[str, Any]]) -> str:
        """Форматирует движимое имущество (транспорт из ГИБДД)."""
        if not data_list:
            return "нет"

        # Преобразуем новый формат с массивом в плоский список для обработки
        all_vehicles = []
        for data in data_list:
            # Проверяем новый формат: {"Есть_транспорт": true, "Транспортные_средства": [...]}
            if "Есть_транспорт" in data:
                if data.get("Есть_транспорт") is True and "Транспортные_средства" in data:
                    # Добавляем все ТС из массива
                    vehicles_array = data.get("Транспортные_средства", [])
                    if isinstance(vehicles_array, list):
                        all_vehicles.extend(vehicles_array)
                # Если Есть_транспорт = false, пропускаем (справка об отсутствии)
            else:
                # Старый формат: один объект = одно ТС
                all_vehicles.append(data)

        # Дедупликация по VIN (включая нечеткий поиск)
        seen_vins = {} # Map cleaned_vin -> data
        deduplicated = []
        
        for data in all_vehicles:
            raw_vin = data.get("VIN", "")
            
            # Если VIN нет, добавляем как есть
            if not raw_vin:
                deduplicated.append(data)
                continue
                
            # Нормализация VIN (убираем пробелы, тире и приводим к верхнему регистру)
            current_vin = re.sub(r'[^A-Z0-9]', '', raw_vin.upper())
            if not current_vin:
                deduplicated.append(data)
                continue
            
            is_duplicate = False
            best_match_vin = None
            
            # 1. Точное совпадение
            if current_vin in seen_vins:
                is_duplicate = True
                best_match_vin = current_vin
            else:
                # 2. Нечеткий поиск (для борьбы с ошибками OCR: 8 vs 3, D vs 0, 8 vs 6)
                # Порог 0.8 позволяет найти совпадение при ~3 различиях в 17 символах
                best_ratio = 0.0
                for seen_vin in seen_vins:
                    ratio = difflib.SequenceMatcher(None, current_vin, seen_vin).ratio()
                    if ratio > best_ratio:
                        best_ratio = ratio
                        best_match_vin = seen_vin
                
                if best_ratio > 0.8:
                    is_duplicate = True
                    print(f"      [DEDUP] Найден нечеткий дубликат VIN: {current_vin} ~ {best_match_vin} (ratio: {best_ratio:.2f})")
            
            if is_duplicate and best_match_vin:
                # Логика выбора лучшей версии
                existing_data = seen_vins[best_match_vin]
                
                existing_doc_type = str(existing_data.get("Документ", {}).get("Тип", "")).upper()
                current_doc_type = str(data.get("Документ", {}).get("Тип", "")).upper()
                
                # Приоритет: СТС > ПТС > Договор
                def get_priority(doc_type):
                    if "СТС" in doc_type or "СВИДЕТЕЛЬСТВО" in doc_type: return 3
                    if "ПТС" in doc_type or "ПАСПОРТ" in doc_type: return 2
                    return 1
                
                current_prio = get_priority(current_doc_type)
                existing_prio = get_priority(existing_doc_type)
                
                if current_prio > existing_prio:
                    # Заменяем старую запись на новую, более приоритетную
                    deduplicated.remove(existing_data)
                    deduplicated.append(data)
                    # Обновляем маппинг. Если VIN был разный (fuzzy), удаляем старый ключ, добавляем новый
                    if best_match_vin != current_vin:
                        del seen_vins[best_match_vin]
                    seen_vins[current_vin] = data
                    print(f"      [DEDUP] Обновлено: {existing_doc_type} -> {current_doc_type}")
                else:
                    print(f"      [DEDUP] Оставлено существующее: {existing_doc_type} (vs {current_doc_type})")
            else:
                # Новенький
                seen_vins[current_vin] = data
                deduplicated.append(data)

        descriptions = []
        for data in deduplicated:
            # Проверяем, не справка ли об отсутствии
            if data.get("Результат") == "транспорт отсутствует":
                continue
            
            # Проверяем, не снято ли ТС с учета (продано/утилизировано)
            special_notes = data.get("Особые_отметки", "")
            if special_notes and isinstance(special_notes, str):
                special_lower = special_notes.lower()
                # Пропускаем ТС, которые сняты с учета
                if any(keyword in special_lower for keyword in ["снято с учета", "продаж", "продан", "передач", "утилизац"]):
                    continue

            parts = []

            # Тип ТС + Марка и модель (часто GPT объединяет их)
            ts_type = data.get("Тип_ТС", "")
            brand_model = data.get("Марка_модель", "")
            
            # Проверяем, не дублируется ли тип в марке
            if brand_model and ts_type and brand_model.lower().startswith(ts_type.lower()):
                # Тип уже в марке, используем только марку
                parts.append(brand_model)
            elif ts_type and brand_model:
                # Тип и марка отдельно
                parts.append(ts_type)
                parts.append(brand_model)
            elif brand_model:
                # Только марка
                parts.append(brand_model)
            elif ts_type:
                # Только тип
                parts.append(ts_type)

            # Год выпуска
            year = data.get("Год_выпуска", "")
            if year:
                parts.append(f"{year} года выпуска")

            # Государственный номер
            gov_num = data.get("Гос_номер", "")
            if gov_num:
                parts.append(f"гос. номер: {gov_num}")

            # VIN
            vin = data.get("VIN", "")
            if vin:
                parts.append(f"VIN: {vin}")

            # Дата регистрации
            doc = data.get("Документ", {})
            if isinstance(doc, dict):
                reg_date = doc.get("Дата_регистрации", "")
                if reg_date:
                    parts.append(f"дата регистрации: {reg_date}")

            if parts:
                descriptions.append(", ".join(parts))

        return "; ".join(descriptions) if descriptions else "нет"

    @staticmethod
    def format_shares(data_list: List[Dict[str, Any]]) -> str:
        """Форматирует акции и участие в организациях."""
        if not data_list:
            return "нет"

        descriptions = []
        for data in data_list:
            parts = []

            # Название организации
            org = data.get("Организация", {})
            if isinstance(org, dict):
                org_name = org.get("Название", "")
                org_form = org.get("Форма", "")
                org_inn = org.get("ИНН", "")

                if org_name:
                    if org_form:
                        parts.append(f"{org_form} \"{org_name}\"")
                    else:
                        parts.append(f"\"{org_name}\"")

                    if org_inn:
                        parts.append(f"ИНН: {org_inn}")

            # Доля участия
            participant = data.get("Участник", {})
            if isinstance(participant, dict):
                share = participant.get("Доля", "")
                if share:
                    parts.append(f"доля: {share}")

                nominal = participant.get("Номинальная_стоимость", "")
                if nominal:
                    parts.append(f"номинальная стоимость: {nominal}")

            if parts:
                descriptions.append(", ".join(parts))

        return "; ".join(descriptions) if descriptions else "нет"

    @staticmethod
    def format_securities(data_list: List[Dict[str, Any]]) -> str:
        """Форматирует ценные бумаги."""
        if not data_list:
            return "нет"

        descriptions = []
        for data in data_list:
            securities_list = data.get("Ценные_бумаги", [])
            depositary = data.get("Депозитарий", "")
            account = data.get("Номер_счета", "")
            total_value = data.get("Общая_стоимость", "")

            if securities_list:
                for sec in securities_list:
                    parts = []

                    # Тип и эмитент
                    sec_type = sec.get("Тип", "")
                    emitent = sec.get("Эмитент", "")

                    if sec_type:
                        parts.append(sec_type)
                    if emitent:
                        parts.append(f"эмитент: {emitent}")

                    # Количество и стоимость
                    quantity = sec.get("Количество", "")
                    value = sec.get("Текущая_стоимость", "")

                    if quantity:
                        parts.append(f"количество: {quantity}")
                    if value:
                        parts.append(f"стоимость: {value}")

                    # ISIN
                    isin = sec.get("ISIN", "")
                    if isin:
                        parts.append(f"ISIN: {isin}")

                    if parts:
                        descriptions.append(", ".join(parts))

            # Если нет детализации, но есть общая информация
            if not descriptions and (depositary or total_value):
                parts = []
                if depositary:
                    parts.append(f"депозитарий: {depositary}")
                if account:
                    parts.append(f"счет: {account}")
                if total_value:
                    parts.append(f"общая стоимость портфеля: {total_value}")
                if parts:
                    descriptions.append(", ".join(parts))

        return "; ".join(descriptions) if descriptions else "нет"

    @staticmethod
    def format_cash_valuables(data_list: List[Dict[str, Any]]) -> str:
        """Форматирует наличные денежные средства и иное ценное имущество (драгоценности, искусство, коллекции)."""
        if not data_list:
            return "нет"

        descriptions = []

        for data in data_list:
            # Наличные денежные средства
            cash = data.get("Наличные_средства", {})
            if isinstance(cash, dict):
                cash_parts = []
                for currency, amount in cash.items():
                    if amount:
                        if currency == "RUB":
                            cash_parts.append(f"{amount} руб.")
                        elif currency == "USD":
                            cash_parts.append(f"{amount} долларов США")
                        elif currency == "EUR":
                            cash_parts.append(f"{amount} евро")
                        else:
                            cash_parts.append(f"{amount} ({currency})")

                if cash_parts:
                    descriptions.append("наличные денежные средства: " + ", ".join(cash_parts))

            # Имущество (драгоценности, искусство и т.д.)
            items = data.get("Имущество", [])
            for item in items:
                parts = []

                category = item.get("Категория", "")
                description = item.get("Описание", "")
                characteristics = item.get("Характеристики", "")
                value = item.get("Оценочная_стоимость", "")

                # Категория и описание
                if category:
                    parts.append(category)
                if description:
                    parts.append(description)

                # Характеристики
                if characteristics:
                    parts.append(characteristics)

                # Стоимость
                if value:
                    parts.append(f"оценочная стоимость: {value}")

                if parts:
                    descriptions.append(", ".join(parts))

            # Общая стоимость (если указана отдельно)
            total = data.get("Общая_стоимость", "")
            if total and not items:  # Если нет детализации, но есть общая сумма
                descriptions.append(f"имущество на общую сумму: {total}")

        return "; ".join(descriptions) if descriptions else "нет"

    @staticmethod
    def format_property_rights(data_list: List[Dict[str, Any]]) -> str:
        """Форматирует имущественные права (дебиторская задолженность).

        ВАЖНО: Фильтрует только те документы, где владелец = КРЕДИТОР.
        Если владелец = ДОЛЖНИК, это не имущественное право, а обязательство!
        """
        if not data_list:
            return "нет"

        descriptions = []
        for data in data_list:
            # КРИТИЧЕСКИ ВАЖНО: Проверяем роль владельца документа
            # Имущественное право = владелец является КРЕДИТОРОМ (ему должны)
            # Если владелец = должник/заёмщик - это обязательство, не право!
            role = data.get("Роль_владельца_документа", "")
            if role:
                role_lower = role.lower()
                # Пропускаем если владелец - должник или заёмщик (это его долг, не право)
                if any(keyword in role_lower for keyword in ["должник", "заемщик", "заёмщик"]):
                    continue
                # Принимаем только если владелец - кредитор (ему должны)
                if "кредитор" not in role_lower:
                    continue

            parts = []

            # Тип документа
            doc_type = data.get("Тип", "")
            if doc_type:
                parts.append(doc_type)

            # Должник (кто должен)
            debtor = data.get("Должник", "")
            if debtor:
                parts.append(f"должник: {debtor}")

            # Сумма
            amount = data.get("Сумма", "")
            if amount:
                parts.append(f"сумма: {amount}")

            # Номер договора/дела
            doc_num = data.get("Номер", "")
            doc_date = data.get("Дата", "")
            if doc_num:
                if doc_date:
                    parts.append(f"документ: {doc_num} от {doc_date}")
                else:
                    parts.append(f"документ: {doc_num}")

            # Срок
            term = data.get("Срок", "")
            if term:
                parts.append(f"срок: {term}")

            if parts:
                descriptions.append(", ".join(parts))

        return "; ".join(descriptions) if descriptions else "нет"

    @staticmethod
    def format_other_income(pension_data: List[Dict[str, Any]], payment_data: List[Dict[str, Any]]) -> str:
        """Форматирует информацию об иных доходах (пособия, ЕДВ, подработки)."""
        descriptions = []

        # Обрабатываем данные из документов "пенсии" (назначенные выплаты)
        for doc in pension_data:
            # --- НОВАЯ ЛОГИКА: Используем готовый текст от GPT, если есть ---
            status_text = doc.get("Текст_статуса")
            benefits = doc.get("Выплаты", [])
            
            if status_text:
                # Если GPT сгенерировал описание
                # Проверяем, не является ли это отказом ("не является")
                if "не является" in status_text.lower() and not benefits:
                     continue # Не добавляем в список доходов
                
                descriptions.append(status_text)
                continue # Переходим к следующему документу

            # --- Fallback: Если текста нет, собираем по старому алгоритму ---
            if not benefits:
                continue
            
            main_pension_type = None
            
            edv_list = []      # Список ЕДВ (Ежемесячных денежных выплат)
            other_list = []    # Прочие пособия

            # 1. Проход по всем выплатам документа
            for benefit in benefits:
                b_type = benefit.get("Вид", "") or ""
                if not b_type:
                    continue
                
                b_type_lower = b_type.lower()
                
                # ИТОГОВАЯ СУММУ пенсии - просто пропускаем, нам сумма в тексте не нужна
                if "суммарный размер" in b_type_lower:
                    continue
                
                # Ищем ЕДВ (это отдельная выплата)
                if "едв" in b_type_lower or "ежемесячная денежная выплата" in b_type_lower:
                    short_desc = "Ежемесячная денежная выплата"
                    if "ветеран" in b_type_lower:
                        short_desc += " ветеранам"
                    elif "инвалид" in b_type_lower:
                        short_desc += " инвалидам"
                    elif "радиац" in b_type_lower:
                        short_desc += " гражданам, подвергшимся воздействию радиации"
                    
                    if short_desc not in edv_list:
                        edv_list.append(short_desc)
                    continue
                
                # Ищем сам ТИП ПЕНСИИ (например "Страховая пенсия по инвалидности")
                # Игнорируем "фиксированную выплату", "повышение" и т.д.
                if "пенси" in b_type_lower:
                    if "фиксированная выплата" in b_type_lower:
                        continue # пропускаем части
                    if "повышение" in b_type_lower:
                        continue # пропускаем надбавки
                    
                    # Это основной тип (если еще не нашли)
                    if not main_pension_type:
                        main_pension_type = b_type
                    continue
                
                # Если это не пенсия и не ЕДВ - скорее всего пособие или другое
                other_text = f"Выплата: {b_type}"
                if other_text not in other_list:
                    other_list.append(other_text)

            # 2. Формируем итоговые строки
            
            # А) ПЕНСИЯ
            if main_pension_type:
                descriptions.append(f"Является получателем пенсии ({main_pension_type})")
            
            # Б) ЕДВ
            for item in edv_list:
                descriptions.append(f"Является получателем {item}")

            # В) ПРОЧЕЕ
            for item in other_list:
                descriptions.append(f"Получает {item}")


        # Обрабатываем данные из документов "выплаты" (фактические выплаты за период)
        for doc in payment_data:
            payments = doc.get("Выплаты", [])
            if not payments:
                continue

            for payment in payments:
                payment_name = payment.get("Наименование", "")
                # Суммы больше не добавляем в текст
                
                if payment_name:
                    payment_lower = payment_name.lower()

                    # Пенсии тоже включаем, так как не всегда есть отдельное поле
                    if "пенси" in payment_lower:
                        desc = f"Должник получает пенсию ({payment_name})"
                    elif "едв" in payment_lower or "ежемесячная денежная выплата" in payment_lower:
                        desc = f"Должник получает ежемесячную денежную выплату"
                    elif "пособи" in payment_lower:
                        desc = f"Должник получает пособие ({payment_name})"
                    else:
                        desc = f"Должник получает выплату: {payment_name}"

                    desc += "."
                    if desc not in descriptions:
                        descriptions.append(desc)

        # Убираем дубликаты - сравниваем по типу выплаты и сумме
        unique_descriptions = []
        seen_benefits = []

        import re

        for desc in descriptions:
            # Извлекаем ключевые слова для сравнения
            desc_lower = desc.lower()

            # Извлекаем тип выплаты из ОТФОРМАТИРОВАННОГО описания
            benefit_type = ""
            if "ежемесячная денежная выплата" in desc_lower:
                benefit_type = "едв"
            elif "пособи" in desc_lower:
                benefit_type = "пособие"
            elif "выплату:" in desc_lower:
                # Извлекаем название после "выплату:"
                match = re.search(r'выплату:\s*([^в]+?)\s+в размере', desc_lower)
                if match:
                    benefit_type = match.group(1).strip()
                else:
                    benefit_type = "прочая_выплата"
            else:
                benefit_type = "прочая_выплата"

            # Извлекаем сумму и нормализуем её
            # Ищем все числа в строке
            amounts = re.findall(r'\d+', desc)
            if amounts:
                # Объединяем первые два числа как основную сумму (рубли и копейки)
                if len(amounts) >= 2:
                    normalized_amount = f"{amounts[0]}.{amounts[1]}"
                else:
                    normalized_amount = amounts[0]
            else:
                normalized_amount = ""

            # Проверяем, не видели ли мы уже такую выплату
            # Если суммы нет - считаем уникальной по полному тексту, чтобы не схлопнуть разные уведомления
            if not normalized_amount:
                benefit_signature = (desc_lower, "")
            else:
                benefit_signature = (benefit_type, normalized_amount)
            
            if benefit_signature not in seen_benefits:
                seen_benefits.append(benefit_signature)
                unique_descriptions.append(desc)

        # Формируем итоговый текст
        if unique_descriptions:
            result = " ".join(unique_descriptions)
            return result
        else:
            return "Получателем пенсий, пособий не является."

    @staticmethod
    def number_to_words_rubles(amount: Decimal) -> str:
        """Конвертирует число в текст прописью для рублей и копеек."""
        try:
            from num2words import num2words
            
            rubles = int(amount)
            kopeks = int((amount - rubles) * 100)

            # Используем num2words для конвертации в слова
            words = num2words(rubles, lang='ru')

            # Форматируем результат
            rubles_formatted = f"{rubles:,}".replace(",", " ")
            result = f"{rubles_formatted} ({words}) руб. {kopeks:02d} коп."
            return result
        except Exception as e:
            # Fallback: если библиотека не работает, возвращаем просто число
            rubles = int(amount)
            kopeks = int((amount - rubles) * 100)
            rubles_formatted = f"{rubles:,}".replace(",", " ")
            return f"{rubles_formatted} руб. {kopeks:02d} коп."

    @staticmethod
    def check_marital_and_children_status(passport: Dict[str, Any], data_map: Dict[str, List[Dict[str, Any]]] = None) -> str:
        """Проверяет семейное положение и наличие детей.
        
        Args:
            passport: данные паспорта
            data_map: словарь всех документов (для проверки свидетельств)
        """
        marital_status = (passport.get("Семейное_положение") or "").lower() if passport else ""
        children = passport.get("Дети", []) if passport else []

        # Проверяем наличие несовершеннолетних детей из паспорта
        has_minors = False
        passport_children_count = 0
        
        if children:
            today = date.today()
            for child in children:
                if not isinstance(child, dict):
                    continue
                passport_children_count += 1
                dob_str = child.get("Дата_рождения")
                if dob_str:
                    try:
                        dob = datetime.strptime(dob_str, "%d.%m.%Y").date()
                        age_years = (today - dob).days / 365.25
                        print(f"[CHILDREN] Ребенок из паспорта: ДР={dob_str}, возраст={age_years:.1f} лет")
                        if age_years < 18:
                            has_minors = True
                            print(f"[CHILDREN] Найден несовершеннолетний ребенок (возраст {age_years:.1f} лет)")
                    except ValueError as e:
                        print(f"[CHILDREN] Ошибка парсинга даты '{dob_str}': {e}")
                        continue

        # ДОПОЛНИТЕЛЬНО: Проверяем свидетельства о рождении детей
        if data_map:
            birth_certificates = data_map.get("свидетельство_о_рождении", [])
            print(f"[CHILDREN] Свидетельств о рождении: {len(birth_certificates)}")
            if birth_certificates:
                today = date.today()
                for cert in birth_certificates:
                    if not isinstance(cert, dict):
                        continue
                    
                    # 1. Проверяем НОВЫЙ формат (массив "Дети")
                    children_array = cert.get("Дети")
                    if children_array and isinstance(children_array, list):
                        for child_data in children_array:
                            if not isinstance(child_data, dict):
                                continue
                            dob_str = child_data.get("Дата_рождения")
                            if dob_str:
                                try:
                                    dob = datetime.strptime(dob_str, "%d.%m.%Y").date()
                                    age_years = (today - dob).days / 365.25
                                    print(f"[CHILDREN] Ребенок из свидетельства (НОВЫЙ формат): ДР={dob_str}, возраст={age_years:.1f} лет")
                                    if age_years < 18:
                                        has_minors = True
                                        print(f"[CHILDREN] Найден несовершеннолетний (НОВЫЙ формат)")
                                except ValueError as e:
                                    continue
                    
                    # 2. Проверяем СТАРЫЙ формат (верхний уровень)
                    dob_str = cert.get("Дата_рождения")
                    if dob_str:
                        try:
                            dob = datetime.strptime(dob_str, "%d.%m.%Y").date()
                            age_years = (today - dob).days / 365.25
                            print(f"[CHILDREN] Ребенок из свидетельства: ДР={dob_str}, возраст={age_years:.1f} лет")
                            if age_years < 18:
                                has_minors = True
                                print(f"[CHILDREN] Найден несовершеннолетний ребенок из свидетельства (возраст {age_years:.1f} лет)")
                        except ValueError as e:
                            print(f"[CHILDREN] Ошибка парсинга даты из свидетельства '{dob_str}': {e}")
                            continue
        
        print(f"[CHILDREN] Итого: has_minors={has_minors}, из паспорта={passport_children_count} детей")

        # Проверяем свидетельство о браке
        has_marriage = False
        if data_map:
            marriage_certs = data_map.get("свидетельство_о_браке", [])
            marriage_certs_new = data_map.get("свидетельство_о_заключении_брака", [])
            if marriage_certs or marriage_certs_new:
                has_marriage = True

        # Проверяем свидетельство о расторжении брака
        has_divorce = False
        if data_map:
            # Проверяем оба варианта ключа
            divorce_certs = data_map.get("свидетельство_о_разводе", [])
            divorce_certs_formal = data_map.get("свидетельство_о_расторжении_брака", [])
            if divorce_certs or divorce_certs_formal:
                has_divorce = True

        # Проверяем, не в браке ли
        # Если есть свидетельство о разводе - считаем что не в браке (даже если есть старое св-во о браке)
        not_married = (
            "не женат" in marital_status or 
            "не замужем" in marital_status or 
            "не в браке" in marital_status or
            "не состоит" in marital_status or
            "холост" in marital_status or
            has_divorce
        )

        # Формируем текст о семейном положении
        parts = []

        # Информация о браке
        if not_married:
            parts.append("Должник в настоящее время в браке не состоит")
        else:
            # Если в браке (есть свидетельство или указано в паспорте)
            if has_marriage or "женат" in marital_status or "замужем" in marital_status:
                parts.append("Должник состоит в браке")
            elif "разведен" in marital_status or "разведена" in marital_status:
                parts.append("Должник разведен")

        # Информация о детях
        if not has_minors:
            parts.append("несовершеннолетних детей на иждивении не имеет")
        else:
            parts.append("имеет несовершеннолетних детей на иждивении")

        if parts:
            result = ", ".join(parts) + "."
            return result

        return ""


    @staticmethod
    def calculate_average_income(data_list_dohody: List[Dict[str, Any]], pension_list: List[Dict[str, Any]] = None) -> Optional[Decimal]:
        """
        Считает средний доход:
        1. 2-НДФЛ (Общая сумма / 12) - берем сумму доходов за самый свежий год
        2. Пенсия + ЕДВ (берем месячную сумму)
        """
        total_monthly_income = Decimal(0)
        has_income = False

        # --- 1. Справки 2-НДФЛ (доходы) ---
        latest_year = None
        current_year_income_sum = Decimal(0)

        # Сначала найдем самый свежий год среди всех справок
        for data in data_list_dohody:
            for statement in data.get("Справки", []) or []:
                try:
                    year = int(str(statement.get("Год", "0")))
                except:
                    year = 0
                if latest_year is None or year > latest_year:
                    latest_year = year

        # Теперь суммируем все доходы за этот год
        if latest_year is not None:
            for data in data_list_dohody:
                for statement in data.get("Справки", []) or []:
                    try:
                        year = int(str(statement.get("Год", "0")))
                    except:
                        year = 0
                    
                    if year == latest_year:
                        amount_str = statement.get("Общая_сумма_дохода")
                        amount = DocumentProcessor.parse_decimal(amount_str)
                        if amount:
                            current_year_income_sum += amount

        if current_year_income_sum > 0:
            total_monthly_income += (current_year_income_sum / Decimal(12))
            has_income = True

        # --- 2. Пенсии и ЕДВ (ежемесячные) ---
        if pension_list:
            for doc in pension_list:
                benefits = doc.get("Выплаты", []) or []
                
                # Попытка 1: Ищем "Суммарный размер" (наиболее надежно)
                found_total_summary = False
                doc_pension_sum = Decimal(0)

                for benefit in benefits:
                    b_type = (benefit.get("Вид") or "").lower()
                    b_amount_str = benefit.get("Размер")
                    b_amount = DocumentProcessor.parse_decimal(b_amount_str)
                    
                    if not b_amount: continue
                    
                    # Ищем ключевую фразу "Суммарный (общий) размер..."
                    if "суммарный размер" in b_type:
                        doc_pension_sum = b_amount # Обычно это и есть полная сумма
                        found_total_summary = True
                        break # Нашли итог, выходим

                # Попытка 2: Если "Суммарный размер" не найден, складываем части (страховая + фиксированная)
                if not found_total_summary:
                    for benefit in benefits:
                        b_type = (benefit.get("Вид") or "").lower()
                        b_amount_str = benefit.get("Размер")
                        b_amount = DocumentProcessor.parse_decimal(b_amount_str)
                        
                        if not b_amount: continue
                        
                        # Игнорируем ЕДВ здесь (добавим отдельно ниже)
                        if "едв" in b_type or "ежемесячная денежная выплата" in b_type:
                            continue
                        
                        # Суммируем компоненты пенсии (Страховая, Накопительная, Фиксированная и т.д.)
                        # Чтобы не захватить лишнего, проверяем ключевые слова
                        if any(x in b_type for x in ["пенсия", "выплата", "повышение"]):
                            doc_pension_sum += b_amount

                total_monthly_income += doc_pension_sum
                if doc_pension_sum > 0:
                    has_income = True

                # --- 3. ЕДВ (всегда отдельно плюсуем, т.к. в суммарный размер пенсии он обычно не входит или идет отдельной строкой) ---
                # В справках СФР "Суммарный размер" обычно включает страховую+фиксированную. ЕДВ идет отдельно.
                for benefit in benefits:
                    b_type = (benefit.get("Вид") or "").lower()
                    b_amount_str = benefit.get("Размер")
                    b_amount = DocumentProcessor.parse_decimal(b_amount_str)
                    
                    if not b_amount: continue

                    if "едв" in b_type or "ежемесячная денежная выплата" in b_type:
                        total_monthly_income += b_amount
                        has_income = True

        return total_monthly_income if has_income else None

    @staticmethod
    def format_creditors_table(data_map: Dict[str, List[Dict[str, Any]]], debtor_fio: str = "", debtor_inn: str = "", debtor_snils: str = "", debtor_address: str = "") -> Dict[str, str]:
        """Генерирует данные для таблицы 'Список кредиторов и должников гражданина'.

        Возвращает словарь с ключами для подстановки в шаблон Word.

        ВАЖНО: НЕ объединяет дубли - каждый кредит идет отдельной строкой.
        Группировка только для нумерации (1.1, 1.2 от одного кредитора).

        Args:
            data_map: словарь с данными документов
            debtor_fio: ФИО должника (из паспорта)
            debtor_inn: ИНН должника
            debtor_snils: СНИЛС должника
            debtor_address: Адрес регистрации должника
        """
        from collections import defaultdict

        # ИСПОЛЬЗУЕМ merge_credit_reports для получения объединенного списка с учетом справок
        merged_credits = DocumentProcessor.merge_credit_reports(data_map)
        
        # Конвертируем объединенные кредиты в формат для таблицы
        all_credits_list = []
        
        for entry in merged_credits:
            if not isinstance(entry, dict):
                continue

            кредитор = entry.get("Кредитор", "").strip()
            if not кредитор:
                continue

            # Данные уже обработаны в merge_credit_reports
            сумма = entry.get("Сумма_задолженности", Decimal(0))
            if сумма <= 0:
                continue

            # Нормализация имени
            кредитор_normalized, кредитор_canonical = DocumentProcessor.normalize_bank_name(кредитор)

            # Получаем данные (уже обработанные в merge_credit_reports)
            инн_кредитора = entry.get("ИНН_кредитора") or ""
            огрн_кредитора = entry.get("ОГРН_кредитора") or ""
            
            # ИСПРАВЛЕНИЕ: GPT иногда путает ИНН и ОГРН
            # ИНН = 10 цифр, ОГРН = 13 цифр (или 15)
            if инн_кредитора and (len(инн_кредитора) == 13 or len(инн_кредитора) == 15):
                # Это ОГРН, а не ИНН!
                if not огрн_кредитора:
                    огрн_кредитора = инн_кредитора
                инн_кредитора = ""
            
            # Получаем адрес СНАЧАЛА (это вызовет RusProfile и закеширует ИНН + адрес)
            адрес_из_документа = entry.get("Адрес") or entry.get("Адрес_кредитора") or ""
            
            # Теперь передаем ИНН в get_bank_address для более точного поиска
            адрес_из_реестра = DocumentProcessor.get_bank_address(кредитор_canonical, inn=инн_кредитора if DocumentProcessor.validate_inn(инн_кредитора) else None)
            
            # ПОТОМ проверяем ИНН (теперь get_bank_inn найдет ИНН в кеше RusProfile)
            if not инн_кредитора:
                инн_кредитора = DocumentProcessor.get_bank_inn(кредитор_canonical)

            # Если адрес из реестра - это просто "г. Москва" (фоллбэк get_bank_address), считаем его некорректным
            if адрес_из_реестра and адрес_из_реестра.strip().lower() == "г. москва":
                адрес_из_реестра = ""
            
            # Специальная обработка для Т-Банка - всегда используем адрес из реестра (Москва)
            # Проверяем по всем вариантам имени
            is_t_bank = (
                "Т-БАНК" in кредитор_canonical.upper() or 
                "Т БАНК" in кредитор_canonical.upper() or
                "ТБАНК" in кредитор_canonical.upper() or
                "ТИНЬКОФФ" in кредитор_canonical.upper() or
                "Т-БАНК" in кредитор.upper() or
                "TBANK" in кредитор_canonical.upper()
            )
            
            if is_t_bank:
                адрес_банка = адрес_из_реестра or "127287, г. Москва, 2-я Хуторская ул., д. 38А, стр. 26"
            else:
                # ЛОГИКА ВЫБОРА АДРЕСА 2.0 (Исправленная)
                # 1. Если есть адрес из реестра (RusProfile) и он нормальный (длиннее 15 символов) - берем его
                if адрес_из_реестра and len(адрес_из_реестра) > 15:
                    адрес_банка = адрес_из_реестра
                # 2. Если адрес из документа нормальный - берем его
                elif адрес_из_документа and len(адрес_из_документа) > 15:
                     адрес_банка = адрес_из_документа
                # 3. Если оба короткие/плохие - берем хоть что-то (приоритет реестру)
                else:
                     адрес_банка = адрес_из_реестра or адрес_из_документа

            тип_кредита = entry.get("Вид") or entry.get("Тип_кредита") or "Потребительский кредит"
            дата_договора = entry.get("Дата_договора") or ""
            сумма_обязательства = entry.get("Сумма_обязательства") or Decimal(0)
            текущая = entry.get("Текущая_задолженность") or Decimal(0)
            просрочка = entry.get("Просрочка") or entry.get("Текущая_просрочка") or Decimal(0)

            all_credits_list.append({
                "Кредитор": кредитор,
                "Кредитор_canonical": кредитор_canonical,
                "Кредитор_normalized": кредитор_normalized,
                "ИНН_кредитора": инн_кредитора,
                "ОГРН_кредитора": огрн_кредитора,
                "Тип_кредита": тип_кредита,
                "Дата_договора": дата_договора,
                "Сумма_обязательства": сумма_обязательства,
                "Адрес_кредитора": адрес_банка,
                "Текущая_задолженность": текущая,
                "Просрочка": просрочка,
                "Сумма": сумма,
                "Штрафы_пени": просрочка,
            })

        # Дедупликация кредитов по уникальному ключу
        # Ключ: кредитор + дата договора + сумма обязательства + текущий долг
        unique_credits = {}
        for credit in all_credits_list:
            # Формируем ключ с учетом всех важных параметров
            key = (
                credit['Кредитор_normalized'],
                credit['Дата_договора'],
                float(credit.get('Сумма_обязательства', 0)),
                float(credit.get('Сумма', 0))
            )
            
            # Если такого ключа еще нет - добавляем
            # Если уже есть - оставляем тот, что с большей суммой долга (более актуальный)
            if key not in unique_credits:
                unique_credits[key] = credit
            else:
                existing = unique_credits[key]
                if credit.get('Сумма', 0) > existing.get('Сумма', 0):
                    unique_credits[key] = credit

        # Группируем уникальные кредиты по нормализованному имени кредитора
        credits_by_creditor = defaultdict(list)
        for credit in unique_credits.values():
            кредитор_normalized = credit["Кредитор_normalized"]
            credits_by_creditor[кредитор_normalized].append(credit)

        # === ОБРАБОТКА ПОСТАНОВЛЕНИЙ ПРИСТАВОВ - ДО СОРТИРОВКИ ===
        # Обрабатываем постановления и добавляем их к существующим кредиторам или как новых кредиторов
        other_debts = []  # Новые кредиторы из постановлений (не в кредитных отчетах)
        
        # Извлекаем судебные акты из кредитных отчетов для дедупликации
        court_acts_from_reports = set()
        for отчет_окб in data_map.get("отчет_окб", []):
            судебные_акты = отчет_окб.get("Судебные_акты", [])
            for акт in судебные_акты:
                номер = акт.get("Номер_дела", "").strip()
                if номер:
                    # Нормализуем номер дела: убираем пробелы, приводим к нижнему регистру
                    номер_normalized = номер.replace(" ", "").lower()
                    court_acts_from_reports.add(номер_normalized)
                    print(f"[COURT_ACT_OKB] Извлечен судебный акт: {номер}")
        
        for отчет_бки in data_map.get("отчет_бки", []):
            судебные_акты = отчет_бки.get("Судебные_акты", [])
            for акт in судебные_акты:
                номер = акт.get("Номер_дела", "").strip()
                if номер:
                    номер_normalized = номер.replace(" ", "").lower()
                    court_acts_from_reports.add(номер_normalized)
                    print(f"[COURT_ACT_BKI] Извлечен судебный акт: {номер}")
        
        for отчет_нбки in data_map.get("отчет_нбки", []):
            судебные_акты = отчет_нбки.get("Судебные_акты", [])
            for акт in судебные_акты:
                номер = акт.get("Номер_дела", "").strip()
                if номер:
                    номер_normalized = номер.replace(" ", "").lower()
                    court_acts_from_reports.add(номер_normalized)
                    print(f"[COURT_ACT_NBKI] Извлечен судебный акт: {номер}")
        
        print(f"[COURT_ACTS] Всего судебных актов в кредитных отчетах: {len(court_acts_from_reports)}")
        
        # Дедупликация по номеру ИП
        seen_ip_numbers = set()
        
        for постановление in data_map.get("постановление_пристава", []):
            # ФИЛЬТР 1: Пропускаем закрытые/прекращенные ИП
            дата_окончания = постановление.get("Дата_окончания")
            if дата_окончания and дата_окончания not in ["", "null", None]:
                print(f"[ПОСТАНОВЛЕНИЕ] Пропускаем закрытое ИП: {постановление.get('Номер_ИП')} (закрыто {дата_окончания})")
                continue
            
            # ФИЛЬТР 2: Дедупликация по номеру ИП
            номер_ип = (постановление.get("Номер_ИП") or "").strip()
            if номер_ип:
                if номер_ип in seen_ip_numbers:
                    print(f"[ПОСТАНОВЛЕНИЕ] Пропускаем дубль ИП: {номер_ип}")
                    continue
                seen_ip_numbers.add(номер_ип)
            
            взыскатель = (постановление.get("Взыскатель") or "").strip()
            взыскатель_lower = взыскатель.lower()

            сумма_str = постановление.get("Итого_взыскание") or постановление.get("Остаток_долга") or постановление.get("Сумма_долга") or ""
            сумма = DocumentProcessor.parse_decimal(сумма_str) or Decimal(0)

            if сумма <= 0:
                continue

            # Пропускаем налоги (они обрабатываются отдельно ниже)
            if any(kw in взыскатель_lower for kw in ["налог", "ифнс", "фнс", "инспекция федеральной налоговой службы"]):
                continue

            # Получаем дополнительные данные
            номер_ип = постановление.get("Номер_ИП") or постановление.get("Номер") or ""
            дата_ип = постановление.get("Дата_постановления") or постановление.get("Дата_возбуждения") or постановление.get("Дата") or ""
            
            # Предмет исполнения (тип долга)
            предмет = постановление.get("Предмет_исполнения") or "Взыскание по постановлению ФССП"
            
            # ВАЖНО: Если взыскатель не указан (таблица ФССП), используем предмет как название
            # но НЕ группируем все вместе - каждое ИП отдельно
            if not взыскатель or взыскатель in ["null", "None"]:
                взыскатель = f"Взыскатель по ИП {номер_ип}" if номер_ип else предмет
                print(f"[ПОСТАНОВЛЕНИЕ] Взыскатель не указан, используем: {взыскатель}")
            
            # Адрес взыскателя
            адрес_взыскателя = постановление.get("Адрес_взыскателя") or ""
            if адрес_взыскателя:
                адрес_взыскателя = ", ".join(filter(None, [x.strip() for x in адрес_взыскателя.split(",")]))
            
            # Информация об органе, выдавшем документ + номер дела
            орган = постановление.get("Орган_выдавший") or ""
            адрес_органа = постановление.get("Адрес_органа") or ""
            номер_дела = постановление.get("Номер_дела") or ""
            
            # ФИЛЬТР 3: Дедупликация с судебными актами из кредитных отчетов
            if номер_дела:
                номер_дела_normalized = номер_дела.replace(" ", "").lower()
                if номер_дела_normalized in court_acts_from_reports:
                    print(f"[ПОСТАНОВЛЕНИЕ_DEDUP] Пропускаем постановление - судебный акт уже в кредитном отчете: {номер_дела}")
                    continue
            
            # Формируем информацию для поля "Дата_договора"
            дата_display = []
            if орган:
                орган_clean = ", ".join(filter(None, [x.strip() for x in орган.split(",")]))
                дата_display.append(орган_clean)
            if адрес_органа:
                адрес_органа_clean = ", ".join(filter(None, [x.strip() for x in адрес_органа.split(",")]))
                дата_display.append(f"Адрес: {адрес_органа_clean}")
            if номер_дела:
                дата_display.append(f"по делу № {номер_дела}")
            
            дата_договора_text = " ".join(дата_display) if дата_display else дата_ип

            # Нормализуем название взыскателя
            взыскатель_normalized, взыскатель_canonical = DocumentProcessor.normalize_bank_name(взыскатель)

            # Пытаемся получить ИНН взыскателя (только если взыскатель указан явно)
            инн_взыскателя = постановление.get("ИНН_взыскателя") or постановление.get("ИНН_кредитора") or ""
            if not инн_взыскателя and взыскатель and not взыскатель.startswith("Взыскатель по ИП"):
                # Если ИНН не указан в постановлении - пробуем найти через get_bank_inn
                инн_взыскателя = DocumentProcessor.get_bank_inn(взыскатель_canonical)

            # Пытаемся получить ИНН взыскателя (только если взыскатель указан явно)
            инн_взыскателя = постановление.get("ИНН_взыскателя") or постановление.get("ИНН_кредитора") or ""
            if not инн_взыскателя and взыскатель and not взыскатель.startswith("Взыскатель по ИП"):
                # Если ИНН не указан в постановлении - пробуем найти через get_bank_inn
                инн_взыскателя = DocumentProcessor.get_bank_inn(взыскатель_canonical)

            # Проверяем, стоит ли добавлять к существующему кредитору
            # НЕ группируем если взыскатель не указан (таблица ФССП)
            should_group = (
                взыскатель_normalized in credits_by_creditor and 
                not взыскатель.startswith("Взыскатель по ИП")
            )
            
            if should_group:
                # Кредитор уже есть - добавляем постановление как дополнительный договор к нему
                print(f"[ПОСТАНОВЛЕНИЕ] Добавляем к существующему кредитору: {взыскатель_canonical}")
                credits_by_creditor[взыскатель_normalized].append({
                    "Кредитор": взыскатель,
                    "Кредитор_canonical": взыскатель_canonical,
                    "Кредитор_normalized": взыскатель_normalized,
                    "ИНН_кредитора": инн_взыскателя,
                    "ОГРН_кредитора": "",
                    "Тип_кредита": предмет,
                    "Дата_договора": дата_договора_text,
                    "Сумма_обязательства": сумма,
                    "Адрес_кредитора": адрес_взыскателя,
                    "Текущая_задолженность": сумма,
                    "Просрочка": Decimal(0),
                    "Сумма": сумма,
                    "Штрафы_пени": Decimal(0),
                })
            else:
                # Новый кредитор - добавляем в other_debts для последующей обработки
                # Для ИП без взыскателя - кредитор пустой, показываем только предмет исполнения
                кредитор_display = "" if взыскатель.startswith("Взыскатель по ИП") else взыскатель_canonical
                print(f"[ПОСТАНОВЛЕНИЕ] Новый долг: {предмет} (взыскатель: {кредитор_display or '(не указан)'})")
                other_debts.append({
                    "Кредитор": взыскатель,
                    "Кредитор_canonical": кредитор_display,
                    "Кредитор_normalized": взыскатель_normalized,
                    "ИНН_кредитора": инн_взыскателя,
                    "Адрес_кредитора": адрес_взыскателя,
                    "Тип_кредита": предмет,
                    "Дата_договора": дата_договора_text,
                    "Номер_ИП": номер_ип,
                    "Сумма": сумма,
                })

        # Сортируем кредиторов по общей сумме всех их кредитов (убывание)
        sorted_creditors = sorted(
            credits_by_creditor.items(),
            key=lambda x: sum(c["Сумма"] for c in x[1]),
            reverse=True
        )

        # Генерируем строки таблицы - ГРУППИРУЕМ ПО БАНКАМ
        table_rows = []
        counter = 1

        for кредитор_normalized, credits_list in sorted_creditors:
            # Каждый договор = отдельная строка в таблице
            первый_кредит = credits_list[0]
            
            # ИСПОЛЬЗУЕМ КАНОНИЧЕСКОЕ НАЗВАНИЕ (правильное форматирование)
            кредитор_display = первый_кредит["Кредитор_canonical"]
            
            # --- НОВАЯ ЛОГИКА: Сначала найти ИНН, потом Адрес ---
            # 1. Поиск ИНН по всем кредитам этой группы (а не только в первом)
            found_inn = None
            for credit in credits_list:
                cand = credit.get("ИНН_кредитора")
                if cand and str(cand).strip() and str(cand).strip().replace(' ', '').isdigit():
                    cand_clean = str(cand).strip().replace(' ', '')
                    # ВАЛИДАЦИЯ: Проверяем контрольную сумму ИНН
                    if DocumentProcessor.validate_inn(cand_clean):
                        found_inn = cand_clean
                        print(f"[DEBUG_INN] Нашел валидный ИНН внутри группы (кредит от {credit.get('Дата_договора')}): {found_inn}")
                        break
                    else:
                        print(f"[DEBUG_INN] ИНН из документа НЕ прошел валидацию (кредит от {credit.get('Дата_договора')}): {cand_clean} - игнорирую")

            # Адрес: приоритет полному адресу из реестра ЦБ РФ, но С УЧЕТОМ ИНН
            # Теперь передаем найденный ИНН, чтобы точно найти адрес той компании, чей ИНН в документе
            адрес_из_реестра = DocumentProcessor.get_bank_address(кредитор_display, inn=found_inn)
            адрес_из_документа = первый_кредит["Адрес_кредитора"]
            
            # Определяем финальный адрес для использования
            адрес = адрес_из_реестра if адрес_из_реестра else адрес_из_документа

            # 2. Если не нашли в группе (или он некорректный), ищем через RusProfile
            # Даже если нашли, если это банк, лучше проверить через RusProfile (там точнее)
            should_check_rusprofile = getattr(DocumentProcessor, "SHOULD_CHECK_RUSPROFILE", False)
            
            # Если ИНН нет или он подозрительный
            if not found_inn:
                should_check_rusprofile = True
            elif len(found_inn) not in [10, 12]:
                 print(f"[DEBUG_INN] ИНН из документа странной длины ({len(found_inn)}), перепроверяем: {found_inn}")
                 should_check_rusprofile = True
            
            # ФИЛЬТР: Если найденный ИНН совпадает с ИНН должника - это ошибка парсинга парсера
            if found_inn and str(found_inn) == str(debtor_inn):
                 print(f"[DEBUG_INN] Найденный ИНН совпадает с ИНН должника ({found_inn}). Ищем реальный ИНН кредитора...")
                 should_check_rusprofile = True
                 found_inn = None

            if should_check_rusprofile:
                print(f"[DEBUG_INN] ИНН не найден или вызывает сомнения, ищу в реестре/RusProfile для '{кредитор_display}'...")
                rus_inn = DocumentProcessor.get_bank_inn(кредитор_display)
                if rus_inn:
                     found_inn = rus_inn
                     print(f"[DEBUG_INN] Нашел ИНН через RusProfile: {found_inn}")
            
            raw_inn = found_inn
            
            # Очищаем ИНН от мусора
            if raw_inn:
                raw_inn = str(raw_inn).strip()
            
            print(f"[DEBUG_INN] Итоговый ИНН: {raw_inn}")
            
            # ВАЖНО: Если нашли ИНН через RusProfile или в другом кредите, сохраняем во ВСЕ кредиты группы
            if raw_inn:
                for cr in credits_list:
                    cr["ИНН_кредитора"] = raw_inn
            
            # Формируем отображаемую строку
            inn_val_safe = ""
            if raw_inn:
                inn_val_safe = f"ИНН {raw_inn}"
                print(f"[DEBUG_INN] ИНН с префиксом: {inn_val_safe}")

            # Создаем отдельную строку для каждого договора
            for contract_idx, credit in enumerate(credits_list):
                is_first_contract = (contract_idx == 0)
                
                тип_кредита = credit["Тип_кредита"]
                
                # Очищаем тип кредита от суммы в рублях
                import re
                тип_кредита = re.sub(r'\s+в\s+размере[:\s]+[\d\s,.]+\s*руб\.?', '', тип_кредита, flags=re.IGNORECASE)
                тип_кредита = re.sub(r'\s+на\s+сумму[:\s]+[\d\s,.]+\s*руб\.?', '', тип_кредита, flags=re.IGNORECASE)
                тип_кредита = re.sub(r'\s+-\s*[\d\s,.]+\s*руб\.?$', '', тип_кредита)
                тип_кредита = тип_кредита.strip()
                
                дата_текст = f"Договор от {credit['Дата_договора']}" if credit['Дата_договора'] else ""
                сумма_текст = f"{credit['Сумма']:.2f}"
                
                пп = f"1.{counter}" if is_first_contract else ""

                # --- FIX: Убеждаемся что ИНН подставляется во все ячейки ---
                final_inn_value = inn_val_safe if is_first_contract or inn_val_safe else ""
                
                row_data = {
                    "пп_кредит": пп,
                    "Тип_кредита": тип_кредита,
                    "Кредитор": кредитор_display if is_first_contract else "",
                    
                    # ИНН с префиксом "ИНН " (для текстовых полей в заявлениях)
                    "ИНН_кредитора": inn_val_safe,
                    "ИНН_Кредитора": inn_val_safe,  # С заглавной К для шаблона
                    "Инн_кредитора": inn_val_safe,
                    "ИНН": inn_val_safe,
                    "inn_creditor": inn_val_safe,
                    "Inn_creditor": inn_val_safe,
                    
                    # ИНН без префикса - только номер (для таблиц)
                    "ИНН_номер": raw_inn if raw_inn else "",
                    "Инн_номер": raw_inn if raw_inn else "",
                    "Inn_number": raw_inn if raw_inn else "",

                    "Адрес_кредитора": адрес if is_first_contract else "",
                    "Дата_договора": дата_текст,
                    "Полная_сумма_обязательства": сумма_текст,
                    "Задолженность_в_том_числе": сумма_текст,
                    "Штрафы_пени_и_другое": "0.00",
                    # Флаг для объединения ячеек
                    "is_first_contract": is_first_contract,
                    # Данные должника
                    "Фамилия": debtor_fio.split()[0] if debtor_fio and len(debtor_fio.split()) > 0 else "",
                    "Имя": debtor_fio.split()[1] if debtor_fio and len(debtor_fio.split()) > 1 else "",
                    "Отчество": debtor_fio.split()[2] if debtor_fio and len(debtor_fio.split()) > 2 else "",
                    "ИНН_должника": debtor_inn, # Явный ключ для должника
                    # "ИНН": debtor_inn, # ОТКЛЮЧЕНО: Не перезаписываем ИНН кредитора ИНН должника. ИНН в строке кредита = ИНН кредитора
                    "Снилс": debtor_snils,
                    "Адрес_регистрации_должника": debtor_address,
                }
                
                # ЛОГИРУЕМ что реально добавили (ВСЕ КЛЮЧИ)
                if is_first_contract:
                    print(f"[DEBUG_TABLE_ROW] Добавлена строка 1.{counter}:") 
                    print(f"   Кредитор='{row_data['Кредитор']}'")
                    print(f"   ИНН_кредитора (с префиксом)='{row_data['ИНН_кредитора']}'")
                    print(f"   ИНН_номер (чистый)='{row_data['ИНН_номер']}'")
                    print(f"   Адрес_кредитора='{row_data['Адрес_кредитора']}'")
                
                table_rows.append(row_data)
            
            counter += 1

        # Обязательные платежи (налоги, сборы из постановлений и налоговых уведомлений)
        # ВАЖНО: Налоги идут в разделе 2 (2.1, 2.2, 2.3...)
        tax_rows = []
        tax_counter = 1

        # 1. Извлекаем налоги из постановлений пристава
        seen_tax_ip_numbers = set()
        
        for постановление in data_map.get("постановление_пристава", []):
            # ФИЛЬТР: Пропускаем закрытые/прекращенные ИП
            дата_окончания = постановление.get("Дата_окончания")
            if дата_окончания and дата_окончания not in ["", "null", None]:
                continue
            
            # Дедупликация по номеру ИП для налогов
            номер_ип = (постановление.get("Номер_ИП") or "").strip()
            if номер_ип and номер_ип in seen_tax_ip_numbers:
                continue
            if номер_ип:
                seen_tax_ip_numbers.add(номер_ип)
            
            взыскатель = (постановление.get("Взыскатель") or "").strip()
            взыскатель_lower = взыскатель.lower()

            сумма_str = постановление.get("Итого_взыскание") or постановление.get("Остаток_долга") or постановление.get("Сумма_долга") or ""
            сумма = DocumentProcessor.parse_decimal(сумма_str) or Decimal(0)

            if сумма <= 0:
                continue

            # Добавляем только налоги в раздел 2
            if any(kw in взыскатель_lower for kw in ["налог", "ифнс", "фнс", "инспекция федеральной налоговой службы"]):
                tax_rows.append({
                    "пп_обяз_платеж": f"2.{tax_counter}",
                    "Налог_сбор_или_иной_обяз_платеж": взыскатель,
                    "Сумма_обяз_платежа": f"{сумма:.2f}",
                    "Штрафы_пени_и_другое_обяз_платежа": "0.00",
                })
                tax_counter += 1

        # 2. Извлекаем налоги из налоговых уведомлений
        for уведомление in data_map.get("налоговое_уведомление", []):
            налоги = уведомление.get("Налоги", [])
            
            # Если есть детализация по налогам - используем её
            if налоги and len(налоги) > 0:
                for налог in налоги:
                    if not isinstance(налог, dict):
                        continue

                    сумма = DocumentProcessor.parse_decimal(налог.get("Сумма")) or Decimal(0)
                    if сумма <= 0:
                        continue

                    вид = налог.get("Вид", "Налог")
                    период = налог.get("Период", "")

                    # Формируем название
                    if период:
                        название = f"{вид} за {период} г."
                    else:
                        название = вид

                    tax_rows.append({
                        "пп_обяз_платеж": f"2.{tax_counter}",
                        "Налог_сбор_или_иной_обяз_платеж": название,
                        "Сумма_обяз_платежа": f"{сумма:.2f}",
                        "Штрафы_пени_и_другое_обяз_платежа": "0.00",
                    })
                    tax_counter += 1
            else:
                # Если детализации нет (Госуслуги/ЕНС), но есть общая сумма - добавляем одной строкой
                всего_к_уплате = DocumentProcessor.parse_decimal(уведомление.get("Всего_к_уплате"))
                if всего_к_уплате and всего_к_уплате > 0:
                    дата = уведомление.get("Дата", "")
                    название = f"Задолженность по налогам по состоянию на {дата}" if дата else "Задолженность по налогам"
                    
                    tax_rows.append({
                        "пп_обяз_платеж": f"2.{tax_counter}",
                        "Налог_сбор_или_иной_обяз_платеж": название,
                        "Сумма_обяз_платежа": f"{всего_к_уплате:.2f}",
                        "Штрафы_пени_и_другое_обяз_платежа": "0.00",
                    })
                    tax_counter += 1

        # 3. Добавляем другие долги (из постановлений, не налоги) в раздел 1 ПОСЛЕ банков
        # Эти долги - новые кредиторы, которые не были в кредитных отчетах
        for debt in other_debts:
            кредитор_canonical = debt["Кредитор_canonical"]
            
            # Для ИП без взыскателя (кредитор пустой) - не ищем ИНН и адрес
            if кредитор_canonical:
                # Адрес: приоритет адресу из реестра ЦБ РФ
                адрес_из_реестра = DocumentProcessor.get_bank_address(кредитор_canonical)
                адрес_из_документа = debt["Адрес_кредитора"]
                адрес = адрес_из_документа or адрес_из_реестра
                
                # ИНН кредитора: используем уже найденный из постановления, если нет - ищем снова
                raw_inn = debt.get("ИНН_кредитора") or DocumentProcessor.get_bank_inn(кредитор_canonical)
                инн_display = f"ИНН {raw_inn}" if raw_inn else ""
            else:
                # Для ИП без взыскателя - оставляем пустыми
                адрес = ""
                raw_inn = ""
                инн_display = ""
            
            table_rows.append({
                "пп_кредит": f"1.{counter}",
                "Тип_кредита": debt["Тип_кредита"],
                "Кредитор": кредитор_canonical,
                
                # ИНН с префиксом "ИНН " (для текстовых полей)
                "ИНН_кредитора": инн_display,
                "ИНН_Кредитора": инн_display,  # С заглавной К для шаблона
                "ИНН": инн_display,
                
                # ИНН без префикса - только номер (для таблиц)
                "ИНН_номер": raw_inn if raw_inn else "",
                "Инн_номер": raw_inn if raw_inn else "",
                "Inn_number": raw_inn if raw_inn else "",
                
                "Адрес_кредитора": адрес,
                "Дата_договора": debt["Дата_договора"],
                "Полная_сумма_обязательства": f"{debt['Сумма']:.2f}",
                "Задолженность_в_том_числе": f"{debt['Сумма']:.2f}",
                "Штрафы_пени_и_другое": "0.00",
                # Флаг для объединения ячеек (первый договор кредитора)
                "is_first_contract": True,
                # Данные должника
                "Фамилия": debtor_fio.split()[0] if debtor_fio and len(debtor_fio.split()) > 0 else "",
                "Имя": debtor_fio.split()[1] if debtor_fio and len(debtor_fio.split()) > 1 else "",
                "Отчество": debtor_fio.split()[2] if debtor_fio and len(debtor_fio.split()) > 2 else "",
                "ИНН_должника": debtor_inn,
                "Снилс": debtor_snils,
                "Адрес_регистрации_должника": debtor_address,
            })
            counter += 1

        # Формируем результат
        result = {}

        # ДЛЯ ДИНАМИЧЕСКИХ ТАБЛИЦ: возвращаем списки объектов
        result["credits"] = table_rows  # Список всех кредитов
        result["taxes"] = tax_rows      # Список всех налогов

        # СТАРЫЙ ФОРМАТ: для совместимости с простыми шаблонами
        # Добавляем ВСЕ кредиты с номерами
        for i, row in enumerate(table_rows):
            for key, value in row.items():
                # Создаем уникальные ключи для каждой строки
                # Например: пп_кредит_1, пп_кредит_2, ...
                result[f"{key}_{i+1}"] = value

        # Также добавляем первый кредит БЕЗ номера (для совместимости)
        if table_rows:
            for key, value in table_rows[0].items():
                result[key] = value

        # Добавляем общее количество кредитов
        result["количество_кредитов"] = str(len(table_rows))

        # Если есть налоги - добавляем все
        for i, row in enumerate(tax_rows):
            for key, value in row.items():
                result[f"{key}_{i+1}"] = value

        # Первый налог БЕЗ номера
        if tax_rows:
            for key, value in tax_rows[0].items():
                result[key] = value

        result["количество_налогов"] = str(len(tax_rows))

        return result

    @staticmethod
    def format_bank_accounts_table(счета_docs: List[Dict[str, Any]]) -> Dict[str, Any]:
        """Формирует таблицу банковских счетов для описи имущества.

        Args:
            счета_docs: список документов типа "счета"

        Returns:
            Словарь с ключом "счета" (список для Jinja2)
        """
        счета_rows = []
        counter = 1

        print(f"\n[DEBUG] Обработка банковских счетов ({len(счета_docs)} док.)")

        for doc in счета_docs:
            счета_list = doc.get("Счета", [])
            print(f"  Документ содержит {len(счета_list)} счетов")

            for i, счет in enumerate(счета_list):
                if not isinstance(счет, dict):
                    print(f"    [SKIP] Счет #{i+1}: не словарь ({type(счет)})")
                    continue

                банк = счет.get("Банк") or ""
                if not банк:
                    print(f"    [SKIP] Счет #{i+1}: нет названия банка")
                    continue

                # Получаем адрес из документа (GPT должен извлечь отдельно)
                адрес_из_документа = (счет.get("Адрес_банка") or "").strip()

                # Нормализуем название банка (с правильными кавычками)
                _, банк_canonical = DocumentProcessor.normalize_bank_name(банк)
                
                # Получаем адрес из реестра ЦБ РФ
                адрес_из_реестра = DocumentProcessor.get_bank_address(банк_canonical)
                
                # Приоритет: адрес из документа (если GPT извлёк), иначе из реестра
                if адрес_из_документа:
                    адрес_банка = f"{банк_canonical}, {адрес_из_документа}"
                elif адрес_из_реестра:
                    адрес_банка = f"{банк_canonical}, {адрес_из_реестра}"
                else:
                    адрес_банка = банк_canonical

                тип_счета = счет.get("Тип") or счет.get("Статус") or "Текущий счет"
                номер = счет.get("Номер") or ""
                дата_открытия = счет.get("Дата_открытия") or ""
                остаток = счет.get("Остаток") or ""

                # Если остаток "0.00" или "0" - оставляем пустым
                if остаток in ("0.00", "0", "None"):
                    остаток = ""

                счета_rows.append({
                    "счета_пп": f"3.{counter}",
                    "Наименование_и_адрес_банка_или_иной_кредитной_организации": адрес_банка,
                    "Вид_счета": тип_счета,
                    "Дата_открытия_счета": дата_открытия,
                    "Остаток_на_счете": остаток,
                })
                counter += 1

        return {"счета": счета_rows}

    @staticmethod
    def format_realty_table(егрн_docs: List[Dict[str, Any]], owner_fio: str = "", inventory_list: Optional[List[Dict[str, Any]]] = None) -> Dict[str, Any]:
        """Формирует таблицу недвижимости для описи имущества.

        Args:
            егрн_docs: список документов типа "егрн_выписка"
            owner_fio: ФИО владельца (из паспорта) для фильтрации
            inventory_list: данные из "Описи имущества" (для fallback если нет выписки ЕГРН)

        Returns:
            Словарь с ключами для таблиц недвижимости
        """
        земельные = []
        жилые_дома = []
        квартиры = []
        гаражи = []
        иное_недвижимое = []
        
        # Список для дедупликации (кадастровые номера и адреса)
        processed_cadastral = set()
        processed_addresses = set()
        
        # --- ФУНКЦИЯ ДОБАВЛЕНИЯ В СПИСКИ ---
        def add_to_category(obj_data, category_counters):
            тип_lower = (obj_data.get("Тип") or "").lower()
            назначение_lower = (obj_data.get("Назначение") or "").lower()
            адрес_lower = (obj_data.get("Адрес") or "").lower()
            тип_объекта = obj_data.get("Тип") or obj_data.get("Назначение") or "Недвижимость"

            base_obj = {
                "Вид_собственности": obj_data.get("Вид_права", "собственность"),
                "Местонахождение_адрес": obj_data.get("Адрес", ""),
                "Площадь": obj_data.get("Площадь", ""),
                "Основание_приобретения_и_стоимость": obj_data.get("Основание", ""),
                "Сведения_о_залоге_и_залогодержателе": obj_data.get("Залог", ""),
            }

            if "земельн" in тип_lower or "участок" in тип_lower:
                земельные.append({
                    **base_obj,
                    "Кадастровый_номер": obj_data.get("Кадастровый_номер", ""),
                    "Номер_в_списке": category_counters['земля'],
                })
                category_counters['земля'] += 1

            elif (
                ("жил" in тип_lower and "дом" in тип_lower) or 
                "дача" in тип_lower or 
                "садовый дом" in тип_lower or
                ("жил" in назначение_lower and "дом" in назначение_lower) or
                ("здание" in тип_lower and ("жил" in назначение_lower or "жил" in адрес_lower))
            ):
                название = f"{category_counters['дом']}) {тип_объекта}" if category_counters['дом'] > 1 else тип_объекта
                жилые_дома.append({
                    **base_obj,
                    "Вид_и_наименование": название,
                })
                category_counters['дом'] += 1

            elif (
                "квартир" in тип_lower or 
                "комнат" in тип_lower or 
                "кв." in тип_lower or
                "квартира" in назначение_lower or
                ("помещение" in тип_lower and "жил" in назначение_lower)
            ):
                название = f"{category_counters['квартира']}) {тип_объекта}" if category_counters['квартира'] > 1 else тип_объекта
                квартиры.append({
                    **base_obj,
                    "Вид_и_наименование": название,
                })
                category_counters['квартира'] += 1

            elif "гараж" in тип_lower or "бокс" in тип_lower or "машино-место" in тип_lower:
                название = f"{category_counters['гараж']}) {тип_объекта}" if category_counters['гараж'] > 1 else тип_объекта
                гаражи.append({
                    **base_obj,
                    "Вид_и_наименование": название,
                })
                category_counters['гараж'] += 1

            else:
                название = f"{category_counters['иное']}) {тип_объекта}" if category_counters['иное'] > 1 else тип_объекта
                иное_недвижимое.append({
                    **base_obj,
                    "Вид_и_наименование_имущества": название,
                })
                category_counters['иное'] += 1
        
        category_counters = {
            'земля': 1, 'дом': 1, 'квартира': 1, 'гараж': 1, 'иное': 1
        }

        print(f"\n[DEBUG] Обработка недвижимости ({len(егрн_docs)} док.)")
        
        for doc in егрн_docs:
            print(f"  [EGRN] Документ: {doc.get('Кадастровый_номер', 'без номера')}, Адрес: {doc.get('Адрес', 'без адреса')}")
            
            if not isinstance(doc, dict):
                print(f"    [SKIP] Не словарь ({type(doc)})")
                continue

            # Проверяем если это уведомление об отсутствии - пропускаем
            if not doc.get("Есть_недвижимость", True):
                print(f"    [SKIP] Есть_недвижимость=False")
                continue
            if doc.get("Результат") == "сведения отсутствуют":
                continue

            # НОВАЯ СТРУКТУРА: массив "Объекты"
            объекты_список = doc.get("Объекты", [])
            if not объекты_список:
                объекты_список = [doc]
            
            for obj in объекты_список:
                # Получаем правообладателей
                правообладатели = obj.get("Правообладатели", [])

                # ФИЛЬТР: Пропускаем проданные объекты
                has_active_ownership = False
                for право in правообладатели:
                    дата_прекращения = право.get("Дата_прекращения_права") or право.get("дата_прекращения_права") or право.get("дата_государственной_регистрации_прекращения_права") or право.get("Дата_государственной_регистрации_прекращения_права") or ""
                    
                    term_val = str(дата_прекращения).strip().lower()
                    is_terminated = False
                    
                    if term_val:
                        # 1. Если явно написано "отсутствуют" или "нет данных" - право АКТИВНО
                        if (
                            "отсутствуют" in term_val or 
                            "не зарегистрировано" in term_val or 
                            "нет" in term_val or
                            "действующее" in term_val or
                            "активно" in term_val or
                            term_val in ["null", "none", "-"]
                        ):
                             is_terminated = False
                        # 2. Если есть цифры (похоже на дату) - право ПРЕКРАЩЕНО
                        elif any(char.isdigit() for char in term_val):
                             is_terminated = True
                        # 3. Иначе (текст без цифр) - считаем активным
                        else:
                             is_terminated = False
                    else:
                        # Пустая строка - активно
                        is_terminated = False
                    
                    if not is_terminated:
                        has_active_ownership = True
                        break
                
                if not has_active_ownership:
                    print(f"      [SKIP] Объект {obj.get('Кадастровый_номер')} пропущен (есть дата прекращения права: {дата_прекращения})")
                    continue

                # Проверяем есть ли наш человек среди правообладателей
                owner_found = False
                owner_data = None

                if owner_fio:
                    for право in правообладатели:
                        фио = право.get("ФИО", "")
                        if owner_fio and фио:
                            # Разбиваем ФИО на части для сравнения
                            owner_fio_clean = owner_fio.strip().lower()
                            фио_clean = фио.strip().lower()
                            
                            owner_parts = owner_fio_clean.split()
                            право_parts = фио_clean.split()

                            is_match = False
                            
                            if owner_parts and право_parts:
                                fam1 = owner_parts[0]
                                fam2 = право_parts[0]
                                
                                # 1. Точное совпадение
                                is_match = (fam1 == fam2)
                                
                                # 2. Совпадение мужской/женский род (Иванов == Иванова)
                                if not is_match:
                                    if len(fam1) > 2 and len(fam2) > 2:
                                        # Батищева -> Батищев
                                        if fam1.endswith('а') and fam1[:-1] == fam2:
                                            is_match = True
                                        # Батищев -> Батищева
                                        elif fam2.endswith('а') and fam2[:-1] == fam1:
                                            is_match = True
                                
                                # 3. Совпадение по значимой части (80%)
                                if not is_match and len(fam1) > 4 and len(fam2) > 4:
                                    min_len = min(len(fam1), len(fam2))
                                    check_len = int(min_len * 0.8) # 80% совпадения начала
                                    if fam1[:check_len] == fam2[:check_len]:
                                        is_match = True
                            
                            # 4. Полное вхождение
                            if not is_match:
                                if owner_fio_clean in фио_clean or фио_clean in owner_fio_clean:
                                    is_match = True

                            if is_match:
                                owner_found = True
                                owner_data = право
                                break
                    
                    if not owner_found:
                        print(f"      [SKIP] Владелец {owner_fio} не найден среди: {[p.get('ФИО') for p in правообладатели]}")
                        continue
                else:
                    if правообладатели:
                        owner_data = правообладатели[0]
                        owner_found = True

                # Получаем данные объекта
                объект = obj.get("Объект", {})

                кадастр = obj.get("Кадастровый_номер", "")
                адрес = объект.get("Адрес") or obj.get("Адрес", "")
                площадь = объект.get("Площадь") or obj.get("Площадь", "")
                тип_объекта = объект.get("Вид") or obj.get("Тип_объекта", "")
                назначение = объект.get("Назначение", "")

                # Дедупликация по кадастровому
                if кадастр and кадастр in processed_cadastral:
                    continue
                if кадастр:
                    processed_cadastral.add(кадастр)
                
                # Дедупликация по адресу 
                if адрес:
                    # Нормализация для сравнения: нижний регистр + удаление пробелов
                    addr_clean = " ".join(адрес.lower().split())
                    if addr_clean in processed_addresses:
                        continue
                    processed_addresses.add(addr_clean)

                # Определяем вид собственности и долю
                вид_права = ""
                доля = ""
                документ_основание = ""
                тип_собственности = ""
                
                if owner_data:
                    вид_права = owner_data.get("Вид_права", "")
                    доля = owner_data.get("Доля", "")
                    документ_основание = owner_data.get("Документ_основание", "") or owner_data.get("Документ", "")
                    тип_собственности = owner_data.get("Тип_собственности", "")

                # Формируем полный вид собственности
                части_собственности = []
                
                # 1. Тип собственности
                if тип_собственности:
                    normalized_type = " ".join(тип_собственности.split()).lower()
                    clean_parts = []
                    seen_parts = set()
                    for part in тип_собственности.split(","):
                        part_clean = part.strip()
                        part_lower = part_clean.lower()
                        is_common_share = "общедолевая" in part_lower or "общая долевая" in part_lower
                        if is_common_share:
                             if "common_share" in seen_parts: continue
                             seen_parts.add("common_share")
                        clean_parts.append(part_clean)
                    тип_собственности_fixed = ", ".join(clean_parts)
                    if тип_собственности_fixed: части_собственности.append(тип_собственности_fixed)

                # 2. Вид права
                if вид_права:
                    вид_права_lower = вид_права.lower()
                    тип_собственности_lower = тип_собственности.lower() if тип_собственности else ""
                    if not (вид_права_lower in тип_собственности_lower or тип_собственности_lower in вид_права_lower):
                        части_собственности.append(вид_права)
                
                # 3. Доля
                if доля and доля != "1/1":
                    части_собственности.append(f"доля в праве: {доля}")
                
                # 4. Другие собственники
                if тип_собственности and "совместн" in тип_собственности.lower() and len(правообладатели) > 1:
                    другие = [п.get("ФИО", "") for п in правообладатели if п != owner_data]
                    if другие:
                        части_собственности.append(f"совместно с: {', '.join(другие)}")
                
                итог_вид_права = ", ".join(части_собственности) if части_собственности else ""

                # Основание приобретения и стоимость
                части_основания = []
                if документ_основание:
                    части_основания.append(документ_основание)
                
                кадастровая_стоимость = obj.get("Кадастровая_стоимость", "")
                if кадастровая_стоимость:
                    части_основания.append(f"Кадастровая стоимость: {кадастровая_стоимость}")
                
                итог_основание = "; ".join(части_основания) if части_основания else ""

                # Обременения
                обременения = obj.get("Обременения", [])
                залоги = []
                if isinstance(обременения, list):
                    for обр in обременения:
                        if not isinstance(обр, dict): continue
                        if обр.get("Погашено"): continue
                        вид = обр.get("Вид", "")
                        залогодержатель_имя = обр.get("Залогодержатель", "")
                        if ("ипотек" in вид.lower() or "залог" in вид.lower()) and залогодержатель_имя:
                            залоги.append(f"{вид} ({залогодержатель_имя})")
                
                итог_залог = "; ".join(залоги) if залоги else ""

                # ПОДГОТОВКА ДЛЯ ДОБАВЛЕНИЯ
                prepared_data = {
                    "Тип": тип_объекта,
                    "Назначение": назначение,
                    "Адрес": адрес,
                    "Вид_права": итог_вид_права,
                    "Площадь": площадь,
                    "Основание": итог_основание,
                    "Залог": итог_залог,
                    "Кадастровый_номер": кадастр
                }
                
                add_to_category(prepared_data, category_counters)

        # ДОБАВЛЕНИЕ ИЗ ИНВЕНТАРЯ (ЕСЛИ ЕСТЬ)
        if inventory_list:
            flat_items = []
            for item in inventory_list:
                # Если структура: [{"Недвижимость": [...]}, ...]
                if item.get("Недвижимость") and isinstance(item.get("Недвижимость"), list):
                    flat_items.extend(item["Недвижимость"])
                # Если структура плоская или другой формат
                elif item.get("Вид") or item.get("Тип") or item.get("Наименование"):
                     flat_items.append(item)

            for item in flat_items:
                raw_type = item.get("Вид") or item.get("Тип") or item.get("Наименование") or "Имущество"
                # Фильтр: игнорируем транспорт если он попал в список
                if "авто" in raw_type.lower() or "транспорт" in raw_type.lower():
                    continue

                raw_addr = item.get("Адрес") or item.get("Местоположение") or item.get("Местонахождение") or ""
                raw_cadastral = item.get("Кадастровый_номер") or item.get("Кадастр") or ""
                
                # Дедупликация
                if raw_cadastral and raw_cadastral in processed_cadastral:
                    continue
                if raw_addr:
                    addr_clean = " ".join(raw_addr.lower().split())
                    if addr_clean in processed_addresses:
                        continue
                    processed_addresses.add(addr_clean)
                
                # Подготовка данных
                prepared_inv = {
                    "Тип": raw_type,
                    "Назначение": raw_type, # Используем тип как назначение если нет другого
                    "Адрес": raw_addr,
                    "Вид_права": item.get("Вид_собственности") or item.get("Право") or "Собственность",
                    "Площадь": item.get("Площадь", ""),
                    "Основание": item.get("Основание_приобретения") or item.get("Документы_основания") or item.get("Основание", ""),
                    "Залог": item.get("Сведения_о_залоге") or item.get("Залог", ""),
                    "Кадастровый_номер": raw_cadastral
                }
                
                add_to_category(prepared_inv, category_counters)

        # Если списки пустые, добавляем по одному заполнителю с пустыми полями.
        # Это предотвращает удаление строк в шаблоне при рендеринге docxtpl
        # — вместо удаления строки будет отрендерена пустая строка, сохраняя макет.
        if not земельные:
            земельные.append({
                "Кадастровый_номер": "",
                "Вид_собственности": "",
                "Местонахождение_адрес": "",
                "Площадь": "",
                "Основание_приобретения_и_стоимость": "",
                "Сведения_о_залоге_и_залогодержателе": "",
                "Номер_в_списке": "",
            })

        if not жилые_дома:
            жилые_дома.append({
                "Вид_и_наименование": "",
                "Вид_собственности": "",
                "Местонахождение_адрес": "",
                "Площадь": "",
                "Основание_приобретения_и_стоимость": "",
                "Сведения_о_залоге_и_залогодержателе": "",
            })

        if not квартиры:
            квартиры.append({
                "Вид_и_наименование": "",
                "Вид_собственности": "",
                "Местонахождение_адрес": "",
                "Площадь": "",
                "Основание_приобретения_и_стоимость": "",
                "Сведения_о_залоге_и_залогодержателе": "",
            })

        if not гаражи:
            гаражи.append({
                "Вид_и_наименование": "",
                "Вид_собственности": "",
                "Местонахождение_адрес": "",
                "Площадь": "",
                "Основание_приобретения_и_стоимость": "",
                "Сведения_о_залоге_и_залогодержателе": "",
            })

        if not иное_недвижимое:
            иное_недвижимое.append({
                "Вид_и_наименование_имущества": "",
                "Вид_собственности": "",
                "Местонахождение_адрес": "",
                "Площадь": "",
                "Основание_приобретения_и_стоимость": "",
                "Сведения_о_залоге_и_залогодержателе": "",
            })

        return {
            "земельные_участки": земельные,
            "жилые_дома": жилые_дома,
            "квартиры": квартиры,
            "гаражи": гаражи,
            "иное_недвижимое": иное_недвижимое,
        }

    @staticmethod
    def format_vehicles_table(гибдд_docs: List[Dict[str, Any]]) -> Dict[str, Any]:
        """Формирует таблицу транспортных средств для описи имущества.

        Args:
            гибдд_docs: список документов типа "гибдд"

        Returns:
            Словарь с ключами для разных типов транспорта и "Нету_гибдд" (текст если справки нет)
        """
        print(f"[VEHICLES_TABLE] Вызов функции, количество документов: {len(гибдд_docs)}")
        
        # Преобразуем новый формат с массивом в плоский список для обработки
        all_vehicles = []
        for data in гибдд_docs:
            # Проверяем новый формат: {"Есть_транспорт": true, "Транспортные_средства": [...]}
            if "Есть_транспорт" in data:
                if data.get("Есть_транспорт") is True and "Транспортные_средства" in data:
                    # Добавляем все ТС из массива
                    vehicles_array = data.get("Транспортные_средства", [])
                    if isinstance(vehicles_array, list):
                        all_vehicles.extend(vehicles_array)
                # Если Есть_транспорт = false, пропускаем (справка об отсутствии)
            else:
                # Старый формат: один объект = одно ТС
                all_vehicles.append(data)
        
        print(f"[VEHICLES_TABLE] После извлечения из массивов: {len(all_vehicles)} ТС")
        
        # Дедупликация по VIN (включая нечеткий поиск)
        seen_vins = {} # Map cleaned_vin -> data
        deduplicated = []
        
        for data in all_vehicles:
            raw_vin = data.get("VIN", "")
            
            # Если VIN нет, добавляем как есть
            if not raw_vin:
                deduplicated.append(data)
                continue
                
            # Нормализация VIN (убираем пробелы, тире и приводим к верхнему регистру)
            current_vin = re.sub(r'[^A-Z0-9]', '', raw_vin.upper())
            if not current_vin:
                deduplicated.append(data)
                continue
            
            is_duplicate = False
            best_match_vin = None
            
            # 1. Точное совпадение
            if current_vin in seen_vins:
                is_duplicate = True
                best_match_vin = current_vin
            else:
                # 2. Нечеткий поиск (для борьбы с ошибками OCR: 8 vs 3, D vs 0, 8 vs 6)
                # Порог 0.8 позволяет найти совпадение при ~3 различиях в 17 символах
                best_ratio = 0.0
                for seen_vin in seen_vins:
                    ratio = difflib.SequenceMatcher(None, current_vin, seen_vin).ratio()
                    if ratio > best_ratio:
                        best_ratio = ratio
                        best_match_vin = seen_vin
                
                if best_ratio > 0.8:
                    is_duplicate = True
                    print(f"      [DEDUP] Найден нечеткий дубликат VIN: {current_vin} ~ {best_match_vin} (ratio: {best_ratio:.2f})")
            
            if is_duplicate and best_match_vin:
                # Логика выбора лучшей версии
                existing_data = seen_vins[best_match_vin]
                
                existing_doc_type = str(existing_data.get("Документ", {}).get("Тип", "")).upper()
                current_doc_type = str(data.get("Документ", {}).get("Тип", "")).upper()
                
                # Приоритет: СТС > ПТС > Договор
                def get_priority(doc_type):
                    if "СТС" in doc_type or "СВИДЕТЕЛЬСТВО" in doc_type: return 3
                    if "ПТС" in doc_type or "ПАСПОРТ" in doc_type: return 2
                    return 1
                
                current_prio = get_priority(current_doc_type)
                existing_prio = get_priority(existing_doc_type)
                
                if current_prio > existing_prio:
                    # Заменяем старую запись на новую, более приоритетную
                    deduplicated.remove(existing_data)
                    deduplicated.append(data)
                    # Обновляем маппинг. Если VIN был разный (fuzzy), удаляем старый ключ, добавляем новый
                    if best_match_vin != current_vin:
                        del seen_vins[best_match_vin]
                    seen_vins[current_vin] = data
                    print(f"      [DEDUP] Обновлено: {existing_doc_type} -> {current_doc_type}")
                else:
                    print(f"      [DEDUP] Оставлено существующее: {existing_doc_type} (vs {current_doc_type})")
            else:
                # Новенький
                seen_vins[current_vin] = data
                deduplicated.append(data)
        
        print(f"[VEHICLES_TABLE] После дедупликации: {len(deduplicated)} документов")
        for i, doc in enumerate(deduplicated):
            print(f"[VEHICLES_TABLE] Документ {i}: {doc}")
        
        автомобили = []  # легковые
        грузовики = []
        мото = []
        сельхоз = []
        водный = []
        воздушный = []
        
        counter_auto = 1
        counter_truck = 1
        counter_moto = 1
        counter_agro = 1
        counter_water = 1
        counter_air = 1
        
        # Проверяем, есть ли справка ГИБДД
        has_gibdd = len(гибдд_docs) > 0

        for doc in deduplicated:
            if not isinstance(doc, dict):
                continue

            # Проверяем особые отметки
            special_notes = doc.get("Особые_отметки", "")
            if special_notes:
                special_lower = special_notes.lower()
                # УДАЛЕНО: Проверка на "отсутствуют" давала ложные срабатывания (например "Аресты отсутствуют")
                
                # Пропускаем проданные/снятые с учета
                if any(keyword in special_lower for keyword in 
                       ["снято с учета", "продаж", "продан", "передач", "утилизац"]):
                    continue

            марка = doc.get("Марка_модель") or ""
            вин = doc.get("VIN") or ""
            год = doc.get("Год_выпуска") or ""
            номер = doc.get("Гос_номер") or doc.get("Регистрационный_знак") or doc.get("Номер") or ""
            тип_тс = (doc.get("Тип_ТС") or "").lower()

            # Отладка: выводим что извлечено
            print(f"[VEHICLES_TABLE] Обработка ТС:")
            print(f"  Тип_ТС: '{тип_тс}'")
            print(f"  Марка_модель: '{марка}'")
            print(f"  VIN: '{вин}'")
            print(f"  Год_выпуска: '{год}'")
            print(f"  Гос_номер: '{номер}'")

            # Пропускаем если нет марки, VIN и номера (пустой документ)
            if not марка and not вин and not номер:
                print(f"  [SKIP] Пропущено: нет марки, VIN и гос. номера")
                continue

            # Единое описание для вида и марки
            вид_марка = марка
            if not вид_марка and тип_тс:
                вид_марка = тип_тс.capitalize()
            if not вид_марка and номер:
                вид_марка = f"ТС гос. номер {номер}"
            
            # Определяем тип транспорта

            # Идентификационный номер - это VIN
            id_номер = вин

            # Извлекаем данные из документа
            вид_собственности = doc.get("Вид_собственности") or ""
            стоимость = doc.get("Стоимость") or ""
            залог = doc.get("Сведения_о_залоге") or ""

            vehicle_entry = {
                "Вид_марка_модель": вид_марка,
                "Идентификационный_номер": id_номер,
                "Вид_собственности": вид_собственности,
                "Место_нахождения": "",
                "Стоимость": стоимость,
                "Сведения_о_залоге": залог,
            }

            # Распределяем по категориям на основе типа ТС
            if "грузов" in тип_тс or "truck" in тип_тс:
                vehicle_entry["авто_пп"] = str(counter_truck)
                грузовики.append(vehicle_entry)
                counter_truck += 1
            elif "мото" in тип_тс or "motorcycle" in тип_тс or "мотоцикл" in тип_тс:
                vehicle_entry["авто_пп"] = str(counter_moto)
                мото.append(vehicle_entry)
                counter_moto += 1
            elif "сельск" in тип_тс or "трактор" in тип_тс or "комбайн" in тип_тс:
                vehicle_entry["авто_пп"] = str(counter_agro)
                сельхоз.append(vehicle_entry)
                counter_agro += 1
            elif "водн" in тип_тс or "лодк" in тип_тс or "катер" in тип_тс or "яхт" in тип_тс:
                vehicle_entry["авто_пп"] = str(counter_water)
                водный.append(vehicle_entry)
                counter_water += 1
            elif "воздуш" in тип_тс or "самолет" in тип_тс or "вертолет" in тип_тс:
                vehicle_entry["авто_пп"] = str(counter_air)
                воздушный.append(vehicle_entry)
                counter_air += 1
            else:
                # По умолчанию - легковой автомобиль
                vehicle_entry["авто_пп"] = str(counter_auto)
                автомобили.append(vehicle_entry)
                counter_auto += 1
        
        # Формируем текст для случая отсутствия справки ГИБДД
        # Новая логика:
        # - Если есть хотя бы один документ с "Является_справкой_ГИБДД": true → текст НЕ выводится
        # - Если есть только ПТС/СТС (без справки) → текст выводится
        # - Если документов вообще нет → текст выводится
        gibdd_notice = ""
        has_gibdd_spravka = any(doc.get("Является_справкой_ГИБДД") == True for doc in deduplicated)
        
        if not has_gibdd_spravka:
            gibdd_notice = "Должник направил заявление в ГИБДД на получение справки о зарегистрированных транспортных средствах, на дату подачи заявления справка не получена, обязуется предоставить до даты первого судебного заседания."

        # Если списки пустые, добавляем один "пустой" элемент, чтобы таблица в шаблоне не схлопнулась
        # (в шаблоне {%tr for ...%} удаляет строку, если список пуст).
        # Нам нужно, чтобы строка осталась, но с пустыми значениями.
        empty_vehicle = {
            "Вид_марка_модель": "",
            "Идентификационный_номер": "",
            "Вид_собственности": "",
            "Место_нахождения": "",
            "Стоимость": "",
            "Сведения_о_залоге": "",
            "авто_пп": "", # Пустой номер
            # ...другие поля, которые могут использоваться в шаблоне...
        }

        # Для каждого списка проверяем: если пуст - ставим заглушку
        # Используем копию empty_vehicle для каждого, чтобы не было ссылок на один объект
        if not автомобили: 
             автомобили = [empty_vehicle.copy()]
        if not грузовики: 
             грузовики = [empty_vehicle.copy()]
        if not мото: 
             мото = [empty_vehicle.copy()]
        if not сельхоз: 
             сельхоз = [empty_vehicle.copy()]
        if not водный: 
             водный = [empty_vehicle.copy()]
        if not воздушный: 
             воздушный = [empty_vehicle.copy()]

        # "Иное" в коде выше не собиралось отдельно, но в запросе пользователя есть цикл {% for другое in иное %}
        # Значит, нужно добавить и этот ключ, если он ожидается.
        # В текущем коде сборщика "иного" нет (все падает в автомобили или другие категории).
        # Добавим пустой список "иное" с заглушкой.
        иное = [empty_vehicle.copy()]
        
        # Исправляем возвращаемый словарь: добавляем ключ "иное"


        return {
            "автомобили": автомобили,
            "грузовики": грузовики,
            "мото": мото,
            "сельхоз": сельхоз,
            "водный": водный,
            "воздушный": воздушный,
            "иное": иное,
            "Нету_гибдд": gibdd_notice
        }

    @staticmethod
    def normalize_date(date_str: str) -> str:
        """Нормализует дату к формату ДД.ММ.ГГГГ.
        
        Примеры:
        - "09-09-2009" → "09.09.2009"
        - "09/09/2009" → "09.09.2009"
        - "09.09.2009" → "09.09.2009"
        """
        if not date_str:
            return ""
        
        # Заменяем дефисы и слэши на точки
        normalized = date_str.replace("-", ".").replace("/", ".")
        
        return normalized.strip()
    
    @staticmethod
    def merge_credit_reports(data_map: Dict[str, List[Dict[str, Any]]]) -> List[Dict[str, Any]]:
        """Объединяет кредиты из ОКБ, БКИ, НБКИ отчетов, убирает дубликаты.

        Дедупликация по ИНН/ОГРН + дата договора (не только по названию кредитора).
        Возвращает список уникальных кредитов для таблицы "Список кредиторов".
        """
        all_credits = {}  # {ключ_дедупликации: данные_кредита}
        seen_keys = set()  # для отладки

        print("\n" + "="*80)
        print("ДЕДУПЛИКАЦИЯ КРЕДИТОВ")
        print("="*80)

        for key in ("отчет_окб", "отчет_бки", "отчет_нбки"):
            reports_count = len(data_map.get(key, []))
            if reports_count > 0:
                print(f"\n📄 Обработка отчета: {key.upper()} ({reports_count} шт.)")
            
            for report in data_map.get(key, []):
                # DEBUG: Покажем все ключи в отчете
                print(f"   [DEBUG] Ключи в отчете {key}: {list(report.keys())}")
                
                collection = (
                    report.get("Кредиты")
                    or report.get("Договоры")
                    or []
                )
                
                if not collection:
                    print(f"   [WARNING] Кредиты/Договоры не найдены в отчете {key}")
                else:
                    print(f"   [DEBUG] Найдено записей в коллекции: {len(collection)}")

                for entry in collection:
                    if not isinstance(entry, dict):
                        continue

                    кредитор = (entry.get("Кредитор") or "").strip()
                    if not кредитор:
                        continue
                    
                    print(f"\n   → Извлечен: {кредитор}")

                    # УЛУЧШЕННАЯ НОРМАЛИЗАЦИЯ: используем новую функцию
                    кредитор_normalized, кредитор_canonical = DocumentProcessor.normalize_bank_name(кредитор)

                    # Определяем тип кредитора
                    кредитор_lower = кредитор.lower()
                    if any(word in кредитор_lower for word in ["мфо", "мкк", "мфк", "пко", "коллектор", "микрофинанс", "микрокредит"]):
                        тип_кредитора = "МФО"
                    else:
                        тип_кредитора = "БАНК"

                    # Вычисляем сумму долга
                    основной = DocumentProcessor.parse_decimal(entry.get("Основной_долг"))
                    проценты = DocumentProcessor.parse_decimal(entry.get("Проценты"))
                    штрафы = DocumentProcessor.parse_decimal(entry.get("Штрафы"))
                    
                    # Инициализируем переменные для текущей задолженности и просрочки
                    текущая = DocumentProcessor.parse_decimal(entry.get("Текущая_задолженность"))
                    просрочка = DocumentProcessor.parse_decimal(entry.get("Просрочка") or entry.get("Текущая_просрочка"))

                    if основной or проценты or штрафы:
                        сумма = (основной or Decimal(0)) + (проценты or Decimal(0)) + (штрафы or Decimal(0))
                    else:
                        # Для кредитных отчетов: берем поле "Сумма" (уже сложенную нейронкой)
                        # Также проверяем другие возможные ключи
                        сумма = (
                            DocumentProcessor.parse_decimal(entry.get("Сумма")) or 
                            DocumentProcessor.parse_decimal(entry.get("Сумма_задолженности")) or
                            DocumentProcessor.parse_decimal(entry.get("Общая_сумма_задолженности")) or
                            DocumentProcessor.parse_decimal(entry.get("Текущий_долг")) or
                            Decimal(0)
                        )
                        
                        # Если поля "Сумма" нет, складываем текущую и просрочку
                        if сумма <= 0:
                            сумма = (текущая or Decimal(0)) + (просрочка or Decimal(0))

                    # Пропускаем кредиты с нулевой задолженностью
                    if сумма <= 0:
                        print(f"      [SKIP] Кредит {кредитор} пропущен: Сумма={сумма} (Осн={основной}, Проц={проценты}, Штр={штрафы}, Др.ключи={entry.get('Общая_сумма_задолженности')}/{entry.get('Сумма_задолженности')})")
                        continue

                    # === ЛОГИКА ДЕДУПЛИКАЦИИ: НАЗВАНИЕ + ДАТА ДОГОВОРА + ПЕРВОНАЧАЛЬНАЯ СУММА ===
                    # Извлекаем дату договора из разных полей
                    дата_договора_raw = (entry.get("Дата_сделки") or entry.get("Дата_начала_договора") or entry.get("Дата_договора") or "").strip()
                    
                    # НОРМАЛИЗУЕМ ДАТУ: 09-09-2009 → 09.09.2009
                    дата_договора = DocumentProcessor.normalize_date(дата_договора_raw)
                    
                    # Извлекаем ИНН/ОГРН для сохранения (но не для дедупликации)
                    инн = (entry.get("ИНН_кредитора") or entry.get("ИНН") or "").strip()
                    огрн = (entry.get("ОГРН_кредитора") or entry.get("ОГРН") or "").strip()
                    
                    # Первоначальная сумма кредита (сколько взяли изначально)
                    сумма_обязательства = DocumentProcessor.parse_decimal(entry.get("Сумма_обязательства")) or Decimal(0)
                    
                    print(f"      • Дата договора: {дата_договора or 'НЕТ'} (исходная: {дата_договора_raw})")
                    if инн:
                        print(f"      • ИНН: {инн}")
                    print(f"      • Сумма обязательства: {сумма_обязательства if сумма_обязательства > 0 else 'НЕТ'}")
                    print(f"      • Текущий долг: {сумма}")

                    # Формируем ключ дедупликации:
                    # КЛЮЧ: кредитор + дата + первоначальная сумма (БЕЗ текущей суммы долга!)
                    # Текущая сумма долга может отличаться в ОКБ и БКИ, поэтому не используем её в ключе
                    if сумма_обязательства > 100:
                        сумма_для_ключа = int(сумма_обязательства)
                        dedup_key = f"{кредитор_normalized}|{дата_договора}|{сумма_для_ключа}"
                    elif дата_договора:
                        # Если есть дата договора - доверяем ей и не используем текущую сумму (она может "плавать")
                        dedup_key = f"{кредитор_normalized}|{дата_договора}|NO_INIT"
                    else:
                        # Нет ни даты, ни первоначальной суммы - используем текущий долг для различения
                        текущая_сумма_для_ключа = f"{сумма:.2f}"
                        dedup_key = f"{кредитор_normalized}|NO_DATE|NO_INIT|{текущая_сумма_для_ключа}"
                    
                    # Проверяем дубликаты с учётом погрешности дат (±1 день)
                    # Это нужно для случаев когда ОКБ и БКИ указывают разные даты для одного договора
                    found_similar = False
                    similar_key = None
                    
                    if дата_договора and сумма_обязательства > 100:
                        from datetime import datetime, timedelta
                        try:
                            base_date = datetime.strptime(дата_договора, "%d.%m.%Y")
                            # Проверяем ±1 день по кредитору + дата + первоначальная сумма
                            for delta in [-1, 0, 1]:
                                check_date = (base_date + timedelta(days=delta)).strftime("%d.%m.%Y")
                                check_key = f"{кредитор_normalized}|{check_date}|{сумма_для_ключа}"
                                if check_key in all_credits and check_key != dedup_key:
                                    found_similar = True
                                    similar_key = check_key
                                    break
                        except:
                            pass

                    # 3. Если с ±1 день не нашли, ищем по fuzzy-совпадению кредитора
                    # ВАЖНО: Строгая проверка дат и сумм, иначе разные кредиты одного банка сольются!
                    if not found_similar:
                        for ex_key in list(all_credits.keys()):
                                try:
                                    ex_parts = ex_key.split('|')
                                    cur_parts = dedup_key.split('|')
                                    
                                    # Ключ: Имя|Дата|Сумма
                                    if len(ex_parts) >= 3 and len(cur_parts) >= 3:
                                        ex_name_norm = ex_parts[0]
                                        ex_date = ex_parts[1]
                                        ex_sum_init = ex_parts[2]
                                        
                                        cur_date = cur_parts[1]
                                        cur_sum_init = cur_parts[2]
                                        
                                        # Сравниваем название fuzzy
                                        name_ratio = difflib.SequenceMatcher(None, ex_name_norm, кредитор_normalized).ratio()
                                        
                                        if name_ratio > 0.85:
                                            # Если названия похожи, то ОБЯЗАТЕЛЬНО проверяем дату или сумму!
                                            
                                            # 1. Проверка ДАТЫ
                                            dates_match = False
                                            has_dates = (ex_date != "NO_DATE" and cur_date != "NO_DATE")
                                            
                                            if has_dates:
                                                 try:
                                                     d1 = datetime.strptime(ex_date, "%d.%m.%Y")
                                                     d2 = datetime.strptime(cur_date, "%d.%m.%Y")
                                                     delta = abs((d1 - d2).days)
                                                     if delta <= 10:  # Разрешаем разбежку до 10 дней
                                                         dates_match = True
                                                 except:
                                                     dates_match = (ex_date == cur_date)
                                            
                                            # 2. Проверка СУММЫ (Initial Amount)
                                            sums_match = False
                                            has_sums = (ex_sum_init != "NO_INIT" and cur_sum_init != "NO_INIT")
                                            
                                            if has_sums:
                                                try:
                                                    s1 = float(ex_sum_init)
                                                    s2 = float(cur_sum_init)
                                                    if abs(s1 - s2) < 1.0: # Почти точное совпадение
                                                        sums_match = True
                                                except:
                                                    sums_match = (str(ex_sum_init) == str(cur_sum_init))
                                            
                                            # ЛОГИКА РЕШЕНИЯ О СЛИЯНИИ (MERGE DECISION)
                                            should_merge = False
                                            
                                            # А. Если есть даты у обоих - мержим ТОЛЬКО если даты совпадают
                                            if has_dates:
                                                if dates_match:
                                                    # КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Даже если даты совпали (особенно с разбегом),
                                                    # если суммы кардинально разные - это РАЗНЫЕ кредиты!
                                                    if has_sums and not sums_match:
                                                        try:
                                                            s1 = float(ex_sum_init)
                                                            s2 = float(cur_sum_init)
                                                            # Если разница больше 5000 и больше 10% - не мержим
                                                            diff = abs(s1 - s2)
                                                            if diff > 5000 and s1 > 0 and (diff / s1) > 0.1:
                                                                should_merge = False
                                                            else:
                                                                should_merge = True
                                                        except:
                                                            should_merge = True
                                                    else:
                                                        should_merge = True
                                                else:
                                                    should_merge = False # Даты разные -> разные кредиты
                                            
                                            # Б. Если дат нет (хотя бы у одного), пробуем по сумме
                                            else:
                                                # Если ключи имеют 4 части (т.е. содержат текущую сумму как 4-й элемент), 
                                                # сравниваем её, так как NO_INIT в 3-й части нам не поможет
                                                if len(ex_parts) == 4 and len(cur_parts) == 4:
                                                     try:
                                                         s1 = float(ex_parts[3])
                                                         s2 = float(cur_parts[3])
                                                         sums_match = (abs(s1 - s2) < 5.0) # +/- 5 рублей
                                                         should_merge = sums_match
                                                     except:
                                                         sums_match = (str(ex_parts[3]) == str(cur_parts[3]))
                                                         should_merge = sums_match
                                                elif has_sums and sums_match:
                                                    should_merge = True
                                                else:
                                                    # Без дат и без совпадения начальных сумм - но название очень похоже?
                                                    # Если название > 0.95 (почти полный дубль) и сумм нет - считаем одним кредитом.
                                                    if name_ratio > 0.95:
                                                        should_merge = True
                                                    else:
                                                        should_merge = False 
                                            
                                            if not should_merge:
                                                # print(f"      [DEBUG] Не мержим '{ex_name_norm}' и '{кредитор_normalized}' (даты={dates_match}, суммы={sums_match})")
                                                continue
                                                
                                            print(f"      [DEBUG] Fuzzy-совпадение (Название+Дата/Сумма): '{ex_name_norm}' <-> '{кредитор_normalized}'")
                                            found_similar = True
                                            similar_key = ex_key
                                            break
                                except Exception as e:
                                    pass

                    # Проверяем, встречался ли уже такой кредит (точное совпадение, ±1 день или fuzzy-name)
                    target_key = similar_key if found_similar else dedup_key
                    
                    if target_key in all_credits:
                        # Дубликат найден — берём запись с большей суммой (более актуальные данные)
                        existing_sum = all_credits[target_key].get("Сумма_задолженности", Decimal(0))
                        if сумма > existing_sum:
                            if found_similar and target_key != dedup_key:
                                print(f"      ✓ ДУБЛИКАТ ±1день (обновлён): {existing_sum} → {сумма}")
                            else:
                                print(f"      ✓ ДУБЛИКАТ (обновлён): {existing_sum} → {сумма}")
                            print(f"      Ключ: {target_key}")
                            all_credits[target_key] = {
                                "Кредитор": кредитор_canonical,
                                "ИНН_кредитора": инн or None,
                                "ОГРН_кредитора": огрн or None,
                                "Дата_договора": дата_договора or None,
                                "Сумма_обязательства": сумма_обязательства,
                                "Сумма_задолженности": сумма,
                                "Тип_кредитора": тип_кредитора,
                                "Вид": entry.get("Вид") or entry.get("Тип_кредита") or None,
                                "Текущая_задолженность": текущая,
                                "Текущая_просрочка": просрочка,
                                "Основной_долг": основной,
                                "Проценты": проценты,
                                "Штрафы": штрафы,
                                "Просрочка": просрочка,
                                "Источник": key,
                            }
                        else:
                            # Даже если сумма такая же/меньше, проверяем качество данных
                            existing_v = all_credits[target_key]
                            existing_inn = existing_v.get("ИНН_кредитора")
                            
                            should_force_update = False
                            
                            # 1. Если у нового есть ИНН, а у старого нет - обновляем
                            if (инн and DocumentProcessor.validate_inn(str(инн))) and (not existing_inn or not DocumentProcessor.validate_inn(str(existing_inn))):
                                should_force_update = True
                                print(f"      ✓ ОБНОВЛЕНИЕ (найден валидный ИНН): {existing_inn or 'нет'} -> {инн}")
                            
                            # 2. Если нашли fuzzy-дубликат (похожее имя), возможно новое имя правильнее?
                            # Например "Трумвират" (без ИНН) -> "Триумвират" (с ИНН)
                            elif found_similar and инн and not existing_inn:
                                should_force_update = True
                                print(f"      ✓ ОБНОВЛЕНИЕ (fuzzy-match с лучшими данными): {existing_v['Кредитор']} -> {кредитор_canonical}")

                            if should_force_update:
                                all_credits[target_key] = {
                                    "Кредитор": кредитор_canonical,
                                    "ИНН_кредитора": инн,
                                    "ОГРН_кредитора": огрн or existing_v.get("ОГРН_кредитора"),
                                    "Дата_договора": дата_договора or existing_v.get("Дата_договора"),
                                    "Сумма_обязательства": max(сумма_обязательства, existing_v.get("Сумма_обязательства", Decimal(0))),
                                    "Сумма_задолженности": max(сумма, existing_sum),
                                    "Тип_кредитора": тип_кредитора,
                                    "Вид": entry.get("Вид") or entry.get("Тип_кредита") or existing_v.get("Вид"),
                                    "Текущая_задолженность": текущая or existing_v.get("Текущая_задолженность"),
                                    "Текущая_просрочка": просрочка or existing_v.get("Текущая_просрочка"),
                                    "Основной_долг": основной or existing_v.get("Основной_долг"),
                                    "Проценты": проценты or existing_v.get("Проценты"),
                                    "Штрафы": штрафы or existing_v.get("Штрафы"),
                                    "Просрочка": просрочка or existing_v.get("Просрочка"),
                                    "Источник": key,
                                }
                            else:
                                if found_similar and target_key != dedup_key:
                                    print(f"      ✓ ДУБЛИКАТ ±1день/fuzzy (пропущен): {сумма} <= {existing_sum}")
                                else:
                                    print(f"      ✓ ДУБЛИКАТ (пропущен): {сумма} <= {existing_sum}")
                                print(f"      Ключ: {target_key}")
                    else:
                        # Новый кредит? Проверяем эвристически на дубликаты (разные даты, но одна суть)
                        heuristic_dup_key = None
                        
                        for existing_k, existing_v in all_credits.items():
                            if existing_v["Кредитор"] == кредитор_canonical:
                                # Проверка дат
                                ex_date_str = existing_v.get("Дата_договора")
                                cur_date_str = дата_договора
                                dates_are_different = False

                                if ex_date_str and cur_date_str:
                                    try:
                                        d1 = datetime.strptime(ex_date_str, "%d.%m.%Y")
                                        d2 = datetime.strptime(cur_date_str, "%d.%m.%Y")
                                        if abs((d1 - d2).days) > 60: # Если разница больше 60 дней - это точно разные кредиты
                                            dates_are_different = True
                                    except:
                                        pass
                                
                                if dates_are_different:
                                    continue # Не считаем дубликатом, если даты сильно разные

                                # 1. Совпадение по первоначальной сумме (очень надежно)
                                ex_init = existing_v.get("Сумма_обязательства", Decimal(0))
                                if сумма_обязательства > 100 and abs(ex_init - сумма_обязательства) < Decimal('1.0'):
                                    heuristic_dup_key = existing_k
                                    break
                                
                                # 2. Совпадение по текущей сумме (решает проблему дублей "Иной" vs "Потреб")
                                ex_curr = existing_v.get("Сумма_задолженности", Decimal(0))
                                if abs(ex_curr - сумма) < Decimal('1.0'):
                                    heuristic_dup_key = existing_k
                                    break
                        
                        credit_data_new = {
                            "Кредитор": кредитор_canonical,
                            "ИНН_кредитора": инн or None,
                            "ОГРН_кредитора": огрн or None,
                            "Дата_договора": дата_договора or None,
                            "Сумма_обязательства": сумма_обязательства,
                            "Сумма_задолженности": сумма,
                            "Тип_кредитора": тип_кредитора,
                            "Вид": entry.get("Вид") or entry.get("Тип_кредита") or None,
                            "Текущая_задолженность": текущая,
                            "Текущая_просрочка": просрочка,
                            "Основной_долг": основной,
                            "Проценты": проценты,
                            "Штрафы": штрафы,
                            "Просрочка": просрочка,
                            "Источник": key,
                        }

                        if heuristic_dup_key:
                            # Нашли дубликат по эвристике!
                            existing_v = all_credits[heuristic_dup_key]
                            existing_sum = existing_v.get("Сумма_задолженности", Decimal(0))
                            
                            should_replace = False
                            reason = ""
                            
                            if сумма > existing_sum:
                                should_replace = True
                                reason = f"сумма больше ({сумма} > {existing_sum})"
                            elif сумма < existing_sum:
                                should_replace = False
                                reason = f"сумма меньше ({сумма} < {existing_sum})"
                            else:
                                # Суммы равны. Смотрим на качество даты.
                                ex_date = existing_v.get("Дата_договора")
                                is_ex_bad = not ex_date or "9999" in ex_date
                                is_new_good = дата_договора and "9999" not in дата_договора
                                
                                if is_ex_bad and is_new_good:
                                    should_replace = True
                                    reason = f"лучше дата ({дата_договора} vs {ex_date})"
                                else:
                                    should_replace = False
                                    reason = "дубликат (пропущен)"

                            if should_replace:
                                print(f"      ✓ ДУБЛИКАТ (эвристика, ОБНОВЛЕН): {reason}")
                                print(f"        Удален старый: {heuristic_dup_key}")
                                del all_credits[heuristic_dup_key]
                                
                                print(f"      ✓ НОВЫЙ кредит добавлен (замена)")
                                print(f"      Ключ: {dedup_key}")
                                all_credits[dedup_key] = credit_data_new
                            else:
                                print(f"      ✓ ДУБЛИКАТ (эвристика, пропущен): {reason}")
                        else:
                            # Совсем новый
                            print(f"      ✓ НОВЫЙ кредит добавлен")
                            print(f"      Ключ: {dedup_key}")
                            all_credits[dedup_key] = credit_data_new

        # === ДОБАВЛЯЕМ СПРАВКИ О ЗАДОЛЖЕННОСТИ ===
        # Справки - это официальные документы от кредиторов с актуальной суммой долга
        for справка in data_map.get("справка_о_задолженности", []):
            кредитор = (справка.get("Кредитор") or "").strip()
            if not кредитор:
                continue

            кредитор_normalized, кредитор_canonical = DocumentProcessor.normalize_bank_name(кредитор)

            # Извлекаем итоговую сумму задолженности из справки
            итого = справка.get("Итого_задолженность") or справка.get("Всего_задолженность") or справка.get("Сумма_задолженности")
            сумма = DocumentProcessor.parse_decimal(итого)
            
            if not сумма or сумма <= 0:
                # Если итоговой суммы нет, пробуем сложить компоненты
                основной = DocumentProcessor.parse_decimal(справка.get("Основной_долг"))
                проценты = DocumentProcessor.parse_decimal(справка.get("Проценты"))
                штрафы = DocumentProcessor.parse_decimal(справка.get("Штрафы"))
                сумма = (основной or Decimal(0)) + (проценты or Decimal(0)) + (штрафы or Decimal(0))

            if сумма <= 0:
                continue

            # Определяем тип кредитора
            кредитор_lower = кредитор.lower()
            if any(word in кредитор_lower for word in ["мфо", "мкк", "мфк", "пко", "коллектор", "микрофинанс", "микрокредит"]):
                тип_кредитора = "МФО"
            else:
                тип_кредитора = "БАНК"

            дата_договора = (справка.get("Дата_возникновения") or справка.get("Дата_договора") or справка.get("Дата") or "").strip()
            инн = (справка.get("ИНН") or "").strip()
            огрн = (справка.get("ОГРН") or "").strip()
            
            # Попытка найти существующий кредит в отчетах (Fuzzy Search)
            found_key = None
            found_by = ""
            
            # Проходим по всем уже добавленным кредитам
            for ex_key, ex_data in all_credits.items():
                ex_name = ex_data["Кредитор"]
                ex_date = ex_data.get("Дата_договора", "")
                
                # 1. Сравнение имен (нормализованных)
                # Нормализуем имя из существующей записи для сравнения
                ex_norm, _ = DocumentProcessor.normalize_bank_name(ex_name)
                
                # Пропускаем, если имена совсем разные
                if ex_norm != кредитор_normalized:
                    # Попробуем fuzzy match для имен
                    name_ratio = difflib.SequenceMatcher(None, ex_norm, кредитор_normalized).ratio()
                    if name_ratio < 0.85:
                        continue
                
                # 2. Сравнение дат (если даты есть)
                if дата_договора and ex_date:
                    if дата_договора == ex_date:
                        found_key = ex_key
                        found_by = "exact_date"
                        break
                    
                    # Пытаемся распарсить и сравнить с допуском
                    try:
                        d1 = datetime.strptime(дата_договора, "%d.%m.%Y")
                        d2 = datetime.strptime(ex_date, "%d.%m.%Y")
                        if abs((d1 - d2).days) <= 5: # 5 дней допуск
                            found_key = ex_key
                            found_by = f"fuzzy_date_{abs((d1 - d2).days)}d"
                            break
                    except:
                        pass
            
            if found_key:
                print(f"      [СПРАВКА] Обновление {кредитор_canonical} (найден по {found_by}): {all_credits[found_key]['Сумма_задолженности']} → {сумма} руб.")
                # Обновляем существующую запись данными из справки
                all_credits[found_key]["Сумма_задолженности"] = сумма
                all_credits[found_key]["Источник"] = "справка_о_задолженности"
                if дата_договора:
                    all_credits[found_key]["Дата_договора"] = дата_договора
                if инн and not all_credits[found_key].get("ИНН_кредитора"):
                    all_credits[found_key]["ИНН_кредитора"] = инн
            else:
                # Если не нашли - добавляем как новый
                # Ключ дедупликации: название + дата + текущая сумма (так как начальной нет)
                сумма_для_ключа = int(сумма) if сумма else 0
                dedup_key = f"{кредитор_normalized}|{дата_договора}|{сумма_для_ключа}"
                
                if dedup_key not in all_credits:
                    print(f"      [СПРАВКА] Добавлен {кредитор_canonical}: {сумма} руб. (дата: {дата_договора})")
                    all_credits[dedup_key] = {
                        "Кредитор": кредитор_canonical,
                        "ИНН_кредитора": инн or None,
                        "ОГРН_кредитора": огрн or None,
                        "Дата_договора": дата_договора or None,
                        "Сумма_задолженности": сумма,
                        "Тип_кредитора": тип_кредитора,
                        "Текущая_задолженность": DocumentProcessor.parse_decimal(справка.get("Основной_долг")),
                        "Текущая_просрочка": None,
                        "Основной_долг": DocumentProcessor.parse_decimal(справка.get("Основной_долг")),
                        "Проценты": DocumentProcessor.parse_decimal(справка.get("Проценты")),
                        "Штрафы": DocumentProcessor.parse_decimal(справка.get("Штрафы")),
                        "Просрочка": None,
                        "Источник": "справка_о_задолженности",
                    }

        # Сортируем по убыванию суммы
        result = sorted(all_credits.values(), key=lambda x: x["Сумма_задолженности"], reverse=True)
        
        print("\n" + "="*80)
        print(f"ИТОГО УНИКАЛЬНЫХ КРЕДИТОВ: {len(result)}")
        print("="*80)
        
        return result

    @staticmethod
    def calculate_total_debt(data_map: Dict[str, List[Dict[str, Any]]]) -> Optional[Decimal]:
        """Рассчитывает общую сумму долга из ВСЕХ источников.

        Источники задолженности:
        1. Кредитные отчеты (ОКБ, БКИ) - объединяем все уникальные кредиты
        2. Справки о задолженности от кредиторов
        3. Постановления приставов (ФССП)

        Приоритет:
        1. Автоматический расчёт: сумма всех источников
        """

        total = Decimal(0)
        has_value = False

        # === 1. КРЕДИТНЫЕ ОТЧЕТЫ (ОКБ + БКИ) ===
        # Объединяем все уникальные кредиты из обоих отчетов
        merged_credits = DocumentProcessor.merge_credit_reports(data_map)

        if merged_credits:
            # Суммируем все уникальные кредиты
            credits_total = sum(
                credit.get("Сумма_задолженности", Decimal(0)) 
                for credit in merged_credits
            )
            if credits_total > 0:
                total += credits_total
                has_value = True

        # === 2. ПОСТАНОВЛЕНИЯ ПРИСТАВОВ ===
        # === 2. ПОСТАНОВЛЕНИЯ ПРИСТАВОВ ===
        for постановление in data_map.get("постановление_пристава", []):
            # ФИЛЬТР: Пропускаем закрытые/прекращенные ИП
            дата_окончания = постановление.get("Дата_окончания")
            if дата_окончания and дата_окончания not in ["", "null", None]:
                continue
            
            # Берем итоговую сумму взыскания
            # ПРИОРИТЕТ: Остаток долга (текущий) > Сумма долга (основной) > Итого взыскание (с исполнительским сбором)
            взыскание = (
                постановление.get("Остаток_долга") or 
                постановление.get("Сумма_долга") or 
                постановление.get("Итого_взыскание")
            )
            
            if взыскание:
                parsed = DocumentProcessor.parse_decimal(взыскание)
                if parsed and parsed > 0:
                    total += parsed
                    has_value = True
                    print(f"      [FSSP] Постановление пристава: {parsed} (Raw: {взыскание})")

        # === 3. НАЛОГОВЫЕ УВЕДОМЛЕНИЯ ===
        for уведомление in data_map.get("налоговое_уведомление", []):
            # Сначала пробуем взять общую сумму "Всего к уплате"
            всего_к_уплате = DocumentProcessor.parse_decimal(уведомление.get("Всего_к_уплате"))
            if всего_к_уплате and всего_к_уплате > 0:
                total += всего_к_уплате
                has_value = True
                print(f"      [TAX] Налоговое уведомление: всего к уплате {всего_к_уплате}")
            else:
                # Если общей суммы нет, суммируем по каждому налогу
                налоги = уведомление.get("Налоги", [])
                for налог in налоги:
                    if isinstance(налог, dict):
                        сумма = DocumentProcessor.parse_decimal(налог.get("Сумма"))
                        if сумма and сумма > 0:
                            total += сумма
                            has_value = True

        return total if has_value else None

    @staticmethod
    def convert_to_genitive_initials(full_name: str) -> str:
        if not full_name:
            return ""
        prompt = (
            "Преобразуй ФИО в форму 'Фамилии И.О.' в родительном падеже."
            " Верни только результат без пояснений и дополнительных знаков.\n\n"
            f"ФИО: {full_name}"
        )
        try:
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {"role": "user", "content": prompt}
                ],
            )
            result = response.choices[0].message.content.strip()
            return result.splitlines()[0]
        except Exception:
            return ""

    @staticmethod
    def convert_to_genitive_full(full_name: str) -> str:
        """Преобразует ФИО в родительный падеж (кого? - Кузьмича Николая Павловича)."""
        if not full_name:
            return ""
        prompt = (
            "Преобразуй ФИО в родительный падеж (кого?)."
            " Верни только результат без пояснений и дополнительных знаков.\n\n"
            f"ФИО: {full_name}\n"
            "Пример: Иванов Иван Иванович → Иванова Ивана Ивановича"
        )
        try:
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {"role": "user", "content": prompt}
                ],
            )
            result = response.choices[0].message.content.strip()
            return result.splitlines()[0]
        except Exception:
            return ""

    @staticmethod
    def convert_to_dative_initials(full_name: str) -> str:
        """Преобразует ФИО в форму 'Фамилии И.О.' в дательном падеже (кому?)."""
        if not full_name:
            return ""
        prompt = (
            "Преобразуй ФИО в форму 'Фамилии И.О.' в дательном падеже (кому?)."
            " Верни только результат без пояснений и дополнительных знаков.\n\n"
            f"ФИО: {full_name}"
        )
        try:
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {"role": "user", "content": prompt}
                ],
            )
            result = response.choices[0].message.content.strip()
            return result.splitlines()[0]
        except Exception:
            return ""

    @staticmethod
    def convert_to_dative_full(full_name: str) -> str:
        """Преобразует ФИО в дательный падеж (кому? - Кузьмичу Николаю Павловичу)."""
        if not full_name:
            return ""
        prompt = (
            "Преобразуй ФИО в дательный падеж (кому?)."
            " Верни только результат без пояснений и дополнительных знаков.\n\n"
            f"ФИО: {full_name}\n"
            "Пример: Иванов Иван Иванович → Иванову Ивану Ивановичу"
        )
        try:
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {"role": "user", "content": prompt}
                ],
            )
            result = response.choices[0].message.content.strip()
            return result.splitlines()[0]
        except Exception:
            return ""

    @staticmethod
    def convert_to_accusative_full(full_name: str) -> str:
        """Преобразует ФИО в винительный падеж (кого? - Кузьмича Николая Павловича)."""
        if not full_name:
            return ""
        prompt = (
            "Преобразуй ФИО в винительный падеж (кого?)."
            " Верни только результат без пояснений и дополнительных знаков.\n\n"
            f"ФИО: {full_name}\n"
            "Пример: Иванов Иван Иванович → Иванова Ивана Ивановича"
        )
        try:
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {"role": "user", "content": prompt}
                ],
            )
            result = response.choices[0].message.content.strip()
            return result.splitlines()[0]
        except Exception:
            return ""

    @staticmethod
    def detect_gender(full_name: str) -> str:
        """Определяет пол по ФИО. Возвращает 'м' или 'ж'."""
        if not full_name:
            return "м"

        # Простая логика: если отчество заканчивается на 'вна' или 'чна' - женский
        parts = full_name.split()
        if len(parts) >= 3:
            patronymic = parts[2].lower()
            if patronymic.endswith('вна') or patronymic.endswith('ична'):
                return "ж"

        # Если не определили по отчеству, используем GPT
        prompt = (
            "Определи пол человека по ФИО. Верни ТОЛЬКО одну букву: 'м' для мужского или 'ж' для женского пола."
            " Никаких пояснений, только буква.\n\n"
            f"ФИО: {full_name}"
        )
        try:
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {"role": "user", "content": prompt}
                ],
            )
            result = response.choices[0].message.content.strip().lower()
            if 'ж' in result:
                return "ж"
            else:
                return "м"
        except Exception:
            return "м"

    @staticmethod
    def get_gender_pronoun(gender: str, case: str = "accusative") -> str:
        """Возвращает местоимение в зависимости от пола и падежа."""
        if case == "accusative":  # винительный падеж (признании его/её)
            return "её" if gender == "ж" else "его"
        elif case == "genitive":  # родительный падеж (у него/неё, в отношении него/неё)
            return "неё" if gender == "ж" else "него"
        elif case == "dative":  # дательный падеж (ему/ей)
            return "ей" if gender == "ж" else "ему"
        else:
            return "его" if gender == "м" else "её"

    @staticmethod
    def parse_address_with_gpt(full_address: str, openai_client) -> Dict[str, str]:
        """Парсит адрес прописки на компоненты через GPT."""
        result = {
            "субъект": "",
            "район": "",
            "город": "",
            "населенный_пункт": "",
            "улица": "",
            "дом": "",
            "корпус": "",
            "квартира": "",
        }

        if not full_address or not openai_client:
            return result

        prompt = f"""Разбери адрес на компоненты. Верни ТОЛЬКО JSON без дополнительного текста.

Адрес: {full_address}

JSON формат:
{{
  "субъект": "область/край/республика (например: Новосибирская область, Краснодарский край)",
  "район": "район (если есть, например: Центральный район)",
  "город": "город (например: Новосибирск, город Москва)",
  "населенный_пункт": "поселок/село/деревня (если есть вместо города)",
  "улица": "улица/проспект/переулок с названием (например: ул. Ленина, проспект Мира)",
  "дом": "номер дома (только цифры и буквы, например: 10, 5А)",
  "корпус": "номер корпуса/строения (если есть, например: 2, 1А)",
  "квартира": "номер квартиры (только цифры, например: 45)"
}}

Если компонента нет в адресе - оставь пустую строку "".
"""

        try:
            response = openai_client.chat.completions.create(
                model="gpt-5-mini",
                messages=[{"role": "user", "content": prompt}],
            )
            
            response_text = response.choices[0].message.content.strip()
            
            # Очистка JSON
            cleaned = DocumentProcessor.clean_json_response(response_text)
            if cleaned:
                parsed = json.loads(cleaned)
                for key in result.keys():
                    if key in parsed and parsed[key]:
                        result[key] = str(parsed[key]).strip()
        except Exception as e:
            print(f"      [WARNING] Не удалось разобрать адрес через GPT: {e}")
            # Fallback на regex парсинг
            return DocumentProcessor.parse_address(full_address)

        return result

    @staticmethod
    def parse_address(full_address: str) -> Dict[str, str]:
        """Парсит адрес прописки на компоненты."""
        import re

        result = {
            "субъект": "",
            "район": "",
            "город": "",
            "населенный_пункт": "",
            "улица": "",
            "дом": "",
            "корпус": "",
            "квартира": "",
        }

        if not full_address:
            return result

        # Простой парсинг по ключевым словам
        addr = full_address

        # Субъект (область, край, республика)
        if "область" in addr.lower():
            match = re.search(r'([^,]+область)', addr, re.IGNORECASE)
            if match:
                result["субъект"] = match.group(1).strip()
        elif "край" in addr.lower():
            match = re.search(r'([^,]+край)', addr, re.IGNORECASE)
            if match:
                result["субъект"] = match.group(1).strip()
        elif "республика" in addr.lower():
            match = re.search(r'([^,]+Республика[^,]*)', addr, re.IGNORECASE)
            if match:
                result["субъект"] = match.group(1).strip()

        # Район
        if "район" in addr.lower():
            match = re.search(r'([^,]*район[^,]*)', addr, re.IGNORECASE)
            if match:
                result["район"] = match.group(1).strip()

        # Город
        if re.search(r'\bгород\b|\bгор\.|г\.', addr, re.IGNORECASE):
            match = re.search(r'(?:город|гор\.|г\.)\s*([^,]+)', addr, re.IGNORECASE)
            if match:
                result["город"] = match.group(1).strip()
        elif re.search(r'\b[Гг]ород\s+\w+', addr):
            match = re.search(r'[Гг]ород\s+(\w+)', addr)
            if match:
                result["город"] = match.group(1).strip()

        # Улица, проспект, переулок
        if re.search(r'\b(?:улица|ул\.|проспект|пр-кт|пр\.|переулок|пер\.)', addr, re.IGNORECASE):
            match = re.search(r'(?:улица|ул\.|проспект|пр-кт|пр\.|переулок|пер\.)\s*([^,]+)', addr, re.IGNORECASE)
            if match:
                result["улица"] = match.group(1).strip()

        # Дом
        if re.search(r'\bдом\b|\bд\.', addr, re.IGNORECASE):
            match = re.search(r'(?:дом|д\.)\s*(\d+[а-яА-Я]?)', addr, re.IGNORECASE)
            if match:
                result["дом"] = match.group(1).strip()

        # Корпус
        if re.search(r'\bкорпус\b|\bкорп\.|к\.', addr, re.IGNORECASE):
            match = re.search(r'(?:корпус|корп\.|к\.)\s*(\d+[а-яА-Я]?)', addr, re.IGNORECASE)
            if match:
                result["корпус"] = match.group(1).strip()

        # Квартира
        if re.search(r'\bквартира\b|\bкв\.', addr, re.IGNORECASE):
            match = re.search(r'(?:квартира|кв\.)\s*(\d+)', addr, re.IGNORECASE)
            if match:
                result["квартира"] = match.group(1).strip()

        return result

    @staticmethod
    def extract_deals_from_all_documents(data_map: Dict[str, List[Dict[str, Any]]], owner_fio: str = "") -> List[Dict[str, Any]]:
        """Извлекает информацию о сделках из всех типов документов.

        Собирает сделки из:
        - Документов типа "сделки" (договоры купли-продажи, дарения)
        - ГИБДД (продажа автомобилей)
        - ЕГРН (продажа/дарение недвижимости)
        - Других документов с информацией о сделках

        Args:
            data_map: Словарь с данными всех документов
            owner_fio: ФИО должника для определения его роли в сделке

        Returns:
            Список всех сделок за последние 3 года
        """
        all_deals = []

        # 1. Сделки из документов типа "сделки"
        for deal in data_map.get("сделки", []):
            if isinstance(deal, dict):
                all_deals.append(deal)

        # 2. Сделки из документов ГИБДД (продажа автомобилей)
        for gibdd_doc in data_map.get("гибдд", []):
            if not isinstance(gibdd_doc, dict):
                continue

            # Поддержка нового формата с массивом
            vehicles_to_process = []
            if "Есть_транспорт" in gibdd_doc:
                if gibdd_doc.get("Есть_транспорт") is True and "Транспортные_средства" in gibdd_doc:
                    vehicles_array = gibdd_doc.get("Транспортные_средства", [])
                    if isinstance(vehicles_array, list):
                        vehicles_to_process.extend(vehicles_array)
            else:
                # Старый формат: один объект = одно ТС
                vehicles_to_process.append(gibdd_doc)

            # Обрабатываем каждое ТС
            for vehicle in vehicles_to_process:
                if not isinstance(vehicle, dict):
                    continue

                sdelka = vehicle.get("Сделка")
                if sdelka and isinstance(sdelka, dict) and sdelka.get("Есть_сделка"):
                    # Формируем стандартную структуру сделки
                    deal = {
                        "Тип_сделки": sdelka.get("Тип_сделки", "купля-продажа"),
                        "Дата_сделки": sdelka.get("Дата_сделки"),
                        "Роль_должника": None,
                        "Предмет_сделки": "автомобиль",
                        "Описание": vehicle.get("Марка_модель", ""),
                        "Адрес_или_характеристики": f"VIN: {vehicle.get('VIN', '')}, Гос. номер: {vehicle.get('Гос_номер', '')}",
                        "Кадастровый_номер": None,
                        "Стоимость": sdelka.get("Стоимость"),
                        "Вторая_сторона": None,
                        "Особые_условия": vehicle.get("Особые_отметки")
                    }

                    # Определяем роль должника
                    seller = sdelka.get("Продавец_ФИО", "")
                    buyer = sdelka.get("Покупатель_ФИО", "")

                    if owner_fio and seller and owner_fio.lower() in seller.lower():
                        deal["Роль_должника"] = "продавец"
                        deal["Вторая_сторона"] = buyer
                    elif owner_fio and buyer and owner_fio.lower() in buyer.lower():
                        deal["Роль_должника"] = "покупатель"
                        deal["Вторая_сторона"] = seller
                    else:
                        # Если не можем определить - берем из документа
                        deal["Роль_должника"] = "продавец" if seller else "покупатель"
                        deal["Вторая_сторона"] = buyer or seller

                    all_deals.append(deal)

        # 3. Сделки из документов ЕГРН
        # А) Извлекаем из поля "Сделка" (если GPT заполнил)
        print(f"[DEALS_EGRN] Extracting deals from Сделка field (GPT-provided)")
        for egrn_doc in data_map.get("егрн_выписка", []):
            if not isinstance(egrn_doc, dict):
                continue

            objects = egrn_doc.get("Объекты", [])
            if not objects:
                old_sdelka = egrn_doc.get("Сделка")
                old_obj = egrn_doc.get("Объект", {})
                if old_sdelka:
                    objects = [{"Объект": old_obj, "Сделка": old_sdelka}]
            
            for idx, obj_item in enumerate(objects):
                if not isinstance(obj_item, dict):
                    continue

                sdelka = obj_item.get("Сделка")
                if not (sdelka and isinstance(sdelka, dict) and sdelka.get("Есть_сделка")):
                    continue

                obj = obj_item.get("Объект", {})
                predmet = obj.get("Вид", "недвижимость")
                vid_lower = predmet.lower() if predmet else ""
                
                if "квартир" in vid_lower or "помещен" in vid_lower:
                    predmet_type = "квартира"
                elif "дом" in vid_lower or "здание" in vid_lower:
                    predmet_type = "дом"
                elif "земел" in vid_lower or "участок" in vid_lower:
                    predmet_type = "земельный участок"
                else:
                    predmet_type = predmet or "недвижимость"

                deal = {
                    "Тип_сделки": sdelka.get("Тип_сделки", "купля-продажа"),
                    "Дата_сделки": sdelka.get("Дата_сделки"),
                    "Роль_должника": None,
                    "Предмет_сделки": predmet_type,
                    "Описание": f"{predmet}, площадь: {obj.get('Площадь', '')}",
                    "Адрес_или_характеристики": obj.get("Адрес", ""),
                    "Кадастровый_номер": obj.get("Кадастровый_номер") or egrn_doc.get("Кадастровый_номер"),
                    "Стоимость": sdelka.get("Стоимость") or obj.get("Кадастровая_стоимость") or egrn_doc.get("Кадастровая_стоимость"),
                    "Вторая_сторона": None,
                    "Особые_условия": None
                }

                prev_owner = sdelka.get("Предыдущий_собственник", "")
                new_owner = sdelka.get("Новый_собственник", "")

                if owner_fio and prev_owner and owner_fio.lower() in prev_owner.lower():
                    deal["Роль_должника"] = "продавец"
                    deal["Вторая_сторона"] = new_owner
                elif owner_fio and new_owner and owner_fio.lower() in new_owner.lower():
                    deal["Роль_должника"] = "покупатель"
                    deal["Вторая_сторона"] = prev_owner
                else:
                    deal["Роль_должника"] = "продавец" if prev_owner else "покупатель"
                    deal["Вторая_сторона"] = new_owner or prev_owner

                all_deals.append(deal)
                print(f"[DEALS_EGRN] Added deal from Сделка field: {predmet_type} on {deal['Дата_сделки']}")
        
        # Б) ДОПОЛНИТЕЛЬНО: Извлекаем напрямую из дат правообладателей
        # Логика: если дата в последние 3 года → это сделка
        from datetime import datetime, timedelta
        three_years_ago = datetime.now() - timedelta(days=3*365)
        
        for egrn_doc in data_map.get("егрн_выписка", []):
            if not isinstance(egrn_doc, dict):
                continue
            
            objects = egrn_doc.get("Объекты", [])
            if not objects:
                # Fallback для старой структуры
                old_obj = egrn_doc.get("Объект", {})
                if old_obj:
                    objects = [{"Объект": old_obj}]
            
            for idx, obj_item in enumerate(objects):
                if not isinstance(obj_item, dict):
                    continue
                
                owners = obj_item.get("Правообладатели", [])
                obj = obj_item.get("Объект", {})
                cadastral = obj.get("Кадастровый_номер") or egrn_doc.get("Кадастровый_номер")
                
                print(f"[DEALS_EGRN_DATES] Object {idx} (cadastral: {cadastral}): Checking {len(owners)} owners for deals")
                
                # Флаги для дедупликации: только 1 сделка на объект
                found_sale = False
                found_purchase = False
                
                for owner_idx, owner in enumerate(owners):
                    if not isinstance(owner, dict):
                        continue
                    
                    owner_fio_str = owner.get("ФИО", "N/A")
                    # Поддержка разных вариантов названий полей
                    date_reg = (owner.get("Дата_регистрации") or 
                               owner.get("дата_государственной_регистрации") or 
                               owner.get("Дата_государственной_регистрации") or "")
                    date_end = (owner.get("Дата_прекращения_права") or 
                               owner.get("дата_прекращения_права") or
                               owner.get("дата_государственной_регистрации_прекращения_права") or
                               owner.get("Дата_государственной_регистрации_прекращения_права") or "")
                    
                    print(f"[DEALS_EGRN_DATES]   Owner {owner_idx}: {owner_fio_str}, reg={date_reg}, end={date_end}")
                    print(f"[DEALS_EGRN_DATES]   Owner raw data: {owner}")
                    
                    # Проверяем ПРОДАЖУ (дата прекращения права = дата)
                    if not found_sale and date_end and date_end != "данные отсутствуют":
                        try:
                            end_date = datetime.strptime(date_end, '%d.%m.%Y')
                            if end_date >= three_years_ago:
                                # Это ПРОДАЖА в последние 3 года!
                                predmet = obj.get("Вид", "недвижимость")
                                
                                # Определяем тип предмета
                                vid_lower = predmet.lower() if predmet else ""
                                if "квартир" in vid_lower or "помещен" in vid_lower:
                                    predmet_type = "квартира"
                                elif "дом" in vid_lower or "здание" in vid_lower:
                                    predmet_type = "дом"
                                elif "земел" in vid_lower or "участок" in vid_lower:
                                    predmet_type = "земельный участок"
                                else:
                                    predmet_type = predmet or "недвижимость"
                                
                                sale_deal = {
                                    "Тип_сделки": "продажа",
                                    "Дата_сделки": date_end,
                                    "Роль_должника": "продавец",
                                    "Предмет_сделки": predmet_type,
                                    "Описание": f"{predmet}, площадь: {obj.get('Площадь', '')}",
                                    "Адрес_или_характеристики": obj.get("Адрес", ""),
                                    "Кадастровый_номер": cadastral,
                                    "Стоимость": obj.get("Кадастровая_стоимость") or egrn_doc.get("Кадастровая_стоимость"),
                                    "Вторая_сторона": None,
                                    "Особые_условия": None
                                }
                                print(f"[DEALS_EGRN_DATES] Object {idx}: Found SALE deal - sold on {date_end}")
                                all_deals.append(sale_deal)
                                found_sale = True  # Больше не добавляем продажи для этого объекта
                        except (ValueError, AttributeError) as e:
                            print(f"[DEALS_EGRN_DATES] Could not parse end date {date_end}: {e}")
                    
                    # Проверяем ПОКУПКУ (дата регистрации в последние 3 года + право действует)
                    if not found_purchase and date_end == "данные отсутствуют" and date_reg:
                        try:
                            reg_date = datetime.strptime(date_reg, '%d.%m.%Y')
                            
                            if reg_date >= three_years_ago:
                                # Это покупка в последние 3 года!
                                predmet = obj.get("Вид", "недвижимость")
                                
                                # Определяем тип предмета
                                vid_lower = predmet.lower() if predmet else ""
                                if "квартир" in vid_lower or "помещен" in vid_lower:
                                    predmet_type = "квартира"
                                elif "дом" in vid_lower or "здание" in vid_lower:
                                    predmet_type = "дом"
                                elif "земел" in vid_lower or "участок" in vid_lower:
                                    predmet_type = "земельный участок"
                                else:
                                    predmet_type = predmet or "недвижимость"
                                
                                purchase_deal = {
                                    "Тип_сделки": "покупка",
                                    "Дата_сделки": date_reg,
                                    "Роль_должника": "покупатель",
                                    "Предмет_сделки": predmet_type,
                                    "Описание": f"{predmet}, площадь: {obj.get('Площадь', '')}",
                                    "Адрес_или_характеристики": obj.get("Адрес", ""),
                                    "Кадастровый_номер": cadastral,
                                    "Стоимость": obj.get("Кадастровая_стоимость") or egrn_doc.get("Кадастровая_стоимость"),
                                    "Вторая_сторона": None,
                                    "Особые_условия": None
                                }
                                print(f"[DEALS_EGRN_DATES] Object {idx}: Found PURCHASE deal - bought on {date_reg}")
                                all_deals.append(purchase_deal)
                                found_purchase = True  # Больше не добавляем покупки для этого объекта
                        except (ValueError, AttributeError) as e:
                            print(f"[DEALS_EGRN_DATES] Could not parse date {date_reg}: {e}")

        # Фильтруем по дате - только за последние 3 года
        from datetime import datetime, timedelta
        three_years_ago = datetime.now() - timedelta(days=3*365)

        filtered_deals = []
        for deal in all_deals:
            deal_date_str = deal.get("Дата_сделки")
            if not deal_date_str:
                # Если даты нет, всё равно включаем
                filtered_deals.append(deal)
                continue

            try:
                # Парсим дату в формате ДД.ММ.ГГГГ
                deal_date = datetime.strptime(deal_date_str, '%d.%m.%Y')
                if deal_date >= three_years_ago:
                    filtered_deals.append(deal)
            except (ValueError, TypeError):
                # Если не удалось распарсить дату, всё равно включаем
                filtered_deals.append(deal)

        # ДЕДУПЛИКАЦИЯ: Убираем дубликаты сделок
        deduplicated_deals = []
        seen_keys = set()  # Для отслеживания уже добавленных сделок
        
        for deal in filtered_deals:
            # Создаем уникальный ключ для дедупликации
            predmet = deal.get("Предмет_сделки", "")
            date_str = deal.get("Дата_сделки", "")
            cadastral = deal.get("Кадастровый_номер", "")
            
            # Для автомобилей используем VIN
            if predmet == "автомобиль":
                addr_or_char = deal.get("Адрес_или_характеристики") or ""
                vin = ""
                if "VIN:" in addr_or_char:
                    vin_start = addr_or_char.index("VIN:") + 4
                    vin_part = addr_or_char[vin_start:].split(",")[0].strip()
                    vin = vin_part
                
                if vin and date_str:
                    deal_key = (predmet, date_str, vin)
                else:
                    deal_key = (predmet, date_str, addr_or_char[:50])  # Fallback
            # Для недвижимости используем кадастровый номер
            else:
                if cadastral and date_str:
                    deal_key = (predmet, date_str, cadastral)
                else:
                    # Fallback: используем адрес
                    address = (deal.get("Адрес_или_характеристики") or "")[:100]
                    deal_key = (predmet, date_str, address)
            
            # Проверяем, не добавлена ли уже эта сделка
            if deal_key in seen_keys:
                print(f"[DEALS_DEDUP] Skipping duplicate: {predmet} on {date_str}")
                continue
            
            seen_keys.add(deal_key)
            deduplicated_deals.append(deal)

        return deduplicated_deals

    @staticmethod
    def get_arbitration_court(address: str) -> str:
        """Определяет арбитражный суд по адресу регистрации должника.

        Args:
            address: Полный адрес регистрации

        Returns:
            Название арбитражного суда (например, "Арбитражный суд Челябинской области")
        """
        if not address:
            return "Арбитражный суд"

        prompt = (
            "Определи название арбитражного суда по адресу регистрации должника."
            " Верни ТОЛЬКО название суда без пояснений.\n\n"
            "КРИТИЧЕСКИ ВАЖНО:\n"
            "- Для федеральных городов: 'Арбитражный суд города Москвы', 'Арбитражный суд города Санкт-Петербурга'\n"
            "- Для областей: 'Арбитражный суд [название области с окончанием] области'\n"
            "  * Челябинская область → Арбитражный суд Челябинской области\n"
            "  * Свердловская область → Арбитражный суд Свердловской области\n"
            "  * Новосибирская область → Арбитражный суд Новосибирской области\n"
            "  * Московская область → Арбитражный суд Московской области\n"
            "- Для краев: 'Арбитражный суд [название края с окончанием] края'\n"
            "  * Краснодарский край → Арбитражный суд Краснодарского края\n"
            "  * Красноярский край → Арбитражный суд Красноярского края\n"
            "  * Алтайский край → Арбитражный суд Алтайского края\n"
            "- Для республик: 'Арбитражный суд Республики [название]'\n"
            "  * Республика Башкортостан → Арбитражный суд Республики Башкортостан\n"
            "  * Республика Татарстан → Арбитражный суд Республики Татарстан\n"
            "  * Республика Крым → Арбитражный суд Республики Крым\n\n"
            f"Адрес регистрации: {address}\n\n"
            "Алгоритм:\n"
            "1. Найди в адресе название субъекта РФ (область/край/республика/город федерального значения)\n"
            "2. Преобразуй название в родительный падеж (кого? чего?)\n"
            "3. Добавь 'Арбитражный суд' + название субъекта в правильном падеже\n\n"
            "Примеры:\n"
            "- г. Челябинск, Челябинская область → Арбитражный суд Челябинской области\n"
            "- г. Москва → Арбитражный суд города Москвы\n"
            "- Краснодарский край, г. Краснодар → Арбитражный суд Краснодарского края\n"
            "- Республика Башкортостан, г. Уфа → Арбитражный суд Республики Башкортостан"
        )

        try:
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {"role": "user", "content": prompt}
                ],
            )
            result = response.choices[0].message.content.strip()
            # Берем первую строку (на случай если GPT добавит пояснения)
            court_name = result.splitlines()[0].strip()
            return court_name
        except Exception:
            return "Арбитражный суд"

    @staticmethod
    def format_attachments(pdf_paths: List[Path], openai_client=None) -> str:
        """Форматирует список приложений из файлов через GPT.
        
        GPT добавляет обязательные документы в начало, добавляет "Копия" перед документами
        (кроме квитанций), группирует одинаковые документы.
        """
        if not pdf_paths:
            return ""

        import re
        from collections import Counter

        # Обрабатываем имена файлов
        processed_names = []
        for path in pdf_paths:
            # Убираем расширение
            name = path.stem

            # ВАЖНО: Заменяем ВСЕ подчеркивания на пробелы (для читаемости)
            name = name.replace('_', ' ')

            # Удаляем цифры в начале строки (с точкой или без)
            name_clean = re.sub(r'^\d+\.?\s*', '', name)

            # Убираем нумерацию в скобках типа (1), (2), (10) в конце
            name_clean = re.sub(r'\s*\(\d+\)\s*$', '', name_clean)

            # Убираем нумерацию типа _1_, _2_ в конце
            name_clean = re.sub(r'\s+\d+\s*$', '', name_clean)
            
            # Убираем множественные пробелы
            name_clean = re.sub(r'\s+', ' ', name_clean).strip()

            processed_names.append(name_clean)

        # Подсчитываем количество одинаковых документов
        name_counts = Counter(processed_names)
        
        # Формируем список уникальных документов с количеством
        unique_docs = []
        seen = set()
        
        for name_clean in processed_names:
            if name_clean in seen:
                continue
            seen.add(name_clean)
            count = name_counts[name_clean]
            
            if count > 1:
                unique_docs.append(f"{name_clean} — {count} шт.")
            else:
                unique_docs.append(name_clean)
        
        # Если нет OpenAI клиента, возвращаем простой список
        if not openai_client:
            # Добавляем обязательные документы вручную
            attachments = [
                "1. Заявление на банкротство",
                "2. Ходатайство об отсрочке",
                "3. Ходатайство о реализации имущества",
                "4. Список кредиторов и должников",
                "5. Опись имущества"
            ]
            
            counter = 6
            for doc in unique_docs:
                needs_copy = "квитанц" not in doc.lower()
                if needs_copy:
                    attachments.append(f"{counter}. Копия {doc}")
                else:
                    attachments.append(f"{counter}. {doc}")
                counter += 1

            return "{{LINE_BREAK}}".join(attachments)
        
        # Формируем промпт для GPT
        docs_list = "\n".join([f"- {doc}" for doc in unique_docs])
        
        prompt = f"""Сформируй пронумерованный список приложений к заявлению о банкротстве.

ОБЯЗАТЕЛЬНЫЕ ДОКУМЕНТЫ В НАЧАЛЕ (всегда добавляй, без "Копия"):
1. Заявление на банкротство
2. Ходатайство об отсрочке
3. Ходатайство о реализации имущества
4. Список кредиторов и должников
5. Опись имущества

ДОКУМЕНТЫ ИЗ СПИСКА (начиная с номера 6):
{docs_list}

ПРАВИЛА ФОРМАТИРОВАНИЯ:
1. Перед каждым документом из списка добавляй слово "Копия"
2. ИСКЛЮЧЕНИЕ: НЕ добавляй "Копия" перед почтовыми квитанциями (если в названии есть слово "квитанц")
3. Сохрани количество "— X шт." если оно указано
4. Нумерация должна быть сквозной (1, 2, 3... без пропусков)

ПРИМЕРЫ:
- "Копия Паспорт" -> "6. Копия Паспорт"
- "Справка о задолженности — 3 шт." -> "7. Копия Справка о задолженности — 3 шт."
- "Почтовая квитанция" -> "8. Почтовая квитанция" (БЕЗ "Копия")

Верни только готовый пронумерованный список, без комментариев."""

        try:
            response = openai_client.chat.completions.create(
                model="gpt-5-mini",
                messages=[{"role": "user", "content": prompt}],
            )
            
            result = response.choices[0].message.content.strip()
            return result.replace("\n", "{{LINE_BREAK}}")
            
        except Exception as e:
            print(f"[ERROR] Failed to format attachments with GPT: {e}")
            # Fallback на ручное форматирование
            attachments = [
                "1. Заявление на банкротство",
                "2. Ходатайство об отсрочке",
                "3. Ходатайство о реализации имущества",
                "4. Список кредиторов и должников",
                "5. Опись имущества"
            ]
            counter = 6
            for doc in unique_docs:
                needs_copy = "квитанц" not in doc.lower()
                if needs_copy:
                    attachments.append(f"{counter}. Копия {doc}")
                else:
                    attachments.append(f"{counter}. {doc}")
                counter += 1
            return "{{LINE_BREAK}}".join(attachments)

    @staticmethod
    def prepare_template_context(data_map: Dict[str, List[Dict[str, Any]]], pdf_paths: Optional[List[Path]] = None, openai_client=None) -> Dict[str, str]:
        # DEBUG: Показываем что пришло в prepare_template_context
        print(f"[DEBUG prepare_template_context] data_map keys: {list(data_map.keys())}")
        for key in ["отчет_окб", "отчет_бки", "отчет_нбки"]:
            if key in data_map:
                print(f"[DEBUG] {key}: {len(data_map[key])} отчетов")
                for idx, report in enumerate(data_map[key]):
                    report_keys = list(report.keys()) if isinstance(report, dict) else "NOT A DICT"
                    print(f"  [DEBUG] Отчет {idx+1} ключи: {report_keys}")
        
        passport = DocumentProcessor.select_first_entry(data_map, "паспорт")
        inn = DocumentProcessor.select_first_entry(data_map, "инн")
        snils = DocumentProcessor.select_first_entry(data_map, "снилс")
        labour = DocumentProcessor.select_first_entry(data_map, "трудовая")
        sziils = DocumentProcessor.select_first_entry(data_map, "сзиилс")
        
        # Документы супруга
        passport_spouse = DocumentProcessor.select_first_entry(data_map, "паспорт_супруга")
        inn_spouse = DocumentProcessor.select_first_entry(data_map, "инн_супруга")
        snils_spouse = DocumentProcessor.select_first_entry(data_map, "снилс_супруга")
        marriage_cert = DocumentProcessor.select_first_entry(data_map, "свидетельство_о_заключении_брака")
        
        # Дополнительные источники данных
        criminal_record = DocumentProcessor.select_first_entry(data_map, "справка_о_судимости")
        tax_accounts = DocumentProcessor.select_first_entry(data_map, "налоговая_счета")
        credit_history = DocumentProcessor.select_first_entry(data_map, "отчет_окб")
        if not credit_history:
            credit_history = DocumentProcessor.select_first_entry(data_map, "отчет_нбки")
        pension_account = DocumentProcessor.select_first_entry(data_map, "сзиилс")

        # === ПОЛУЧЕНИЕ ФИО ===
        # Приоритет: паспорт > ОКБ > справка о судимости > налоговая > электронная трудовая
        owner_fio = passport.get("ФИО", "") if passport else ""
        
        if not owner_fio and credit_history:
            owner_fio = credit_history.get("ФИО", "") or ""
        
        if not owner_fio and criminal_record:
            owner_fio = criminal_record.get("ФИО", "") or ""
        
        if not owner_fio and tax_accounts:
            owner_fio = tax_accounts.get("ФИО", "") or ""
        
        if not owner_fio and labour:
            owner_fio = labour.get("ФИО", "") or ""

        # Получаем девичью фамилию из справки о судимости (если есть)
        devichya_familiya = ""
        if criminal_record:
            devichya_familiya = criminal_record.get("Девичья_фамилия", "") or ""
        
        # Если есть девичья фамилия, формируем ФИО с ней в скобках
        # Формат: "Фамилия (Девичья) Имя Отчество"
        fio_with_maiden = owner_fio
        if devichya_familiya and owner_fio:
            fio_parts = owner_fio.split()
            if len(fio_parts) >= 3:
                # Если уже есть скобки в ФИО - не добавляем повторно
                if "(" not in owner_fio:
                    fio_with_maiden = f"{fio_parts[0]} ({devichya_familiya}) {' '.join(fio_parts[1:])}"

        # === ПОПЫТКА ИЗВЛЕЧЬ ДАННЫЕ СУПРУГА ИЗ СВИДЕТЕЛЬСТВА О БРАКЕ ===
        if not passport_spouse and marriage_cert and owner_fio:
            print("[INFO] Паспорт супруга не найден, пробуем извлечь из свидетельства о браке...")
            sp_fio_1 = marriage_cert.get("Супруг_ФИО")
            sp_fio_2 = marriage_cert.get("Супруга_ФИО")
            
            # Определяем кто из них должник
            debtor_is_husband = False
            debtor_is_wife = False
            
            # Вспомогательная функция для сравнения
            def _is_similar(a, b):
                if not a or not b: return False
                return difflib.SequenceMatcher(None, a.lower(), b.lower()).ratio() > 0.85
            
            if sp_fio_1 and _is_similar(owner_fio, sp_fio_1):
                debtor_is_husband = True
            elif sp_fio_2 and _is_similar(owner_fio, sp_fio_2):
                debtor_is_wife = True
                
            spouse_data = {}
            if debtor_is_husband and sp_fio_2:
                spouse_data["ФИО"] = sp_fio_2
                spouse_data["Дата_рождения"] = marriage_cert.get("Супруга_Дата_рождения")
                spouse_data["Место_рождения"] = marriage_cert.get("Супруга_Место_рождения")
            elif debtor_is_wife and sp_fio_1:
                spouse_data["ФИО"] = sp_fio_1
                spouse_data["Дата_рождения"] = marriage_cert.get("Супруг_Дата_рождения")
                spouse_data["Место_рождения"] = marriage_cert.get("Супруг_Место_рождения")

            if spouse_data.get("ФИО"):
                 print(f"[INFO] Извлечен супруг из свидетельства: {spouse_data['ФИО']}")
                 passport_spouse = spouse_data

        # === ПОЛУЧЕНИЕ ИНН ===
        # Приоритет: документ ИНН > справка о судимости > налоговая > кредитные отчеты
        inn_number = ""
        for inn_doc in data_map.get("инн", []):
            if isinstance(inn_doc, dict):
                inn_value = inn_doc.get("ИНН")
                if inn_value and inn_value != "null":
                    inn_number = inn_value
                    break

        if not inn_number and inn:
            inn_number = inn.get("ИНН", "") or ""
        
        if not inn_number and criminal_record:
            inn_number = criminal_record.get("ИНН", "") or ""
        
        if not inn_number and tax_accounts:
            inn_number = tax_accounts.get("ИНН", "") or ""
            
        # Очистка мусорных значений для корректной работы фоллбэка
        if inn_number and str(inn_number).lower().strip() in ["null", "none", "нет", "не указан", "отсутствует"]:
            inn_number = ""
        
        # Фоллбэк на справки о счетах (если там указан ИНН клиента)
        if not inn_number:
            for acc_doc in data_map.get("счета", []):
                if isinstance(acc_doc, dict):
                    # Проверяем ИНН в корне документа (где ФИО клиента)
                    cand_inn = acc_doc.get("ИНН")
                    # Проверяем на мусорное значение
                    if cand_inn and str(cand_inn).lower().strip() in ["null", "none"]:
                        continue
                        
                    if cand_inn and len(cand_inn) == 12: # Простая проверка на длину для физлица
                         inn_number = cand_inn
                         print(f"[ИНН] Найден ИНН в справке о счетах: {inn_number}")
                         break
        
        # Фоллбэк на кредитные отчеты (ОКБ, НБКИ, БКИ)
        if not inn_number and credit_history:
            inn_number = credit_history.get("ИНН_должника", "") or ""

        # === ПОЛУЧЕНИЕ СНИЛС ===
        # Приоритет: документ СНИЛС > лицевой счет ПФР > трудовая > СЗИИЛС
        snils_number = ""
        if snils:
            snils_number = snils.get("СНИЛС", "") or ""
        
        if not snils_number and pension_account:
            snils_number = pension_account.get("СНИЛС", "") or ""
        
        if not snils_number and labour:
            snils_number = labour.get("СНИЛС", "") or ""
        
        if not snils_number and sziils:
            snils_number = sziils.get("СНИЛС", "") or ""

        # Форматированная недвижимость (фильтруем по ФИО из паспорта)
        # Учитываем как полные выписки, так и уведомления об отсутствии (для долевой собственности)
        real_estate_detailed = DocumentProcessor.format_real_estate_detailed(
            data_map.get("егрн_выписка", []), 
            owner_fio=owner_fio,
            notification_list=data_map.get("егрн_уведомление", []),
            inventory_list=data_map.get("ценное_имущество", [])
        )

        average_income = DocumentProcessor.calculate_average_income(
            data_map.get("доходы", []), 
            data_map.get("пенсии", [])
        )
        accounts_count = DocumentProcessor.calculate_bank_accounts(data_map.get("счета", []))

        # Получаем адрес прописки должника
        адрес_прописки = passport.get("Прописка", "") if passport else ""
        
        # Если адрес не в паспорте, проверяем в ОКБ/НБКИ
        if not адрес_прописки and credit_history:
            адрес_прописки = credit_history.get("Адрес_регистрации", "") or ""

        # === НОРМАЛИЗАЦИЯ НАЗВАНИЙ КРЕДИТОРОВ ЧЕРЕЗ GPT ===
        # Собираем все уникальные названия кредиторов
        creditor_names = set()
        for source in ["отчет_окб", "отчет_нбки", "постановление_пристава"]:
            for doc in data_map.get(source, []):
                if isinstance(doc, dict):
                    creditor = doc.get("Кредитор") or doc.get("Наименование_кредитора") or doc.get("Взыскатель") or ""
                    if creditor:
                        creditor_names.add(creditor)
        
        # Нормализуем через GPT
        gpt_normalized = {}
        if creditor_names:
            gpt_normalized = DocumentProcessor.normalize_creditor_names_with_gpt(list(creditor_names))
            
            # Применяем нормализацию к исходным данным
            if gpt_normalized:
                for source in ["отчет_окб", "отчет_нбки", "постановление_пристава"]:
                    for doc in data_map.get(source, []):
                        if isinstance(doc, dict):
                            original = doc.get("Кредитор") or doc.get("Наименование_кредитора") or doc.get("Взыскатель") or ""
                            if original and original in gpt_normalized:
                                normalized = gpt_normalized[original]
                                # Обновляем название в документе
                                if "Кредитор" in doc:
                                    doc["Кредитор"] = normalized
                                if "Наименование_кредитора" in doc:
                                    doc["Наименование_кредитора"] = normalized
                                if "Взыскатель" in doc:
                                    doc["Взыскатель"] = normalized

        # Таблица кредиторов (передаем ФИО, ИНН, СНИЛС, адрес)
        # ВАЖНО: формируем таблицу ПЕРЕД расчетом общей суммы долга
        creditors_table_data = DocumentProcessor.format_creditors_table(
            data_map, 
            debtor_fio=owner_fio,
            debtor_inn=inn_number,
            debtor_snils=snils_number,
            debtor_address=адрес_прописки
        )

        # НОВОЕ: Рассчитываем общую сумму долга из таблиц кредиторов и налогов
        total_debt = Decimal(0)

        # Суммируем долги из таблицы кредиторов (credits)
        for credit in creditors_table_data.get("credits", []):
            # Берем "Задолженность_в_том_числе" (текущая задолженность)
            debt_str = credit.get("Задолженность_в_том_числе", "0")
            debt_amount = DocumentProcessor.parse_decimal(debt_str) or Decimal(0)
            total_debt += debt_amount

            # Добавляем штрафы и пени
            penalties_str = credit.get("Штрафы_пени_и_другое", "0")
            penalties_amount = DocumentProcessor.parse_decimal(penalties_str) or Decimal(0)
            total_debt += penalties_amount

        # Суммируем долги из таблицы налогов (taxes)
        for tax in creditors_table_data.get("taxes", []):
            # Берем сумму обязательного платежа
            tax_str = tax.get("Сумма_обяз_платежа", "0")
            tax_amount = DocumentProcessor.parse_decimal(tax_str) or Decimal(0)
            total_debt += tax_amount

            # Добавляем штрафы и пени по налогам
            tax_penalties_str = tax.get("Штрафы_пени_и_другое_обяз_платежа", "0")
            tax_penalties_amount = DocumentProcessor.parse_decimal(tax_penalties_str) or Decimal(0)
            total_debt += tax_penalties_amount

        # === ПАСПОРТНЫЕ ДАННЫЕ (с fallback на ОКБ) ===
        # Приоритет: паспорт > ОКБ
        series = passport.get("Серия") if passport else ""
        number = passport.get("Номер") if passport else ""
        
        # Если нет в паспорте, берем из ОКБ
        if not series and credit_history:
            series = credit_history.get("Серия_паспорта", "") or ""
        if not number and credit_history:
            number = credit_history.get("Номер_паспорта", "") or ""
        
        if series and number:
            series_number = f"{series} {number}"
        else:
            series_number = str(series or number or "")

        # Форматирование общей суммы долга прописью
        debt_formatted = ""
        if total_debt is not None and total_debt > 0:
            debt_formatted = DocumentProcessor.number_to_words_rubles(total_debt)

        # Проверка семейного положения и детей
        marital_children_text = DocumentProcessor.check_marital_and_children_status(passport, data_map)

        # Фамилия и инициалы - ВАЖНО: Делаем это ДО создания текстов с местоимениями
        fio_initials = passport.get("Фамилия_инициалы", "") if passport else ""
        fio_initials_rp = passport.get("Фамилия_инициалы_рп", "") if passport else ""

        # Используем owner_fio (уже с девичьей фамилией в скобках, если она есть)
        full_name = owner_fio
        if not fio_initials_rp and full_name:
            fio_initials_rp = DocumentProcessor.convert_to_genitive_initials(full_name)

        # Дательный падеж (кому?) - инициалы
        fio_initials_dp = DocumentProcessor.convert_to_dative_initials(full_name)

        # НОВОЕ: Полные ФИО в разных падежах (без инициалов)
        fio_full_rp = DocumentProcessor.convert_to_genitive_full(full_name)  # Кузьмича Николая Павловича
        fio_full_dp = DocumentProcessor.convert_to_dative_full(full_name)    # Кузьмичу Николаю Павловичу
        fio_full_vp = DocumentProcessor.convert_to_accusative_full(full_name) # Кузьмича Николая Павловича

        # Разбиваем ФИО на отдельные компоненты
        # Приоритет: справка о судимости (там поля без девичьей фамилии) > паспорт > простой split
        if criminal_record and criminal_record.get("Фамилия"):
            familia = criminal_record.get("Фамилия", "")
            imya = criminal_record.get("Имя", "")
            otchestvo = criminal_record.get("Отчество", "")
        elif passport and passport.get("Фамилия") and passport.get("Имя"):
            # Берем из паспорта, так как там поля разделены корректно
            familia = passport.get("Фамилия", "")
            imya = passport.get("Имя", "")
            otchestvo = passport.get("Отчество", "")
            
            # ВАЖНО: Проверяем, не попала ли девичья фамилия в поле "Имя" или "Отчество"
            prev_name = passport.get("Прежние_имена_фамилия_отчества") or passport.get("Прежняя_фамилия") or ""
            if prev_name:
                # Нормализуем для сравнения
                prev_lower = prev_name.lower().strip()
                # Если девичья фамилия попала в отчество - убираем отчество
                if otchestvo.lower().strip() == prev_lower:
                    otchestvo = ""
                # Если девичья фамилия попала в имя (очень странно, но вдруг)
                if imya.lower().strip() == prev_lower:
                    # Пытаемся восстановить имя из полного ФИО если возможно, или оставляем пустым
                    imya = "" # Тут сложно восстановить автоматически
        else:
            fio_parts = full_name.split() if full_name else []
            familia = fio_parts[0] if len(fio_parts) > 0 else ""
            
            # Обработка девичьей фамилии в скобках при разбивке через split
            # Пример: Иванова (Сидорова) Анна Петровна
            # parts: [Иванова, (Сидорова), Анна, Петровна]
            
            cleaned_parts = []
            for part in fio_parts:
                # Пропускаем части в скобках (девичьи фамилии)
                if "(" in part or ")" in part:
                    continue
                cleaned_parts.append(part)
            
            # Заново распределяем
            if len(cleaned_parts) > 0:
                familia = cleaned_parts[0]
            if len(cleaned_parts) > 1:
                imya = cleaned_parts[1]
            else:
                imya = ""
            if len(cleaned_parts) > 2:
                otchestvo = " ".join(cleaned_parts[2:])
            else:
                otchestvo = ""

        # Определяем пол
        gender = DocumentProcessor.detect_gender(full_name)

        # Местоимения в зависимости от пола
        pronoun_accusative = DocumentProcessor.get_gender_pronoun(gender, "accusative")  # его/её
        pronoun_genitive = DocumentProcessor.get_gender_pronoun(gender, "genitive")  # него/неё
        pronoun_dative = DocumentProcessor.get_gender_pronoun(gender, "dative")  # ему/ей

        # Административная ответственность (по умолчанию нет)
        admin_responsibility = "Должник не привлекался к административной ответственности за мелкое хищение, умышленное уничтожение или повреждение имущества, неправомерные действия при банкротстве, фиктивное или преднамеренное банкротство."

        # Уголовная ответственность
        # Если есть справка о судимости, используем её данные
        if criminal_record and criminal_record.get("Сведения_о_судимости"):
            судимость_текст = criminal_record.get("Сведения_о_судимости", "").strip()
            # Если в справке указано, что судимости нет (содержит "не имеется", "отсутствует" и т.п.)
            if any(word in судимость_текст.lower() for word in ["не имеется", "отсутствует", "не было", "нет"]):
                criminal_text = f"В отношении должника не имеется сведений об уголовных и административных делах в отношении {pronoun_genitive}, а также о наличии неснятой или непогашенной судимости. {судимость_текст}"
            else:
                # Если есть судимость, используем текст из справки
                criminal_text = f"Сведения о судимости: {судимость_текст}"
        else:
            # По умолчанию (если нет справки о судимости)
            criminal_text = f"В отношении должника не имеется сведений об уголовных и административных делах в отношении {pronoun_genitive}, а также о наличии неснятой или непогашенной судимости."

        # =========================================================
        # УЛУЧШЕННАЯ ЛОГИКА ДЛЯ ПАСПОРТНЫХ ДАННЫХ (с fallback)
        # =========================================================
        
        # --- Место рождения ---
        # 1. Сначала пробуем из паспорта
        place_of_birth = passport.get("Место_рождения") if passport else None
        is_birth_bad = passport.get("Место_рождения_не_распознано", False) if passport else True
        
        # 2. Если помечено как "не распознано" или пустое, пробуем из справки о судимости
        criminal_record = next(iter(data_map.get("справка_о_судимости", [])), None)
        if (not place_of_birth or is_birth_bad) and criminal_record:
            temp_place_of_birth = criminal_record.get("Место_рождения")
            if temp_place_of_birth:
                place_of_birth = temp_place_of_birth
                print("      [DEBUG] Место рождения восстановлено из Справки о судимости (было не распознано)")

        # 3. Если нигде нет
        if not place_of_birth:
            # place_of_birth = "НЕ УДАЛОСЬ РАСПОЗНАТЬ ТЕКСТ (Место рождения)"
            place_of_birth = RichText("НЕ УДАЛОСЬ РАСПОЗНАТЬ ТЕКСТ (Место рождения)", color="FF0000", bold=True)
        
        # --- Адрес регистрации ---
        # 1. Сначала пробуем из паспорта
        raw_registration = passport.get("Прописка") or passport.get("Адрес_регистрации") if passport else None
        is_reg_bad = passport.get("Прописка_не_распознана", False) if passport else True
        registration_address = raw_registration

        # Проверка на "плохое" распознавание + флаг от модели
        # Если raw_registration вообще нет (None), то len() вызовет ошибку, поэтому проверяем порядок условий
        is_really_bad = False
        if is_reg_bad:
            is_really_bad = True
        elif not raw_registration:
             is_really_bad = True
        elif isinstance(raw_registration, str) and (len(raw_registration) < 10 or "не читаем" in raw_registration.lower()):
             is_really_bad = True

        # 2. Поиск в кредитных отчетах
        if is_really_bad:
            print("      [DEBUG] Адрес регистрации: рукописный текст или ошибка. Поиск в кредитных отчетах...")
            found_addr = None
            
            # Собираем всех должников из всех отчетов
            # В process_pdf репорт парсится в структуру {"Кредиты": [...], "Адрес_регистрации": "..."}
            # data_map["отчет_окб"] - это список таких структур
            
            for report_type in ["отчет_окб", "отчет_бки", "отчет_нбки"]:
                reports = data_map.get(report_type, [])
                for report_doc in reports:
                    # Ищем ключи адреса
                    candidate = report_doc.get("Адрес_регистрации") or report_doc.get("Адрес") or report_doc.get("Прописка")
                    if candidate and len(candidate) > 15:
                        found_addr = candidate
                        print(f"      [DEBUG] Адрес восстановлен из {report_type}")
                        break
                if found_addr:
                    break
            
            if found_addr:
                registration_address = found_addr
        
        # 3. Если всё ещё нет адреса
        if not registration_address or len(registration_address) < 5:
            # registration_address = "НЕ УДАЛОСЬ РАСПОЗНАТЬ ТЕКСТ (Адрес регистрации)"
            registration_address = RichText("НЕ УДАЛОСЬ РАСПОЗНАТЬ ТЕКСТ (Адрес регистрации)", color="FF0000", bold=True)

        # Парсим адрес прописки на компоненты через GPT
        # ВАЖНО: Если адрес был восстановлен (registration_address содержит значение), используем его!
        # Ранее использовалось только passport.get("Прописка"), что игнорировало восстановленный адрес
        final_address_for_parsing = registration_address if registration_address and not isinstance(registration_address, RichText) else (passport.get("Прописка", "") if passport else "")
        
        # Если это RichText (ошибка), то парсить не будем
        if isinstance(final_address_for_parsing, RichText) or "НЕ УДАЛОСЬ" in str(final_address_for_parsing):
             адрес_части = DocumentProcessor.parse_address("")
        elif openai_client and final_address_for_parsing:
            print(f"      Разбор адреса через GPT: {final_address_for_parsing}")
            адрес_части = DocumentProcessor.parse_address_with_gpt(str(final_address_for_parsing), openai_client)
        else:
             адрес_части = DocumentProcessor.parse_address(str(final_address_for_parsing) if final_address_for_parsing else "")

        # Получаем текущую дату по Екатеринбургскому времени
        date_components = DocumentProcessor.get_yekaterinburg_date()

        context: Dict[str, Any] = {
            # Основные данные
            "ФИО": fio_with_maiden,
            "Фамилия": familia,
            "Имя": imya,
            "Отчество": otchestvo,
            "Девичья_фамилия": devichya_familiya,
            "Прежние_имена_фамилия_отчества": devichya_familiya,  # Алиас для девичьей фамилии

            # Дата подачи заявления (по Екатеринбургскому времени)
            "АДень": date_components["АДень"],
            "АМесяц": date_components["АМесяц"],
            "АГод": date_components["АГод"],
            "ААГод": date_components["ААГод"],

            # С инициалами (Иванова И.И.)
            "Фамилия_инициалы": fio_initials,
            "Фамилия_инициалы_рп": fio_initials_rp,  # родительный падеж (кого? - Иванова И.И.)
            "Фамилия_инициалы_дп": fio_initials_dp,  # дательный падеж (кому? - Иванову И.И.)

            # НОВОЕ: Полные ФИО без инициалов
            "ФИО_рп": fio_full_rp,  # родительный падеж (кого? - Иванова Ивана Ивановича)
            "ФИО_дп": fio_full_dp,  # дательный падеж (кому? - Иванову Ивану Ивановичу)
            "ФИО_вп": fio_full_vp,  # винительный падеж (кого? - Иванова Ивана Ивановича)

            "Пол": gender,  # "м" или "ж"
            "его_её": pronoun_accusative,  # "его" или "её" (винительный)
            "него_неё": pronoun_genitive,  # "него" или "неё" (родительный)
            "ему_ей": pronoun_dative,  # "ему" или "ей" (дательный)
            "обратился_ась": "обратился" if gender == "м" else "обратилась",  # глагол прошедшего времени
            "Адрес_регистрации": registration_address,
            # Дата рождения: приоритет паспорт > ОКБ
            "Дата_рождения": (passport.get("Дата_рождения", "") if passport else "") or (credit_history.get("Дата_рождения", "") if credit_history else ""),
            # Место рождения: приоритет паспорт > ОКБ
            "Место_рождения": place_of_birth,

            # Арбитражный суд (определяется по адресу регистрации)
            "Арбитражный_суд": DocumentProcessor.get_arbitration_court(registration_address) if isinstance(registration_address, str) else "Арбитражный суд",

            # Компоненты адреса прописки
            "Субъект_прописка": адрес_части.get("субъект", ""),
            "Район_прописка": адрес_части.get("район", ""),
            "Города_прописка": адрес_части.get("город", ""),
            "Населенный_пункт_прописка": адрес_части.get("населенный_пункт", ""),
            "Улица_прописка": адрес_части.get("улица", ""),
            "Номер_дома_прописка": адрес_части.get("дом", ""),
            "Номер_корпуса_прописка": адрес_части.get("корпус", ""),
            "Номер_квартиры_прописка": адрес_части.get("квартира", ""),

            # Паспорт (с fallback на ОКБ - эта логика была старая, обновляем её)
            "Серия_и_номер_пас": series_number,
            
            # Обновленные данные
            "Прописка": registration_address, # Алиас

            # Кем выдан: приоритет паспорт > ОКБ
            "КемВыданПаспорт": (passport.get("Кем_выдан", "") if passport else "") or (credit_history.get("Кем_выдан", "") if credit_history else ""),
            "Кем_выдан_пас": (passport.get("Кем_выдан", "") if passport else "") or (credit_history.get("Кем_выдан", "") if credit_history else ""),
            # Дата выдачи: приоритет паспорт > ОКБ
            "Когда_выдан_пас": (passport.get("Дата_выдачи", "") if passport else "") or (credit_history.get("Дата_выдачи", "") if credit_history else ""),
            "Когда_выда_пас": (passport.get("Дата_выдачи", "") if passport else "") or (credit_history.get("Дата_выдачи", "") if credit_history else ""),
            # Код подразделения: приоритет паспорт > ОКБ
            "Код_подразделения": (passport.get("Код_подразделения", "") if passport else "") or (credit_history.get("Код_подразделения", "") if credit_history else ""),

            # Документы
            "Номер_снилс": snils_number,
            "Номер_инн": inn_number,
            "Снилс": snils_number,  # Алиас для шаблонов
            "ИНН": inn_number,       # Алиас для шаблонов
            "Инн": inn_number,       # Альтернативный алиас

            # Работа
            "Место_работы": DocumentProcessor.extract_employers(labour) if labour else "",
            "Текущ_место_работы": DocumentProcessor.extract_current_job(labour) if labour else "Нет",

            # Супруг (заинтересованное лицо)
            "Заинтересованное_лицо": DocumentProcessor.format_interested_party(passport_spouse, inn_spouse, snils_spouse),

            # Дети и брак
            "Женатзамужем": DocumentProcessor.get_marital_status_key(passport, data_map),
            "Несовершеннолетние_дети": DocumentProcessor.determine_children_status(
                passport.get("Дети") if passport else None,
                data_map
            ),

            # Финансы
            "Общая_сумма_долга": debt_formatted,
            "Средняя_зп_за_месяц": DocumentProcessor.format_decimal(average_income) if average_income else "",
            "Количество_счетов_в_банках": str(accounts_count) if accounts_count is not None else "0",
            "Количество_счетов_текстом": DocumentProcessor.format_count_with_word(
                accounts_count if accounts_count is not None else 0,
                "счет", "счета", "счетов"
            ),

            # Имущество
            "Недвижимое_имущество": real_estate_detailed,
            "Движемое_имущество": DocumentProcessor.format_movable_property(data_map.get("гибдд", [])),
            "Акции_и_иное_участие_в_ком_организациях": DocumentProcessor.format_shares(data_map.get("учредитель", [])),
            "Ценные_бумаги": DocumentProcessor.format_securities(data_map.get("ценные_бумаги", [])),
            "Наличные_и_иное_ценное_имущество": DocumentProcessor.format_cash_valuables(data_map.get("ценное_имущество", [])),
            "имущественные_права": DocumentProcessor.format_property_rights(data_map.get("имущественные_права", [])),

            # Доходы
            "Иные_доходы": DocumentProcessor.format_other_income(
                data_map.get("пенсии", []),
                data_map.get("выплаты", [])
            ),

            # Текстовые блоки
            "Нету_детей_и_брака": marital_children_text,
            "Детей_и_брак": marital_children_text,  # Альтернативное имя ключа для совместимости
            "Административная_ответственность": admin_responsibility,
            "Уголовная_ответственность": criminal_text,

            # Приложения (формируем из списка файлов)
            "Приложения": DocumentProcessor.format_attachments(pdf_paths or [], openai_client),
        }

        # Добавляем данные таблицы кредиторов
        context.update(creditors_table_data)

        # Добавляем данные для описи имущества
        bank_accounts_data = DocumentProcessor.format_bank_accounts_table(data_map.get("счета", []))
        context.update(bank_accounts_data)

        realty_data = DocumentProcessor.format_realty_table(data_map.get("егрн_выписка", []), owner_fio=owner_fio)
        context.update(realty_data)

        vehicles_data = DocumentProcessor.format_vehicles_table(data_map.get("гибдд", []))
        context.update(vehicles_data)

        # Фоллбэки для пустых значений
        fallbacks = {
            "Адрес_регистрации": "не указано",
            "Дата_рождения": "не указано",
            "Место_рождения": "не указано",
            "Серия_и_номер_пас": "не указано",
            "КемВыданПаспорт": "не указано",
            "Кем_выдан_пас": "не указано",
            "Когда_выдан_пас": "не указано",
            "Когда_выда_пас": "не указано",
            "Код_подразделения": "не указано",
            "Номер_снилс": "не указано",
            "Номер_инн": "не указано",
            "Место_работы": "не указано",
            "Средняя_зп_за_месяц": "не указано",
        }

        for key, fallback in fallbacks.items():
            if not context.get(key):
                context[key] = fallback

        # ВАЖНО: Собираем все сделки из разных документов и добавляем обратно в data_map
        # Это нужно для API endpoint /api/debtors/<id>/deals
        all_deals = DocumentProcessor.extract_deals_from_all_documents(data_map, owner_fio)
        if all_deals:
            # Добавляем собранные сделки в data_map, чтобы они сохранились в raw_data
            data_map["сделки"] = all_deals

        # КРИТИЧЕСКИ ВАЖНО: Заменяем все None на пустые строки во всём контексте
        # Это предотвращает появление "None" в Word документах
        context = DocumentProcessor.replace_none_with_empty(context)

        return context

    @staticmethod
    def replace_none_with_empty(obj):
        """Рекурсивно заменяет все None на пустые строки в словарях и списках.

        Args:
            obj: Объект для обработки (dict, list или любой другой тип)

        Returns:
            Обработанный объект с заменёнными None
        """
        if isinstance(obj, dict):
            return {key: DocumentProcessor.replace_none_with_empty(value) for key, value in obj.items()}
        elif isinstance(obj, list):
            return [DocumentProcessor.replace_none_with_empty(item) for item in obj]
        elif obj is None:
            return ""
        else:
            return obj

    @staticmethod
    def merge_table_cells_for_creditors(doc_path: Path, credits_data: List[Dict]) -> None:
        """Объединяет ячейки в таблице кредиторов для одного кредитора с несколькими договорами."""
        from docx import Document
        from docx.oxml import parse_xml
        from docx.oxml.ns import nsdecls
        
        doc = Document(doc_path)
        
        # Ищем таблицу кредиторов — 9 столбцов и есть "Кредитор" в первых 4 строках
        target_table = None
        for table_idx, table in enumerate(doc.tables):
            if len(table.rows) > 2 and len(table.columns) == 9:
                # Проверяем первые 4 строки на наличие "Кредитор"
                found_kreditor = False
                for row_idx in range(min(4, len(table.rows))):
                    row_text = ' '.join(cell.text for cell in table.rows[row_idx].cells)
                    if "Кредитор" in row_text:
                        found_kreditor = True
                        break
                
                if found_kreditor:
                    target_table = table
                    print(f"      [MERGE] Найдена таблица кредиторов: таблица #{table_idx}, строк: {len(table.rows)}, столбцов: {len(table.columns)}")
                    break
        
        if not target_table:
            print(f"      [MERGE] Таблица кредиторов не найдена")
            return
        
        # Находим первую строку данных (пропускаем заголовки)
        data_start_row = 0
        data_end_row = len(target_table.rows) - 1
        
        for row_idx, row in enumerate(target_table.rows):
            # Ищем первую строку где есть № п/п но это не заголовок
            if row_idx > 0 and row.cells[0].text.strip() and row.cells[0].text.strip() != '№ п/п' and row.cells[0].text.strip() != '№\nп/п':
                # Проверяем, есть ли "1.1" или похожее в столбце 0
                if '.' in row.cells[0].text or row.cells[0].text.strip() == '':
                    data_start_row = row_idx
                    break
        
        # Находим конец раздела кредиторов (до начала "Обязательные платежи")
        for row_idx in range(data_start_row, len(target_table.rows)):
            row_text = ' '.join(cell.text for cell in target_table.rows[row_idx].cells).lower()
            if 'обязательные платежи' in row_text or 'налог' in row_text:
                data_end_row = row_idx - 1
                break
        
        if data_start_row == 0:
            return
        
        # Строим группы: для каждого кредитора находим строки в таблице с его договорами
        # Группируем по № п/п в столбце 0 (например "1.1", "1.2" и т.д.)
        creditor_groups = []  # [(first_row, last_row), ...]
        current_pp = None
        first_row_of_group = None
        
        print(f"      [MERGE] Анализ строк от {data_start_row} до {data_end_row}...")
        
        for row_idx in range(data_start_row, data_end_row + 1):
            pp_text = target_table.rows[row_idx].cells[0].text.strip()
            print(f"      [MERGE]   Строка {row_idx}: № п/п = '{pp_text}'")
            
            # Если это новый № п/п (не пустой)
            if pp_text and '.' in pp_text:
                # Закрываем предыдущую группу
                if current_pp is not None and first_row_of_group is not None:
                    if row_idx - first_row_of_group > 1:  # Группа больше 1 строки
                        creditor_groups.append((first_row_of_group, row_idx - 1))
                        print(f"      [MERGE]     → Сохранена группа '{current_pp}': строки {first_row_of_group} - {row_idx - 1}")
                
                # Начинаем новую группу
                current_pp = pp_text
                first_row_of_group = row_idx
            
            # Если это пустой № п/п, продолжаем текущую группу
            elif not pp_text and current_pp is not None:
                pass  # Группа продолжается
        
        # Закрываем последнюю группу
        if current_pp is not None and first_row_of_group is not None:
            if data_end_row - first_row_of_group >= 1:  # Группа больше 1 строки
                creditor_groups.append((first_row_of_group, data_end_row))
                print(f"      [MERGE]     → Сохранена последняя группа '{current_pp}': строки {first_row_of_group} - {data_end_row}")
        
        print(f"      [MERGE] Найдено групп для объединения: {len(creditor_groups)}")
        
        # Объединяем ячейки для каждой группы
        # Столбцы для объединения: 0 (№ п/п), 2 (Кредитор), 3-4 (Адрес - два столбца)
        columns_to_merge = [0, 2, 3, 4]
        
        for first_row_idx, last_row_idx in creditor_groups:
            print(f"      [MERGE] Объединяем группу: строки {first_row_idx} - {last_row_idx}")
            
            for col_idx in columns_to_merge:
                if col_idx >= len(target_table.columns):
                    continue
                
                try:
                    # Устанавливаем vMerge для первой ячейки
                    first_cell = target_table.rows[first_row_idx].cells[col_idx]
                    first_cell_xml = first_cell._element.get_or_add_tcPr()
                    vmerge = parse_xml(f'<w:vMerge {nsdecls("w")} w:val="restart"/>')
                    first_cell_xml.append(vmerge)
                    
                    # Устанавливаем vMerge для остальных ячеек группы
                    for row_idx in range(first_row_idx + 1, last_row_idx + 1):
                        if row_idx >= len(target_table.rows):
                            break
                        cell = target_table.rows[row_idx].cells[col_idx]
                        cell_xml = cell._element.get_or_add_tcPr()
                        vmerge_continue = parse_xml(f'<w:vMerge {nsdecls("w")}/>')
                        cell_xml.append(vmerge_continue)
                except Exception as e:
                    print(f"          [DEBUG] Ошибка объединения столбца {col_idx}: {e}")
        
        doc.save(doc_path)

    @staticmethod
    def fill_docx_template_dynamic(template_path: Path, output_path: Path, context: Dict[str, Any]) -> None:
        """Заполняет Word шаблон с поддержкой динамических таблиц (Jinja2)."""
        doc = DocxTemplate(template_path)

        # Обрабатываем Приложения, превращая переносы строк в Soft Breaks (<w:br/>) через RichText
        if "Приложения" in context and isinstance(context["Приложения"], str):
            text = context["Приложения"]
            if "\n" in text or "{{LINE_BREAK}}" in text:
                try:
                    text_norm = text.replace("{{LINE_BREAK}}", "\n").strip()
                    lines = [line.strip() for line in text_norm.split('\n') if line.strip()]
                    
                    rt = RichText()
                    for i, line in enumerate(lines):
                        # Force Times New Roman 12pt (size 24 half-points)
                        rt.add(line, font='Times New Roman', size=24)
                        if i < len(lines) - 1:
                            rt.add('\n') # \n в RichText = <w:br/> (Shift+Enter)
                    
                    context["Приложения"] = rt
                except Exception as e:
                    print(f"      [WARNING] Error converting Attachments to RichText: {e}")

        # Рендерим шаблон с контекстом
        doc.render(context)

        # Создаем папку если нужно
        output_path.parent.mkdir(parents=True, exist_ok=True)

        # Сохраняем результат
        doc.save(output_path)
        
        # Если это список кредиторов, объединяем ячейки
        if "Список кредиторов" in str(template_path) and "credits" in context:
            try:
                DocumentProcessor.merge_table_cells_for_creditors(output_path, context["credits"])
            except Exception as e:
                print(f"      [WARNING] Не удалось объединить ячейки: {e}")

    @staticmethod
    def fill_docx_template(template_path: Path, output_path: Path, context: Dict[str, str]) -> None:
        """Старая функция для простых шаблонов без динамических таблиц."""
        # Фильтруем списки из контекста (они для динамических шаблонов)
        filtered_context = {
            k: v for k, v in context.items() 
            if not isinstance(v, (list, dict)) or k in []
        }

        with zipfile.ZipFile(template_path, "r") as template_zip:
            xml = template_zip.read("word/document.xml").decode("utf-8")

        # Нормализуем XML: убираем разрывы внутри фигурных скобок {{...}}
        # Word может разбить {{Ключ}} на несколько тегов, что ломает замену
        import re

        # Паттерн для поиска {{ключ}}, разбитых на части
        # Находим {, затем любые символы и теги до }
        pattern = r'\{\{([^}]*)\}\}'

        def normalize_key(match):
            # Извлекаем всё между {{ и }}
            inner = match.group(1)
            # Удаляем все XML теги внутри
            clean = re.sub(r'<[^>]+>', '', inner)
            # Убираем неразрывные пробелы и лишние пробелы по бокам
            clean = clean.replace('\u00A0', ' ').strip()
            # Возвращаем нормализованный ключ
            return f"{{{{{clean}}}}}"

        xml = re.sub(pattern, normalize_key, xml, flags=re.DOTALL)

        # Теперь делаем замену (используем отфильтрованный контекст)
        for key, value in filtered_context.items():
            # Приводим значение к строке, чтобы корректно вставлять в XML
            replacement = str(value) if value is not None else ""
            xml = xml.replace(f"{{{{{key}}}}}", replacement)

        # Заменяем {{LINE_BREAK}} на простой перенос строки в Word
        # Используем <w:br/> для переноса строки внутри одного параграфа
        # Это сохраняет ВСЕ форматирование параграфа (шрифт, размер, интервалы, отступы)
        line_break_xml = '</w:t><w:br/><w:t xml:space="preserve">'
        xml = xml.replace("{{LINE_BREAK}}", line_break_xml)

        output_path.parent.mkdir(parents=True, exist_ok=True)
        with zipfile.ZipFile(template_path, "r") as template_zip, zipfile.ZipFile(
            output_path, "w"
        ) as output_zip:
            for item in template_zip.infolist():
                data = template_zip.read(item.filename)
                if item.filename == "word/document.xml":
                    data = xml.encode("utf-8")
                output_zip.writestr(item, data)

    @staticmethod
    def fill_template(template_path: Path, output_path: Path, context: Dict[str, Any]) -> None:
        """Compatibility wrapper used by `app.py`.

        Tries to render the template as a dynamic Jinja2/DocxTemplate first,
        and falls back to the older XML-replacement implementation if needed.
        """
        # Ensure context has no None values to avoid literal 'None' in documents
        safe_context = DocumentProcessor.replace_none_with_empty(context)

        # First try dynamic rendering
        try:
            DocumentProcessor.fill_docx_template_dynamic(template_path, output_path, safe_context)
            return
        except Exception as e_dynamic:
            # Fallback to simple XML replacement
            try:
                print(f"      INFO - Dynamic render failed for {template_path.name}: {e_dynamic}. Falling back to simple replacement.")
                DocumentProcessor.fill_docx_template(template_path, output_path, safe_context)
                return
            except Exception as e_simple:
                # Re-raise a combined error to help debugging
                raise RuntimeError(f"Both dynamic and simple template rendering failed for {template_path}: dynamic_error={e_dynamic}, simple_error={e_simple}")

    @staticmethod
    def render_bankruptcy_application(context: Dict[str, str]) -> Optional[Path]:
        if not TEMPLATE_DOCX.exists():
            return None
        # Создаем папку для результатов
        OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
        # Сохраняем в папку resultdoc
        output_name = f"{TEMPLATE_DOCX.stem}{FILLED_TEMPLATE_SUFFIX}{TEMPLATE_DOCX.suffix}"
        output_path = OUTPUT_DIR / output_name
        DocumentProcessor.fill_docx_template(TEMPLATE_DOCX, output_path, context)
        return output_path

    @staticmethod
    def generate_all_documents(context: Dict[str, str], debtor_id: Optional[str] = None, lawyer: Optional[str] = None) -> List[Path]:
        """Generate all bankruptcy document templates and return list of paths.
        
        Args:
            context: Template context data
            debtor_id: Unique debtor identifier for file separation
        """
        # Если указан debtor_id, создаем отдельную папку для должника
        if debtor_id:
            output_dir = OUTPUT_DIR / debtor_id
        else:
            output_dir = OUTPUT_DIR
            
        output_dir.mkdir(parents=True, exist_ok=True)
        generated_files = []

        templates = [
            {
                "name": "Заявление о банкротстве",
                "template": None,
                "output": output_dir / "Заявление на банкротство (заполненное).docx",
                "dynamic": False
            },
            {
                "name": "Ходатайство об отсрочке",
                "template": Path("templ") / "Ходатайство об отсрочке.docx",
                "output": output_dir / "Ходатайство об отсрочке (заполненное).docx",
                "dynamic": False
            },
            {
                "name": "Ходатайство о реализации имущества",
                "template": Path("templ") / "Ходатайство о реализации имущества.docx",
                "output": output_dir / "Ходатайство о реализации имущества (заполненное).docx",
                "dynamic": False
            },
            {
                "name": "Список кредиторов и должников",
                "template": Path("templ") / "Список кредиторов и должников.docx",
                "output": output_dir / "Список кредиторов и должников (заполненное).docx",
                "dynamic": True
            },
            {
                "name": "Опись имущества",
                "template": Path("templ") / "Опись имущества.docx",
                "output": output_dir / "Опись имущества (заполненное).docx",
                "dynamic": True
            },
        ]
        # Map template names to possible filenames (same order)
        template_filenames = [
            "Заявление на банкротство.docx",
            "Ходатайство об отсрочке.docx",
            "Ходатайство о реализации имущества.docx",
            "Список кредиторов и должников.docx",
            "Опись имущества.docx",
        ]

        for idx, item in enumerate(templates):
            # Resolve template path: prefer templ/<lawyer>/<filename> if lawyer provided
            filename = template_filenames[idx]
            candidate_paths = []
            if lawyer:
                candidate_paths.append(Path("templ") / lawyer / filename)
            candidate_paths.append(Path("templ") / filename)

            template_path = None
            for p in candidate_paths:
                if p.exists():
                    template_path = p
                    break
            output_path = item["output"]
            is_dynamic = item.get("dynamic", False)

            if not template_path or not template_path.exists():
                # No template found for this item (neither lawyer-specific nor default)
                # Skip generating this document
                # print debug info for visibility
                print(f"      INFO - No template found for '{item['name']}' (checked: {[str(p) for p in candidate_paths]})")
                continue
            else:
                print(f"      INFO - Using template for '{item['name']}': {template_path}")

            try:
                # Use compatibility wrapper that first tries DocxTemplate (dynamic rendering)
                # and falls back to simple XML replacement. This handles placeholders
                # that Word splits across multiple runs.
                DocumentProcessor.fill_template(template_path, output_path, context)

                generated_files.append(output_path)
            except PermissionError:
                print(f"      WARNING - File is open: {output_path.name}")
                continue
            except Exception as e:
                print(f"      ERROR generating {item['name']}: {e}")
                continue

        return generated_files

    @staticmethod
    def process_pdf_with_gpt(pdf_path: Path, prompt: str) -> tuple[str, Optional[int]]:
        """Process PDF file directly with GPT-5.1 (for ОКБ reports).
        
        Returns:
            Tuple of (response_text, error_code) where error_code is HTTP status for errors or None for success
        """
        try:
            # Загружаем PDF файл в OpenAI с purpose="vision"
            uploaded_file = client.files.create(
                file=open(pdf_path, "rb"),
                purpose="vision"
            )
            
            # Отправляем промпт с прикреплённым файлом через SDK
            response = client.chat.completions.create(
                model=GPT_MODEL,
                messages=[
                    {
                        "role": "user",
                        "content": [
                            {
                                "type": "input_file",
                                "input_file_id": uploaded_file.id
                            },
                            {
                                "type": "text",
                                "text": prompt
                            }
                        ]
                    }
                ],
                max_completion_tokens=16000
            )
            
            # Удаляем загруженный файл
            try:
                client.files.delete(uploaded_file.id)
            except:
                pass
            
            return response.choices[0].message.content.strip(), None
        except Exception as exc:
            error_code = None
            error_str = str(exc)
            if "400" in error_str or "Bad Request" in error_str:
                error_code = 400
            elif "500" in error_str or "Internal Server Error" in error_str:
                error_code = 500
            elif "429" in error_str or "Too Many Requests" in error_str:
                error_code = 429
            return f"[Ошибка обработки: {exc}]", error_code

    @staticmethod
    def process_images_with_gpt(image_paths: List[str], prompt: str, model: str = GPT_MODEL) -> tuple[str, Optional[int]]:
        """Process multiple images with GPT-5 Vision in one request."""
        try:
            # Prepare content with all images
            content = [{"type": "text", "text": prompt}]

            for image_path in image_paths:
                base64_image = DocumentProcessor.encode_image_to_base64(image_path)
                content.append({
                    "type": "image_url",
                    "image_url": {
                        "url": f"data:image/jpeg;base64,{base64_image}"
                    }
                })

            response = client.chat.completions.create(
                model=model,
                messages=[
                    {
                        "role": "user",
                        "content": content
                    }
                ],
                max_completion_tokens=16000,  # Максимальное количество токенов для ответа
            )
            return response.choices[0].message.content.strip(), None
        except Exception as exc:  # noqa: BLE001
            # Пытаемся извлечь HTTP код ошибки
            error_code = None
            error_str = str(exc)
            if "400" in error_str or "Bad Request" in error_str:
                error_code = 400
            elif "500" in error_str or "Internal Server Error" in error_str:
                error_code = 500
            elif "429" in error_str or "Too Many Requests" in error_str:
                error_code = 429
            return f"[Ошибка обработки: {exc}]", error_code

    @staticmethod
    def process_pdf_with_assistants(pdf_path: Path, prompt: str) -> tuple[Dict[str, Any], Optional[str]]:
        """Process PDF directly using OpenAI Assistants API (for credit reports)."""
        try:
            # Upload PDF file
            print(f"      Загрузка PDF в OpenAI...", end=" ", flush=True)
            with open(pdf_path, "rb") as f:
                uploaded_file = client.files.create(file=f, purpose="assistants")
            print(f"OK")

            # Create assistant with file_search
            print(f"      Создание ассистента...", end=" ", flush=True)
            assistant = client.beta.assistants.create(
                model="gpt-5-mini",  # Самая мощная модель для максимального качества
                tools=[{"type": "file_search"}],
            )
            print(f"OK")

            # Create thread with attached PDF
            print(f"      Анализ документа с инструкциями ({len(prompt)} симв)...", end=" ", flush=True)
            
            # ВАЖНО: Передаем инструкции НЕ в user message (они игнорируются при большом файле),
            # а в instructions самого Run! ЭТО КЛЮЧЕВОЙ МОМЕНТ!
            
            # 1. Создаем тред с файлом и кратким запросом
            thread = client.beta.threads.create(
                messages=[
                    {
                        "role": "user",
                        "content": "Проанализируй прикрепленный кредитный отчет и извлеки данные в JSON согласно инструкциям.",
                        "attachments": [
                            {
                                "file_id": uploaded_file.id,
                                "tools": [{"type": "file_search"}]
                            }
                        ]
                    }
                ]
            )

            # 2. Запускаем Run, передавая полный промпт как instructions
            # Это заставит модель следовать инструкциям с наивысшим приоритетом
            run = client.beta.threads.runs.create(
                thread_id=thread.id,
                assistant_id=assistant.id,
                instructions=prompt  # <--- ВОТ ГДЕ ДОЛЖЕН БЫТЬ ПРОМПТ!
            )

            # Wait for completion
            max_wait = 300  # 5 minutes timeout for complex reports
            elapsed = 0
            while run.status in ["queued", "in_progress"]:
                time.sleep(2)
                elapsed += 2
                run = client.beta.threads.runs.retrieve(
                    thread_id=thread.id,
                    run_id=run.id,
                )
                if elapsed >= max_wait:
                    print(f"      [TIMEOUT] Превышено время ожидания от Assistants API")
                    raise TimeoutError("Assistants API timeout")
            
            if run.status == "completed":
                print(f"OK")
            elif run.status == "failed":
                error_msg = f"Assistant failed: {run.last_error.message if run.last_error else 'Unknown error'}"
                print(f"      [FAILED] {error_msg}")
                return {}, error_msg
            else:
                print(f"      [STATUS] {run.status}")

            # Get response
            messages = client.beta.threads.messages.list(thread_id=thread.id)
            if not messages.data:
                return {}, "No messages in thread"
                
            response_obj = messages.data[0].content[0]
            if hasattr(response_obj, 'text'):
                response_text = response_obj.text.value
            else:
                response_text = str(response_obj)
            
            # Parse JSON
            cleaned = DocumentProcessor.clean_json_response(response_text)
            try:
                if not cleaned:
                    raise ValueError("Empty response after cleaning")
                data = json.loads(cleaned)
                error = None
            except (json.JSONDecodeError, ValueError) as e:
                # Try to extract JSON from markdown or text
                import re
                json_match = re.search(r'\{.*\}', cleaned, re.DOTALL)
                if json_match:
                    try:
                        data = json.loads(json_match.group(0))
                        error = None
                    except:
                        data = {"raw_response": response_text}
                        error = f"JSON decode error: {str(e)}"
                else:
                    data = {"raw_response": response_text}
                    error = f"JSON decode error: {str(e)}"

            # Cleanup
            try:
                client.beta.assistants.delete(assistant.id)
                client.files.delete(uploaded_file.id)
            except:
                pass

            return data, error

        except Exception as exc:
            return {"error": str(exc)}, str(exc)


    def extract_text_from_pdf(self, pdf_path: Path) -> str:
        """Extracts text from PDF using pdfplumber preserving table layouts."""
        text_content = []
        try:
            import pdfplumber
            with pdfplumber.open(pdf_path) as pdf:
                total_pages = len(pdf.pages)
                # print(f"      [PDFPLUMBER] Processing {total_pages} pages...")
                
                for i, page in enumerate(pdf.pages):
                    page_content = []
                    page_content.append(f"--- Page {i+1} ---")
                    
                    try:
                        # 1. Извлекаем таблицы
                        tables = page.extract_tables()
                        # 2. Извлекаем текст с сохранением layout
                        text = page.extract_text(layout=True)
                        
                        if text:
                            page_content.append(text)
                        
                        if tables:
                            page_content.append(f"\n[TABLES DETECTED: {len(tables)}]")
                            for t_idx, table in enumerate(tables):
                                page_content.append(f"Table {t_idx+1}:")
                                for row in table:
                                    clean_row = [str(cell).replace('\n', ' ') if cell is not None else "" for cell in row]
                                    page_content.append(" | ".join(clean_row))
                                page_content.append("-" * 20)
                                
                    except Exception as e:
                       print(f"      [TEXT] Error extracting page {i+1}: {e}")
                    
                    text_content.append("\n".join(page_content))
            
            return "\n".join(text_content)
            
        except ImportError:
            print("      [WARNING] pdfplumber not installed, falling back to pymupdf")
            # Fallback to PyMuPDF logic
            try:
                doc = pymupdf.open(pdf_path)
                total_pages = len(doc)
                for i in range(total_pages):
                    page = doc[i]
                    text = page.get_text("text") 
                    text_content.append(f"--- Page {i+1} ---\n{text}")
                return "\n".join(text_content)
            except Exception as e:
                 print(f"      [TEXT] Error extracting text: {e}")
                 return ""
        except Exception as e:
            print(f"      [TEXT] Error extracting text with pdfplumber: {e}")
            return ""

    def process_pdf_with_text_extraction(self, pdf_path: Path, doc_type: str, base_prompt: str) -> tuple[Dict[str, Any], Optional[str]]:
        """Process credit report using text extraction and chunking (faster/more accurate for tables)."""
        print(f"      [TEXT] Извлечение текста с помощью PDFPlumber (structured)...")
        full_text = self.extract_text_from_pdf(pdf_path)
        
        if not full_text.strip():
            return {}, "Не удалось извлечь текст из PDF (возможно скан или защита)"
            
        # Split into pages to handle chunking correctly
        # The extraction adds "--- Page X ---" markers, we can split by that or just re-split regular newlines if needed.
        # But wait, extract_text_from_pdf returns a single string.
        # We need to respect the chunking logic: 40 pages per chunk.
        
        # Re-split by the marker we added
        raw_pages = full_text.split("--- Page ")
        # Filter out empty strings (the first split might be empty if it starts with marker)
        pages = [f"--- Page {p}" for p in raw_pages if p.strip()] 
        
        total_pages = len(pages)
        print(f"      [TEXT] Извлечено {total_pages} страниц текста")
        
        # Chunk config
        CHUNK_SIZE = 40  # Pages per chunk
        chunks = []
        current_chunk = []
        
        for p in pages:
            current_chunk.append(p)
            if len(current_chunk) >= CHUNK_SIZE:
                chunks.append("\n".join(current_chunk))
                current_chunk = []
        
        if current_chunk:
            chunks.append("\n".join(current_chunk))
            
        print(f"      [TEXT] Разбито на {len(chunks)} частей для обработки")
        
        all_credits = []
        all_debts_total = 0.0
        found_registration_address = None
        
        for i, chunk_text in enumerate(chunks):
            print(f"      [GPT] Обработка части {i+1}/{len(chunks)} ({len(chunk_text)} символов)...")
            
            try:
                # Add strict JSON instruction
                full_system_prompt = base_prompt + "\n\nВАЖНО: Верни ТОЛЬКО валидный JSON. Не добавляй markdown разметку. Просто текст JSON.\nЕсли в тексте есть Адрес регистрации должника - обязательно включи поле 'Адрес_регистрации' в JSON!"
                
                # Для первой части явно просим адрес
                if i == 0:
                    current_prompt = f"Вот НАЧАЛО кредитного отчета (страницы 1-{CHUNK_SIZE}). ОБЯЗАТЕЛЬНО найди Адрес регистрации должника!\n\n{chunk_text}"
                else:
                    current_prompt = f"Вот часть кредитного отчета (страницы {i*CHUNK_SIZE+1}-{(i+1)*CHUNK_SIZE}). Извлеки данные:\n\n{chunk_text}"

                completion = client.chat.completions.create(
                    model="gpt-5-mini",  # Используем gpt-5-mini для лучшего качества
                    messages=[
                        {"role": "system", "content": full_system_prompt},
                        {"role": "user", "content": current_prompt}
                    ],
                    response_format={"type": "json_object"}
                )
                
                response_content = completion.choices[0].message.content
                cleaned_json = DocumentProcessor.clean_json_response(response_content)
                
                if not cleaned_json:
                    print(f"      [GPT] Пустой ответ от модели")
                    continue
                    
                chunk_data = json.loads(cleaned_json)
                
                # Extract credits list
                chunk_credits = chunk_data.get("Кредиты", [])
                if isinstance(chunk_credits, list):
                    print(f"      [GPT] Найдено {len(chunk_credits)} кредитов в этой части")
                    all_credits.extend(chunk_credits)
                
                # Extract address
                if not found_registration_address:
                    addr = chunk_data.get("Адрес_регистрации") or chunk_data.get("Адрес") or chunk_data.get("Прописка")
                    if addr and isinstance(addr, str) and len(addr) > 10 and "не удал" not in addr.lower():
                        found_registration_address = addr
                        print(f"      [GPT] Адрес регистрации найден: {addr}")

            except Exception as e:
                print(f"      [GPT] Ошибка обработки части {i+1}: {e}")
        
        # Aggregate results
        final_data = {
            "Кредиты": all_credits,
            "Адрес_регистрации": found_registration_address,
            "Судебные_акты": []
        }
        
        # Try to clean up/deduplicate if needed (though sequential pages shouldn't produce duplicates unless same credit spans pages)
        # Note: merge_credit_reports logic handles merging from *multiple files*, here we have one file.
        # We can implement basic cleanup here if needed.
        
        return final_data, None

    def process_pdf(self, pdf_path: Path, debtor_fio: str = "") -> DocumentOutput:
        """Process PDF document with all pages at once using GPT-5.1 Vision.
        
        Args:
            pdf_path: Path to PDF file
            debtor_fio: ФИО должника (для подстановки в промпты, где нужно)
        """
        pdf_path = pdf_path.resolve()

        # Сначала пробуем определить тип по имени файла
        doc_type, base_prompt = self.detect_document_type(pdf_path.name)
        
        # Пропускаем документы категории "общий" - не обрабатываем их
        if doc_type == "общий":
            print(f"   > {pdf_path.name}")
            print(f"      Тип: {doc_type} (пропущен, не обрабатывается)")
            return DocumentOutput(
                filename=pdf_path.name,
                doc_type=doc_type,
                extracted_data={},
                raw_text="",
                status="skipped",
                error="Документ категории 'общий' не обрабатывается"
            )
        
        # Подставляем ФИО должника в промпт (если есть плейсхолдер)
        if debtor_fio and "{debtor_fio}" in base_prompt:
            base_prompt = base_prompt.replace("{debtor_fio}", debtor_fio)

        # Выбираем модель: для критически важных документов (счета, ЕГРН) используем gpt-5.1
        current_model = "gpt-5-mini" if doc_type in ["счета", "егрн_выписка"] else GPT_MODEL
        
        print(f"   > {pdf_path.name}")
        print(f"      Тип: {doc_type} (Модель: {current_model})")
        DocumentProcessor.log_memory("Начало обработки")

        start_time = time.time()

        # ===  СПЕЦИАЛЬНАЯ ОБРАБОТКА ДЛЯ КРЕДИТНЫХ ОТЧЕТОВ ===
        if doc_type in ["отчет_окб", "отчет_бки", "отчет_нбки"]:
            
            # ОТКЛЮЧЕНО ПО ТРЕБОВАНИЮ: НБКИ не обрабатываем
            if doc_type == "отчет_нбки":
                print(f"      [CREDIT] Обработка НБКИ отключена (pass)")
                return DocumentOutput(
                    file=pdf_path.name,
                    document_type=doc_type,
                    pages=0,
                    processing_time_seconds=0.0,
                    data={},
                    error=None,
                    extracted_text=""
                )

            print(f"      [CREDIT] Обработка кредитного отчета (текстовый режим для точности)")
            
            try:
                # 1. Сначала пробуем извлечь текст (быстрее и точнее для таблиц)
                data, error = self.process_pdf_with_text_extraction(pdf_path, doc_type, base_prompt)
                
                # Если текст извлекся и есть данные
                if not error and data and (len(data.get("Кредиты", [])) > 0 or len(data.get("Судебные_акты", [])) > 0):
                    duration = time.time() - start_time
                    print(f"   ✓ Готово (текст) за {duration:.1f} сек. Извлечено {len(data.get('Кредиты', []))} кредитов.")
                    
                    # Count pages for report
                    try:
                        doc = pymupdf.open(pdf_path)
                        num_pages = len(doc)
                        doc.close()
                    except:
                        num_pages = 0
                        
                    return DocumentOutput(
                        file=pdf_path.name,
                        document_type=doc_type,
                        pages=num_pages,
                        processing_time_seconds=duration,
                        data=data,
                        error=None,
                        extracted_text="Text extracted via PyMuPDF"
                    )
                
                # Если текст не извлекся или пусто - fallback на GPT Vision
                print(f"      [FALLBACK] Текстовый слой пуст или не содержит данных. Переход на GPT Vision...")
                
            except Exception as e:
                print(f"      [ERROR] Ошибка текстовой обработки: {e}. Переход на GPT Vision...")

            # === FALLBACK: GPT Vision (старый метод) ===
            # Конвертируем в изображения ПОСТРАНИЧНОО для экономии памяти
            print(f"      Конвертация PDF в изображения (Vision)...", end=" ", flush=True)
            pdf = pdfium.PdfDocument(str(pdf_path))
            total_pages = len(pdf)
            DocumentProcessor.log_memory("После загрузки PDF")
            
            # Обрабатываем все страницы для всех кредитных отчетов (батчевая обработка)
            max_pages = total_pages
            
            # 🚀 КРИТИЧЕСКАЯ ОПТИМИЗАЦИЯ: Конвертируем и сохраняем батчами по 40 страниц
            # Вместо загрузки всех страниц в память сразу
            CONVERSION_BATCH_SIZE = 40
            page_images = []
            
            for batch_start in range(0, max_pages, CONVERSION_BATCH_SIZE):
                batch_end = min(batch_start + CONVERSION_BATCH_SIZE, max_pages)
                batch_pages = []
                
                # Конвертируем батч страниц
                for i in range(batch_start, batch_end):
                    page = pdf[i]
                    bitmap = page.render(scale=2.5)
                    pil_image = bitmap.to_pil()
                    batch_pages.append(pil_image)
                
                # Сохраняем батч и закрываем изображения
                for pil_image in batch_pages:
                    with tempfile.NamedTemporaryFile(suffix=".jpg", delete=False) as tmp_file:
                        tmp_path = tmp_file.name
                        pil_image.save(tmp_path, 'JPEG', quality=95, optimize=True)
                        page_images.append(tmp_path)
                    pil_image.close()
                
                # Очищаем батч из памяти
                batch_pages.clear()
                import gc
                gc.collect()
            
            print(f"OK ({len(page_images)} из {total_pages} стр.)")
            pdf.close()
            import gc
            gc.collect()
            DocumentProcessor.log_memory("После конвертации и сохранения изображений")

            # Определяем batch size и overlap в зависимости от типа отчета
            if doc_type == "отчет_нбки":
                BATCH_SIZE = 50  # НБКИ: средние батчи (уменьшено для памяти)
                OVERLAP_PAGES = 20  # НБКИ: overlap 20 страниц
            elif doc_type == "отчет_окб":
                BATCH_SIZE = 50  # ОКБ: средние батчи (уменьшено для памяти)
                OVERLAP_PAGES = 20  # ОКБ: overlap 20 страниц
            elif doc_type == "постановление_пристава":
                BATCH_SIZE = 50  # Постановления: большие батчи
                OVERLAP_PAGES = 5  # Постановления: overlap 5 страниц
            else:  # БКИ
                BATCH_SIZE = 50  # БКИ: маленькие батчи (самые большие отчеты!)
                OVERLAP_PAGES = 20  # БКИ: overlap 20 страниц
            
            print(f"      [DEBUG] page_images count: {len(page_images)}, BATCH_SIZE: {BATCH_SIZE}")
            use_batch = len(page_images) > BATCH_SIZE
            print(f"      [DEBUG] use_batch: {use_batch}")
            batch_size = BATCH_SIZE  # Может уменьшаться при ошибках

            extracted_data = {}
            error = None

            if use_batch:
                print(f"      [BATCH] Документ большой ({len(page_images)} стр.), обработка батчами по {batch_size} стр. (overlap: {OVERLAP_PAGES})")
                
                all_credits = []
                batch_num = 0
                retry_smaller = False
                
                for start_idx in range(0, len(page_images), batch_size):
                    end_idx = min(start_idx + batch_size, len(page_images))
                    
                    # Добавляем overlap с предыдущим батчом (кроме первого)
                    if OVERLAP_PAGES > 0 and start_idx > 0:
                        overlap_start = max(0, start_idx - OVERLAP_PAGES)
                        batch_pages = page_images[overlap_start:end_idx]
                        actual_start = overlap_start + 1  # Для отображения в логе
                    else:
                        batch_pages = page_images[start_idx:end_idx]
                        actual_start = start_idx + 1
                    
                    batch_num += 1
                    
                    # Специальные промпты для разных типов отчетов
                    if doc_type == "постановление_пристава":
                        # Для постановлений - извлекаем массив постановлений
                        overlap_info = ""
                        if start_idx > 0:
                            overlap_info = f"""
КОНТЕКСТ: Первые {OVERLAP_PAGES} страницы - это КОНТЕКСТ из предыдущего батча для связности.
НЕ извлекай данные повторно из этих {OVERLAP_PAGES} страниц!
Используй их только для понимания контекста, если постановление начинается на них.
Анализируй и извлекай данные ТОЛЬКО из НОВЫХ страниц (начиная со страницы {OVERLAP_PAGES + 1})
Нормализуй название банка, МФК, МКК, например: сбербанк - ПАО «Сбербанк», псб банк - ПАО «Банк ПСБ», Вэзбанкир ООО МФК - ООО МФК «Вэббанкир», МФК Юпитер 6 ООО - ООО МФК «Юпитер 6» и т.д.
"""
                        
                        batch_prompt = f"""Постановление судебного пристава-исполнителя о возбуждении исполнительного производства или о расчёте задолженности.

ФОРМАТ: Если текст в CAPS LOCK — приведи к нормальному виду.
Нормализуй название банка, МФК, МКК, например: сбербанк - ПАО «Сбербанк», псб банк - ПАО «Банк ПСБ», Вэзбанкир ООО МФК - ООО МФК «Вэббанкир», МФК Юпитер 6 ООО - ООО МФК «Юпитер 6» и т.д.

{overlap_info}

🔴 КРИТИЧЕСКИ ВАЖНО - В ДОКУМЕНТЕ НЕСКОЛЬКО ПОСТАНОВЛЕНИЙ:

Проанализируй ВСЕ страницы документа и найди ВСЕ постановления.

КАК НАЙТИ ПОСТАНОВЛЕНИЯ:
Каждое новое постановление начинается с заголовка "Постановление о возбуждении исполнительного производства" + дата.
Ищи эту фразу на КАЖДОЙ странице! Сколько раз встретилась эта фраза = столько постановлений в документе.

ПРИМЕР СТРУКТУРЫ:
"Постановление о возбуждении исполнительного производства
03.12.2021 Долгодеревенское
Судебный пристав-исполнитель... № 152632/21/74064-ИП..."

Затем идёт другое постановление:
"Постановление о возбуждении исполнительного производства
11.01.2023 Долгодеревенское
Судебный пристав-исполнитель... № 164/23/74064-ИП..."

Извлеки данные из КАЖДОГО постановления отдельно!

ДЛЯ КАЖДОГО ПОСТАНОВЛЕНИЯ:
- Номер исполнительного производства
- Дата постановления
- Судебный пристав: ФИО, отдел ФССП
- Должник: ФИО
- Взыскатель (кредитор): название организации или ФИО
- Адрес взыскателя (если указан)
- Предмет исполнения
- Основание: исполнительный лист, судебный приказ (номер, дата)
- Орган, выдавший исполнительный документ
- Адрес органа
- Номер дела
- Сумма взыскания
- ИТОГОВАЯ СУММА ВЗЫСКАНИЯ (ищи "Итого" или "Всего взыскать")

Нормализуй название банка, например: ПАО «Сбербанк», ПАО «Банк ПСБ» и т.д.

ФОРМАТ ОТВЕТА - ОБЯЗАТЕЛЬНО МАССИВ:
{{
  "Постановления": [
    {{
      "Номер_ИП": "404852/25/74059-ИП",
      "Дата_постановления": "29.10.2025",
      "Пристав": "Кащенко Юлия Александровна",
      "Отдел_ФССП": "...",
      "Должник": "Овчинников Игорь Юрьевич",
      "Взыскатель": "ПАО «ВТБ»",
      "Адрес_взыскателя": "191144, Санкт-Петербург...",
      "Предмет_исполнения": "...",
      "Основание": "исполнительная надпись нотариуса",
      "Номер_основания": "У-0004721706",
      "Дата_основания": "28.10.2025",
      "Орган_выдавший": "Нотариус Сугуров Марат Жасланович",
      "Адрес_органа": "455045, Челябинская обл...",
      "Номер_дела": "У-0004721706",
      "Сумма_долга": 96391.98,
      "Проценты": 48815.70,
      "Расходы": 1558.00,
      "Итого_взыскание": 146765.68
    }}
  ]
}}

ВАЖНО: 
- Если одно постановление — массив из 1 элемента!
- Если несколько — массив из N элементов!
- Суммы ТОЛЬКО числами, без текста
- Пустые значения = null

ВЕРНИ ТОЛЬКО ВАЛИДНЫЙ JSON БЕЗ ДОПОЛНИТЕЛЬНОГО ТЕКСТА!
Никакого текста до или после JSON!
Только чистый JSON формат!
"""
                    elif doc_type == "отчет_окб":
                        # Добавляем информацию об overlap в промпт
                        overlap_info = ""
                        if start_idx > 0:
                            overlap_info = f"""
КОНТЕКСТ: Первые {OVERLAP_PAGES} страницы - это КОНТЕКСТ из предыдущего батча для связности.
НЕ извлекай данные повторно из этих {OVERLAP_PAGES} страниц!
Используй их только для понимания контекста, если таблица или раздел начинается на них.
Анализируй и извлекай данные ТОЛЬКО из НОВЫХ страниц (начиная со страницы {OVERLAP_PAGES + 1})
Когда наткнешься на таблицу "ЗАКРЫТЫЕ КРЕДИТНЫЕ ДОГОВОРЫ", начинай игнорировать договоры и не учитывать их.
Нормализуй название банка, МФК, МКК, например: сбербанк - ПАО «Сбербанк», псб банк - ПАО «Банк ПСБ», Вэзбанкир ООО МФК - ООО МФК «Вэббанкир», МФК Юпитер 6 ООО - ООО МФК «Юпитер 6» и т.д.
"""
                        
                        batch_prompt = base_prompt + overlap_info + f"""Ты помощник для юриста, твоя задача собрать действующие кредиты для банкротства.

СТРУКТУРА ОТЧЕТА ОКБ:
1. Краткая таблица "ДЕЙСТВУЮЩИЕ КРЕДИТНЫЕ ДОГОВОРЫ" (в начале).
2. Детальные блоки по каждому кредиту (далее в документе).

Найди в начале документа (Персональные данные):
- Адрес регистрации (прописка).
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

ИЗВЛЕКАЙ ДАННЫЕ ПО КРЕДИТАМ ТОЛЬКО ИЗ ДЕТАЛЬНЫХ БЛОКОВ! Краткую таблицу используй только для навигации.
ВНИМАНИЕ: Извлекай АБСОЛЮТНО ВСЕ текущие кредиты, не пропускай ни одного блока! Если блок видишь - извлекай.

АЛГОРИТМ ДЛЯ КАЖДОГО КРЕДИТА (БЛОКА):

1. **Кредитор**: Название банка/МФО в заголовке блока (например, "1. БАНК ГПБ АО..."). Нормализуй название (ГПБ -> АО «ГПБ», Сбербанк -> ПАО «Сбербанк»).
2. **Вид**: Тип кредита из заголовка (Кредитная карта, Потребительский кредит, Овердрафт). "Вид займа (кредита)" - бери это если "Тип кредитной линии" стоит "-", если в "Тип кредитной линии" что-то стоит, то бери от туда минуя "Вид займа (кредита)".
3. **Дата_сделки**: 
   - Ищи подраздел "Общие сведения о сделке".
   - Поле: "Дата совершения сделки" (например, 16 сентября 2024).
   - НЕ путай с датой обновления информации!
4. **Сумма_обязательства** (Сколько БРАЛИ):
   - Ищи подраздел "Общие сведения о сделке".
   - Поле: "Сумма и валюта обязательства" (например, 600 000 р.) Бери самую первую сумму!.
5. **Сумма** (Текущий долг/Сколько ДОЛЖНЫ):
   - Ищи подраздел "Сведения о сумме задолженности".
   - Внутри таблица. Нам нужна строка "Общая" (или первая строка с цифрами).
   - Столбец "Всего". Это и есть полная сумма долга (тело + проценты).
   - Пример: Если в строке "Общая" столбце "Всего" стоит "517 346,28 р." — бери это число.

ПРИМЕР РАЗБОРА ИЗ СКРИНШОТА:
- Блок: БАНК ГПБ АО (Кредит «овердрафт»)
- Дата совершения сделки: 16 сентября 2024 -> "16.09.2024"
- Сумма и валюта обязательства: 600 000 р. -> 600000 (Это Сумма_обязательства)
- Таблица "Сведения о сумме задолженности":
  Строка "Общая" -> Столбец "Всего" -> 517 346,28 р. -> 517346.28 (Это Сумма - текущий долг!)
  (Не бери строку "Срочная" для суммы, бери "Общая"!)

ВАЖНО:
- Если кредит закрыт (статус "Закрыт", "Счет закрыт") — ИГНОРИРУЙ его.
- Ищи 10-значный ИНН кредитора в блоке "Сведения об источнике" (ИНН или ИНН/ОГРН).

JSON:
{{
  "Адрес_регистрации": "...",
  "ИНН_должника": "null",
  "Кредиты": [
    {{
      "Кредитор": "...",
      "ИНН_кредитора": "...",
      "Вид": "...",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": 0.00,
      "Сумма": 0.00
    }}
  ],
  "Судебные_акты": []
}}"""
                    elif doc_type == "отчет_бки":
                        # Для БКИ - даты в основной таблице
                        batch_prompt = base_prompt + f"""Ты помощник юриста. Обработай отчет Скоринг Бюро (БКИ).

В конце отчета (Персональные данные) найди:
- Адрес регистрации (место жительства, прописка).
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

Ищи таблицу "Действующие кредиты, займы, карты". Мы берем данные оттуда.

НОРМАЛИЗАЦИЯ БАНКОВ:
- "ГПБ (ОАО)", "ГПБ (АО)", "Газпромбанк" -> "АО «ГПБ»"
- "Сбербанк" -> "ПАО «Сбербанк»"
- "ВТБ" -> "Банк ВТБ (ПАО)"
- "Коллекторы: ООО Профессиональная Коллекторская Организация «Интел Коллект» (ранее - ООО «Интел коллект») Коллект» (ранее - ООО «Интел коллект»)" -> "ООО ПКО «Интел Коллект»"
    То есть убрать "Коллекторы:", убрать "(ранее - ...)", оставить только "ООО ПКО «Интел Коллект»"

ДЛЯ КАЖДОЙ СТРОКИ ТАБЛИЦЫ:
1. **Кредитор**: Название банка (колонка 1). Нормализуй!
2. **Вид**: Тип обязательства (под названием банка, например "Овердрафт", "Ипотека").
3. **Дата_сделки**: Колонка "Дата начала обязательства".
4. **Сумма_обязательства** (Сколько взяли): Колонка "Сумма / лимит обязательства".
5. **Сумма** (Текущий долг):
   - В СКРИНШОТЕ есть колонки: "Срочный основной долг", "Текущая просрочка", "Общая задолженность".
   - ПРИОРИТЕТ 1: Если есть колонка "Общая" (в блоке задолженность) или "Текущая задолженность" с полной суммой — бери её.
   - ПРИОРИТЕТ 2 (как на фото):
     Сумма = "Срочный основной долг" + "Текущая просрочка (осн.долг, %, пени...)".
   - В примере со скриншота:
     Банк ГПБ (ОАО) от 16.09.2024.
     Срочный осн. долг = 473 797.03
     Текущая просрочка = отсутствует (0)
     ИТОГО Сумма = 473 797.03
   - Другой пример (с просрочкой):
     Срочный долг = 813 242.73
     Просрочка = 29 763.00
     ИТОГО Сумма = 813242.73 + 29763.00 = 843005.73

ВАЖНО:
- Игнорируй закрытые кредиты.
- Если в колонке "Сумма / лимит обязательства" стоит 0 (для овердрафта бывает), но есть текущий долг — бери долг как есть.

JSON:
{{
  "Адрес_регистрации": "...",
  "Кредиты": [
    {{
      "Кредитор": "...",
      "Вид": "...",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": 0.00,
      "Сумма": 0.00
    }}
  ]
}}"""
                    elif doc_type == "отчет_нбки":
                        # НБКИ промпт с информацией об overlap
                        overlap_info = ""
                        if start_idx > 0:
                            overlap_info = f"""
КОНТЕКСТ: Первые {OVERLAP_PAGES} страницы - это КОНТЕКСТ из предыдущего батча для связности.
НЕ извлекай данные повторно из этих {OVERLAP_PAGES} страниц!
Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!
Используй их только для понимания контекста, если раздел кредитора начинается на них.
Анализируй и извлекай данные ТОЛЬКО из НОВЫХ страниц (начиная со страницы {OVERLAP_PAGES + 1})
Если наткнешься на раздел с заголвком "Сведения о долговой нагрузке заемщика без заключенного потребительского займа" — значит список кредиторов закончился, дальше не смотри!.
Нормализуй название банка, МФК, МКК, например: сбербанк - ПАО «Сбербанк», псб банк - ПАО «Банк ПСБ», Вэзбанкир ООО МФК - ООО МФК «Вэббанкир», МФК Юпитер 6 ООО - ООО МФК «Юпитер 6» и т.д.
"""
                        
                        batch_prompt = base_prompt + overlap_info + f"""

ИНСТРУКЦИЯ ПО ОБРАБОТКЕ ЧАСТЕЙ ДОКУМЕНТА (БАТЧИ):
1. Если видишь таблицу или список кредитов без явного заголовка "Обязательства и их исполнение" — ЭТО ПРОДОЛЖЕНИЕ! ОБЯЗАТЕЛЬНО ИЗВЛЕКАЙ ВСЕ КРЕДИТЫ С ЭТОЙ СТРАНИЦЫ!
2. Не пропускай кредиты только потому, что не нашел заголовок раздела. Ищи структуру кредитных записей.

В начале документа (Субъект кредитной истории):
- Адрес регистрации (место жительства, адрес, прописка), самая первая запись о регистрации актуальная, бери её.
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

Найди раздел с заголовком "Обязательства и их исполнение" - там начинается список кредиторов, а заканчивается перед разделом с заголовком "Сведения о долговой нагрузке заемщика без заключенного потребительского займа".

Нормализуй название банка, МФК, МКК.
ПРИМЕРЫ ОБЯЗАТЕЛЬНОЙ НОРМАЛИЗАЦИИ:
- "Сбербанк" -> "ПАО «Сбербанк»"
- "ПСБ", "Промсвязьбанк" -> "ПАО «Промсвязьбанк»"
- "ГПБ", "Газпромбанк", "Ф-Л ГПБ (АО)", "ГПБ (АО)" -> "АО «ГПБ»"
- "ВТБ" -> "Банк ВТБ (ПАО)"
- "Альфа-Банк", "АЛЬФА-БАНК" -> "АО «Альфа-Банк»"
- "Тинькофф", "Т-Банк" -> "АО «Т-Банк»"
- "Совкомбанк" -> "ПАО «Совкомбанк»"

ВАЖНО - ИГНОРИРУЙ ПОГАШЕННЫЕ КРЕДИТЫ:
- Если в столбце "Прекращение обязательства" написано "Надлежащее исполнение обязательства" — кредит ПОГАШЕН, НЕ ВКЛЮЧАЙ его в список! А если стоит "Н/Д" — кредит ДЕЙСТВУЮЩИЙ, ВКЛЮЧАЙ его!
- Если обязательство прекращено — пропускай этот кредит!
- Если в столбце "Состояние" стоит "Статус: Счет закрыт" - кредит ПОГАШЕН, НЕ ВКЛЮЧАЙ его в список!
- Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!

Для КАЖДОГО ДЕЙСТВУЮЩЕГО кредитора:
1. Название банка/кредитора (заголовок раздела)
2. В таблице "Основные сведения об обязательстве":
   - Столбец "Сумма и валюта обязательства" = первоначальная сумма займа (Сумма_обязательства)
   - Столбец "Дата возникновения обязательства" = дата сделки (Дата_сделки)
   - Столбец "ОГРН/ИНН" - там два числа через пробел. ПЕРВОЕ (13 цифр) - это ОГРН, НЕ НУЖНО. ВТОРОЕ (10 цифр) - это ИНН, ОБЯЗАТЕЛЬНО извлеки!
   - Тип кредита/займа (из столбца "Вид займа (кредита)" или аналогичной: Потребительский кредит, Кредитная карта, Микрозайм, Ипотека, Автокредит, и т.д.)
3. В таблице "Задолженность":
   - Найди САМУЮ ПОСЛЕДНЮЮ строку в таблице
   - Возьми значение из столбца "Сумма" (это текущий долг)
   - ИГНОРИРУЙ другие столбцы ("Сумма по осн.долгу", "Сумма по %%", и т.д.)

КРИТИЧЕСКИ ВАЖНО ПРО ИЗВЛЕЧЕНИЕ СПИСКА КРЕДИТОВ:
- ЗАПРЕЩЕНО ОБЪЕДИНЯТЬ КРЕДИТЫ ПО НАЗВАНИЮ БАНКА или ТИПУ!
- Один блок в таблице (один заголовок "Субъект", "Сделка" или номер по порядку) = ОДИН отдельный JSON-объект.
- Если в отчете 5 разных записей от "АО «Т-Банк»" (например, 2 кредитные карты, 1 кредит наличными, 2 ипотеки) - верни РОВНО 5 объектов JSON!
- Если в отчете 8 записей от "АО «ГПБ»" (например, 3 Овердрафта и 5 Потребительских) - верни РОВНО 8 объектов JSON!
- СМОТРИ НА ТИП КРЕДИТА (Овердрафт, Кредитная линия и т.д.). Разные типы - это РАЗНЫЕ кредиты.
- ДАЖЕ ЕСЛИ ТИП КРЕДИТА СОВПАДАЕТ (например, два "Потребительский кредит" от одного банка) - ЭТО РАЗНЫЕ КРЕДИТЫ, если у них разные даты, суммы или счета.
- ДАЖЕ ЕСЛИ СУММЫ ПОХОЖИ - ЭТО РАЗНЫЕ КРЕДИТЫ.
- НИКОГДА НЕ ГРУППИРУЙ. ЛУЧШЕ ДУБЛИ, ЧЕМ ПОТЕРЯ ДАННЫХ.
- КАЖДАЯ СТРОКА ТАБЛИЦЫ "Сведения о счете" ИЛИ КАЖДЫЙ БЛОК ИНФОРМАЦИИ О КРЕДИТЕ ДОЛЖЕН СТАТЬ ОТДЕЛЬНЫМ ЭЛЕМЕНТОМ МАССИВА "Кредиты".

КРИТИЧЕСКИ ВАЖНО ПРО ИЗВЛЕЧЕНИЕ ДАННЫХ:
- Берем ТОЛЬКО последнюю(нижнюю) строку таблицы "Задолженность"
- Берем ТОЛЬКО столбец "Сумма" из этой строки
- Это и есть текущий долг
- НЕ суммируем ничего, просто берем одно значение!
- В столбце "ОГРН/ИНН" берем ВТОРОЕ число (10 цифр) - это ИНН!
- Если наткнешься на раздел с заголовком "Сведения о долговой нагрузке заемщика без заключенного потребительского займа" — прекращай сбор данных!
- Точно перепиши суммы и даты для каждого кредита, чтобы они не перепутались.
- Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!

Нормализуй название банка, обрати внимание на список примеров выше. "ГПБ (АО)" -> "АО «ГПБ»".

ВЕРНИ ТОЛЬКО ВАЛИДНЫЙ JSON БЕЗ ДОПОЛНИТЕЛЬНОГО ТЕКСТА!
Никакого текста до или после JSON!
Только чистый JSON формат!

JSON (СТРОГО этот формат) - Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!:
{{
  "Адрес_регистрации": "...",
  "ИНН_должника": "Null",
  "Кредиты": [
    {{
      "Кредитор": "название (нормализованное)",
      "ИНН_кредитора": "ВТОРОЕ число из столбца ОГРН/ИНН (10 цифр)",
      "Вид": "тип кредита (Потребительский кредит/Кредитная карта/Микрозайм/Ипотека/Автокредит/другое)",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": "первоначальная сумма",
      "Сумма": "последняя строка таблицы Задолженность, столбец Сумма"
    }}
  ]
}}"""

                    try:
                        # Выводим информацию о батче с учётом overlap
                        if OVERLAP_PAGES > 0 and start_idx > 0:
                            print(f"         Батч {batch_num} ({actual_start}-{end_idx}, overlap: {OVERLAP_PAGES} стр.)...", end=" ", flush=True)
                        else:
                            print(f"         Батч {batch_num} ({actual_start}-{end_idx})...", end=" ", flush=True)
                        
                        response_text, error_code = self.process_images_with_gpt(batch_pages, batch_prompt)
                        
                        # Проверяем ошибки 400/500 - запрос слишком большой
                        if error_code in [400, 500]:
                            print(f"ERROR {error_code}")
                            if batch_size > 30:
                                # Уменьшаем размер батча и повторяем этот батч
                                new_batch_size = batch_size // 2
                                print(f"         [AUTO] Уменьшаю размер батча до {new_batch_size} стр. и повторяю...")
                                batch_size = new_batch_size
                                retry_smaller = True
                                # Откатываемся назад, чтобы обработать этот батч заново
                                start_idx -= batch_size
                                continue
                            else:
                                # Батч уже слишком маленький - пропускаем
                                print(f"         [SKIP] Батч слишком маленький, пропускаем")
                                if not error:
                                    error = f"Batch {batch_num} failed with error {error_code}"
                                continue
                        
                        cleaned = self.clean_json_response(response_text)
                        
                        # Проверка: если GPT вернул текстовое сообщение вместо JSON (нехватка контекста)
                        if cleaned and ("нужна дополнительная" in cleaned.lower() or "пришлите" in cleaned.lower() or "не могу" in cleaned.lower()):
                            print(f"SKIP (недостаточно контекста в батче)")
                            continue
                        
                        try:
                            batch_data = json.loads(cleaned) if cleaned else {}
                            
                            # FALLBACK: если GPT вернул "Договоры" вместо "Кредиты"
                            if "Договоры" in batch_data and "Кредиты" not in batch_data:
                                print(f"         [FIX] GPT вернул 'Договоры', переименовываю в 'Кредиты'")
                                batch_data["Кредиты"] = batch_data.pop("Договоры")
                            
                            # Обработка постановлений
                            if doc_type == "постановление_пристава":
                                if "Постановления" in batch_data and isinstance(batch_data["Постановления"], list):
                                    batch_items = batch_data["Постановления"]
                                    all_credits.extend(batch_items)
                                    
                                    if len(batch_items) == 0:
                                        print(f"OK (0 постановлений)")
                                    else:
                                        print(f"OK ({len(batch_items)} постановлений)")
                                    
                                    # Логирование извлечённых постановлений
                                    if batch_items:
                                        print(f"         [ИЗВЛЕЧЕНО]:")
                                        for idx, item in enumerate(batch_items, 1):
                                            ip_num = item.get("Номер_ИП", "???")
                                            date = item.get("Дата_постановления", "???")
                                            creditor = item.get("Взыскатель", "???")
                                            total = item.get("Итого_взыскание", "???")
                                            print(f"           {idx}. {ip_num} | Дата: {date} | Взыскатель: {creditor} | Итого: {total}")
                                else:
                                    print("OK (0 постановлений)")
                                    print(f"         [DEBUG] Нет ключа 'Постановления' в ответе. Полный JSON:")
                                    print(f"         {json.dumps(batch_data, ensure_ascii=False, indent=2)[:800]}")
                            else:
                                # Добавляем кредиты из этого батча
                                if "Кредиты" in batch_data and isinstance(batch_data["Кредиты"], list):
                                    batch_credits = batch_data["Кредиты"]
                                    
                                    # Дополняем ИНН из реестра, если GPT не извлек
                                    for credit in batch_credits:
                                        if not credit.get("ИНН_кредитора"):
                                            creditor_name = credit.get("Кредитор", "")
                                            if creditor_name:
                                                _, canonical_name = DocumentProcessor.normalize_bank_name(creditor_name)
                                                inn = DocumentProcessor.get_bank_inn(canonical_name)
                                                if inn:
                                                    credit["ИНН_кредитора"] = inn
                                    
                                    all_credits.extend(batch_credits)
                                    
                                    # ДИАГНОСТИКА: выводим ответ GPT когда 0 кредитов
                                    if len(batch_credits) == 0:
                                        print(f"OK (0 кредитов)")
                                        print(f"         [DEBUG] GPT вернул пустой массив. Ответ (первые 800 символов):")
                                        print(f"         {response_text[:800]}")
                                    else:
                                        print(f"OK ({len(batch_credits)} кредитов)")
                                    
                                    # Логирование извлечённых кредитов
                                    if batch_credits:
                                        print(f"         [ИЗВЛЕЧЕНО]:")
                                        for idx, credit in enumerate(batch_credits, 1):
                                            creditor = credit.get("Кредитор", "???")
                                            date = credit.get("Дата_сделки", "???")
                                            initial = credit.get("Сумма_обязательства", "???")
                                            debt = credit.get("Сумма", "???")
                                            inn = credit.get("ИНН_кредитора", "")
                                            inn_text = f" | ИНН: {inn}" if inn else ""
                                            print(f"           {idx}. {creditor} | Дата: {date} | Начальная: {initial} | Долг: {debt}{inn_text}")
                                else:
                                    print("OK (0 кредитов)")
                                    print(f"         [DEBUG] Нет ключа 'Кредиты' в ответе. Полный JSON:")
                                    print(f"         {json.dumps(batch_data, ensure_ascii=False, indent=2)[:800]}")
                        except json.JSONDecodeError as json_err:
                            print(f"SKIP (JSON parse error: {str(json_err)[:50]})")
                            print(f"         [DEBUG] Cleaned response (first 500 chars):")
                            print(f"         {cleaned[:500] if cleaned else 'EMPTY'}")
                            print(f"         [DEBUG] Original response (first 500 chars):")
                            print(f"         {response_text[:500]}")
                            
                    except Exception as e:
                        print(f"ERROR ({str(e)[:40]})")
                        if not error:
                            error = f"Batch {batch_num}: {str(e)}"
                    
                    # 🚀 ОПТИМИЗАЦИЯ ПАМЯТИ: НЕ удаляем файлы здесь, т.к. следующий батч с overlap их использует!
                    # Удалим ВСЕ файлы после обработки всех батчей
                    finally:
                        # Удаляем только ссылки на изображения батча из памяти
                        batch_pages = None
                        # Запускаем сборку мусора после каждого батча для экономии памяти
                        import gc
                        gc.collect()
                        DocumentProcessor.log_memory(f"После батча {batch_num}")
                
                # Объединяем результаты всех батчей
                if doc_type == "постановление_пристава":
                    extracted_data = {"Постановления": all_credits}
                    print(f"      [BATCH] Всего извлечено постановлений: {len(all_credits)}")
                else:
                    extracted_data = {"Кредиты": all_credits}
                    print(f"      [BATCH] Всего извлечено кредиторов: {len(all_credits)}")
                
                # ДИАГНОСТИКА: если 0 кредитов в большом отчете - выводим подробности
                if len(all_credits) == 0 and len(page_images) > 10:
                    print(f"      [WARNING] ⚠️ Большой отчет ({len(page_images)} стр.) но 0 кредитов! Возможные причины:")
                    print(f"         1. GPT не нашел таблицу 'ДЕЙСТВУЮЩИЕ КРЕДИТНЫЕ ДОГОВОРЫ'")
                    print(f"         2. Все договоры отфильтрованы по правилам исключения (постановления/ФССП)")
                    print(f"         3. GPT вернул невалидный JSON")
                    print(f"         4. Все кредиты закрыты")
                    print(f"         Рекомендация: проверьте первые страницы отчета вручную")
                
            else:
                # Для небольших документов - обычная обработка
                print(f"      [DEBUG] Используется ОБЫЧНАЯ обработка (не батч), страниц: {len(page_images)}")
                if doc_type == "отчет_окб":
                    simple_prompt = base_prompt + f"""Ты помощник для юриста, твоя задача собрать действующие кредиты для банкротства.

СТРУКТУРА ОТЧЕТА ОКБ:
1. Краткая таблица "ДЕЙСТВУЮЩИЕ КРЕДИТНЫЕ ДОГОВОРЫ" (в начале).
2. Детальные блоки по каждому кредиту (далее в документе).

Найди в начале документа (Персональные данные):
- Адрес регистрации (прописка).
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

ИЗВЛЕКАЙ ДАННЫЕ ПО КРЕДИТАМ ТОЛЬКО ИЗ ДЕТАЛЬНЫХ БЛОКОВ! Краткую таблицу используй только для навигации.

АЛГОРИТМ ДЛЯ КАЖДОГО КРЕДИТА (БЛОКА):

1. **Кредитор**: Название банка/МФО в заголовке блока (например, "1. БАНК ГПБ АО..."). Нормализуй название (ГПБ -> АО «ГПБ», Сбербанк -> ПАО «Сбербанк»).
2. **Вид**: Тип кредита из заголовка (Кредитная карта, Потребительский кредит, Овердрафт). "Вид займа (кредита)" - бери это если "Тип кредитной линии" стоит "-", если в "Тип кредитной линии" что-то стоит, то бери от туда минуя "Вид займа (кредита)".
3. **Дата_сделки**: 
   - Ищи подраздел "Общие сведения о сделке".
   - Поле: "Дата совершения сделки" (например, 16 сентября 2024).
   - НЕ путай с датой обновления информации!
4. **Сумма_обязательства** (Сколько БРАЛИ):
   - Ищи подраздел "Общие сведения о сделке".
   - Поле: "Сумма и валюта обязательства" (например, 600 000 р.) Бери самую первую сумму!.
5. **Сумма** (Текущий долг/Сколько ДОЛЖНЫ):
   - Ищи подраздел "Сведения о сумме задолженности".
   - Внутри таблица. Нам нужна строка "Общая" (или первая строка с цифрами).
   - Столбец "Всего". Это и есть полная сумма долга (тело + проценты).
   - Пример: Если в строке "Общая" столбце "Всего" стоит "517 346,28 р." — бери это число.

ПРИМЕР РАЗБОРА ИЗ СКРИНШОТА:
- Блок: БАНК ГПБ АО (Кредит «овердрафт»)
- Дата совершения сделки: 16 сентября 2024 -> "16.09.2024"
- Сумма и валюта обязательства: 600 000 р. -> 600000 (Это Сумма_обязательства)
- Таблица "Сведения о сумме задолженности":
  Строка "Общая" -> Столбец "Всего" -> 517 346,28 р. -> 517346.28 (Это Сумма - текущий долг!)
  (Не бери строку "Срочная" для суммы, бери "Общая"!)

ВАЖНО:
- Если кредит закрыт (статус "Закрыт", "Счет закрыт") — ИГНОРИРУЙ его.
- Ищи 10-значный ИНН кредитора в блоке "Сведения об источнике" (ИНН или ИНН/ОГРН).

JSON:
{{
  "Адрес_регистрации": "...",
  "ИНН_должника": "null",
  "Кредиты": [
    {{
      "Кредитор": "...",
      "ИНН_кредитора": "...",
      "Вид": "...",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": 0.00,
      "Сумма": 0.00
    }}
  ],
  "Судебные_акты": []
}}"""
                elif doc_type == "отчет_бки":
                    simple_prompt = base_prompt + f"""Ты помощник юриста. Обработай отчет Скоринг Бюро (БКИ).

В конце отчета (Персональные данные) найди:
- Адрес регистрации (место жительства, прописка).
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

Ищи таблицу "Действующие кредиты, займы, карты". Мы берем данные оттуда.

НОРМАЛИЗАЦИЯ БАНКОВ:
- "ГПБ (ОАО)", "ГПБ (АО)", "Газпромбанк" -> "АО «ГПБ»"
- "Сбербанк" -> "ПАО «Сбербанк»"
- "ВТБ" -> "Банк ВТБ (ПАО)"
- "Коллекторы: ООО Профессиональная Коллекторская Организация «Интел Коллект» (ранее - ООО «Интел коллект») Коллект» (ранее - ООО «Интел коллект»)" -> "ООО ПКО «Интел Коллект»"
    То есть убрать "Коллекторы:", убрать "(ранее - ...)", оставить только "ООО ПКО «Интел Коллект»"

ДЛЯ КАЖДОЙ СТРОКИ ТАБЛИЦЫ:
1. **Кредитор**: Название банка (колонка 1). Нормализуй!
2. **Вид**: Тип обязательства (под названием банка, например "Овердрафт", "Ипотека").
3. **Дата_сделки**: Колонка "Дата начала обязательства".
4. **Сумма_обязательства** (Сколько взяли): Колонка "Сумма / лимит обязательства".
5. **Сумма** (Текущий долг):
   - В СКРИНШОТЕ есть колонки: "Срочный основной долг", "Текущая просрочка", "Общая задолженность".
   - ПРИОРИТЕТ 1: Если есть колонка "Общая" (в блоке задолженность) или "Текущая задолженность" с полной суммой — бери её.
   - ПРИОРИТЕТ 2 (как на фото):
     Сумма = "Срочный основной долг" + "Текущая просрочка (осн.долг, %, пени...)".
   - В примере со скриншота:
     Банк ГПБ (ОАО) от 16.09.2024.
     Срочный осн. долг = 473 797.03
     Текущая просрочка = отсутствует (0)
     ИТОГО Сумма = 473 797.03
   - Другой пример (с просрочкой):
     Срочный долг = 813 242.73
     Просрочка = 29 763.00
     ИТОГО Сумма = 813242.73 + 29763.00 = 843005.73

ВАЖНО:
- Игнорируй закрытые кредиты.
- Если в колонке "Сумма / лимит обязательства" стоит 0 (для овердрафта бывает), но есть текущий долг — бери долг как есть.

JSON:
{{
  "Адрес_регистрации": "...",
  "Кредиты": [
    {{
      "Кредитор": "...",
      "Вид": "...",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": 0.00,
      "Сумма": 0.00
    }}
  ]
}}"""
                else:  # отчет_нбки
                    simple_prompt = base_prompt + f"""

ИНСТРУКЦИЯ ПО ОБРАБОТКЕ ЧАСТЕЙ ДОКУМЕНТА (БАТЧИ):
1. Если видишь таблицу или список кредитов без явного заголовка "Обязательства и их исполнение" — ЭТО ПРОДОЛЖЕНИЕ! ОБЯЗАТЕЛЬНО ИЗВЛЕКАЙ ВСЕ КРЕДИТЫ С ЭТОЙ СТРАНИЦЫ!
2. Не пропускай кредиты только потому, что не нашел заголовок раздела. Ищи структуру кредитных записей.

В начале документа (Субъект кредитной истории):
- Адрес регистрации (место жительства, адрес, прописка), самая первая запись о регистрации актуальная, бери её.
- Извлекай адрес 1:1 как написано в отчете, ничего не меняя (Убери только почтовый индекс и убери капс, заглавные буквы только пусть будут, там где они уместны - если нет, добавь сам заглавные буквы там где надо)! Это очень важно!

Найди раздел с заголовком "Обязательства и их исполнение" - там начинается список кредиторов, а заканчивается перед разделом с заголовком "Сведения о долговой нагрузке заемщика без заключенного потребительского займа".

Нормализуй название банка, МФК, МКК.
ПРИМЕРЫ ОБЯЗАТЕЛЬНОЙ НОРМАЛИЗАЦИИ:
- "Сбербанк" -> "ПАО «Сбербанк»"
- "ПСБ", "Промсвязьбанк" -> "ПАО «Промсвязьбанк»"
- "ГПБ", "Газпромбанк", "Ф-Л ГПБ (АО)", "ГПБ (АО)" -> "АО «ГПБ»"
- "ВТБ" -> "Банк ВТБ (ПАО)"
- "Альфа-Банк", "АЛЬФА-БАНК" -> "АО «Альфа-Банк»"
- "Тинькофф", "Т-Банк" -> "АО «Т-Банк»"
- "Совкомбанк" -> "ПАО «Совкомбанк»"

ВАЖНО - ИГНОРИРУЙ ПОГАШЕННЫЕ КРЕДИТЫ:
- Если в столбце "Прекращение обязательства" написано "Надлежащее исполнение обязательства" — кредит ПОГАШЕН, НЕ ВКЛЮЧАЙ его в список! А если стоит "Н/Д" — кредит ДЕЙСТВУЮЩИЙ, ВКЛЮЧАЙ его!
- Если обязательство прекращено — пропускай этот кредит!
- Если в столбце "Состояние" стоит "Статус: Счет закрыт" - кредит ПОГАШЕН, НЕ ВКЛЮЧАЙ его в список!
- Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!

Для КАЖДОГО ДЕЙСТВУЮЩЕГО кредитора:
1. Название банка/кредитора (заголовок раздела)
2. В таблице "Основные сведения об обязательстве":
   - Столбец "Сумма и валюта обязательства" = первоначальная сумма займа (Сумма_обязательства)
   - Столбец "Дата возникновения обязательства" = дата сделки (Дата_сделки)
   - Столбец "ОГРН/ИНН" - там два числа через пробел. ПЕРВОЕ (13 цифр) - это ОГРН, НЕ НУЖНО. ВТОРОЕ (10 цифр) - это ИНН, ОБЯЗАТЕЛЬНО извлеки!
   - Тип кредита/займа (из столбца "Вид займа (кредита)" или аналогичной: Потребительский кредит, Кредитная карта, Микрозайм, Ипотека, Автокредит, и т.д.)
3. В таблице "Задолженность":
   - Найди САМУЮ ПОСЛЕДНЮЮ строку в таблице
   - Возьми значение из столбца "Сумма" (это текущий долг)
   - ИГНОРИРУЙ другие столбцы ("Сумма по осн.долгу", "Сумма по %%", и т.д.)

КРИТИЧЕСКИ ВАЖНО ПРО ИЗВЛЕЧЕНИЕ СПИСКА КРЕДИТОВ:
- ЗАПРЕЩЕНО ОБЪЕДИНЯТЬ КРЕДИТЫ ПО НАЗВАНИЮ БАНКА или ТИПУ!
- Один блок в таблице (один заголовок "Субъект", "Сделка" или номер по порядку) = ОДИН отдельный JSON-объект.
- Если в отчете 5 разных записей от "АО «Т-Банк»" (например, 2 кредитные карты, 1 кредит наличными, 2 ипотеки) - верни РОВНО 5 объектов JSON!
- Если в отчете 8 записей от "АО «ГПБ»" (например, 3 Овердрафта и 5 Потребительских) - верни РОВНО 8 объектов JSON!
- СМОТРИ НА ТИП КРЕДИТА (Овердрафт, Кредитная линия и т.д.). Разные типы - это РАЗНЫЕ кредиты.
- ДАЖЕ ЕСЛИ ТИП КРЕДИТА СОВПАДАЕТ (например, два "Потребительский кредит" от одного банка) - ЭТО РАЗНЫЕ КРЕДИТЫ, если у них разные даты, суммы или счета.
- ДАЖЕ ЕСЛИ СУММЫ ПОХОЖИ - ЭТО РАЗНЫЕ КРЕДИТЫ.
- НИКОГДА НЕ ГРУППИРУЙ. ЛУЧШЕ ДУБЛИ, ЧЕМ ПОТЕРЯ ДАННЫХ.
- КАЖДАЯ СТРОКА ТАБЛИЦЫ "Сведения о счете" ИЛИ КАЖДЫЙ БЛОК ИНФОРМАЦИИ О КРЕДИТЕ ДОЛЖЕН СТАТЬ ОТДЕЛЬНЫМ ЭЛЕМЕНТОМ МАССИВА "Кредиты".

КРИТИЧЕСКИ ВАЖНО ПРО ИЗВЛЕЧЕНИЕ ДАННЫХ:
- Берем ТОЛЬКО последнюю(нижнюю) строку таблицы "Задолженность"
- Берем ТОЛЬКО столбец "Сумма" из этой строки
- Это и есть текущий долг
- НЕ суммируем ничего, просто берем одно значение!
- В столбце "ОГРН/ИНН" берем ВТОРОЕ число (10 цифр) - это ИНН!
- Если наткнешься на раздел с заголовком "Сведения о долговой нагрузке заемщика без заключенного потребительского займа" — прекращай сбор данных!
- Точно перепиши суммы и даты для каждого кредита, чтобы они не перепутались.
- Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!

Нормализуй название банка, обрати внимание на список примеров выше. "ГПБ (АО)" -> "АО «ГПБ»".

ВЕРНИ ТОЛЬКО ВАЛИДНЫЙ JSON БЕЗ ДОПОЛНИТЕЛЬНОГО ТЕКСТА!
Никакого текста до или после JSON!
Только чистый JSON формат!

JSON (СТРОГО этот формат) - Включай ТОЛЬКО текущие (действующие) кредиты с задолженностью, где "Прекращение обязательства" стоит "Н/Д"!:
{{
  "Адрес_регистрации": "...",
  "ИНН_должника": "Null",
  "Кредиты": [
    {{
      "Кредитор": "название (нормализованное)",
      "ИНН_кредитора": "ВТОРОЕ число из столбца ОГРН/ИНН (10 цифр)",
      "Вид": "тип кредита (Потребительский кредит/Кредитная карта/Микрозайм/Ипотека/Автокредит/другое)",
      "Дата_сделки": "ДД.ММ.ГГГГ",
      "Сумма_обязательства": "первоначальная сумма",
      "Сумма": "последняя строка таблицы Задолженность, столбец Сумма"
    }}
  ]
}}"""

                try:
                    print(f"      Обработка ({len(page_images)} стр.)...", end=" ", flush=True)
                    response_text, error_code = self.process_images_with_gpt(page_images, simple_prompt)
                    
                    # Если ошибка 400/500 - переключаемся на батч-режим
                    if error_code in [400, 500]:
                        print(f"ERROR {error_code}, переключаюсь на батч-режим...")
                        use_batch = True
                        batch_size = 30  # Начинаем с меньшего размера
                        # Рекурсивно вызываем батч-обработку выше
                        # Но это проще сделать через отдельную функцию. Пока просто обрабатываем ошибку
                        extracted_data = {}
                        error = f"Document too large (error {error_code}), but batch processing not available in this code path"
                    else:
                        cleaned = self.clean_json_response(response_text)
                        try:
                            extracted_data = json.loads(cleaned) if cleaned else {}
                            
                            # FALLBACK: если GPT вернул "Договоры" вместо "Кредиты"
                            if "Договоры" in extracted_data and "Кредиты" not in extracted_data:
                                print(f"      [FIX] GPT вернул 'Договоры', переименовываю в 'Кредиты'")
                                extracted_data["Кредиты"] = extracted_data.pop("Договоры")
                            
                            # Обработка постановлений (небатчевый режим)
                            if doc_type == "постановление_пристава":
                                if "Постановления" in extracted_data and isinstance(extracted_data["Постановления"], list):
                                    postanovleniya = extracted_data["Постановления"]
                                    print(f"OK ({len(postanovleniya)} постановлений)")
                                    
                                    # Логирование извлечённых постановлений
                                    if postanovleniya:
                                        print(f"      [ИЗВЛЕЧЕНО]:")
                                        for idx, item in enumerate(postanovleniya, 1):
                                            ip_num = item.get("Номер_ИП", "???")
                                            date = item.get("Дата_постановления", "???")
                                            creditor = item.get("Взыскатель", "???")
                                            total = item.get("Итого_взыскание", "???")
                                            print(f"        {idx}. {ip_num} | Дата: {date} | Взыскатель: {creditor} | Итого: {total}")
                                else:
                                    print(f"OK (0 постановлений)")
                                    print(f"      [DEBUG] Нет ключа 'Постановления' в ответе")
                            else:
                                # Обработка кредитных отчетов
                                credits = extracted_data.get("Кредиты", [])
                                credit_count = len(credits)
                                print(f"OK ({credit_count} кредитов)")
                                
                                # Логирование извлечённых кредитов
                                if credits:
                                    print(f"      [ИЗВЛЕЧЕНО]:")
                                    for idx, credit in enumerate(credits, 1):
                                        creditor = credit.get("Кредитор", "???")
                                        date = credit.get("Дата_сделки", "???")
                                        initial = credit.get("Сумма_обязательства", "???")
                                        debt = credit.get("Сумма", "???")
                                        inn = credit.get("ИНН_кредитора", "")
                                        
                                        # Если ИНН нет - пробуем получить из словаря или RusProfile
                                        if not inn and creditor != "???":
                                            _, canonical_name = DocumentProcessor.normalize_bank_name(creditor)
                                            inn = DocumentProcessor.get_bank_inn(canonical_name)
                                            if inn:
                                                credit["ИНН_кредитора"] = inn
                                                print(f"        {idx}. {creditor} | Дата: {date} | Начальная: {initial} | Долг: {debt} | ИНН: {inn} (получен)")
                                            else:
                                                print(f"        {idx}. {creditor} | Дата: {date} | Начальная: {initial} | Долг: {debt}")
                                        else:
                                            print(f"        {idx}. {creditor} | Дата: {date} | Начальная: {initial} | Долг: {debt} | ИНН: {inn}")
                        except json.JSONDecodeError as json_err:
                            print(f"ERROR (JSON parse failed)")
                            print(f"      [DEBUG] Ответ GPT (первые 500 символов):")
                            print(f"      {response_text[:500]}")
                            extracted_data = {}
                            error = f"JSON parse error: {str(json_err)}"
                except Exception as e:
                    print(f"ERROR ({str(e)[:50]})")
                    extracted_data = {}
                    error = str(e)

            
            # 🚀 ОПТИМИЗАЦИЯ ПАМЯТИ: Удаляем временные файлы и очищаем память
            print(f"      [MEMORY] Удаление {len(page_images)} временных файлов...")
            for tmp_path in page_images:
                try:
                    os.unlink(tmp_path)
                except:
                    pass
            
            # Очищаем список и запускаем сборщик мусора
            page_images.clear()
            all_credits = None
            extracted_data_temp = extracted_data.copy() if extracted_data else {}
            extracted_data = None
            import gc
            gc.collect()
            extracted_data = extracted_data_temp
            print(f"      [MEMORY] Память освобождена")
            DocumentProcessor.log_memory("После финальной очистки (кредитные отчеты)")

            elapsed = time.time() - start_time
            
            print(f"      [DEBUG] Возврат DocumentOutput: pages={total_pages}, error={error}, data keys={list(extracted_data.keys()) if extracted_data else 'None'}")

            return DocumentOutput(
                file=pdf_path.name,
                document_type=doc_type,
                pages=total_pages,
                processing_time_seconds=round(elapsed, 2),
                data=extracted_data,
                error=error,
                extracted_text=None,
            )

        # FALLBACK: Если PDF API не сработал, используем старый метод (Vision API)
        # Этот блок больше не используется для кредитных отчётов, но оставляем на случай отладки
        if False and doc_type in ["отчет_окб_OLD", "отчет_бки_OLD", "отчет_нбки_OLD"]:
            print(f"      [CREDIT] Обработка кредитного отчета (Vision API - FALLBACK)")

            # Конвертируем PDF в изображения (ВСЕ страницы - чтобы не потерять долги!)
            print(f"      Конвертация PDF в изображения...", end=" ", flush=True)
            pdf = pdfium.PdfDocument(str(pdf_path))
            total_pages = len(pdf)  # ОБРАБАТЫВАЕМ ВСЕ СТРАНИЦЫ
            pages = []
            for i in range(total_pages):
                page = pdf[i]
                bitmap = page.render(scale=2.5)  # scale 2.5 ≈ 200 DPI
                pil_image = bitmap.to_pil()
                pages.append(pil_image)
            print(f"OK ({len(pages)} стр.)")

            # Сохраняем страницы во временные файлы
            print(f"      Сохранение страниц...", end=" ", flush=True)
            page_images = []
            for i, page in enumerate(pages):
                with tempfile.NamedTemporaryFile(suffix=".jpg", delete=False) as tmp_file:
                    tmp_path = tmp_file.name
                    page.save(tmp_path, 'JPEG', quality=95, optimize=True)
                    page_images.append(tmp_path)
            print("OK")

            # БАТЧИНГ: Если страниц > 100, разбиваем на порции
            # Увеличен размер батча до 100 стр., чтобы большинство отчётов обрабатывались целиком
            # Это критически важно для МФК/МКК, т.к. их данные могут быть разбросаны по документу
            BATCH_SIZE = 100  # максимум страниц за раз
            all_credits = []
            error = None

            if len(page_images) > BATCH_SIZE:
                print(f"      [BATCH] Документ большой ({len(page_images)} стр.), обработка по {BATCH_SIZE} стр...")
                
                # Разбиваем на батчи
                for batch_start in range(0, len(page_images), BATCH_SIZE):
                    batch_end = min(batch_start + BATCH_SIZE, len(page_images))
                    batch_images = page_images[batch_start:batch_end]
                    batch_num = (batch_start // BATCH_SIZE) + 1
                    total_batches = (len(page_images) + BATCH_SIZE - 1) // BATCH_SIZE
                    
                    # Промпт для батча
                    batch_prompt = base_prompt + f"""

Это ЧАСТЬ {batch_num}/{total_batches} кредитного отчета (стр. {batch_start+1}-{batch_end} из {len(page_images)}).
Извлеки ВСЕ кредиты из этой части!

КРИТИЧЕСКИ ВАЖНО:
1. Поле 'Прекращение обязательств': 'Н/Д' → долг активен, дата → погашен
2. Раздел 'Задолженность' — последняя строка > 0 → есть долг
3. Извлекай: ИНН, дату договора, тип, сумму
4. Один кредитор несколько раз → отдельные записи с разными датами!

ВЕРНИ ТОЛЬКО ВАЛИДНЫЙ JSON БЕЗ ДОПОЛНИТЕЛЬНОГО ТЕКСТА!
Никакого текста до или после JSON!
Только чистый JSON формат!

Нормализуй название банка, например: ПАО «Сбербанк», ПАО «Банк ПСБ» и т.д.

ВЕРНИ ТОЛЬКО JSON со списком кредитов!"""
                    
                    try:
                        print(f"      Батч {batch_num}/{total_batches} (стр. {batch_start+1}-{batch_end})...", end=" ", flush=True)
                        response_text, error_code = self.process_images_with_gpt(batch_images, batch_prompt)
                        cleaned = self.clean_json_response(response_text)
                        batch_data = json.loads(cleaned) if cleaned else {}
                        
                        # Извлекаем кредиты из батча
                        batch_credits = batch_data.get("Кредиты") or batch_data.get("Договоры") or []
                        all_credits.extend(batch_credits)
                        print(f"OK (+{len(batch_credits)} кред.)")
                    except Exception as e:
                        print(f"ERROR ({str(e)[:30]})")
                        if not error:  # Сохраняем первую ошибку
                            error = str(e)
                        continue
                
                # Формируем финальный результат
                extracted_data = {
                    "Кредиты": all_credits,
                    "Количество_активных_кредитов": len(all_credits)
                }
            else:
                # Обычная обработка для небольших отчётов (≤25 стр)
                credit_prompt = base_prompt + f"""

Это кредитный отчет ({len(pages)} стр.).
ПРОАНАЛИЗИРУЙ ВСЕ СТРАНИЦЫ!

КРИТИЧЕСКИ ВАЖНО:
1. Поле 'Прекращение обязательств': 'Н/Д' → долг активен, дата → погашен
2. Раздел 'Задолженность' — последняя строка > 0 → есть долг
3. Извлекай: ИНН, дату договора, тип, сумму задолженности
4. Один кредитор несколько раз → отдельные записи!

ВЕРНИ ТОЛЬКО ВАЛИДНЫЙ JSON БЕЗ ДОПОЛНИТЕЛЬНОГО ТЕКСТА!
Никакого текста до или после JSON!
Только чистый JSON формат!

Нормализуй название банка, например: ПАО «Сбербанк», ПАО «Банк ПСБ» и т.д.

ВЕРНИ ТОЛЬКО JSON!"""

                try:
                    print(f"      Обработка GPT-5.1 ({len(pages)} стр.)...", end=" ", flush=True)
                    response_text, error_code = self.process_images_with_gpt(page_images, credit_prompt)
                    cleaned = self.clean_json_response(response_text)
                    extracted_data = json.loads(cleaned) if cleaned else {}
                    print("OK")
                except Exception as e:
                    print(f"ERROR ({str(e)[:50]})")
                    extracted_data = {}
                    error = str(e)
            
            # 🚀 ОПТИМИЗАЦИЯ ПАМЯТИ: Удаляем временные файлы и очищаем память
            print(f"      [MEMORY] Удаление {len(page_images)} временных файлов...")
            for tmp_path in page_images:
                try:
                    os.unlink(tmp_path)
                except:
                    pass
            
            # Очищаем список и запускаем сборщик мусора
            page_images.clear()
            all_credits = None
            extracted_data_temp = extracted_data.copy() if extracted_data else {}
            extracted_data = None
            import gc
            gc.collect()
            extracted_data = extracted_data_temp
            print(f"      [MEMORY] Память освобождена")
            DocumentProcessor.log_memory("После финальной очистки (небольшие отчеты)")

            elapsed = time.time() - start_time
            total_pages = len(pages)

            return DocumentOutput(
                file=pdf_path.name,
                document_type=doc_type,
                pages=total_pages,
                processing_time_seconds=round(elapsed, 2),
                data=extracted_data,
                error=error,
                extracted_text=None,
            )

        # === ОБЫЧНАЯ ОБРАБОТКА ДЛЯ ОСТАЛЬНЫХ ДОКУМЕНТОВ ===
        # Convert PDF to images using pypdfium2
        print(f"      Конвертация PDF в изображения...", end=" ", flush=True)
        pdf = pdfium.PdfDocument(str(pdf_path))
        total_pages = len(pdf)
        DocumentProcessor.log_memory("После загрузки PDF (обычный)")
        
        pages = []
        for i in range(total_pages):
            page = pdf[i]
            bitmap = page.render(scale=2.0)  # scale 2.0 ≈ 150 DPI
            pil_image = bitmap.to_pil()
            pages.append(pil_image)
        print(f"OK ({total_pages} стр.)")
        DocumentProcessor.log_memory("После рендера страниц")
        
        saved_page_paths: List[str] = []
        extracted_data: Dict[str, Any] = {}
        error: Optional[str] = None

        try:
            # Save all pages as temporary JPEG files
            print(f"      Сохранение страниц...", end=" ", flush=True)
            for page in pages:
                with tempfile.NamedTemporaryFile(suffix=".jpg", delete=False) as tmp_file:
                    tmp_path = tmp_file.name
                    page.save(tmp_path, "JPEG", quality=95, optimize=True)
                    saved_page_paths.append(tmp_path)
            print(f"OK")
            
            # Очищаем PIL объекты
            for page in pages:
                page.close()
            pages.clear()
            pdf.close()
            import gc
            gc.collect()
            DocumentProcessor.log_memory("После сохранения и очистки PIL")

            # Create multi-page prompt
            multi_page_prompt = f"""{base_prompt}

Это документ содержит {len(pages)} страниц(ы). Все страницы представлены ниже.
Проанализируй ВСЕ изображения и извлеки данные согласно инструкции.
Объедини информацию со всех страниц в один JSON объект.
Верни результат СТРОГО в формате JSON.
Нормализуй название банка, например: ПАО «Сбербанк», ПАО «Банк ПСБ» и т.д."""

            print(f"      Обработка всех страниц GPT-5...", end=" ", flush=True)

            # Process all pages at once with GPT-5 Vision
            response_text, error_code = self.process_images_with_gpt(saved_page_paths, multi_page_prompt)
            cleaned = self.clean_json_response(response_text)

            # ЛОГИРОВАНИЕ для налогового уведомления
            if doc_type == "налоговое_уведомление":
                print(f"\n      [DEBUG] GPT ответ (первые 1000 символов):")
                print(f"      {response_text[:1000]}")
                print(f"      [DEBUG] Cleaned JSON (первые 800 символов):")
                print(f"      {cleaned[:800] if cleaned else 'EMPTY'}")

            try:
                extracted_data = json.loads(cleaned)
                if error_code:
                    print(f"WARN - Error {error_code}")
                else:
                    print(f"OK")
                
                # ЛОГИРОВАНИЕ извлеченных данных для налогового уведомления
                if doc_type == "налоговое_уведомление":
                    print(f"      [DEBUG] Извлеченные данные:")
                    print(f"         ФИО: {extracted_data.get('ФИО', 'НЕТ')}")
                    print(f"         ИНН: {extracted_data.get('ИНН', 'НЕТ')}")
                    print(f"         Дата: {extracted_data.get('Дата', 'НЕТ')}")
                    print(f"         Всего_к_уплате: {extracted_data.get('Всего_к_уплате', 'НЕТ')}")
                    print(f"         Налоги: {len(extracted_data.get('Налоги', []))} шт.")
            except json.JSONDecodeError as e:
                error = f"JSON decode error: {str(e)}"
                extracted_data = {"raw_response": response_text, "error": error}
                print(f"WARN - JSON error")
                
                # ЛОГИРОВАНИЕ ошибки для налогового уведомления
                if doc_type == "налоговое_уведомление":
                    print(f"      [DEBUG] JSON parse error: {str(e)}")
                    print(f"      [DEBUG] Попытка парсинга строки:")
                    print(f"      {cleaned[:500] if cleaned else 'EMPTY'}")
        except Exception as exc:
            error = str(exc)
            extracted_data = {"error": str(exc)}
            print(f"ERROR: {str(exc)[:50]}")
        finally:
            # Clean up temporary files
            for tmp_path in saved_page_paths:
                try:
                    os.remove(tmp_path)
                except OSError:
                    continue
            
            # Очищаем память
            saved_page_paths.clear()
            import gc
            gc.collect()
            DocumentProcessor.log_memory("После удаления временных файлов (обычный)")

        elapsed = time.time() - start_time
        return DocumentOutput(
            file=pdf_path.name,
            document_type=doc_type,
            pages=len(pages),
            processing_time_seconds=round(elapsed, 2),
            data=extracted_data,
            error=error,
            extracted_text=None,
        )

    def process_batch(
        self,
        pdf_paths: Iterable[Path],
        output_json: Optional[Path] = None,
        debtor_id: Optional[str] = None,
        lawyer: Optional[str] = None,
    ) -> tuple[List[DocumentOutput], Dict[str, List[Dict[str, Any]]], List[Path]]:
        import time as time_module
        batch_start_time = time_module.time()
        print(f"[TIMING] process_batch started")
        
        results: List[DocumentOutput] = []
        aggregated: Dict[str, List[Dict[str, Any]]] = {}
        pdf_list = list(pdf_paths)  # Конвертируем в список для повторного использования

        # Сортируем файлы: паспорт обрабатываем первым, потом остальные
        def sort_key(path: Path) -> tuple[int, str]:
            filename_lower = path.name.lower()
            if "паспорт" in filename_lower or "passport" in filename_lower:
                return (0, path.name)  # паспорт первым
            elif "егрн" in filename_lower or "выписка" in filename_lower:
                return (2, path.name)  # ЕГРН после паспорта, но перед остальными
            else:
                return (1, path.name)  # остальные документы

        sorted_pdf_list = sorted(pdf_list, key=sort_key)

        # Переменная для хранения ФИО должника (извлекается из паспорта)
        debtor_fio = ""

        for idx, pdf in enumerate(sorted_pdf_list, 1):
            file_start_time = time_module.time()
            print(f"[TIMING] Processing file {idx}/{len(sorted_pdf_list)}: {pdf.name}")
            try:
                result = self.process_pdf(pdf, debtor_fio=debtor_fio)
            except Exception as exc:  # noqa: BLE001
                result = DocumentOutput(
                    file=pdf.name,
                    document_type="ошибка",
                    pages=0,
                    processing_time_seconds=0.0,
                    data={},
                    error=str(exc),
                )
            file_elapsed = time_module.time() - file_start_time
            print(f"[TIMING] File {pdf.name} completed in {file_elapsed:.1f}s")
            results.append(result)
            
            # DEBUG: Проверяем почему данные не добавляются в aggregated
            print(f"      [DEBUG] result.error: {result.error}")
            print(f"      [DEBUG] isinstance(result.data, dict): {isinstance(result.data, dict)}")
            print(f"      [DEBUG] 'raw_output' in result.data: {'raw_output' in result.data if isinstance(result.data, dict) else 'N/A'}")
            
            if not result.error and isinstance(result.data, dict) and "raw_output" not in result.data:
                # DEBUG: Показываем ключи в result.data для кредитных отчетов
                if result.document_type in ["отчет_окб", "отчет_бки", "отчет_нбки"]:
                    print(f"      [DEBUG] Ключи в {result.document_type}: {list(result.data.keys())}")
                    if "Кредиты" in result.data:
                        print(f"      [DEBUG] Кредитов в ответе: {len(result.data.get('Кредиты', []))}")
                    if "Договоры" in result.data:
                        print(f"      [DEBUG] Договоров в ответе: {len(result.data.get('Договоры', []))}")
                
                # Специальная обработка для постановлений с батчевой обработкой
                if result.document_type == "постановление_пристава" and "Постановления" in result.data:
                    # Разворачиваем массив постановлений и добавляем каждое отдельно
                    постановления = result.data.get("Постановления", [])
                    if isinstance(постановления, list):
                        for постановление in постановления:
                            aggregated.setdefault(result.document_type, []).append(постановление)
                        print(f"      [BATCH] Развернуто {len(постановления)} постановлений в aggregated")
                    else:
                        # Если это не список - добавляем как есть
                        aggregated.setdefault(result.document_type, []).append(result.data)
                else:
                    # Обычная обработка для всех остальных документов
                    aggregated.setdefault(result.document_type, []).append(result.data)
                    
                    # DEBUG: Логируем добавление кредитных отчетов в aggregated
                    if result.document_type in ["отчет_окб", "отчет_бки", "отчет_нбки"]:
                        current_count = len(aggregated.get(result.document_type, []))
                        print(f"      [DEBUG] Добавлен {result.document_type} в aggregated (всего отчетов типа {result.document_type}: {current_count})")
                
                # Извлекаем ФИО должника из паспорта для использования в других документах
                if result.document_type == "паспорт" and "ФИО" in result.data:
                    debtor_fio = result.data.get("ФИО", "")
                    if debtor_fio:
                        print(f"      [ФИО ДОЛЖНИКА] Извлечено: {debtor_fio}")

        processing_elapsed = time_module.time() - batch_start_time
        print(f"[TIMING] All files processed in {processing_elapsed:.1f}s")
        
        # Передаем список файлов для формирования приложений (используем исходный порядок для приложений)
        context_start_time = time_module.time()
        print(f"[TIMING] Preparing template context...")
        template_context = self.prepare_template_context(aggregated, pdf_list, client)
        context_elapsed = time_module.time() - context_start_time
        print(f"[TIMING] Template context prepared in {context_elapsed:.1f}s")
        
        templates_start_time = time_module.time()
        print(f"[TIMING] Generating documents...")
        filled_templates = self.generate_all_documents(template_context, debtor_id=debtor_id, lawyer=lawyer)
        templates_elapsed = time_module.time() - templates_start_time
        print(f"[TIMING] Documents generated in {templates_elapsed:.1f}s")
        
        if output_json:
            json_start_time = time_module.time()
            # Сохраняем template_context для последующего редактирования и регенерации
            # Сначала нормализуем маркеры переноса строки в реальные '\n',
            # затем конвертируем RichText объекты в строки для JSON сериализации
            normalized_context = DocumentProcessor._normalize_line_breaks(template_context)
            serializable_context = self._make_json_serializable(normalized_context)
            with open(output_json, "w", encoding="utf-8") as handle:
                json.dump(serializable_context, handle, ensure_ascii=False, indent=2)
            json_elapsed = time_module.time() - json_start_time
            print(f"[TIMING] JSON saved in {json_elapsed:.1f}s")
        
        total_elapsed = time_module.time() - batch_start_time
        print(f"[TIMING] process_batch completed in {total_elapsed:.1f}s")
        return results, aggregated, filled_templates

    @staticmethod
    def _make_json_serializable(obj: Any) -> Any:
        """Конвертирует RichText объекты и другие несериализуемые типы в строки."""
        from docxtpl import RichText
        
        if isinstance(obj, RichText):
            # RichText содержит XML-разметку, нужно извлечь чистый текст
            # Получаем строковое представление
            rich_text_str = str(obj)
            
            # Удаляем все XML-теги, извлекая только текстовое содержимое
            # Паттерн ищет содержимое между <w:t>...</w:t> или <w:t xml:space="preserve">...</w:t>
            text_content = re.findall(r'<w:t[^>]*>(.*?)</w:t>', rich_text_str)
            
            if text_content:
                # Объединяем найденные фрагменты текста
                return ''.join(text_content)
            else:
                # Если паттерн не нашел ничего, пробуем просто удалить все XML теги
                clean_text = re.sub(r'<[^>]+>', '', rich_text_str)
                return clean_text.strip() if clean_text.strip() else ""
        elif isinstance(obj, dict):
            return {key: DocumentProcessor._make_json_serializable(value) for key, value in obj.items()}
        elif isinstance(obj, list):
            return [DocumentProcessor._make_json_serializable(item) for item in obj]
        elif isinstance(obj, (str, int, float, bool, type(None))):
            return obj
        else:
            # Для других типов пытаемся привести к строке
            return str(obj)

    @staticmethod
    def _normalize_line_breaks(obj: Any) -> Any:
        """Рекурсивно заменяет маркеры переноса строки на настоящий '\n' в строках перед сериализацией.

        Заменяем шаблонные маркеры вида '{{LINE_BREAK}}', '{{ LINE_BREAK }}', '{LINE_BREAK}'
        и отдельные токены 'LINE_BREAK' (как отдельное слово) на реальный перевод строки.
        """
        import re

        if isinstance(obj, dict):
            return {k: DocumentProcessor._normalize_line_breaks(v) for k, v in obj.items()}
        if isinstance(obj, list):
            return [DocumentProcessor._normalize_line_breaks(v) for v in obj]
        if isinstance(obj, str):
            # replace brace variants
            s = re.sub(r"\{\{\s*LINE_BREAK\s*\}\}|\{LINE_BREAK\}", "\n", obj)
            # replace standalone token LINE_BREAK when it's a separate word
            s = re.sub(r"(?<!\w)LINE_BREAK(?!\w)", "\n", s)
            return s
        return obj


__all__ = ["DocumentProcessor", "DocumentOutput", "GPT_MODEL"]


# === CLI Interface ===
def main():
    """Запуск обработки документов из командной строки."""
    import sys
    from pathlib import Path

    print("=" * 70)
    print("  PROCESSING BANKRUPTCY DOCUMENTS")
    print("=" * 70)
    print()

    # Запрашиваем папку с документами
    if len(sys.argv) > 1:
        input_folder = Path(sys.argv[1])
    else:
        folder_input = input("Введите путь к папке с PDF документами: ").strip().strip('"')
        if not folder_input:
            print("ERROR - Путь не указан!")
            return
        input_folder = Path(folder_input)

    if not input_folder.exists():
        print(f"ERROR - Папка не найдена: {input_folder}")
        return

    if not input_folder.is_dir():
        print(f"ERROR - Это не папка: {input_folder}")
        return

    # Ищем PDF файлы
    pdf_files = list(input_folder.glob("*.pdf"))

    if not pdf_files:
        print(f"ERROR - В папке не найдено PDF файлов: {input_folder}")
        return

    print(f"OK - Найдено {len(pdf_files)} PDF файл(ов):")
    for i, pdf in enumerate(pdf_files, 1):
        print(f"   {i}. {pdf.name}")
    print()

    # Создаем процессор
    processor = DocumentProcessor()

    # Запускаем обработку
    print("Начинаем обработку документов...")
    print()

    output_json = input_folder / "результат_обработки.json"

    overall_start = time.time()

    try:
        results, aggregated, filled_template = processor.process_batch(
            pdf_files,
            output_json=output_json
        )

        overall_elapsed = time.time() - overall_start

        print()
        print("=" * 70)
        print("  PROCESSING COMPLETE")
        print("=" * 70)
        print(f"  Total time: {overall_elapsed:.1f} seconds ({overall_elapsed/60:.1f} min)")
        print()

        # Показываем результаты
        for result in results:
            status = "[+]" if not result.error else "[-]"
            print(f"{status} {result.file}")
            print(f"   Тип: {result.document_type}")
            print(f"   Страниц: {result.pages}")
            print(f"   Время: {result.processing_time_seconds} сек")
            if result.error:
                print(f"   WARNING - Ошибка: {result.error}")
            print()

        # Сохраненные файлы
        print("Created files:")
        print(f"   > {output_json.name} - JSON с извлеченными данными")


        if filled_template:
            for tmpl in filled_template:
                print(f"   > {tmpl.name}")

        print()
        print(f"All files saved to: {input_folder}")
        print()

        # Показываем статистику
        total_docs = len(results)
        successful = sum(1 for r in results if not r.error)
        failed = total_docs - successful

        print("Statistics:")
        print(f"   Всего документов: {total_docs}")
        print(f"   Обработано успешно: {successful}")
        if failed > 0:
            print(f"   С ошибками: {failed}")

        print()
        print("Done! Check the filled template.")

    except KeyboardInterrupt:
        print()
        print("WARNING - Processing interrupted by user")
    except Exception as exc:
        print()
        print(f"ERROR - {exc}")
        try:
            import traceback
            traceback.print_exc()
        except Exception as te:
            print(f"[TRACEBACK ERROR] Failed to print traceback: {te}")


if __name__ == "__main__":
    main()