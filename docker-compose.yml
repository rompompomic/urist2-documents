services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DEBUG=False
      - HOST=0.0.0.0
      - PORT=5000
    env_file:
      - .env
    # Ограничения ресурсов
    mem_limit: 7g
    mem_reservation: 4g
    # cpus: 2
    volumes:
      # Сохраняем данные между перезапусками
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./resultdoc:/app/resultdoc
      - ./cbr_data:/app/cbr_data
      - ./debtors.db:/app/debtors.db
      # Монтируем код для автоматического обновления без пересборки
      - ./processor.py:/app/processor.py
      - ./app.py:/app/app.py
      - ./templ:/app/templ
      - ./templates:/app/templates
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  # Опционально: Nginx reverse proxy для HTTPS
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - web
  #   restart: unless-stopped
